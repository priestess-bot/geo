# Schema v2 incremental migrations

This directory remains empty while the pre-cutover baseline is being assembled.
After the baseline is released, add immutable, ordered SQL files here and list
their paths and SHA-256 digests in `manifest.json` under `migration_files`.
