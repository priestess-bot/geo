# 工程实施审计日志

本文件记录从设计文档进入工程实现后的关键动作，作为代码层审计链的人工侧补充。运行时系统内的正式审计对象为 `AuditEvent / EvidenceLink / ScoreContribution / ReportExport / TraceabilityBundle`。

## 2026-06-09 · M0 工程骨架

- 目标：把 AU 首发 Evidence-first 设计落成第一个可验证工程切片。
- 范围：仓库骨架、FastAPI API 壳、Next.js 控制台壳、核心 Python 契约、审计/评分模型、SQL 迁移、Docker Compose、CI、ADR。
- 数据契约：覆盖 `MarketProfile`、`AnswerRun`、`RawAnswer`、`AnswerCitation`、`EvidenceAsset`、`AnswerAnalysis`、`SourceGraph`、`CompetitorBenchmark`、`VisibilityScoreSnapshot`、`CollectionCost`、`AuditEvent`、`ReportExport`、`ScoreContribution`、`EvidenceLink` 关联表。
- 代码落盘：
  - `packages/geno_core/`：核心 dataclass、接口 Protocol、AU market profile、`au_visibility_v1` 评分、审计 hash/event builder、stub 后端。
  - `apps/api/`：FastAPI 壳，提供 `/health`、`/v1/market-profiles/au`、`/v1/contracts`。
  - `apps/web/`：Next.js 控制台壳，展示 AU evidence-first MVP 状态。
  - `infra/`：PostgreSQL+pgvector、MinIO、API、Web 的 Compose 配置；P0a/P0b/P0c 数据表迁移与回滚脚本。Postgres 初始化只挂载 `migrations/up/`，避免首次起库误执行回滚脚本。
  - `.github/workflows/ci.yml`、`Makefile`、`tests/`、`decisions/0001-m0-lightweight-open-source-stack.md`。
- 文档同步：`README.md` 已从“文档库”更新为“文档 + M0 工程骨架”；`PROJECT-PLAN.md` 已把 M0 真实状态同步为 `[~]`，并区分已完成项与待补项。
- 验证项：
  - `make test`：通过，5 个 Python contract tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析，包含 postgres/pgvector、MinIO、API、Web。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成接口/数据/迁移/壳/测试基线，不代表 M1-M7 业务流已完成；LiteLLM 运行时服务、真实采集器、worker runtime、lint、真实迁移起服仍在后续任务内。
- 相关计划：`PROJECT-PLAN.md` 的 M0。

## 2026-06-10 · M1 AU 项目启动包

- 目标：把 M1 的 AU MarketProfile、1 个行业模板、100 条 Prompt Pack、3-5 个竞品和项目配置入口落成可验证工程切片。
- 范围：租户/项目/成员/品牌/竞品/行业模板/PromptQuestion/ProjectBootstrap 数据契约；AU DTC 电商行业模板；100 条 AU 英文 Prompt Pack；FastAPI M1 端点；API/core contract tests；PLAN/README/前端状态同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `Tenant`、`Project`、`ProjectMember`、`BrandEntity`、`CompetitorEntity`、`IndustryProfile`、`PromptQuestion`、`ProjectBootstrap`。
  - `packages/geno_core/geno_core/industry.py`：新增 `build_au_dtc_ecommerce_profile()`。
  - `packages/geno_core/geno_core/prompt_pack.py`：新增 `build_au_dtc_prompt_pack()`，固定生成 100 条 AU Prompt，覆盖 10 个 intent type，显式城市类只覆盖 Sydney/Melbourne/Brisbane。
  - `packages/geno_core/geno_core/bootstrap.py`：新增 `build_au_project_bootstrap()`，生成 AU 项目、DTC 行业、3-5 个竞品、100 条 prompt 和 `project_bootstrap_created` 审计事件。
  - `apps/api/geno_api/main.py`：新增 `/v1/industry-profiles/au/dtc-ecommerce`、`/v1/prompt-packs/au/dtc-ecommerce`、`/v1/project-bootstraps/au/dtc-ecommerce`。
  - `infra/db/migrations/up/0001_init.sql` 与 `down/0001_init.down.sql`：补 `tenants`、`projects`、`project_members` 数据表。
- 验证项：
  - `make install-api-deps`：安装 FastAPI/API 测试依赖。
  - `make test`：通过，10 个 contract/API tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析，Postgres 仍只挂载 `migrations/up/`。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片提供可审计的项目启动包和 API 返回，不代表项目/品牌/竞品的真实持久化 CRUD、权限系统、CSV/XLSX 导入或采集运行已经完成；这些仍在 M1/M2 后续任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M1。

## 2026-06-10 · M2a Stable Evidence Chain fixture

- 目标：打通稳定采集链路的工程骨架，证明 Perplexity/OpenAI 采集后端可插拔、P0a 采样计划可计算、Raw Evidence Store 数据契约可生成、成本与审计链可追踪。
- 范围：`AnswerRun`、`RawAnswer`、`AnswerCitation`、`EvidenceAsset`、`CollectorLog`、`CollectionCost`、`RawEvidenceRecord`、`CollectionPlan` 数据契约；fixture Perplexity/OpenAI 后端；Static AU GeoProvider；P0a 2400 planned_runs 计划；小样本 fixture evidence slice；API/core tests；PLAN/worker README/前端状态同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 M2a raw evidence 与 collection cost dataclass。
  - `packages/geno_core/geno_core/collectors.py`：新增 `FixturePerplexitySonarCollector`、`FixtureOpenAIWebSearchCollector`，实现同一个 `CollectorBackend` 契约，返回 answer/citation/screenshot/html/raw_payload。
  - `packages/geno_core/geno_core/collection.py`：新增 `build_p0a_collection_plan()`、`collect_prompt_once()`、`run_fixture_collection_slice()`。
  - `packages/geno_core/geno_core/geo.py`：新增 `StaticAUGeoProvider`，覆盖 Australia/Sydney/Melbourne/Brisbane 等 AU 城市地理参数。
  - `apps/api/geno_api/main.py`：新增 `/v1/collection-plans/au/p0a`、`/v1/evidence-runs/au/p0a-fixture-slice`。
  - `workers/collector_worker/README.md`：说明 fixture runner 与后续真实 API adapter 的关系。
- 验证项：
  - `make test`：通过，15 个 contract/API tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片是 deterministic fixture evidence chain，不代表真实 Perplexity Sonar API 或 OpenAI web search API 已接入；真实凭证、限流、重试、失败审计、人工补录和持久化 worker runtime 仍在 M2a 后续任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M2a。

## 2026-06-10 · M2a API adapter shell + worker CLI

- 目标：把 M2a 从 fixture evidence chain 推进到真实 API adapter shell 和可运行 worker CLI，同时保持“未配置凭证不伪装成功”的审计口径。
- 外部接口核对：
  - OpenAI web search：按 OpenAI Responses API 的 web search tool 形态封装，payload 使用 `tools: [{"type": "web_search_preview"}]`。
  - Perplexity Sonar：按 Perplexity chat completions / Sonar 形态封装，payload 使用 `model: "sonar"` 与 `messages`，解析 `citations` / `search_results`。
- 代码落盘：
  - `packages/geno_core/geno_core/collectors.py`：新增 `PerplexitySonarCollector`、`OpenAIWebSearchCollector`、`JsonHttpClient`、`CollectorConfigurationError`；支持 payload 构造、response parse、env API key 检查。
  - `packages/geno_core/geno_core/collection.py`：新增 `collect_prompt_with_failure_record()` 与 `CollectionFailureRecord` 路径；异常会生成 failed `AnswerRun`、`CollectorLog`、`CollectionCost` 和 `answer_run_failed` 审计事件。
  - `workers/collector_worker/run_collection_slice.py`：新增 worker CLI，支持 `--mode fixture` 和 `--mode api`。
  - `Makefile`：新增 `worker-fixture`。
  - `tests/`：新增真实 adapter payload/parse、未配置 key 失败审计、worker CLI fixture/api 模式测试。
- 验证项：
  - `make test`：通过，20 个 contract/API/worker tests 全部 OK。
  - `make worker-fixture`：通过，生成 8 条 fixture collection records，失败数 0。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：真实 API adapter shell 已实现，但本地没有使用真实 `PERPLEXITY_API_KEY` / `OPENAI_API_KEY` 发起外部采集验收；外部 API 成功率、限流、重试策略和成本口径仍需凭证联调后关闭。
- 相关计划：`PROJECT-PLAN.md` 的 M2a。

## 2026-06-10 · M2b Google spike framework

- 目标：把 Google AIO / AI Mode 高风险 spike 变成可执行、可审计、可解释的工程框架，明确 pass/fail gate 与 limited coverage 口径。
- 范围：30 条高意图 prompt 选择；Google spike 计划 `30 prompts × 2 surfaces × 2 geo × k=2 = 240 planned_runs`；Google AIO / AI Mode fixture collectors；Playwright/ThirdParty/Manual collector shell；失败分类枚举；Google spike gate；API/worker/test/PLAN/README/前端同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `GoogleSpikePlan`、`GoogleSpikeGateResult`、Google spike failure reason。
  - `packages/geno_core/geno_core/google_spike.py`：新增 `select_google_spike_prompts()`、`build_google_spike_plan()`、`evaluate_google_spike_gate()`。
  - `packages/geno_core/geno_core/collectors.py`：新增 `FixtureGoogleAIOCollector`、`FixtureGoogleAIModeCollector`、`FixtureThirdPartySerpCollector`、`FixtureManualBackfillCollector`，以及 `PlaywrightGoogleAIOCollector`、`PlaywrightAIModeCollector`、`ThirdPartySerpCollector`、`ManualBackfillCollector` shell。
  - `apps/api/geno_api/main.py`：新增 `/v1/google-spikes/au/plan`、`/v1/google-spikes/au/fixture-gate`。
  - `workers/collector_worker/run_collection_slice.py`：新增 `--mode google-fixture`。
  - `Makefile`：新增 `worker-google-fixture`。
- 验证项：
  - `make test`：通过，26 个 contract/API/worker tests 全部 OK。
  - `make worker-google-fixture`：通过，生成 240 条 Google fixture records，gate 为 pass，`google_aio_completed_runs=120`。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 Google spike 的计划、collector shell、fixture gate 和 limited coverage 判定，不代表真实 Google AIO / AI Mode 浏览器采集、第三方 SERP API 或人工补录工作台已经完成；真实 spike 仍需浏览器运行环境、供应商凭证或人工补录输入后执行。
- 相关计划：`PROJECT-PLAN.md` 的 M2b。

## 2026-06-10 · M3 Rule Parser + AUVisibilityScore

- 目标：把 M2a 的 raw evidence records 转成可解释的 AnswerAnalysis、AUVisibilityScore 聚合快照、ScoreContribution 解释包和评分审计事件。
- 范围：规则解析器；品牌提及/推荐/排名/竞品/引用/本地相关性/情绪/新鲜度/竞品份额解析；`au_visibility_v1` 聚合评分；Trigger Rate vs Mention/Recommendation Rate 双分母；k 次采样分数离散度；ScoreContribution；API/core tests；PLAN/README/前端/SQL 迁移同步。
- 代码落盘：
  - `packages/geno_core/geno_core/parser.py`：新增 `RuleBasedAnswerParser`。
  - `packages/geno_core/geno_core/scoring.py`：新增 `score_answer_analyses()`、`AggregateScoreResult`，输出 `VisibilityScoreSnapshot`、8 个 `ScoreContribution` 和 `visibility_score_snapshot_created` 审计事件。
  - `packages/geno_core/geno_core/analysis_pipeline.py`：新增 `analyze_and_score_records()`，串联 fixture records → parser → aggregate score。
  - `apps/api/geno_api/main.py`：新增 `/v1/visibility-scores/au/p0a-fixture`。
  - `infra/db/migrations/up/0001_init.sql`：`visibility_score_snapshots` 补 `dispersion` 字段。
- 验证项：
  - `make test`：通过，29 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成规则解析和聚合评分闭环，不代表 LLM-as-judge、人工实体消歧工作流、真实外部采集数据上的评分校准已经完成；这些仍在 M3/P1 后续任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M3。

## 2026-06-10 · M4 Citation Graph + Competitor Benchmark

- 目标：把 M2/M3 的 evidence 与 analyses 转成 Citation Graph、source gap 和 3-5 个竞品 Benchmark，并保证每个 graph/benchmark 数字能回溯到 `answer_run_ids`。
- 范围：PG 邻接表口径的 graph 数据契约；source domain/type/topic 聚合；SourceGraphEvidence 链路；高权重 source_type gap；竞品 mention rate、citation overlap、本地相关性均值；API/core tests；PLAN/README/前端/SQL 迁移同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `SourceGraphNode`、`SourceGraphEvidence`、`SourceGap`、`CompetitorBenchmark`、`CitationGraphResult`。
  - `packages/geno_core/geno_core/graph.py`：新增 `build_citation_graph()`。
  - `apps/api/geno_api/main.py`：新增 `/v1/citation-graphs/au/p0a-fixture`。
  - `infra/db/migrations/up/0001_init.sql`：`source_graphs` 补 `citation_count` 字段。
  - `packages/geno_core/geno_core/bootstrap.py`：默认 AU DTC 竞品改为 DTC/睡眠电商场景，便于 fixture benchmark 形成非零对照。
- 验证项：
  - `make test`：通过，31 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 graph/benchmark 计算和 API 输出，不代表完整交互式监测看板、筛选导出或 Neo4j 替换验证已经完成；这些仍在 M4/P1 后续任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M4。

## 2026-06-10 · M5 Markdown/CSV Evidence Report Export

- 目标：生成可审计、不可覆盖的客户报告快照，让报告数字能沿 `ReportExport -> VisibilityScoreSnapshot -> ScoreContribution -> AnswerAnalysis -> AnswerRun -> RawAnswer/AnswerCitation/EvidenceAsset` 回溯。
- 范围：`ReportExport` 快照；Markdown 方法说明与分数解释；CSV 原始证据附录；report evidence answer_run_ids；导出审计事件；API/core tests；PLAN/README/前端/SQL 迁移同步。
- 代码落盘：
  - `packages/geno_core/geno_core/report.py`：新增 `MarkdownCsvReportExporter`、`EvidenceReport`，输出 Markdown、CSV、`ReportExport`、`report_export_created` 审计事件。
  - `packages/geno_core/geno_core/models.py`：`ReportExport` 补 `markdown_url`。
  - `apps/api/geno_api/main.py`：新增 `/v1/reports/au/p0a-fixture`。
  - `infra/db/migrations/up/0001_init.sql`：`report_exports` 补 `markdown_url` 字段。
- 验证项：
  - `make test`：通过，33 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 Markdown/CSV 报告和审计快照，不代表 PDF 渲染、白标代理商工作流、真实 Google spike 结论或 API-vs-浏览器差异抽检已完成；这些仍在 M5/P1 后续任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M5。

## 2026-06-10 · M6 Action Plan + Retest comparison

- 目标：把 M4 的 source gap 与 M3/M5 的评分快照转成可执行 ActionRecommendation，并建立 T0/T+7/T+14/T+30 同口径复测计划、前后窗口对比和审计事件。
- 范围：`ActionRecommendation`、`RetestSchedule`、`RetestComparison` 数据契约；source gap/低提及率/低推荐率转任务；任务 owner/status/next_check_date；复测 offsets；前后分数 delta/trend；Action Plan 与 Retest Comparison 审计事件；API/core tests；PLAN/README/前端/SQL 迁移同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `ActionRecommendation`、`RetestSchedule`、`RetestComparison`。
  - `packages/geno_core/geno_core/action_plan.py`：新增 `build_action_recommendations()`、`build_retest_schedule()`、`compare_retest_windows()`、`build_action_plan_audit_event()`、`build_retest_comparison_audit_event()`。source gap 类 action 若没有同类 citation node，会回退绑定本次评分窗口的 `answer_run_ids`，避免任务无证据来源。
  - `apps/api/geno_api/main.py`：新增 `/v1/action-plans/au/p0a-fixture`，返回 actions、retest schedule、retest comparison、action audit event、comparison audit event。
  - `infra/db/migrations/up/0001_init.sql` 与 `down/0001_init.down.sql`：新增 action/retest 相关表与回滚。
  - `tests/`：新增 M6 core/API 合约测试，覆盖 action evidence refs、T+7/14/30 offsets、trend 分类和审计事件。
- 验证项：
  - `make test`：通过，36 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 M6 的 deterministic fixture 工程闭环，不代表 Temporal 可重放调度、真实复测采集窗口、报告模板/UI 中的前后变化展示、实时预警系统或外部工单/CMS 集成已经完成；这些仍在 M6/P1 后续产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M6。

## 2026-06-10 · M7 Knowledge Facts + Content Draft + Integrations fixture

- 目标：把 P2 的本地事实库、证据驱动内容草稿和集成接入点落成可测试工程切片，同时保持“内容只能是待人工审核草稿、不自动发布”的审计口径。
- 范围：`LocalizedKnowledgeFact`、`KnowledgeSearchResult`、`ContentDraft`、`IntegrationConnector`、`ManualDistributionRecord` 数据契约；AU 优先事实检索与 global fallback 标记；source gap/action 转内容草稿；草稿绑定 evidence/source_gap/knowledge_fact；集成 connector 计划对象；手动分发记录；API/core tests；PLAN/README/前端/SQL 迁移同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 M7 知识事实、内容草稿、集成与手动分发 dataclass。
  - `packages/geno_core/geno_core/knowledge.py`：新增 `build_localized_knowledge_facts()`、`search_knowledge_facts()`、`build_content_drafts()`、`build_integration_connectors()`、`build_manual_distribution_records()`、`build_content_engine_audit_event()`。检索结果在 AU 事实不足或需要提示本地化缺口时保留 `fallback_used` 标记。
  - `apps/api/geno_api/main.py`：新增 `/v1/content-engines/au/p0a-fixture`，返回 facts/search results/content drafts/connectors/manual distribution/audit event；`/v1/contracts` 增加 M7 contracts。
  - `infra/db/migrations/up/0001_init.sql` 与 `down/0001_init.down.sql`：新增 M7 相关表与回滚。
  - `tests/`：新增 M7 core/API 合约测试，覆盖 AU/global fallback、草稿人工审核状态、evidence/source_gap/knowledge_fact 绑定、connector 数量、手动分发记录和审计事件。
- 验证项：
  - `make test`：通过，39 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 deterministic fixture 内容闭环，不代表 pgvector 持久化检索、真实 LLMGateway 内容生成、人工审核 UI、真实 OAuth/API 集成、自动 CMS 草稿创建或更广平台采集已经完成；这些仍在 M7/P2 后续产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M7。

## 2026-06-10 · Traceability Bundle / EvidenceLink query fixture

- 目标：把已分散在采集、评分、图谱、报告、Action 和内容草稿里的 `answer_run_ids`、`AuditEvent`、`ScoreContribution`、`ReportExport` 组织成一个可查询的 provenance/解释链路，证明报告数值可以反查到原始证据。
- 范围：`EvidenceLink`、`TraceabilityBundle` 数据契约；报告到评分快照、评分贡献、原始 answer run、raw answer、citation、asset、source graph、action、content draft、audit event 的聚合链路；API/core tests；PLAN/README/前端/SQL 迁移同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `EvidenceLink`、`TraceabilityBundle`。
  - `packages/geno_core/geno_core/traceability.py`：新增 `build_traceability_bundle()`，从 `ReportExport`、`VisibilityScoreSnapshot`、`ScoreContribution`、`RawEvidenceRecord`、`CitationGraphResult`、`ActionRecommendation`、`ContentDraft`、`AuditEvent` 生成统一溯源包。
  - `apps/api/geno_api/main.py`：新增 `/v1/traceability/au/p0a-fixture`；`/v1/contracts` 增加 Traceability contracts。
  - `infra/db/migrations/up/0001_init.sql` 与 `down/0001_init.down.sql`：新增 `evidence_links`、`traceability_bundles` 表与回滚。
  - `tests/`：新增 core/API 合约测试，覆盖 `ReportExport -> VisibilityScoreSnapshot -> ScoreContribution -> AnswerRun -> RawAnswer/AnswerCitation/EvidenceAsset` 链路、action/content draft 证据链接和 API 可查询返回。
- 验证项：
  - `make test`：通过，41 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 deterministic fixture provenance 查询闭环，不代表持久化查询 UI、完整人工补录/实体确认审计链、真实外部数据集或报告 PDF 内交互式钻取已经完成；这些仍在后续产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 全局 DoD 与 P0c 可售报告验收门槛。

## 2026-06-10 · PostgreSQL repository runtime mapping

- 目标：补齐运行时持久化边界，让 M2a-M7 的核心对象不只停留在 dataclass/API fixture，而是有明确的 PostgreSQL 写入仓储和可测试 SQL 映射。
- 范围：Raw Evidence、AnswerAnalysis、VisibilityScoreSnapshot、ScoreContribution、CitationGraph、CompetitorBenchmark、ReportExport、ActionRecommendation、RetestSchedule、ContentDraft、IntegrationConnector、ManualDistributionRecord、EvidenceLink、TraceabilityBundle、AuditEvent 的 repository 写入；stable UUID `AnswerAnalysis.id`；core tests；PLAN/README/前端/API contracts 同步。
- 代码落盘：
  - `packages/geno_core/geno_core/repository.py`：新增 `PostgresEvidenceRepository`，使用 DB-API 风格 connection/cursor 协议，不强绑 ORM；提供 `save_raw_evidence_records()`、`save_answer_analyses()`、`save_score_snapshot()`、`save_citation_graph()`、`save_report_export()`、`save_action_plan()`、`save_content_engine()`、`save_traceability_bundle()`、`save_audit_events()`。
  - `packages/geno_core/geno_core/parser.py`：`AnswerAnalysis.id` 改为稳定 UUID，匹配 PostgreSQL `uuid` 表字段。
  - `apps/api/geno_api/main.py`：`/v1/contracts` 增加 persistence contracts。
  - `tests/test_core_contracts.py`：新增 recording connection 仓储测试，验证核心 pipeline 会写入关键 runtime 表，并触发 commit。
- 验证项：
  - `make test`：通过，42 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 repository SQL 映射和可测试写入边界，不代表真实 psycopg 连接池、事务重试、生产迁移执行、RLS、多租户鉴权或运行时 API 写库开关已经完成；这些仍在后续 runtime 产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M0/M2a 与全局 DoD。

## 2026-06-10 · DATABASE_URL runtime connection + worker persist switch

- 目标：把上一切片的 repository SQL 映射接到真实运行入口，让 collector worker 可以在显式开启时把采集成功和采集失败的 evidence/audit/cost 写入 PostgreSQL。
- 范围：`DATABASE_URL` connection factory；psycopg 懒加载；JSONB 与 UUID/UUID[] runtime 参数适配；worker `--persist`；失败采集 `CollectionFailureRecord` 写入；Docker worker profile；core/worker/API contract tests；PLAN/README/worker README/前端状态同步。
- 代码落盘：
  - `packages/geno_core/geno_core/runtime.py`：新增 `connect_postgres_from_env()`、`build_repository_from_env()`、`RuntimePersistenceError`；缺少 `DATABASE_URL` 时明确失败。
  - `packages/geno_core/geno_core/repository.py`：JSONB 字段在 psycopg 存在时使用 `Jsonb` 适配；`uuid[]` 字段转 UUID 对象；新增 `save_collection_failure_records()`，保存 failed `AnswerRun`、`CollectorLog`、`CollectionCost` 与 `AuditEvent`。
  - `workers/collector_worker/run_collection_slice.py`：新增 `--persist`；默认只打印 JSON；显式开启后保存成功/失败采集记录；配置缺失时 exit code `2`。
  - `apps/api/requirements.txt`：新增 `psycopg[binary]==3.3.4`。
  - `infra/docker-compose.yml` 与 `apps/api/Dockerfile`：新增 worker profile，镜像内复制 `workers/`。
  - `tests/test_core_contracts.py` 与 `tests/test_worker_cli.py`：新增 runtime connection、失败记录写入和 worker persist 配置失败测试。
  - `README.md`、`workers/collector_worker/README.md`、`PROJECT-PLAN.md`、`apps/api/geno_api/main.py`、`apps/web/app/page.tsx`：同步运行时持久化状态。
- 验证项：
  - `make test`：通过，46 个 contract/API/worker tests 全部 OK。
  - `make docker-config` 与 `docker compose -f infra/docker-compose.yml --profile worker config`：通过，默认服务与 worker profile 配置均可解析。
  - `PYTHONPATH=packages/geno_core:apps/api python3 workers/collector_worker/run_collection_slice.py --mode fixture --prompt-limit 1`：通过，输出 4 条 fixture records，默认 `persistence.enabled=false`。
  - `PYTHONPATH=packages/geno_core:apps/api python3 workers/collector_worker/run_collection_slice.py --mode fixture --prompt-limit 1 --persist`：在缺少 `DATABASE_URL` 时按预期 exit code `2`，输出配置缺失错误。
  - `docker compose -p geno-runtime-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，镜像安装 `psycopg` 后真实启动 PostgreSQL/MinIO，worker 输出 `persistence.enabled=true`、`raw_evidence_records=4`。
  - `docker compose -p geno-runtime-test -f infra/docker-compose.yml exec -T postgres psql ...`：确认 `answer_runs=4`、`raw_answers=4`、`audit_events=4`、`collection_costs=4`；验证后已执行 `docker compose -p geno-runtime-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
- 当前边界：本切片已完成 Docker 内 PostgreSQL 端到端写库验证，但尚未实现连接池、事务重试、运行时迁移命令、对象存储上传、持久化查询 API/UI、RLS 或多租户鉴权；这些仍在后续 runtime 产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M0/M2a 与全局 DoD。

## 2026-06-10 · Runtime Evidence Read Model + API

- 目标：在 worker 已能写 PostgreSQL 后，补齐从持久化表读回 evidence 的 runtime 查询入口，让 API 不再只能返回 fixture 产物。
- 范围：`RuntimeEvidenceRun`、`RuntimeEvidencePage` 数据契约；`AnswerRun -> RawAnswer -> AnswerCitation/EvidenceAsset/CollectorLog/CollectionCost/AuditEvent` 聚合查询；`/v1/evidence-runs/runtime` endpoint；缺少 `DATABASE_URL` 时的 503 保护；core/API contract tests；PLAN/README/前端状态同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 runtime evidence read model。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_evidence_runs()` 与 `_load_runtime_evidence_run()`，支持按 `project_id`、`platform`、`status` 过滤和 `limit/offset` 分页。
  - `packages/geno_core/geno_core/runtime.py`：新增 `close_repository_connection()`，API 请求结束后关闭底层连接。
  - `apps/api/geno_api/main.py`：新增 `/v1/evidence-runs/runtime`；contracts 增加 `RuntimeEvidenceRun`、`RuntimeEvidencePage` 与 runtime endpoint。
  - `tests/test_core_contracts.py`：新增 recording read model 测试，验证 raw answer、citation、asset、collector log、cost、audit event 聚合。
  - `tests/test_api_contracts.py`：新增 runtime endpoint 未配置持久化时返回 503 的 API contract。
  - `README.md`、`PROJECT-PLAN.md`、`apps/web/app/page.tsx`：同步 runtime read/write 状态。
- 验证项：
  - `make test`：通过，48 个 contract/API/worker tests 全部 OK。
  - `make docker-config` 与 `docker compose -f infra/docker-compose.yml --profile worker config`：通过。
  - `docker compose -p geno-runtime-read-test -f infra/docker-compose.yml up --build -d api` + `docker compose -p geno-runtime-read-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 写入 4 条 runtime evidence。
  - `curl -fsS 'http://localhost:8000/v1/evidence-runs/runtime?limit=2'`：通过，API 返回 `total_count=4`，单条记录含 raw answer、citations、evidence assets、collector logs、collection cost 与 audit events。
  - `docker compose -p geno-runtime-read-test -f infra/docker-compose.yml exec -T postgres psql ...`：确认 `answer_runs=4`、`raw_answers=4`、`answer_citations=12`、`evidence_assets=8`、`audit_events=4`；验证后已执行 `docker compose -p geno-runtime-read-test -f infra/docker-compose.yml down -v` 清理容器与卷。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 runtime evidence API 的代码、contract 覆盖和 Docker 写入后读回 E2E，但尚未补前端持久化证据列表页、连接池、RLS/多租户鉴权、报告级 runtime 查询或真实外部采集数据 E2E；这些仍在后续 runtime 产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M0/M2a、全局 DoD 与 P0c 可售报告验收门槛。

## 2026-06-10 · Project Bootstrap persistence + prompt-linked runtime evidence

- 目标：补齐 M1 与 M2a 之间的持久化断点，让 worker 写 evidence 前先写 AU 项目启动包、品牌/竞品和 100 条 prompt，并让 runtime evidence API 能直接读回 prompt 文本、intent、priority、prompt version。
- 范围：`ProjectBootstrap` PostgreSQL 幂等写入；`Tenant/Project/ProjectMember/BrandEntity/CompetitorEntity/PromptQuestion` 元数据持久化；`answer_runs` 读取时 join `prompt_questions`；worker `--persist` 输出持久化摘要；core/worker/API contract tests；README/PLAN/worker README/前端状态同步。
- 代码落盘：
  - `packages/geno_core/geno_core/repository.py`：新增 `save_project_bootstrap()`，写入 `market_profiles`、`industry_profiles`、`tenants`、`projects`、`project_members`、`brand_entities`、`competitor_entities`、`prompt_questions` 与 bootstrap `AuditEvent`；`list_runtime_evidence_runs()` 改为从 `answer_runs ar` left join `prompt_questions pq`，返回 prompt text/intent/priority/version。
  - `workers/collector_worker/run_collection_slice.py`：`--persist` 时先调用 `save_project_bootstrap()`，再写 successful/failed collection records；输出 `project_bootstrap=true`、`prompt_questions=100`、`project_id`、`tenant_id`。
  - `apps/api/geno_api/main.py`：`/v1/contracts` 增加 bootstrap persistence 与 prompt metadata contracts。
  - `tests/test_core_contracts.py`：新增 ProjectBootstrap repository 写入测试；runtime evidence read model 测试增加 prompt metadata 断言和 join SQL 断言。
  - `README.md`、`PROJECT-PLAN.md`、`workers/collector_worker/README.md`、`apps/web/app/page.tsx`：同步 AU 启动包/prompt 元数据持久化和 prompt-linked runtime evidence 状态。
- 验证项：
  - `make test`：通过，49 个 contract/API/worker tests 全部 OK。
  - `make docker-config` 与 `docker compose -f infra/docker-compose.yml --profile worker config`：通过，默认服务与 worker profile 配置均可解析。
  - `docker compose -p geno-bootstrap-read-test -f infra/docker-compose.yml up --build -d api` + `docker compose -p geno-bootstrap-read-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `project_bootstrap=true`、`prompt_questions=100`、`competitors=4`、`raw_evidence_records=4`。
  - `GET http://localhost:8000/v1/evidence-runs/runtime?limit=1`：通过，API 返回 `total_count=4`，单条 `answer_run` 含 `prompt_text="Is ExampleBrand good in Australia?"`、`prompt_intent_type="brand_awareness"`、`prompt_priority=1`、`prompt_version="au_dtc_ecommerce_v1"`。
  - PostgreSQL 计数验证：`projects=1`、`prompt_questions=100`、`competitor_entities=4`、`answer_runs=4`、`raw_answers=4`、`answer_citations=12`、`evidence_assets=8`、`audit_events=5`；join 验证 `answer_runs -> prompt_questions` 可读回 prompt text/intent/version；验证后已执行 `docker compose -p geno-bootstrap-read-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片补齐 worker 入口的启动包持久化和 evidence->prompt 可读追溯；尚未实现控制台创建/编辑项目 CRUD、RLS/多租户鉴权、prompt 批量导入、`answer_runs.prompt_question_id` 外键收紧、连接池或持久化证据列表 UI。
- 相关计划：`PROJECT-PLAN.md` 的 M1、M2a、全局 DoD 与审计链风险项。

## 2026-06-10 · Runtime Score read API + worker analysis persistence

- 目标：补齐 M3 从 fixture 到 runtime 的持久化查询断点，让同一批 worker 采集结果可以继续解析、评分、保存解释包，并通过 API 从数据库读回 `VisibilityScoreSnapshot -> ScoreContribution -> AnswerRun/PromptQuestion -> AnswerAnalysis/AuditEvent`。
- 范围：runtime score read model；`visibility_score_snapshots` 分页查询；`score_contributions`、`score_snapshot_runs`、`answer_runs`、`prompt_questions`、`answer_analyses`、`audit_events` 聚合；worker `--persist-analysis`；Docker worker profile；core/API/worker tests；README/PLAN/worker README/前端状态同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeScoreSnapshotRun`、`RuntimeScoreSnapshot`、`RuntimeScoreSnapshotPage`。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_score_snapshots()` 与 `_load_runtime_score_snapshot()`，支持 `project_id`、`scope_type`、`limit/offset`；读回 score contributions、关联 answer run prompt metadata、最新 answer analysis 和评分审计事件。
  - `apps/api/geno_api/main.py`：新增 `/v1/visibility-scores/runtime`，缺少 `DATABASE_URL` 时返回 503；contracts 增加 runtime score types 和 endpoint。
  - `workers/collector_worker/run_collection_slice.py`：新增 `--persist-analysis`，要求同时传 `--persist`；对成功采集记录运行 `analyze_and_score_records()` 并保存 `AnswerAnalysis`、`VisibilityScoreSnapshot`、`ScoreContribution`、`ScoreSnapshotRun` 和评分 `AuditEvent`。
  - `infra/docker-compose.yml`：worker profile 启用 `--persist-analysis`，Docker E2E 默认覆盖证据写入后评分写入。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`、`tests/test_worker_cli.py`：新增 runtime score read model、runtime score 503 和 `--persist-analysis` 参数约束测试。
  - `README.md`、`PROJECT-PLAN.md`、`workers/collector_worker/README.md`、`apps/web/app/page.tsx`：同步 runtime score API 与 worker analysis persistence 状态。
- 验证项：
  - `make test`：通过，52 个 contract/API/worker tests 全部 OK。
  - `make docker-config` 与 `docker compose -f infra/docker-compose.yml --profile worker config`：通过，worker profile 配置包含 `--persist-analysis`。
  - `docker compose -p geno-runtime-score-test -f infra/docker-compose.yml up --build -d api` + `docker compose -p geno-runtime-score-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `analysis.enabled=true`、`analysis_count=4`、`score_contributions=8`、`final_score=87.35`。
  - `GET http://localhost:8000/v1/visibility-scores/runtime?limit=1`：通过，API 返回 `total_count=1`，单条记录含 1 个 snapshot、8 个 contributions、4 个 answer runs、prompt text、`rule_based_v1` answer analysis 和 1 个评分 audit event。
  - PostgreSQL 计数验证：`answer_runs=4`、`answer_analyses=4`、`visibility_score_snapshots=1`、`score_contributions=8`、`score_snapshot_runs=32`、`audit_events=6`；验证后已执行 `docker compose -p geno-runtime-score-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 M3 评分解释包的 runtime 写入和读取，但尚未实现持久化报告生成 API、前端 runtime score/detail 页面、真实外部采集后评分校准、公式权重后台配置或 RLS/多租户鉴权。
- 相关计划：`PROJECT-PLAN.md` 的 M3、M5、全局 DoD 与审计链风险项。

## 2026-06-10 · Runtime Citation Graph API + source gap persistence

- 目标：补齐 M4 从 fixture 到 runtime 的持久化查询断点，让 worker 在证据、解析、评分之后继续保存 citation graph、source gaps 和 competitor benchmarks，并通过 API 从数据库读回图谱/竞品/信源缺口。
- 范围：`source_gaps` 表；runtime citation graph read model；`source_graphs`、`source_graph_evidence`、`source_gaps`、`competitor_benchmarks` 聚合查询；source graph node 到 answer run/prompt 的追溯；worker `--persist-analysis` 保存 graph；core/API tests；README/PLAN/worker README/前端状态同步。
- 代码落盘：
  - `infra/db/migrations/up/0001_init.sql` 与 `down/0001_init.down.sql`：新增 `source_gaps` 表和回滚。
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeCitationGraphNode`、`RuntimeCitationGraph`、`RuntimeCitationGraphPage`。
  - `packages/geno_core/geno_core/repository.py`：`save_citation_graph()` 新增 source gap 持久化；新增 `list_runtime_citation_graphs()` 与 `_load_runtime_citation_graph()`，读回 source graph nodes、source graph evidence、source gaps、competitor benchmarks 和 answer run prompt metadata。
  - `apps/api/geno_api/main.py`：新增 `/v1/citation-graphs/runtime`，缺少 `DATABASE_URL` 时返回 503；contracts 增加 runtime graph types 和 endpoint。
  - `workers/collector_worker/run_collection_slice.py`：`--persist-analysis` 在保存 score 后继续构建并保存 citation graph、source gaps 和 competitor benchmarks；输出 graph 计数摘要。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：新增 runtime citation graph read model、runtime graph 503 和 contracts 测试；repository mapping 测试覆盖 `source_gaps`。
  - `README.md`、`PROJECT-PLAN.md`、`workers/collector_worker/README.md`、`apps/web/app/page.tsx`：同步 runtime citation graph API 与 source gap persistence 状态。
- 验证项：
  - `make test`：通过，54 个 contract/API/worker tests 全部 OK。
  - `make docker-config` 与 `docker compose -f infra/docker-compose.yml --profile worker config`：通过，worker profile 仍包含 `--persist-analysis`。
  - `docker compose -p geno-runtime-graph-test -f infra/docker-compose.yml up --build -d api`：通过，新库迁移成功创建 `source_gaps` 表。
  - `docker compose -p geno-runtime-graph-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `source_graph_nodes=3`、`source_graph_evidence=12`、`source_gaps=3`、`competitor_benchmarks=4`。
  - `GET http://localhost:8000/v1/citation-graphs/runtime?limit=1`：通过，API 返回 `total_count=1`，单条记录含 3 个 source graph nodes、12 条 evidence links、3 个 source gaps、4 个 competitor benchmarks，并可从 node 读回 prompt text。
  - PostgreSQL 计数验证：`source_graphs=3`、`source_graph_evidence=12`、`source_gaps=3`、`competitor_benchmarks=4`、`answer_runs=4`、`answer_analyses=4`；join 验证 `source_graph_evidence -> answer_runs -> prompt_questions` 可读回 prompt text；验证后已执行 `docker compose -p geno-runtime-graph-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 M4 图谱/竞品/信源缺口的 runtime 写入和读取，但尚未实现前端 graph dashboard、持久化报告生成 API、Neo4j 替换演示、source gap 人工确认或真实外部采集数据校准。
- 相关计划：`PROJECT-PLAN.md` 的 M4、M5、P0c 可售报告验收门槛与审计链风险项。

## 2026-06-10 · Runtime Report API + worker report snapshot persistence

- 目标：补齐 M5 从 fixture 到 runtime 的持久化查询断点，让 worker 在证据、评分、图谱之后继续生成并保存 Evidence Report 快照，并通过 API 从数据库读回 `ReportExport -> ReportEvidence -> VisibilityScoreSnapshot -> AnswerRun/PromptQuestion -> CitationGraph`。
- 范围：runtime report read model；`report_exports`、`report_evidence`、`visibility_score_snapshots`、`answer_runs`、`prompt_questions`、`audit_events` 聚合查询；worker `--persist-analysis` 保存 M5 `ReportExport`；API/core tests；README/PLAN/worker README/前端状态同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeReportExport`、`RuntimeReportExportPage`。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_report_exports()` 与 `_load_runtime_report_export()`，读回 report export、score snapshots、report evidence answer runs、report audit event，并嵌入 runtime citation graph。
  - `apps/api/geno_api/main.py`：新增 `/v1/reports/runtime`，缺少 `DATABASE_URL` 时返回 503；contracts 增加 runtime report types 和 endpoint。
  - `workers/collector_worker/run_collection_slice.py`：`--persist-analysis` 在保存 score 与 graph 后调用 `MarkdownCsvReportExporter` 生成 report snapshot 并保存 `ReportExport`、`ReportEvidence` 和 report audit event；输出 `report_export_id` 与 report evidence 计数。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：新增 runtime report read model、runtime report 503 和 contracts 测试。
  - `README.md`、`PROJECT-PLAN.md`、`workers/collector_worker/README.md`、`apps/web/app/page.tsx`：同步 runtime report API 与 worker report snapshot persistence 状态。
- 验证项：
  - `make test`：通过，56 个 contract/API/worker tests 全部 OK。
  - `make docker-config` 与 `docker compose -f infra/docker-compose.yml --profile worker config`：通过，worker profile 仍包含 `--persist-analysis`。
  - `docker compose -p geno-runtime-report-test -f infra/docker-compose.yml up --build -d api` + `docker compose -p geno-runtime-report-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `report_export_id=335c876f-9fef-56fc-a449-a3fd4e134506`、`report_evidence_answer_runs=4`。
  - `GET http://localhost:8000/v1/reports/runtime?limit=1`：通过，API 返回 `total_count=1`，单条记录含 `report_version=worker-runtime-v1`、`sample_size=4`、1 个 score snapshot、4 个 answer runs、prompt text、3 个 graph nodes、3 个 source gaps 和 1 个 report audit event。
  - PostgreSQL 计数验证：`report_exports=1`、`report_evidence=4`、`visibility_score_snapshots=1`、`source_graphs=3`、`answer_runs=4`、`audit_events=7`；join 验证 `report_evidence -> answer_runs -> prompt_questions` 可读回 prompt text；验证后已执行 `docker compose -p geno-runtime-report-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片完成 M5 报告快照的 runtime 写入和读取，但尚未实现对象存储真实上传、PDF renderer、前端报告页、白标报告、多版本报告下载或真实 Google spike 结论写入报告。
- 相关计划：`PROJECT-PLAN.md` 的 M5、P0c 可售报告验收门槛与审计链风险项。

## 2026-06-10 · Runtime Action Plan API + worker retest persistence

- 目标：补齐 M6 从 fixture 到 runtime 的持久化查询断点，让 worker 在报告快照之后继续生成并保存 Action Plan 与复测对比，并通过 API 从数据库读回 `RetestSchedule -> ActionRecommendation -> RetestComparison -> AnswerRun/PromptQuestion -> AuditEvent`。
- 范围：runtime action plan read model；`retest_schedules`、`action_recommendations`、`retest_comparisons`、`answer_runs`、`prompt_questions`、`audit_events` 聚合查询；worker `--persist-analysis` 保存 M6 action/retest 产物；API/core tests；README/PLAN/worker README/前端状态同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeActionPlan`、`RuntimeActionPlanPage`。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_action_plans()` 与 `_load_runtime_action_plan()`，支持 `project_id`、`status`、`limit/offset`；读回 action recommendations、retest schedule、retest comparisons、关联 answer run prompt metadata，以及 action plan / retest comparison audit events。
  - `apps/api/geno_api/main.py`：新增 `/v1/action-plans/runtime`，缺少 `DATABASE_URL` 时返回 503；contracts 增加 runtime action plan types 和 endpoint。
  - `workers/collector_worker/run_collection_slice.py`：`--persist-analysis` 在保存 report snapshot 后调用 Action Planner，保存 `ActionRecommendation`、`RetestSchedule`、`RetestComparison`、`action_plan_created` 和 `retest_comparison_created` 审计事件；输出 action/retest 摘要。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：新增 runtime action plan read model、runtime action plan 503 和 contracts 测试。
  - `README.md`、`PROJECT-PLAN.md`、`workers/collector_worker/README.md`、`apps/web/app/page.tsx`：同步 runtime action plan API 与 worker retest persistence 状态。
- 验证项：
  - `make test`：通过，58 个 contract/API/worker tests 全部 OK。
  - `make docker-config` 与 `docker compose -f infra/docker-compose.yml --profile worker config`：通过，worker profile 仍包含 `--persist-analysis`。
  - `docker compose -p geno-runtime-action-test -f infra/docker-compose.yml up --build -d api` + `docker compose -p geno-runtime-action-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `action_recommendations=3`、`retest_schedule_id=668877f8-a627-57a7-8734-8ff352a81363`、`retest_comparison_id=1369fae6-e152-53e2-bdb5-fef064402822`、`retest_trend=improved`。
  - `GET http://localhost:8000/v1/action-plans/runtime?limit=1`：通过，API 返回 `total_count=1`，单条记录含 3 条 action recommendations、T+0/7/14/30 retest schedule、1 个 RetestComparison、4 个 answer runs、prompt text 和 `action_plan_created` / `retest_comparison_created` 两类 audit events。
  - PostgreSQL 计数验证：`action_recommendations=3`、`retest_schedules=1`、`retest_comparisons=1`、`answer_runs=4`、`audit_events=9`；audit event 分布为 `answer_run_collected=4`、`project_bootstrap_created=1`、`visibility_score_snapshot_created=1`、`report_export_created=1`、`action_plan_created=1`、`retest_comparison_created=1`；验证后已执行 `docker compose -p geno-runtime-action-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片目标是 runtime 写入和读取 Action Plan/复测对比；Temporal 可重放调度、任务 owner UI、状态流转、报告模板展示、预警订阅和真实复测窗口采集仍在后续 M6 产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M6、全局 DoD 与审计链风险项。

## 2026-06-10 · Runtime Content Engine API + worker M7 persistence

- 目标：补齐 M7 从 fixture 到 runtime 的持久化查询断点，让 worker 在 Action Plan/复测之后继续保存 Knowledge Facts、Content Drafts、Integration Connectors 和 Manual Distribution Records，并通过 API 从数据库读回 `ContentDraft -> LocalizedKnowledgeFact -> ActionRecommendation -> AnswerRun/PromptQuestion -> ManualDistributionRecord/IntegrationConnector/AuditEvent`。
- 范围：runtime content engine read model；`localized_knowledge_facts`、`content_drafts`、`integration_connectors`、`manual_distribution_records`、`action_recommendations`、`answer_runs`、`prompt_questions`、`audit_events` 聚合查询；worker `--persist-analysis` 保存 M7 content/integration 产物；API/core tests；README/PLAN/worker README/前端状态同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeContentDraft`、`RuntimeContentEngine`、`RuntimeContentEnginePage`。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_content_engines()`、`_load_runtime_content_engine()` 与 `_load_runtime_content_draft()`，支持 `project_id`、`review_status`、`limit/offset`；读回 content drafts、knowledge facts、target prompts、evidence answer runs、source action、manual distribution records、integration connectors 和 content engine audit event。
  - `apps/api/geno_api/main.py`：新增 `/v1/content-engines/runtime`，缺少 `DATABASE_URL` 时返回 503；contracts 增加 runtime content engine types 和 endpoint。
  - `workers/collector_worker/run_collection_slice.py`：`--persist-analysis` 在保存 action/retest 后继续生成并保存 `LocalizedKnowledgeFact`、`ContentDraft`、`IntegrationConnector`、`ManualDistributionRecord` 和 `content_engine_fixture_created` 审计事件；输出 M7 摘要。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：新增 runtime content engine read model、runtime content engine 503 和 contracts 测试。
  - `README.md`、`PROJECT-PLAN.md`、`workers/collector_worker/README.md`、`apps/web/app/page.tsx`：同步 runtime content engine API 与 worker M7 persistence 状态。
- 验证项：
  - `make test`：通过，60 个 contract/API/worker tests 全部 OK。
  - `make docker-config` 与 `docker compose -f infra/docker-compose.yml --profile worker config`：通过，worker profile 仍包含 `--persist-analysis`。
  - `docker compose -p geno-runtime-content-test -f infra/docker-compose.yml up --build -d api` + `docker compose -p geno-runtime-content-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `knowledge_facts=7`、`content_drafts=3`、`integration_connectors=7`、`manual_distribution_records=3`。
  - `GET http://localhost:8000/v1/content-engines/runtime?limit=1`：通过，API 返回 `total_count=1`，单条记录含 7 个 knowledge facts、3 个 content drafts、7 个 integration connectors、3 个 manual distribution records；单个 draft 可读回 target prompt、knowledge fact、evidence answer run prompt text、source action、manual distribution record 和 `content_engine_fixture_created` audit event。
  - PostgreSQL 计数验证：`localized_knowledge_facts=7`、`content_drafts=3`、`integration_connectors=7`、`manual_distribution_records=3`、`action_recommendations=3`、`answer_runs=4`、`audit_events=10`；audit event 分布为 `answer_run_collected=4`、`project_bootstrap_created=1`、`visibility_score_snapshot_created=1`、`report_export_created=1`、`action_plan_created=1`、`retest_comparison_created=1`、`content_engine_fixture_created=1`；验证后已执行 `docker compose -p geno-runtime-content-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
  - `git diff --check`：通过，无空白差异问题。
- 当前边界：本切片目标是 M7 runtime 写入和读取；pgvector 检索、LLMGateway 真实生成、人工审核 UI、OAuth/API 集成、CMS 草稿创建、分发状态回写和内容效果评估仍在后续 M7 产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的 M7、全局 DoD 与审计链风险项。

## 2026-06-10 · Runtime Console MVP

- 目标：把 Next.js 控制台从静态模块状态页推进到可演示的 runtime 控制台，直接读取 evidence / score / citation graph / report / action plan / content engine API，展示证据、评分解释、信源缺口、报告快照、行动复测和内容草稿摘要。
- 范围：Next.js server-rendered console；Docker Web 内部 API 地址；Web 可重复构建；Next/PostCSS 安全补丁；Playwright 桌面/移动端截图验证；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：替换静态模块卡片，新增 Runtime Evidence Console，服务端读取 `/v1/evidence-runs/runtime`、`/v1/visibility-scores/runtime`、`/v1/citation-graphs/runtime`、`/v1/reports/runtime`、`/v1/action-plans/runtime`、`/v1/content-engines/runtime`，并在无数据/接口失败时提示运行 worker。
  - `apps/web/app/globals.css`：重做工作台式布局，包含 6 个指标块和 6 个 runtime 面板，覆盖桌面/移动端响应式约束。
  - `infra/docker-compose.yml`：Web 服务新增 `API_INTERNAL_BASE_URL=http://api:8000`，容器内 SSR 使用内部服务地址，页面展示浏览器可访问的 `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000`。
  - `apps/web/package.json` 与 `apps/web/package-lock.json`：Next 升级到 `15.5.19`，并用 npm `overrides` 将 `postcss` 固定到 `8.5.10`；`npm audit --omit=dev --registry=https://registry.npmjs.org --json` 返回 0 vulnerabilities。
  - `apps/web/Dockerfile` 与 `.dockerignore`：Web 镜像改用 `npm ci`，并排除本机 `node_modules` / `.next` 等构建产物，保证 Docker 构建可复现。
  - `apps/web/tsconfig.json`、`apps/web/next-env.d.ts`：Next build 自动补齐 TypeScript plugin 与 `.next/types` include。
- 验证项：
  - `make test`：通过，60 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Web 服务包含 `API_INTERNAL_BASE_URL` 与 `NEXT_PUBLIC_API_BASE_URL`。
  - `npm run build`：通过，Next.js `15.5.19` 编译、类型检查、动态首页构建均成功。
  - `npm audit --omit=dev --registry=https://registry.npmjs.org --json`：通过，prod dependencies 0 vulnerabilities；`npm ls postcss` 确认 `postcss@8.5.10 overridden`。
  - `docker compose -p geno-runtime-console-test -f infra/docker-compose.yml up --build -d api web`：通过，API/Web/PostgreSQL/MinIO 均启动；Web Docker `npm ci` 返回 0 vulnerabilities。
  - `docker compose -p geno-runtime-console-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，写入 4 条 evidence、1 个 score、3 个 source gaps、1 个 report、3 个 actions、3 个 content drafts。
  - `curl -fsS http://localhost:3000`：通过，HTML 含 `Runtime Evidence Console`、`Evidence runs=4`、`Final score=87.35`、`Source gaps=3`、`Open actions=3`、`Content drafts=3`。
  - Playwright Firefox 桌面 `1440x1100` 与移动端 `390x1200`：通过，断言 6 个 `.metric`、6 个 `.panel`，关键文本 `Runtime Evidence Console`、`http://localhost:8000`、`87.35`、`Citation Graph`、`Action & Retest`、`Content Engine`、`PENDING_HUMAN_REVIEW` 均存在；截图保存为 `/tmp/geno-runtime-console-desktop.png` 与 `/tmp/geno-runtime-console-mobile.png`。
  - 验证后已执行 `docker compose -p geno-runtime-console-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
- 当前边界：本切片完成可演示 Runtime Console 摘要页；项目创建/编辑、筛选、详情钻取、报告下载、人工审核动作、实时刷新、鉴权/RLS 和 design partner 真实数据仍在后续产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的全局 DoD、M4 看板、M5 报告、M6 Action/Retest、M7 Content Engine 与审计链风险项。

## 2026-06-10 · Runtime Traceability Detail API + worker bundle persistence

- 目标：补齐“可审计、可溯源、可解释”在 runtime 层的详情查询断点，让 worker 持久化分析链路时同步保存 `TraceabilityBundle`，并通过 API 一次性读回报告、评分解释、原始证据、图谱、action、content、audit event 与 evidence links。
- 范围：runtime traceability read model；`traceability_bundles`、`evidence_links`、`report_exports`、`visibility_score_snapshots`、`score_contributions`、`answer_runs`、`raw_answers`、`answer_citations`、`evidence_assets`、`source_graphs`、`action_recommendations`、`content_drafts`、`audit_events` 聚合查询；worker `--persist-analysis` 保存 TraceabilityBundle；API/core tests；README/PLAN 同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeTraceabilityDetail`，聚合 `traceability_bundle`、`RuntimeScoreSnapshot`、`RuntimeEvidenceRun`、`RuntimeCitationGraph`、`RuntimeContentDraft`、actions、audit events 与 evidence links。
  - `packages/geno_core/geno_core/repository.py`：新增 `get_runtime_traceability_detail()`，支持按 `project_id` 或 `report_export_id` 定位 bundle；新增 `_load_report_export_by_id()`、`_load_score_snapshot_by_id()`、`_load_evidence_run_by_id()`、`_load_action_recommendation_by_id()`、`_load_runtime_content_draft_by_id()`、`_load_audit_event_by_id()`。
  - `workers/collector_worker/run_collection_slice.py`：`--persist-analysis` 在保存 content engine 后调用 `build_traceability_bundle()` 和 `save_traceability_bundle()`，输出 `traceability_bundle_id` 与 evidence link 计数。
  - `apps/api/geno_api/main.py`：新增 `/v1/traceability/runtime`，缺少 `DATABASE_URL` 时返回 503，找不到 bundle 时返回 404；contracts 增加 `RuntimeTraceabilityDetail` 与 runtime traceability endpoint。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：新增 runtime traceability detail repository read model 测试、runtime traceability 503 测试与 contracts 断言。
  - `README.md`、`PROJECT-PLAN.md`：同步 runtime traceability API、worker bundle persistence 与前端详情 UI 仍待接的边界。
- 验证项：
  - `python3 -m py_compile packages/geno_core/geno_core/repository.py apps/api/geno_api/main.py workers/collector_worker/run_collection_slice.py`：通过。
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_api_contracts tests.test_core_contracts.CoreContractsTest.test_postgres_repository_reads_runtime_traceability_detail`：通过，22 个 API/目标 core tests OK。
  - `make test`：通过，62 个 contract/API/worker tests 全部 OK。
- 当前边界：本切片完成 runtime traceability 后端读写闭环；Next.js 详情钻取页、报告下载页、按节点展开的审计可视化、鉴权/RLS、多租户隔离、真实 design partner 数据和外部采集 E2E 仍在后续产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的全局 DoD、P0c 可售报告验收、M0 runtime 底座与审计链风险项。

## 2026-06-10 · Runtime Console Traceability Detail panel

- 目标：把 `/v1/traceability/runtime` 接入 Next.js Runtime Console，让可审计溯源链不只停留在 API，而能在控制台首屏看到报告、评分、证据、图谱、action、content、audit event 与 evidence link 的聚合摘要。
- 范围：Next.js server-rendered console；runtime traceability fetch；Traceability Detail 全宽面板；桌面/移动端响应式样式；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：新增 `TraceabilityDetail` 类型，`endpoints.traceability=/v1/traceability/runtime`；RuntimeData 增加 traceability；指标区新增 `Trace links`；Dashboard 新增 `Traceability Detail` 全宽面板，展示 bundle explanation、各类链路节点数量、前 5 条 `EvidenceLink` 与前 5 条 `AuditEvent`。
  - `apps/web/app/globals.css`：metrics 调整为 7 列；新增 `.panelWide`、`.traceGrid`、`.traceSummary`、`.traceColumn` 和移动端布局，保证 traceability 面板在桌面/移动端不溢出。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Console 已展示 Traceability Detail，但交互式节点级详情钻取仍待接。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建均成功。
  - `make test`：通过，62 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-trace-console-test -f infra/docker-compose.yml up --build -d api web` + `docker compose -p geno-trace-console-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `traceability_bundle_id=99acbd27-a178-53da-9ecc-eaab1cd66e43`、`evidence_links=18`。
  - `GET http://localhost:8000/v1/traceability/runtime`：通过，API 返回 `report_export_ids`、`score_snapshot_ids`、4 个 `answer_run_ids`、`raw_answer_ids`、`answer_citation_ids`、`evidence_asset_ids` 与 evidence links。
  - Playwright 桌面 `1440x1200` 与移动端 `390x1400`：通过，断言 7 个 `.metric`、7 个 `.panel`，关键文本 `Traceability Detail`、`Trace links`、`Evidence Links`、`Audit Trail`、`contains_score_snapshot`、`answer_run_collected` 和 `Report worker-runtime-v1 traces 4 answer runs` 均可见；截图保存为 `/tmp/geno-trace-console-desktop.png` 与 `/tmp/geno-trace-console-mobile.png`。
  - 验证后已执行 `docker compose -p geno-trace-console-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
- 当前边界：本切片完成 traceability 聚合详情在控制台摘要级展示；点击节点展开原始 answer/citation/asset、报告下载页、筛选、鉴权/RLS、真实 design partner 数据和审计图谱可视化仍在后续产品化任务中。
- 相关计划：`PROJECT-PLAN.md` 的全局 DoD、M4 看板、P0c 可售报告验收与审计链风险项。

## 2026-06-10 · Runtime Console node-level traceability drilldown

- 目标：把 Traceability Detail 从摘要面板推进到节点级 details 钻取，使控制台能展开查看 score components、answer evidence、citation/asset nodes、actions/content drafts 与 audit event nodes。
- 范围：Next.js server-rendered console；TraceabilityDetail 前端类型扩展；`<details>` 节点级钻取 UI；桌面/移动端滚动与换行约束；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：扩展 `EvidenceRun`、`ScoreSnapshot`、`TraceabilityDetail` 类型，增加 `shortId()`；在 `Traceability Detail` 面板新增 `Node Drilldown`，包含 `Score Components`、`Answer Evidence`、`Citation & Asset Nodes`、`Actions & Content Drafts`、`Audit Event Nodes` 五组 details。
  - `apps/web/app/globals.css`：新增 `.traceDrilldown`、`.detailGrid`、`details`、`summary`、`.nodeList` 等样式，控制节点列表滚动、换行、层级和移动端单列布局。
  - `README.md`、`PROJECT-PLAN.md`：同步节点级 details 钻取已落；筛选、深链路、报告下载和图谱可视化仍待接。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建均成功。
  - `make test`：通过，62 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-node-drilldown-test -f infra/docker-compose.yml up --build -d api web` + `docker compose -p geno-node-drilldown-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `traceability_bundle_id=17547859-2c49-5b60-9adb-95a8f14d6204`、`evidence_links=18`。
  - Playwright 桌面 `1440x1500` 与移动端 `390x1600`：通过，断言 `Node Drilldown`、`Score Components`、`Answer Evidence`、`Citation & Asset Nodes`、`Actions & Content Drafts`、`Audit Event Nodes`、`MentionScore`、`raw_payload_hash` 和 `answer_run_collected` 均可见；断言 5 个 `details` 与至少 10 个 `.nodeList li`；截图保存为 `/tmp/geno-node-drilldown-desktop.png` 与 `/tmp/geno-node-drilldown-mobile.png`。
  - 验证后已执行 `docker compose -p geno-node-drilldown-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
- 当前边界：本切片完成 SSR details 级节点展开；还不是完整交互式图谱、跨页面深链路、筛选导出、报告下载、鉴权/RLS 或真实 design partner 数据浏览器验收。
- 相关计划：`PROJECT-PLAN.md` 的全局 DoD、M4 看板、P0c 可售报告验收与审计链风险项。

## 2026-06-10 · Runtime Report artifact download API + console links

- 目标：补齐 M5 报告下载缺口，让冻结的 `ReportExport` 不只在 runtime API 中可读，还能通过 HTTP 下载 Markdown/CSV artifact，并在 Runtime Console 中提供下载入口。
- 范围：runtime report artifact read model；Markdown/CSV artifact 渲染；FastAPI 下载 endpoint；Report Snapshot 下载链接；core/API tests；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeReportArtifact`，记录 `report_export`、`artifact_type`、`filename`、`media_type`、`content` 与 `content_hash`。
  - `packages/geno_core/geno_core/repository.py`：新增 `get_runtime_report_artifact()`；复用冻结 `ReportExport` 与 `_load_runtime_report_export()`，渲染 Markdown/CSV；Markdown 包含方法说明、评分、证据附录、Citation Graph、source gaps、audit summary 与 traceability 说明；CSV 包含 report/answer run/prompt/platform/geo/access_method/trigger fields。
  - `apps/api/geno_api/main.py`：新增 `/v1/reports/runtime/{report_export_id}/artifact?type=markdown|csv`，返回 `Content-Disposition` 下载头和 `X-GENO-Report-Artifact-Hash`；contracts 增加 `RuntimeReportArtifact` 和 artifact endpoint。
  - `apps/web/app/page.tsx`、`apps/web/app/globals.css`：Report Snapshot 面板新增 `Download Markdown` / `Download CSV` 链接和下载按钮样式。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：新增 Markdown/CSV artifact 渲染测试、artifact endpoint 503 测试和 contracts 断言。
  - `README.md`、`PROJECT-PLAN.md`：同步 runtime report artifact API 与控制台下载入口；PDF renderer 与 MinIO 上传仍列为后续任务。
- 验证项：
  - `python3 -m py_compile packages/geno_core/geno_core/repository.py apps/api/geno_api/main.py`：通过。
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_core_contracts.CoreContractsTest.test_postgres_repository_renders_runtime_report_artifact tests.test_core_contracts.CoreContractsTest.test_postgres_repository_renders_runtime_report_csv_artifact tests.test_api_contracts.ApiContractsTest.test_runtime_report_artifact_endpoint_requires_persistence_config tests.test_api_contracts.ApiContractsTest.test_contracts_include_m7_content_integrations`：通过，4 个目标测试 OK。
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建均成功。
  - `make test`：通过，65 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-report-download-test -f infra/docker-compose.yml up --build -d api web` + `docker compose -p geno-report-download-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `report_export_id=335c876f-9fef-56fc-a449-a3fd4e134506`。
  - `GET /v1/reports/runtime/335c876f-9fef-56fc-a449-a3fd4e134506/artifact?type=markdown`：通过，返回 `Content-Disposition: attachment; filename="worker-runtime-v1.md"`、`Content-Type: text/markdown; charset=utf-8`、`X-GENO-Report-Artifact-Hash`，正文含 `GENO AU Evidence Report`、`ReportExport -> VisibilityScoreSnapshot`。
  - `GET /v1/reports/runtime/335c876f-9fef-56fc-a449-a3fd4e134506/artifact?type=csv`：通过，返回 `Content-Disposition: attachment; filename="worker-runtime-v1.csv"`、`Content-Type: text/csv; charset=utf-8`、`X-GENO-Report-Artifact-Hash`，正文含 `answer_run_id` 与 prompt text。
  - Playwright 桌面 `1440x1500` 与移动端 `390x1600`：通过，断言 `Report Snapshot`、`Download Markdown`、`Download CSV` 均可见，且链接 href 分别指向 `/v1/reports/runtime/{report_export_id}/artifact?type=markdown|csv`；截图保存为 `/tmp/geno-report-download-desktop.png` 与 `/tmp/geno-report-download-mobile.png`。
  - 验证后已执行 `docker compose -p geno-report-download-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
- 当前边界：本切片完成 Markdown/CSV runtime artifact 的 HTTP 下载；PDF renderer、MinIO/S3 真上传、报告下载历史页、权限校验、白标模板和真实 design partner 报告样式仍待接。
- 相关计划：`PROJECT-PLAN.md` 的 M5、P0c 可售报告验收和审计链风险项。

## 2026-06-10 · Runtime Report basic PDF artifact download

- 目标：继续补齐 M5 报告交付能力，在已有 Markdown/CSV runtime artifact 基础上增加 PDF 下载，使客户可交付报告具备基础 PDF 产物。
- 范围：`RuntimeReportArtifact` binary content 支持；冻结 `ReportExport` 再生成基础文本型 PDF；FastAPI artifact endpoint 放开 `type=pdf`；Runtime Console 增加 PDF 下载入口；core/API tests；README/PLAN 同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：`RuntimeReportArtifact.content` 支持 `str | bytes`，覆盖文本与二进制 artifact。
  - `packages/geno_core/geno_core/repository.py`：新增基础 PDF renderer，复用 `_render_runtime_report_markdown()` 作为内容源，生成合法 `%PDF-1.4` 文档；artifact hash 改为 SHA-256，适配 Markdown/CSV/PDF 三类内容。
  - `apps/api/geno_api/main.py`：`/v1/reports/runtime/{report_export_id}/artifact` 支持 `type=pdf`，返回 `application/pdf` 与下载头。
  - `apps/web/app/page.tsx`：Report Snapshot 面板新增 `Download PDF`。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：新增 PDF artifact 渲染测试与 endpoint 503 contract 测试。
  - `README.md`、`PROJECT-PLAN.md`：同步 Markdown/CSV/PDF 下载入口，并把“白标 PDF 模板 renderer / MinIO 上传”保留为后续增强。
- 验证项：
  - `python3 -m py_compile packages/geno_core/geno_core/repository.py apps/api/geno_api/main.py`：通过。
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_core_contracts.CoreContractsTest.test_postgres_repository_renders_runtime_report_artifact tests.test_core_contracts.CoreContractsTest.test_postgres_repository_renders_runtime_report_csv_artifact tests.test_core_contracts.CoreContractsTest.test_postgres_repository_renders_runtime_report_pdf_artifact tests.test_api_contracts.ApiContractsTest.test_runtime_report_artifact_endpoint_requires_persistence_config tests.test_api_contracts.ApiContractsTest.test_runtime_report_pdf_artifact_endpoint_requires_persistence_config`：通过，5 个目标测试 OK。
  - `make test`：通过，67 个 contract/API/worker tests 全部 OK。
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建均成功。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-pdf-artifact-test -f infra/docker-compose.yml up --build -d api web` + `docker compose -p geno-pdf-artifact-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `report_export_id=335c876f-9fef-56fc-a449-a3fd4e134506`。
  - `GET /v1/reports/runtime/335c876f-9fef-56fc-a449-a3fd4e134506/artifact?type=pdf`：通过，返回 `Content-Disposition: attachment; filename="worker-runtime-v1.pdf"`、`Content-Type: application/pdf`、`X-GENO-Report-Artifact-Hash: 41c00e48f8d9ec9811898e85e06738fd0ba260cced3de2dc74b3bad976956236`；下载文件大小 2933 bytes，`sha256` 前缀 `41c00e48f8d9ec98`，以 `%PDF-1.4` 开头且包含 `%%EOF`。
  - Playwright 桌面 `1440x1500` 与移动端 `390x1600`：通过，断言 `Report Snapshot`、`Download Markdown`、`Download CSV`、`Download PDF` 均可见，且 PDF 链接 href 指向 `/v1/reports/runtime/335c876f-9fef-56fc-a449-a3fd4e134506/artifact?type=pdf`；截图保存为 `/tmp/geno-pdf-download-desktop.png` 与 `/tmp/geno-pdf-download-mobile.png`。
  - 验证后已执行 `docker compose -p geno-pdf-artifact-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
- 当前边界：本切片提供基础文本型 PDF artifact，可审计内容与 Markdown 同源；尚不是品牌白标排版、分页目录、图表渲染、对象存储归档或报告历史管理。
- 相关计划：`PROJECT-PLAN.md` 的 M5、P0c 可售报告验收和报告交付风险项。

## 2026-06-10 · Runtime Report artifact MinIO/S3-compatible archive

- 目标：补齐 M5 报告产物归档缺口，让 worker 在生成冻结 `ReportExport` 后把 Markdown/CSV/PDF artifact 写入 MinIO/S3-compatible 对象存储，并用审计事件记录归档结果。
- 范围：共享 PDF renderer；`EvidenceReport` 增加 `pdf_content`；S3-compatible object store client；worker artifact archive；Compose 对象存储凭证；Makefile 默认持久化分析链路；core/API tests；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/report.py`：`MarkdownCsvReportExporter` 仍保持兼容类名，但现在同时生成 Markdown/CSV/PDF 三类 artifact；`ReportExport.pdf_url` 由导出器填入；审计 `after` 增加 artifact URL 与 hash。
  - `packages/geno_core/geno_core/repository.py`：runtime PDF artifact 复用 `report.py` 的 `render_markdown_pdf()`，避免重复维护 PDF 渲染逻辑。
  - `packages/geno_core/geno_core/object_store.py`：新增最小 S3-compatible object store client，支持 bucket ensure、SigV4 PUT、S3 URI 解析、SHA-256 content hash 与 `archive_report_artifacts()`。
  - `packages/geno_core/geno_core/runtime.py`、`__init__.py`：新增 `build_object_store_from_env()`。
  - `workers/collector_worker/run_collection_slice.py`：`--persist-analysis` 生成报告后，在配置 `OBJECT_STORE_ENDPOINT` 时归档 Markdown/PDF/CSV，并写 `report_artifacts_archived` 审计事件；worker 输出 `report_artifacts` summary。
  - `infra/docker-compose.yml`：API/worker 注入 MinIO 连接所需 `OBJECT_STORE_BUCKET`、`OBJECT_STORE_ACCESS_KEY`、`OBJECT_STORE_SECRET_KEY`、`OBJECT_STORE_REGION`。
  - `Makefile`：`worker-fixture-persist` 默认启用 `--persist-analysis`，覆盖完整可演示链路。
  - `apps/api/geno_api/main.py`、`tests/*`：fixture report endpoint 增加 PDF metadata；contracts 增加 object store builder；core tests 覆盖报告 PDF 产物和三 artifact 归档请求。
  - `README.md`、`PROJECT-PLAN.md`：同步 M5 Markdown/CSV/PDF 导出、MinIO/S3-compatible artifact 归档和失败即退出边界。
- 验证项：
  - `python3 -m py_compile packages/geno_core/geno_core/report.py packages/geno_core/geno_core/repository.py packages/geno_core/geno_core/object_store.py packages/geno_core/geno_core/runtime.py apps/api/geno_api/main.py workers/collector_worker/run_collection_slice.py`：通过。
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_core_contracts.CoreContractsTest.test_m5_report_export_freezes_snapshot_and_evidence_refs tests.test_core_contracts.CoreContractsTest.test_report_artifacts_archive_to_s3_compatible_store tests.test_core_contracts.CoreContractsTest.test_postgres_repository_renders_runtime_report_pdf_artifact tests.test_api_contracts.ApiContractsTest.test_m5_report_fixture_endpoint tests.test_api_contracts.ApiContractsTest.test_contracts_include_m7_content_integrations`：通过，5 个目标测试 OK。
  - `make test`：通过，68 个 contract/API/worker tests 全部 OK。
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建均成功。
  - `make docker-config`：通过，Compose 配置包含 API/worker 的 `OBJECT_STORE_ENDPOINT`、bucket、access key、secret key 和 region。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-artifact-archive-test -f infra/docker-compose.yml up --build -d api web` + `docker compose -p geno-artifact-archive-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 输出 `report_artifacts.enabled=true`，三份对象分别为 `s3://geno-reports/6624961f-36ae-539b-9d48-51619b42e37e/worker-runtime-v1.md`、`.pdf`、`.csv`，并输出各自 SHA-256 与 ETag。
  - MinIO HEAD 验证：`worker-runtime-v1.md`、`worker-runtime-v1.pdf`、`worker-runtime-v1.csv` 三个对象均存在。
  - `GET /v1/reports/runtime/335c876f-9fef-56fc-a449-a3fd4e134506/artifact?type=markdown|csv|pdf`：三类下载均返回 200；PDF 响应 `Content-Type: application/pdf`，文件以 `%PDF-1.4` 开头且包含 `%%EOF`。
  - PostgreSQL 审计事件验证：`audit_events` 包含 `report_artifacts_archived=1`，同时保留 `report_export_created=1`、`visibility_score_snapshot_created=1`、`answer_run_collected=4` 等事件。
  - Playwright 桌面 `1440x1500`：通过，断言 Report Snapshot 与 `Download Markdown` / `Download CSV` / `Download PDF` 均可见且 href 指向 runtime artifact endpoint；截图保存为 `/tmp/geno-artifact-archive-console.png`。
  - 验证后已执行 `docker compose -p geno-artifact-archive-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
- 当前边界：本切片完成对象存储归档和审计事件；还不是白标 PDF 模板、报告历史管理、权限签名下载 URL 或多租户 bucket policy。
- 相关计划：`PROJECT-PLAN.md` 的 M0 对象存储底座、M5 可售报告与审计链风险项。

## 2026-06-10 · Runtime AU project bootstrap create/read API

- 目标：补齐 M1/P0a 项目初始化的 runtime API 缺口，让 AU DTC design partner 启动包不只作为 fixture 或 worker 内部流程存在，而能通过 API 创建并从 PostgreSQL 读回项目、租户、品牌、竞品、prompt 计数与审计事件。
- 范围：runtime project read model；AU/DTC project bootstrap create endpoint；runtime project list endpoint；contracts；core/API tests；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeProject`、`RuntimeProjectPage`。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_projects()` 与 `_load_runtime_project()`，聚合 `projects -> tenants -> brand_entities -> competitor_entities -> prompt_questions -> audit_events`。
  - `apps/api/geno_api/main.py`：新增 `POST /v1/projects/runtime/au/dtc-ecommerce`，调用 `build_au_project_bootstrap()` 并持久化；新增 `GET /v1/projects/runtime`，支持 `market_code/limit/offset` 查询；contracts 增加 runtime project 类型和 endpoint。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：新增 repository runtime project 读取测试、runtime project endpoint 503 测试和 contracts 断言。
  - `README.md`、`PROJECT-PLAN.md`：同步 runtime project create/read API 与 M1 进度边界。
- 验证项：
  - `python3 -m py_compile packages/geno_core/geno_core/bootstrap.py packages/geno_core/geno_core/models.py packages/geno_core/geno_core/repository.py apps/api/geno_api/main.py`：通过。
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_core_contracts.CoreContractsTest.test_m1_project_bootstrap_audit_event_id_is_stable tests.test_core_contracts.CoreContractsTest.test_postgres_repository_reads_runtime_project_page tests.test_api_contracts.ApiContractsTest.test_runtime_project_create_endpoint_requires_persistence_config tests.test_api_contracts.ApiContractsTest.test_runtime_projects_endpoint_requires_persistence_config tests.test_api_contracts.ApiContractsTest.test_contracts_include_m7_content_integrations`：通过，5 个目标测试 OK。
  - `make test`：通过，71 个 contract/API/worker tests 全部 OK。
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建均成功。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-runtime-project-test -f infra/docker-compose.yml up --build -d api`：通过，API/PostgreSQL/MinIO 启动成功。
  - Docker runtime API E2E：连续两次 `POST /v1/projects/runtime/au/dtc-ecommerce` 返回相同 `project_id=6624961f-36ae-539b-9d48-51619b42e37e` 与相同稳定 `audit_event_id=f2905799-b8aa-599d-8930-53ef259702e1`；`GET /v1/projects/runtime?market_code=AU&limit=20` 返回 `total_count=1`、`prompt_count=100`、`competitor_count=4`、brand `ExampleBrand`、audit event `project_bootstrap_created`。
  - PostgreSQL 计数验证：`projects=1`、`prompt_questions=100`、`competitor_entities=4`、`project_bootstrap_created` audit events `=1`，证明重复 POST 不重复污染项目或启动审计事件。
  - 验证后已执行 `docker compose -p geno-runtime-project-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
- 当前边界：本切片完成固定 AU/DTC design partner 模板的 API 创建和读取；还不是可配置项目创建表单、控制台 CRUD、鉴权/RLS、角色权限 UI 或多租户 self-service onboarding。
- 相关计划：`PROJECT-PLAN.md` 的 M1、全局可追溯 DoD 与 P0a design partner 试点门槛。

## 2026-06-10 · Runtime Prompt Pack read API

- 目标：补齐 M1/P0a prompt 可审计读取缺口，让 AU 项目创建后能直接通过 runtime API 分页/过滤读回 100 条 `PromptQuestion`，而不是只能从采集后的 evidence 页面间接看到 prompt 文本。
- 范围：runtime prompt page read model；prompt list endpoint；contracts；core/API tests；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimePromptPage`。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_prompts()`，支持 `project_id`、`market_code`、`intent_type`、`city`、`status`、`limit`、`offset` 过滤，按 `priority/id` 稳定排序。
  - `apps/api/geno_api/main.py`：新增 `GET /v1/prompts/runtime`，contracts 增加 prompt page 类型和 endpoint。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：新增 repository prompt page 测试、endpoint 503 测试和 contracts 断言。
  - `README.md`、`PROJECT-PLAN.md`：同步 runtime prompt API 与 M1 可追溯状态。
- 验证项：
  - `python3 -m py_compile packages/geno_core/geno_core/models.py packages/geno_core/geno_core/repository.py apps/api/geno_api/main.py`：通过。
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_core_contracts.CoreContractsTest.test_postgres_repository_reads_runtime_prompt_page tests.test_api_contracts.ApiContractsTest.test_runtime_prompts_endpoint_requires_persistence_config tests.test_api_contracts.ApiContractsTest.test_contracts_include_m7_content_integrations`：通过，3 个目标测试 OK。
  - `make test`：通过，74 个 contract/API/worker tests 全部 OK。
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建均成功。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-runtime-prompt-test -f infra/docker-compose.yml up --build -d api`：通过，API/PostgreSQL/MinIO 启动成功。
  - Docker runtime API E2E：`POST /v1/projects/runtime/au/dtc-ecommerce` 创建项目后，`GET /v1/prompts/runtime?project_id=6624961f-36ae-539b-9d48-51619b42e37e&intent_type=brand_awareness&limit=20` 返回 `total_count=10`，首条为 `Is ExampleBrand good in Australia?`，`prompt_version=au_dtc_ecommerce_v1`；`GET /v1/prompts/runtime?city=Brisbane` 返回 `total_count=7` 且全部记录 city 为 `Brisbane`。
  - PostgreSQL 计数验证：10 个 intent type 各 10 条 prompt，总计 100 条，覆盖 `brand_awareness`、`category_recommendation`、`city_category_recommendation`、`competitor_comparison`、`review_reputation`、`purchase_decision`、`price`、`service_coverage`、`local_trust`、`alternative`。
  - 验证后已执行 `docker compose -p geno-runtime-prompt-test -f infra/docker-compose.yml down -v` 清理测试容器与卷。
- 当前边界：本切片完成 prompt 读取 API；还不是 prompt 编辑、批量导入、版本 diff、控制台 prompt 管理页或 prompt 级权限审计。
- 相关计划：`PROJECT-PLAN.md` 的 M1 Prompt Pack、全局可追溯 DoD 与 P0a design partner 试点门槛。

## 2026-06-10 · Runtime Console Prompt Pack panel

- 目标：把 `/v1/prompts/runtime` 接入 Next.js Runtime Console，让 M1 Prompt Pack 不只在 API 层可审计读回，也能在控制台首屏看到 100 条 AU prompt 的覆盖情况和样本文本。
- 范围：Next.js server-rendered console；runtime prompt fetch；Prompt Pack 面板；指标区 prompt 总数；桌面/移动端样式；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：新增 `RuntimePrompt` 类型，`endpoints.prompts=/v1/prompts/runtime?market_code=AU&limit=20`；`RuntimeData` 增加 prompts；指标区新增 `Prompts`；Dashboard 新增 `Prompt Pack` 面板，展示 total/loaded、intent type 覆盖、city 覆盖、brand/language 和前 5 条 prompt 样本。
  - `apps/web/app/globals.css`：metrics 调整为 8 列；新增 prompt 列表滚动与 `<small>` 元信息样式，避免面板文本在桌面/移动端溢出。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Console 已展示 Prompt Pack。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建成功。
  - `make test`：通过，74 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-prompt-console-test -f infra/docker-compose.yml up --build -d api web` + `docker compose -p geno-prompt-console-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 写入 `prompt_questions=100`、`raw_evidence_records=4`、`traceability_bundle_id=54274a84-e18a-5267-ba56-2e362e8152e2`。
  - `GET /v1/prompts/runtime?market_code=AU&limit=20`：通过，返回 `total_count=100`、`records=20`、首条 prompt `Best DTC ecommerce products in Australia`，`prompt_version=au_dtc_ecommerce_v1`。
  - Playwright 桌面 `1440x1600`：通过，断言 `Prompt Pack`、`Total prompts`、`Intent types`、`Cities`、`Is ExampleBrand good in Australia?` 与 `Traceability Detail` 均可见；截图保存为 `/tmp/geno-prompt-console.png`。
  - Playwright 移动 `390x1200`：通过，断言 `Prompt Pack`、`Total prompts`、`Is ExampleBrand good in Australia?` 与 `Traceability Detail` 均可见；截图保存为 `/tmp/geno-prompt-console-mobile.png`。
- 当前边界：本切片完成 prompt pack 只读可视化；还不是 prompt 编辑、批量导入、版本 diff、prompt 级审计、项目创建表单或多租户权限控制台。
- 相关计划：`PROJECT-PLAN.md` 的 M1 Prompt Pack、M4 监测看板和全局可追溯 DoD。

## 2026-06-10 · Runtime Console Project Bootstrap panel

- 目标：把 `/v1/projects/runtime` 与 `POST /v1/projects/runtime/au/dtc-ecommerce` 接入 Next.js Runtime Console，让固定 AU/DTC design partner 启动包可以从控制台创建，并从 PostgreSQL 读回项目、品牌、竞品、prompt 计数和启动审计事件。
- 范围：Next.js server action；runtime project fetch；单 endpoint 降级读取；Project Bootstrap 面板；创建按钮；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：新增 `RuntimeProject` 类型、`endpoints.projects=/v1/projects/runtime?market_code=AU&limit=1`、`createAuRuntimeProject()` server action；`fetchRuntimeData()` 改为逐 endpoint 降级，`/v1/traceability/runtime` 404 不再清空整页；指标区新增 `Projects`；Dashboard 新增 `Project Bootstrap` 面板，展示 tenant/project/market/industry/brand/category/prompt/competitor/audit，并提供 `Create AU project` 按钮。
  - `apps/web/app/globals.css`：metrics 调整为 9 列；新增 `.actionButton` 与 `.auditLine` 样式。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Console 已支持固定 AU/DTC 启动包创建与只读项目摘要。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建成功。
  - `make test`：通过，74 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-project-console-test -f infra/docker-compose.yml up --build -d api web`：通过，空库 API/Web 启动成功。
  - Playwright 桌面 `1440x1600`：通过，先看到 `Project Bootstrap` 与 `Create AU project`，点击按钮后页面读回 `Design Partner AU`、`ExampleBrand`、`dtc_ecommerce`、`Competitors`、`Emma Sleep`、`project_bootstrap_created`、`Prompt Pack` 与 prompt 样本；截图保存为 `/tmp/geno-project-bootstrap-console.png`。
  - Runtime API 验证：`GET /v1/projects/runtime?market_code=AU&limit=20` 返回 `total_count=1`、`prompt_count=100`、`competitors=4`、audit `project_bootstrap_created`；`GET /v1/prompts/runtime?market_code=AU&limit=20` 返回 `total_count=100`、`records=20`、首条 prompt `Best DTC ecommerce products in Australia`。
  - Playwright 移动 `390x1300`：通过，断言 `Project Bootstrap`、`Create AU project`、`Design Partner AU`、`ExampleBrand` 与 `Prompt Pack` 均可见；截图保存为 `/tmp/geno-project-bootstrap-console-mobile.png`。
- 当前边界：本切片完成固定 AU/DTC 模板创建入口和只读摘要；还不是可配置项目创建表单、品牌/竞品 CRUD、角色权限、RLS/鉴权或多租户 self-service onboarding。
- 相关计划：`PROJECT-PLAN.md` 的 M1 租户/项目/品牌/竞品、M1 Prompt Pack 和全局可追溯 DoD。

## 2026-06-10 · Runtime Console Evidence Runs detail panel

- 目标：把 `/v1/evidence-runs/runtime` 从摘要卡片推进到可审计明细展示，让控制台能直接查看每条 answer run 的 prompt、平台、城市、access method、触发状态、采样、collector、cost、raw hash、citation、asset 和 audit。
- 范围：Next.js server-rendered console；`EvidenceRun` 类型补全；全宽 Evidence Runs 面板；桌面/移动端明细样式；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：扩展 `EvidenceRun` 类型，补入 `access_method`、`answer_present`、`surface_triggered`、`sample_index/sample_size`、`collector_backend_id`、`collector_version`、`prompt_priority`、`prompt_version`、`collector_logs`、`collection_cost` 与 audit 字段；新增 `boolText()`；新增全宽 `Evidence Runs` 面板，每条 run 用 `<details>` 展开，并展示 facts、citations、assets、collector log 和 audit event。
  - `apps/web/app/globals.css`：新增 `.evidenceGrid`、`.evidenceItem`、`.evidenceBody`、`.evidenceFacts`、`.evidenceColumns` 响应式布局。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Console 已展示 Evidence Runs 明细。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建成功。
  - `make test`：通过，74 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题。
  - `docker compose -p geno-evidence-console-test -f infra/docker-compose.yml up --build -d api web` + `docker compose -p geno-evidence-console-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 写入 `raw_evidence_records=4`、`score_contributions=8`、`traceability_bundle_id=0131ca46-4f95-5f30-a35d-0e5a5decd503`。
  - `GET /v1/evidence-runs/runtime?limit=5`：通过，返回 `total_count=4`、`records=4`、首条 `platform=chatgpt`、`access_method=official_api`、`answer_present=true`、`citations=3`、`evidence_assets=2`、audit `answer_run_collected` 与 `raw_payload_hash`。
  - Playwright 桌面 `1440x1800`：通过，断言 `Evidence Runs`、`4 stored runs`、`official_api`、`openai.web_search.fixture`、`Raw hash`、`answer_run_collected`、`collection_completed`、`Citations`、`Assets & Audit` 与 prompt 文本均可见；截图保存为 `/tmp/geno-evidence-runs-console.png`。
  - Playwright 移动 `390x1500`：通过，断言 `Evidence Runs`、`4 stored runs`、`official_api`、`Raw hash` 与 `Assets & Audit` 均可见；截图保存为 `/tmp/geno-evidence-runs-console-mobile.png`。
- 当前边界：本切片完成 evidence runs 只读明细；还不是筛选、排序、单 answer run 深链路详情页、截图/HTML 预览、人工补录或权限审计 UI。
- 相关计划：`PROJECT-PLAN.md` 的 M2a Raw Evidence Store、M4 监测看板和全局可追溯 DoD。

## 2026-06-10 · Runtime Console Score Contributions detail panel

- 目标：把 `/v1/visibility-scores/runtime` 的 `ScoreContribution` 从摘要数字推进到完整解释包展示，让控制台直接可见 8 个评分组件的原始分、权重、加权贡献、分母、正负证据、confidence note 和关联 answer run。
- 范围：Next.js server-rendered console；`ScoreSnapshot` 类型补全；全宽 Score Contributions 面板；桌面/移动端评分解释样式；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：扩展 `ScoreSnapshot` 类型，补入 `scope_type/scope_value/dispersion`、`weight`、`evidence_answer_run_ids`、`confidence_note`、answer run 与 analysis confidence；新增全宽 `Score Contributions` 面板，展示 final score、公式、双分母、离散度、8 个贡献组件和关联 answer runs。
  - `apps/web/app/globals.css`：新增 `.scoreDetail`、`.scoreSummary`、`.scoreTotal`、`.contributionGrid`、`.contributionItem`、`.contributionFacts`、`.evidenceNote`、`.scoreRuns` 响应式布局。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Console 已展示完整 Score Contributions 解释包。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js `15.5.19` 编译、类型检查和动态首页构建成功。
  - `make test`：通过，74 个 contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置仍可生成。
  - `git diff --check`：通过，无空白差异问题；修正 `.contributionItem header` 的 `align-items: start` 为 `flex-start` 后，前端构建无 autoprefixer warning。
  - `docker compose -p geno-score-console-test -f infra/docker-compose.yml up --build -d api web` + `docker compose -p geno-score-console-test -f infra/docker-compose.yml --profile worker run --build --rm collector-worker`：通过，worker 写入 `score_contributions=8`、`final_score=87.35`、`traceability_bundle_id=6dc5d295-5db6-5103-b330-39dd975fd8bc`。
  - `GET /v1/visibility-scores/runtime?limit=1`：通过，返回 `total_count=1`、`final_score=87.3500`、`contributions=8`、首个 contribution 含 `weight=0.1600`、`denominator=answer`、`evidence_answer_run_ids=4`、`answer_runs=4` 与 audit `visibility_score_snapshot_created`。
  - Playwright 桌面 `1440x2100`：通过，断言 `Score Contributions`、`Final score`、`87.35`、`MentionScore`、`RecommendationScore`、`Raw score`、`Denominator`、`Positive evidence`、`Negative evidence`、`Linked Answer Runs` 与 `parser confidence` 均可见；断言 `.contributionItem` 数量不少于 8；截图保存为 `/tmp/geno-score-contributions-console.png`。
  - Playwright 移动 `390x1600`：通过，断言 `Score Contributions`、`Final score`、`MentionScore`、`Positive evidence` 与 `Linked Answer Runs` 均可见；截图保存为 `/tmp/geno-score-contributions-console-mobile.png`。
- 当前边界：本切片完成评分解释包只读展示；还不是评分公式配置 UI、人工复核留痕、组件级筛选、历史版本对比或图表化趋势分析。
- 相关计划：`PROJECT-PLAN.md` 的 M3 Answer Parser + AUVisibilityScore、P0a 稳定链路验收门槛和全局可追溯 DoD。

## 2026-06-10 · Runtime Console Citation Graph & Competitors detail panel

- 目标：把 `/v1/citation-graphs/runtime` 从 source gap 摘要推进到 M4 可审计明细展示，让控制台能直接查看 source nodes、source gaps、graph evidence links 和竞品 benchmark。
- 范围：Next.js server-rendered console；`CitationGraph` 类型补全；全宽 Citation Graph & Competitors 面板；桌面/移动端图谱明细样式；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：扩展 `CitationGraph` 类型，补入 `RuntimeCitationGraphNode.node`、node answer runs、`evidence_links`、source gap 权重/观测数、competitor benchmark `payload` 和 `answer_run_ids`；新增全宽 `Citation Graph & Competitors` 面板，展示 source nodes、source gaps、competitor benchmarks 与 graph evidence links。
  - `apps/web/app/globals.css`：新增 `.graphDetail`、`.graphColumns`、`.graphSection`、`.benchmarkGrid`、`.benchmarkItem` 响应式布局。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Console 已展示 Citation Graph & Competitors 明细。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make test`：通过，74 个 Python contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用 5432，本次使用 compose override 暂映射 Postgres `15432:5432`、MinIO `19000/19001`，容器内服务名与 `DATABASE_URL` 保持不变；`docker compose -p geno-graph-console-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`source_graph_nodes=3`、`source_graph_evidence=12`、`source_gaps=3`、`competitor_benchmarks=4`、`traceability_bundle_id=edbf7534-c5df-5544-a5f7-b73e5c760aa2`、`evidence_links=18`。
  - `GET /v1/citation-graphs/runtime?limit=1` 字段校验：通过，返回 `total_count=1`、`records[0].nodes=3`、`evidence_links=12`、`source_gaps=3`、`competitor_benchmarks=4`；首个 node 为 `compare.example / comparison_site / citation_count=4 / runs=4`，首个 gap 为 `marketplace / missing_high_weight_source_type`，首个 benchmark 为 `Ecosa` 且含 mention/citation/local relevance payload，首个 evidence link 为 `cited_by_answer`。
  - Playwright 桌面 `1440x2200`：通过，断言 `Citation Graph & Competitors`、`Source Nodes`、`Source Gaps`、`Competitor Benchmarks`、`Graph Evidence Links`、`compare.example`、`examplebrand.example`、`Ecosa`、`Emma Sleep`、`cited_by_answer` 均可见；截图保存为 `/tmp/geno-graph-console.png`。
  - Playwright 移动 `390x1600`：通过，断言 `Citation Graph & Competitors`、`Source Nodes`、`Source Gaps`、`Competitor Benchmarks`、`Graph Evidence Links`、`Ecosa`、`Emma Sleep`、`cited_by_answer` 均可见；截图保存为 `/tmp/geno-graph-console-mobile.png`。
- 当前边界：本切片完成 citation graph / competitor benchmark 只读明细；还不是交互式图可视化、节点过滤、source gap 编辑、Neo4j 替换演示或竞品趋势分析。
- 相关计划：`PROJECT-PLAN.md` 的 M4 Citation Graph + Competitor Benchmark、P0c 可售报告验收门槛和全局可追溯 DoD。

## 2026-06-10 · Runtime Console Report Method & Evidence Appendix detail panel

- 目标：把 M5 Evidence Report Export 从下载入口推进到控制台内可审计展示，让客户交付报告的冻结方法、采样窗口、平台覆盖、双分母评分、平台权重、证据附录和审计摘要可直接查看。
- 范围：Next.js server-rendered console；`ReportExport` 类型补全；全宽 `Report Method & Evidence Appendix` 面板；报告详情响应式样式；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：扩展 `ReportExport` 类型，补入 `market_code`、`report_type`、`prompt_version`、`scoring_formula_version`、`platform_weights_snapshot`、`window_start/window_end`、`methodology_hash`、`pdf_url`、`score_snapshots`、report answer runs、`citation_graph` 与 report audit events；新增 `Report Method & Evidence Appendix` 面板，展示 frozen methodology、score snapshot、platform weights、evidence appendix、citation/audit summary。
  - `apps/web/app/globals.css`：新增 `.reportDetail`、`.reportSection`、`.reportMethod` 响应式布局。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Console 已展示报告方法和证据附录详情。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make test`：通过，74 个 Python contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用 5432，本次使用 compose override 暂映射 Postgres `15432:5432`、MinIO `19000/19001`，容器内服务名与 `DATABASE_URL` 保持不变；`docker compose -p geno-report-detail-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`source_graph_nodes=3`、`report_export_id=335c876f-9fef-56fc-a449-a3fd4e134506`、`report_evidence_answer_runs=4`、`traceability_bundle_id=98250fb0-d7df-5b09-8c9f-193a0150b2da`。
  - `GET /v1/reports/runtime?limit=1` 字段校验：通过，返回 `total_count=1`；报告为 `worker-runtime-v1 / AU / worker_runtime / sample_size=4`；`prompt_version=au_dtc_ecommerce_v1`、`scoring_formula_version=au_visibility_v1`、`methodology_hash=1b879316939569da53360ce4958839d9dcf9c0a6bfe3763c4cda83e9f7bf1537`；平台权重 `chatgpt=0.3`、`perplexity=0.25`；score snapshot 为 `final_score=87.3500`、`trigger_rate=1.0000`、`mention_rate=1.0000`、`recommendation_rate=1.0000`、`dispersion=1.0500`；answer runs 覆盖 `chatgpt/perplexity`、`official_api`、`Australia/Sydney`；citation graph 为 `nodes=3`、`evidence_links=12`、`source_gaps=3`、`competitor_benchmarks=4`；report audit events 为 `report_export_created` 与 `report_artifacts_archived`；Markdown/PDF/CSV URL 均为冻结 `s3://geno-reports/...`。
  - Playwright 桌面 `1440x2400`：通过，断言 `Report Method & Evidence Appendix`、`Frozen Methodology`、`Method hash`、`Window start`、`Window end`、`Platforms`、`Access methods`、`Score Snapshot`、`Trigger rate`、`Mention rate`、`Recommendation`、`Platform Weights`、`Evidence Appendix`、`Citation & Audit Summary`、`report_export_created`、`report_artifacts_archived`、`official_api`、`perplexity`、`chatgpt`、`worker_runtime` 均可见；截图保存为 `/tmp/geno-report-detail-console.png`。
  - Playwright 移动 `390x1800`：通过，断言 `Report Method & Evidence Appendix`、`Frozen Methodology`、`Evidence Appendix`、`Citation & Audit Summary`、`report_artifacts_archived`、`official_api` 均可见；截图保存为 `/tmp/geno-report-detail-console-mobile.png`。
- 当前边界：本切片完成报告方法与证据附录的只读展示；还不是白标 PDF 模板、图表化报告、报告历史页、权限校验、真实 Google spike 方法结论或 API-vs-浏览器差异抽检展示。
- 相关计划：`PROJECT-PLAN.md` 的 M5 Evidence Report Export、P0c 可售报告验收门槛和全局可追溯 DoD。

## 2026-06-10 · Runtime Console Action Plan & Retest Detail panel

- 目标：把 M6 Action Plan + Retest 从控制台摘要推进到可审计明细展示，让任务建议、owner/status/next check、source gap、evidence answer runs、T+0/7/14/30 复测计划、前后分数对比和 action/retest audit trail 可直接查看。
- 范围：Next.js server-rendered console；`ActionPlan` 类型补全；全宽 `Action Plan & Retest Detail` 面板；任务卡和复测详情响应式样式；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：扩展 `ActionPlan` 类型，补入 `RetestSchedule` id/sample/scheduled dates/answer run ids、`ActionRecommendation` description/owner/source gap/evidence refs/next check、`RetestComparison` baseline/retest/delta/trend/run ids、关联 answer runs 与 audit events；新增 `Action Plan & Retest Detail` 面板，展示 retest schedule、action recommendations、retest comparison、evidence runs 与 audit trail。
  - `apps/web/app/globals.css`：新增 `.actionDetail`、`.actionSection`、`.actionSchedule`、`.actionCards`、`.actionCard` 响应式布局。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Console 已展示 Action Plan & Retest Detail。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make test`：通过，74 个 Python contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用 5432，本次使用 compose override 暂映射 Postgres `15432:5432`、MinIO `19000/19001`，容器内服务名与 `DATABASE_URL` 保持不变；`docker compose -p geno-action-detail-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`source_graph_nodes=3`、`action_recommendations=3`、`retest_schedule_id=668877f8-a627-57a7-8734-8ff352a81363`、`retest_comparison_id=97c73f97-a3ed-5639-b4fd-945031b3ef55`、`retest_trend=improved`、`traceability_bundle_id=91f4a0d7-09a8-545f-9398-9c047f021b88`。
  - `GET /v1/action-plans/runtime?limit=1` 字段校验：通过，返回 `total_count=1`；schedule 为 `au_dtc_ecommerce_v1 / sample_size=1 / offsets=[0,7,14,30] / scheduled_dates=4 / answer_run_ids=4`；actions 为 3 条，首条为 `Strengthen AU local_media evidence / medium / open / owner=system / source_gap_type=missing_high_weight_source_type / evidence_answer_run_ids=4 / related_source_types=['local_media']`；comparison 为 `baseline_score=87.3500`、`retest_score=89.8500`、`score_delta=2.5000`、`trend=improved`、baseline/retest answer runs 均为 4；answer runs 覆盖 `chatgpt/perplexity` 与 `Australia/Sydney`；audit events 为 `action_plan_created` 与 `retest_comparison_created`。
  - Playwright 桌面 `1440x2600`：通过，断言 `Action Plan & Retest Detail`、`Retest Schedule`、`Action Recommendations`、`Retest Comparison`、`Evidence Runs & Audit`、`Audit Trail`、`Strengthen AU local_media evidence`、`missing_high_weight_source_type`、`Next check`、`Baseline score`、`Retest score`、`Score delta`、`improved`、`action_plan_created`、`retest_comparison_created`、`official_api`、`perplexity`、`chatgpt` 均可见；截图保存为 `/tmp/geno-action-detail-console.png`。
  - Playwright 移动 `390x1900`：通过，断言 `Action Plan & Retest Detail`、`Retest Schedule`、`Action Recommendations`、`Retest Comparison`、`Evidence Runs & Audit`、`action_plan_created`、`retest_comparison_created` 均可见；截图保存为 `/tmp/geno-action-detail-console-mobile.png`。
- 当前边界：本切片完成 action/retest 只读明细；还不是 Temporal 可重放调度、真实 T+7/14/30 自动回采、任务编辑/指派工作台、报告模板正式嵌入或外部工单集成。
- 相关计划：`PROJECT-PLAN.md` 的 M6 Action Plan + 复测、P1 复测验收门槛和全局可追溯 DoD。

## 2026-06-10 · Runtime Console Content Engine Detail panel

- 目标：把 M7 Content Engine 从摘要推进到可审计明细展示，让本地 facts、证据绑定草稿、target questions、evidence runs、source action、connector 计划、manual distribution records 与 content audit trail 可直接查看。
- 范围：Next.js server-rendered console；`ContentEngine` 类型补全；全宽 `Content Engine Detail` 面板；facts/connectors/drafts/manual distribution 响应式样式；README/PLAN/审计日志同步。
- 代码落盘：
  - `apps/web/app/page.tsx`：扩展 `ContentEngine` 前端类型，补入 `LocalizedKnowledgeFact`、`ContentDraft`、`IntegrationConnector`、`ManualDistributionRecord`、关联 `PromptQuestion/AnswerRun/ActionRecommendation/AuditEvent` 字段；新增 `Content Engine Detail` 面板，展示 knowledge facts、connectors、content drafts、target questions、evidence runs、source action、manual distribution 与 audit trail。
  - `apps/web/app/globals.css`：新增 `.contentDetail`、`.contentSection`、`.connectorGrid`、`.connectorItem`、`.contentDraftGrid`、`.contentDraftCard`、`.contentBinding` 响应式布局。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Console 已展示 Content Engine Detail。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make test`：通过，74 个 Python contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用 5432，本次使用 compose override 暂映射 Postgres `15432:5432`、MinIO `19000/19001`，容器内服务名与 `DATABASE_URL` 保持不变；`docker compose -p geno-content-detail-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`source_graph_nodes=3`、`action_recommendations=3`、`knowledge_facts=7`、`content_drafts=3`、`integration_connectors=7`、`manual_distribution_records=3`、`traceability_bundle_id=fcbfce70-ebdc-507f-81f9-3d09c688f6ac`、`evidence_links=18`。
  - `GET /v1/content-engines/runtime?limit=1` 字段校验：通过，返回 `total_count=1`、`knowledge_facts=7`、`content_drafts=3`、`integration_connectors=7`、`manual_distribution_records=3`、`audit_events=1 / content_engine_fixture_created`；首个 fact 为 `AU / aud_pricing / active / confidence=0.6800`；首个 draft 为 `How to choose DTC ecommerce products in Australia for industry_site / pending_human_review`，并关联 5 条 target questions、5 条 facts、4 条 answer runs、source action `Strengthen AU industry_site evidence` 和 1 条 manual distribution record；首个 connector 为 `cloudflare / planned / api_token / capabilities=2`。
  - Playwright 桌面 `1440x2600`：通过，断言 `Content Engine Detail`、`Localized Knowledge Facts`、`Integration Connectors`、`Content Drafts`、`Manual Distribution & Audit`、`pending_human_review`、`content_engine_fixture_created`、`google_search_console`、`shopify`、`cloudflare`、`Source action`、`Australian returns policy` 均可见；截图保存为 `/tmp/geno-content-detail-console.png`。
  - Playwright 移动 `390x1900`：通过，断言 `Content Engine Detail`、`Localized Knowledge Facts`、`Integration Connectors`、`Content Drafts`、`Manual Distribution & Audit`、`pending_human_review`、`content_engine_fixture_created`、`cloudflare` 均可见；截图保存为 `/tmp/geno-content-detail-console-mobile.png`。
- 当前边界：本切片完成 M7 内容引擎只读明细；还不是 LLMGateway 真实内容生成、人工审核工作台、pgvector runtime 检索、真实 OAuth/API 集成、CMS 草稿创建、分发状态回写或内容效果评估。
- 相关计划：`PROJECT-PLAN.md` 的 M7 Knowledge Facts + Content Engine + Integrations、M4 看板和全局可追溯 DoD。

## 2026-06-10 · Runtime Console URL filters + evidence read-model filters

- 目标：补齐 Runtime Console 的基础筛选能力，让 demo 和审计复盘可以通过 URL 查询参数固定 `platform/evidence city/intent_type` 范围，并让 evidence read model 在数据库层按同一口径过滤。
- 范围：FastAPI runtime evidence endpoint；PostgreSQL repository evidence read model；Next.js server-rendered Runtime Filters；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/repository.py`：`list_runtime_evidence_runs()` 新增 `city` 与 `intent_type` 过滤，count 查询和列表查询均通过 `answer_runs ar LEFT JOIN prompt_questions pq` 同步应用过滤条件。
  - `apps/api/geno_api/main.py`：`/v1/evidence-runs/runtime` 暴露 `city` 与 `intent_type` query 参数。
  - `apps/web/app/page.tsx`：新增 `RuntimeFilters`、URL query builder、`searchParams` 读取和 `Runtime Filters` 表单；evidence runtime API 请求会带入当前 `platform/evidence city/intent_type`，prompt runtime API 同步按 `intent_type` 过滤，页面同时展示实际 API 查询路径。
  - `apps/web/app/globals.css`：新增筛选条、select、reset link 与移动端响应式样式。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：覆盖 evidence read model 过滤 SQL 与带筛选参数的 runtime endpoint 持久化配置错误。
  - `README.md`、`PROJECT-PLAN.md`：同步基础 URL 筛选已落，导出/排序/保存视图/跨页面深链路仍待接。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make test`：通过，75 个 Python contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用 5432，本次使用 compose override 暂映射 Postgres `15432:5432`、MinIO `19000/19001`，容器内服务名与 `DATABASE_URL` 保持不变；`docker compose -p geno-filter-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`source_graph_nodes=3`、`action_recommendations=3`、`knowledge_facts=7`、`content_drafts=3`、`integration_connectors=7`、`manual_distribution_records=3`、`traceability_bundle_id=75373e3c-2a99-5f81-aca7-246974978434`、`evidence_links=18`。
  - `GET /v1/evidence-runs/runtime?platform=perplexity&city=Sydney&intent_type=brand_awareness&limit=20` 字段校验：通过，未过滤 evidence 为 4 条；过滤后 `total_count=1`，返回 run 为 `perplexity / Sydney / brand_awareness / Is ExampleBrand good in Australia?`；`city=Melbourne` 同条件返回 `total_count=0`。
  - `GET /v1/prompts/runtime?market_code=AU&intent_type=brand_awareness&limit=20` 字段校验：通过，返回 `total_count=10`，全部为 `brand_awareness`。
  - Playwright 桌面 `1440x2400`：通过，断言 `Runtime Filters`、`perplexity / Sydney / brand_awareness`、`Evidence city`、实际 prompts/evidence query path、`perplexity · Sydney` 和 `brand_awareness` 均可见；三个 select 默认值正确；截图保存为 `/tmp/geno-runtime-filters-console.png`。
  - Playwright 移动 `390x1600`：通过，断言 `Runtime Filters`、`perplexity / Sydney / brand_awareness`、`Evidence city`、`Evidence query`、`perplexity · Sydney` 均可见；三个 select 默认值正确；截图保存为 `/tmp/geno-runtime-filters-console-mobile.png`。
- 当前边界：本切片完成 platform/evidence city/intent_type 只读筛选；还不是多维聚合看板、排序、CSV 导出、保存视图、跨页面深链路、权限过滤或图谱可视化筛选。
- 相关计划：`PROJECT-PLAN.md` 的 M4 监测看板、P1 看板筛选与导出、全局可追溯 DoD 与审计链风险项。

## 2026-06-10 · Runtime Evidence filtered CSV export

- 目标：把 Runtime Filters 从只读筛选推进到可交付导出，让当前 `platform/evidence city/intent_type` 过滤范围内的 evidence runs 可以即时下载 CSV，并通过 hash header 做交付完整性校验。
- 范围：runtime evidence CSV export read model；FastAPI CSV endpoint；Runtime Console 下载入口；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeEvidenceExport`，记录 export type、filename、media type、content hash、filters、total count 和 row count。
  - `packages/geno_core/geno_core/repository.py`：新增 `_render_runtime_evidence_csv()` 与 `export_runtime_evidence_csv()`，复用 `list_runtime_evidence_runs()` 的同一组过滤条件，导出 answer run、prompt metadata、platform/surface/city、采样、collector、raw hash、citation/asset/audit count 与 total cost。
  - `apps/api/geno_api/main.py`：新增 `/v1/evidence-runs/runtime/export.csv`，返回 `text/csv` 附件，并写入 `X-GENO-Evidence-Export-Hash`、row count、total count headers；`/v1/contracts` 同步新增契约和 endpoint。
  - `apps/web/app/page.tsx`：Runtime Filters 增加 `Export Evidence CSV` 下载入口，并展示 export query path。
  - `apps/web/app/globals.css`：调整筛选表单列宽，容纳新增导出入口。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：覆盖 repository CSV 导出、hash/row count/filter metadata、endpoint 持久化配置错误与 contracts 列表。
  - `README.md`、`PROJECT-PLAN.md`：同步筛选后 evidence CSV 导出已落，报告级导出/排序/保存视图仍待接。
- 验证项：
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make test`：通过，77 个 Python contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用 8000，本次使用 compose `!override` 将 API/Web 暂映射到 `18000:8000`、`13000:3000`，Postgres/MinIO 不暴露宿主端口；`docker compose -p geno-evidence-export-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`knowledge_facts=7`、`content_drafts=3`、`traceability_bundle_id=b242513c-b873-5ea2-bed8-dc2cd8b67fae`、`evidence_links=18`。
  - `GET /v1/evidence-runs/runtime/export.csv?platform=perplexity&city=Sydney&intent_type=brand_awareness&limit=200` 字段校验：通过，`Content-Disposition=attachment; filename="runtime-evidence.csv"`，`X-GENO-Evidence-Export-Hash=8e017c38698f1a6bf18139b4f003977fb211ffff68be0f2f89b013902010c390`，`X-GENO-Evidence-Export-Row-Count=1`，`X-GENO-Evidence-Export-Total-Count=1`；CSV 首行过滤结果为 `perplexity / Sydney / brand_awareness / Is ExampleBrand good in Australia?`，且 `raw_payload_hash=df26830f849fe344da988e88c7420510633d0ad59ba39d182e69efb75c5ddbd9`。
  - Playwright 桌面 `1440x1200`：通过，断言 `Runtime Filters`、`Export Evidence CSV`、`Export query`、`/v1/evidence-runs/runtime/export.csv?platform=perplexity&city=Sydney&intent_type=brand_awareness&limit=200`、`perplexity / Sydney / brand_awareness`、`1 stored runs`、`perplexity · Sydney` 均可见；三个 select 默认值正确；导出链接指向 `http://localhost:18000/v1/evidence-runs/runtime/export.csv?platform=perplexity&city=Sydney&intent_type=brand_awareness&limit=200`；截图保存为 `/tmp/geno-evidence-export-console.png`。
  - Playwright 移动 `390x1200`：通过，断言 `Runtime Filters`、`Export Evidence CSV` 与 export query 均可见；导出链接指向同一 API；截图保存为 `/tmp/geno-evidence-export-console-mobile.png`。
- 当前边界：本切片完成即时 evidence CSV 导出；还不是异步导出任务、保存视图、权限隔离、报告级筛选导出、排序或多维聚合导出。
- 相关计划：`PROJECT-PLAN.md` 的 M4 监测看板、P1 看板筛选与导出、M5 可售报告导出和全局可追溯 DoD。

## 2026-06-10 · Runtime Evidence controlled sort

- 目标：把 Runtime Filters 的 evidence 列表从固定时间倒序推进到可审计排序，让控制台和 CSV export 使用同一 `sort` 口径，支持按采集时间、成本、citation 数和 audit event 数排序。
- 范围：runtime evidence read model 排序白名单；FastAPI `sort` query；CSV export sort header；Runtime Console 排序选择器；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：`RuntimeEvidencePage` 新增 `sort` 字段，响应中回传实际生效排序。
  - `packages/geno_core/geno_core/repository.py`：新增 `RUNTIME_EVIDENCE_SORTS` 与 `_runtime_evidence_sort()`，只允许 `collected_at_desc`、`collected_at_asc`、`cost_desc`、`cost_asc`、`citation_count_desc`、`audit_count_desc` 固定排序片段；未知 sort 回退到 `collected_at_desc`，避免外部参数拼接进 SQL。
  - `apps/api/geno_api/main.py`：`/v1/evidence-runs/runtime` 和 `/v1/evidence-runs/runtime/export.csv` 新增 `sort` query；CSV 响应新增 `X-GENO-Evidence-Export-Sort`。
  - `apps/web/app/page.tsx`：Runtime Filters 增加 `Sort evidence` 选择器，evidence list 和 Evidence CSV export 共用同一 sort query，并展示当前 sort。
  - `apps/web/app/globals.css`：调整筛选表单网格，容纳排序选择器。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：覆盖受控排序 SQL、CSV export sort metadata、未知 sort 回退和带 sort 的 endpoint 配置错误。
  - `README.md`、`PROJECT-PLAN.md`：同步 Evidence Sort 已落，报告级导出和保存视图仍待接。
- 验证项：
  - `make test`：通过，78 个 Python contract/API/worker tests 全部 OK。
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用 8000，本次使用 compose `!override` 将 API/Web 暂映射到 `18000:8000`、`13000:3000`，Postgres/MinIO 不暴露宿主端口；`docker compose -p geno-evidence-sort-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`knowledge_facts=7`、`content_drafts=3`、`traceability_bundle_id=5ef7810f-cc91-5f7b-b9ca-87d62cb3f70d`、`evidence_links=18`。
  - `GET /v1/evidence-runs/runtime?sort=cost_desc&limit=4` 字段校验：通过，返回 `sort=cost_desc`、`total_count=4`，`collection_cost.total_cost` 顺序为 `0.0035, 0.0035, 0.0025, 0.0025`。
  - 未知 sort 回退校验：`GET /v1/evidence-runs/runtime?sort=cc.total_cost%20DESC%3B%20DROP%20TABLE%20answer_runs&limit=1` 返回 `sort=collected_at_desc`，证明外部参数不会直接进入 SQL。
  - `GET /v1/evidence-runs/runtime/export.csv?sort=cost_desc&limit=4` 字段校验：通过，`X-GENO-Evidence-Export-Sort=cost_desc`，`X-GENO-Evidence-Export-Row-Count=4`，CSV `total_cost` 顺序为 `0.003500, 0.003500, 0.002500, 0.002500`。
  - Playwright 桌面 `1440x1200`：通过，断言 `Runtime Filters`、`Sort evidence`、`Evidence sort`、`cost_desc`、`/v1/evidence-runs/runtime?platform=perplexity&sort=cost_desc&limit=5`、`/v1/evidence-runs/runtime/export.csv?platform=perplexity&sort=cost_desc&limit=200` 均可见；`platform=perplexity` 与 `sort=cost_desc` select 默认值正确；导出链接指向 `http://localhost:18000/v1/evidence-runs/runtime/export.csv?platform=perplexity&sort=cost_desc&limit=200`；截图保存为 `/tmp/geno-evidence-sort-console.png`。
  - Playwright 移动 `390x1200`：通过，断言 `Runtime Filters`、`Sort evidence` 与 export query 均可见；`sort=cost_desc` select 默认值正确；导出链接指向同一 API；截图保存为 `/tmp/geno-evidence-sort-console-mobile.png`。
- 当前边界：本切片完成 runtime evidence 列表和即时 CSV 的受控排序；还不是保存视图、报告级筛选导出、多维聚合排序或用户级权限排序偏好。
- 相关计划：`PROJECT-PLAN.md` 的 M4 监测看板、P1 看板筛选与导出和全局可追溯 DoD。

## 2026-06-10 · Runtime Saved Views

- 目标：把 Runtime Filters 从临时 URL 状态推进到可持久化、可审计的 saved view，让当前 evidence 筛选、排序、query path 和 export path 能保存、复用并形成审计事件。
- 范围：PostgreSQL `runtime_saved_views` 表；runtime saved views read/write model；FastAPI GET/POST endpoint；Runtime Console 保存视图表单和最近视图列表；README/PLAN/审计日志同步。
- 代码落盘：
  - `infra/db/migrations/up/0001_init.sql`、`infra/db/migrations/down/0001_init.down.sql`：新增 `runtime_saved_views` 表、`idx_runtime_saved_views_project` 索引和回滚删除项；字段包含 `project_id/name/view_type/filters/sort/query_path/export_path/created_by/created_at/updated_at`，并以 `(project_id, name)` 做 upsert 唯一键。
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeSavedView`、`RuntimeSavedViewPage`、`RuntimeSavedViewInput`。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_saved_views()`、`save_runtime_saved_view()` 和 `_load_runtime_saved_view()`；保存时生成稳定 saved view id，upsert 当前视图，并写入 `runtime_saved_view_saved` AuditEvent，`after_hash` 对应保存后的视图 payload。
  - `apps/api/geno_api/main.py`：新增 `RuntimeSavedViewRequest`、`GET /v1/runtime-saved-views`、`POST /v1/runtime-saved-views`，并在 `/v1/contracts` 暴露 saved view 模型和 endpoint。
  - `apps/web/app/page.tsx`：Runtime Console 新增 `saveCurrentRuntimeView` server action、Saved Views 数据读取、保存视图表单、最近视图列表和 saved view 页面链接。
  - `apps/web/app/globals.css`：新增 saved view 保存表单、列表和响应式布局样式。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：覆盖 repository save/list、审计事件写入、endpoint 持久化配置错误与 contracts 列表。
  - `README.md`、`PROJECT-PLAN.md`：同步 Runtime Saved Views 已落，报告级导出仍待接。
- 验证项：
  - `make test`：通过，82 个 Python contract/API/worker tests 全部 OK。
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用 8000，本次使用 compose `!override` 将 API/Web 暂映射到 `18000:8000`、`13000:3000`，Postgres/MinIO 不暴露宿主端口；`docker compose -p geno-saved-views-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`knowledge_facts=7`、`content_drafts=3`、`traceability_bundle_id=7028b5f2-9ddf-5d18-a7f8-407ab79c0d7c`、`evidence_links=18`。
  - `POST /v1/runtime-saved-views` 字段校验：通过，保存 `Perplexity Sydney cost view`，返回 `sort=cost_desc`、`filters.city=Sydney`，首条 audit event 为 `runtime_saved_view_saved` 且 `after_hash` 非空。
  - `GET /v1/runtime-saved-views?view_type=runtime_evidence&limit=5` 字段校验：通过，返回 `total_count=1`，首条记录为 `Perplexity Sydney cost view` 并带 `runtime_saved_view_saved` audit event。
  - `GET /v1/contracts` 校验：通过，`persistence` 包含 `RuntimeSavedView` 和 `/v1/runtime-saved-views`。
  - Playwright 桌面 `1440x1400`：通过，断言 `Saved Views`、`Saved view name`、evidence query、export query 可见；填写 `Playwright saved view` 后点击 `Save view`，页面刷新后展示 saved view、`runtime_saved_view_saved` 和 `cost_desc`；截图保存为 `/tmp/geno-saved-views-console.png`。
  - Playwright 移动 `390x1400`：通过，断言 `Saved Views`、`Playwright saved view` 和 `runtime_saved_view_saved` 可见；截图保存为 `/tmp/geno-saved-views-console-mobile.png`。
- 当前边界：本切片完成 runtime evidence saved views 的持久化和审计；还不是用户权限隔离、团队共享权限、删除/重命名 UI、保存视图分组、报告级筛选导出或跨页面深链路。
- 相关计划：`PROJECT-PLAN.md` 的 M4 监测看板、P1 看板筛选与导出和全局可追溯 DoD。

## 2026-06-10 · Runtime Report artifact filters + sort

- 目标：把 Report Snapshot 的 Markdown/CSV/PDF 下载与当前 Runtime Filters/Evidence Sort 对齐，让交付报告的证据附录可以按 `platform/city/intent_type/status/sort` 即时过滤和排序，同时保持冻结 `ReportExport`、score snapshot、methodology hash 与对象存储 URL 不被改写。
- 范围：runtime report artifact read model；FastAPI artifact query/header；Runtime Console 下载链接；report CSV 附录字段；core/API tests；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：`RuntimeReportArtifact` 增加 `filters`、`filter_hash`、`sort`、`total_count`、`row_count`，用于标记本次下载附录的实际过滤、排序和行数。
  - `packages/geno_core/geno_core/repository.py`：新增 `_filter_runtime_report_answer_runs()`，复用 runtime evidence 的受控排序白名单；`get_runtime_report_artifact()` 在读取冻结 `ReportExport -> ReportEvidence` 后只过滤即时渲染用的 `RuntimeReportExport.answer_runs`，不更新数据库快照；report CSV 附录补充 `prompt_intent_type`、`total_cost`、`citation_count`、`audit_event_count`。
  - `apps/api/geno_api/main.py`：`/v1/reports/runtime/{report_export_id}/artifact` 新增 `platform/city/intent_type/status/sort` query，响应新增 `X-GENO-Report-Artifact-Filter-Hash`、`X-GENO-Report-Artifact-Sort`、`X-GENO-Report-Artifact-Row-Count`、`X-GENO-Report-Artifact-Total-Count` headers。
  - `apps/web/app/page.tsx`：Report Snapshot 的 Markdown/CSV/PDF 链接继承当前 Runtime Filters 和实际 `evidenceSort`，并展示 artifact query path，便于审计复盘。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：覆盖带筛选/排序 query 的 artifact endpoint 配置错误、CSV artifact 过滤后只输出匹配 evidence、filter hash、sort、row/total count。
  - `README.md`、`PROJECT-PLAN.md`：同步报告 artifact 附录级筛选/排序下载已落，以及“不改写冻结报告”的边界。
- 验证项：
  - `make test`：通过，82 个 Python contract/API/worker tests 全部 OK。
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用默认端口，本次使用 compose `!override` 将 API/Web 暂映射到 `18000:8000`、`13000:3000`，Postgres/MinIO 不暴露宿主端口；`docker compose -p geno-report-filter-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`report_export_id=335c876f-9fef-56fc-a449-a3fd4e134506`、`report_evidence_answer_runs=4`、`traceability_bundle_id=35bc6e1c-1250-5563-be24-97a325e5b922`、`evidence_links=18`，并归档 Markdown/PDF/CSV artifact 到 `s3://geno-reports/6624961f-36ae-539b-9d48-51619b42e37e/worker-runtime-v1.*`。
  - `GET /v1/reports/runtime/335c876f-9fef-56fc-a449-a3fd4e134506/artifact?type=csv&platform=perplexity&city=Sydney&intent_type=brand_awareness&sort=cost_desc` 字段校验：通过，`Content-Type=text/csv; charset=utf-8`，`Content-Disposition=attachment; filename="worker-runtime-v1.csv"`，`X-GENO-Report-Artifact-Hash=04f32a8d154e5de1c23b0e2d4ab10eb7f2c6ec6701017c10fa426501dc3d60b6`，`X-GENO-Report-Artifact-Filter-Hash=0a0ee2c4f2b58b1af61fbcef59ccc90b5373c69d417a875d3efdfb12c6906713`，`X-GENO-Report-Artifact-Sort=cost_desc`，`X-GENO-Report-Artifact-Row-Count=1`，`X-GENO-Report-Artifact-Total-Count=4`；CSV 唯一行是 `perplexity / Sydney / brand_awareness / Is ExampleBrand good in Australia?`，且不包含 Melbourne alternative prompt。
  - Markdown/PDF artifact 同参数校验：通过，Markdown 含目标 prompt 且不含 Melbourne prompt；PDF 响应 `Content-Type=application/pdf`、`X-GENO-Report-Artifact-Sort=cost_desc`、`X-GENO-Report-Artifact-Row-Count=1`，内容以 `%PDF-1.4` 开头。
  - Playwright 桌面 `1440x1500`：通过，断言 `Report Snapshot`、`Artifact filters`、`Download Markdown`、`Download CSV`、`Download PDF` 均可见；CSV artifact query path 为 `/v1/reports/runtime/335c876f-9fef-56fc-a449-a3fd4e134506/artifact?type=csv&platform=perplexity&city=Sydney&intent_type=brand_awareness&sort=cost_desc`；三类下载链接都带当前筛选/排序参数；截图保存为 `/tmp/geno-report-artifact-filter-console.png`。
  - Playwright 移动 `390x1400`：通过，断言 `Report Snapshot`、`Artifact filters`、`Download CSV` 可见；CSV 下载链接包含 `platform=perplexity`、`city=Sydney`、`intent_type=brand_awareness`、`sort=cost_desc`；截图保存为 `/tmp/geno-report-artifact-filter-console-mobile.png`。
  - 验证后已执行 `docker compose -p geno-report-filter-test ... down -v` 清理测试容器与卷。
- 当前边界：本切片完成 report artifact 的即时附录筛选/排序下载；还不是异步报告导出任务、白标 PDF 模板、权限签名下载 URL、报告历史管理或把筛选视图固化成新的 `ReportExport` 版本。
- 相关计划：`PROJECT-PLAN.md` 的 M4 看板筛选与导出、M5 Evidence Report Export、全局可追溯 DoD 与审计链风险项。

## 2026-06-10 · Runtime Manual Backfill evidence path

- 目标：补齐人工补录最小可审计路径，让 Google AI Mode/AIO spike 在浏览器或第三方 API 不稳定时，也能把人工采集的答案、citation、截图/HTML URL 写成标准 Raw Evidence，并进入同一套 evidence/audit/CSV/report 附录读模型。
- 范围：manual backfill input model；RawEvidenceRecord 构建器；runtime prompt 元数据读取；FastAPI manual backfill endpoint；Runtime Console 表单；core/API tests；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `ManualBackfillInput`，定义人工补录需要的 prompt、平台、surface、答案、citation、截图/HTML、提交人和备注字段。
  - `packages/geno_core/geno_core/collection.py`：新增 `build_manual_backfill_record()`，把人工输入转换为标准 `AnswerRun(access_method=manual)`、`RawAnswer(raw_payload_hash)`、`AnswerCitation`、`EvidenceAsset`、`CollectorLog`、`CollectionCost(total_cost=0)` 和 `manual_backfill_recorded` AuditEvent。
  - `packages/geno_core/geno_core/repository.py`：新增 `get_runtime_prompt()`，API 写入前从 PostgreSQL 读取已有 `PromptQuestion` 元数据，避免前端伪造 project/market/city/language。
  - `apps/api/geno_api/main.py`：新增 `ManualBackfillRequest` 与 `POST /v1/evidence-runs/runtime/manual-backfill`；成功后复用 `save_raw_evidence_records()` 入库，并返回 answer_run id、raw payload hash、citation/asset count 和审计事件 id。
  - `apps/web/app/page.tsx`、`apps/web/app/globals.css`：Prompt Pack 面板新增 `Manual Backfill` 表单，支持提交平台/surface、answer text、citation URLs、截图/HTML URL 和 notes；响应式样式同步。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：覆盖人工补录 record 构建、repository 映射到主证据表、endpoint 缺 persistence 503 与 `/v1/contracts` 暴露。
  - `README.md`、`PROJECT-PLAN.md`：同步人工补录最小路径已落，批量文件流、真实 Google browser/third-party 路径仍待接。
- 验证项：
  - `make test`：通过，85 个 Python contract/API/worker tests 全部 OK。
  - `npm run build`（`apps/web`）：通过，Next.js 15.5.19 production build 成功。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用默认端口，本次使用 compose `!override` 将 API/Web 暂映射到 `18000:8000`、`13000:3000`，Postgres/MinIO 不暴露宿主端口；`docker compose -p geno-manual-backfill-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`report_export_id=335c876f-9fef-56fc-a449-a3fd4e134506`、`traceability_bundle_id=adae26ac-7ecd-58e1-8bb3-d21b913d91b1`、`evidence_links=18`。
  - `POST /v1/evidence-runs/runtime/manual-backfill` 字段校验：通过，使用 prompt `002f3468-1b66-506b-8318-d832c7d6308e` 写入 Google AI Mode 人工答案，返回 `answer_run_id=b51ac142-01a6-5d2e-8497-3f1e1ec4c69e`、`raw_payload_hash=f6e7d079cb0bfa64f47e8cf9669bbe4514148c92f135a3c2d049fe8b0af7a2c7`、`citation_count=2`、`evidence_asset_count=2`。
  - `GET /v1/evidence-runs/runtime?platform=google&sort=collected_at_desc&limit=5` 字段校验：通过，读回 `platform=google`、`surface=google_ai_mode`、`access_method=manual`、`collector_backend_id=google.manual_backfill`、`audit_event=manual_backfill_recorded`、2 citations、2 assets、`collection_cost.total_cost=0.000000`，raw hash 与 POST 响应一致。
  - `GET /v1/evidence-runs/runtime/export.csv?platform=google&sort=collected_at_desc&limit=20` 字段校验：通过，CSV 包含该 `answer_run_id` 且 `access_method=manual`，`X-GENO-Evidence-Export-Row-Count=1`。
  - Playwright 桌面 `1440x1600`：通过，断言 `Manual Backfill` 表单可见，Runtime Filters 的 `platform` select 可保持 `google`，提交后 `Latest Evidence` 为 google，`Evidence Runs` 中可见 `manual` 与 `manual_backfill_recorded`；截图保存为 `/tmp/geno-manual-backfill-console.png`。
  - Playwright 移动 `390x1600`：通过，断言 `Manual Backfill`、`Save backfill`、`manual_backfill_recorded` 可见，平台筛选保持 `google`；截图保存为 `/tmp/geno-manual-backfill-console-mobile.png`。
  - 验证后已执行 `docker compose -p geno-manual-backfill-test ... down -v` 清理测试容器与卷。
- 当前边界：本切片完成单条 prompt 的人工补录和审计入库；还不是批量 CSV/XLSX 补录、人工实体确认、审核队列、权限隔离或 Google spike 全量真实运行。
- 相关计划：`PROJECT-PLAN.md` 的 M2a Audit/Provenance、M2b ManualBackfillCollector、全局可追溯 DoD 与审计链风险项。

## 2026-06-10 · Runtime Entity Alias confirmation path

- 目标：补齐实体/别名确认的最小可审计闭环，让品牌、竞品、域名、产品线、母公司等人工确认结果可以写入 `entity_aliases`，并产生可复盘的 `entity_alias_confirmed` 审计事件。
- 范围：entity alias input/runtime read model；PostgreSQL repository 确认与列表查询；FastAPI runtime entity alias endpoints；Runtime Console Project Bootstrap 表单；core/API tests；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `EntityAlias`、`EntityAliasInput`、`RuntimeEntityAlias`、`RuntimeEntityAliasPage`。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_entity_aliases()`、`confirm_entity_alias()` 和 `_load_runtime_entity_alias()`；确认时校验目标 `brand_entities` / `competitor_entities` 存在，用稳定 alias id 幂等写入 `entity_aliases`，并同事务写入 `entity_alias_confirmed` AuditEvent。
  - `apps/api/geno_api/main.py`：新增 `EntityAliasConfirmRequest`、`GET /v1/entity-aliases/runtime`、`POST /v1/entity-aliases/runtime/confirm`，并在 `/v1/contracts` 暴露 entity alias runtime 契约。
  - `apps/web/app/page.tsx`、`apps/web/app/globals.css`：Project Bootstrap 面板新增 `Entity Alias` 表单，支持选择当前项目的 brand/competitor，确认 alias/domain/product/parent_company，并展示最近确认记录与 audit hash。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：覆盖 repository 确认写入审计、runtime alias 列表聚合审计、endpoint 缺 persistence 503 与 `/v1/contracts` 暴露。
  - `README.md`、`PROJECT-PLAN.md`：同步 entity alias confirmation 最小路径已落，批量消歧队列、parser alias 使用和同名候选推荐仍待接。
- 验证项：
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_core_contracts.CoreContractsTest.test_postgres_repository_confirms_entity_alias_with_audit_event tests.test_core_contracts.CoreContractsTest.test_postgres_repository_lists_runtime_entity_aliases_with_audit_events tests.test_api_contracts.ApiContractsTest.test_runtime_entity_aliases_endpoint_requires_persistence_config tests.test_api_contracts.ApiContractsTest.test_runtime_entity_alias_confirm_endpoint_requires_persistence_config tests.test_api_contracts.ApiContractsTest.test_contracts_include_m7_content_integrations`：通过，5 个目标测试 OK。
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_api_contracts tests.test_core_contracts`：通过，84 个 Python contract/API/core tests 全部 OK。
  - `make test`：通过，89 个 Python contract/API/worker tests 全部 OK。
  - `npm --prefix apps/web run build`：通过，Next.js 15.5.19 production build 成功。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web + worker E2E：通过。因本机已有其他服务占用默认端口，本次使用 compose `!override` 将 API/Web 暂映射到 `18000:8000`、`13000:3000`，Postgres/MinIO 不暴露宿主端口；`docker compose -p geno-entity-alias-test ... up --build -d api web` 后运行 worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`report_export_id=335c876f-9fef-56fc-a449-a3fd4e134506`、`traceability_bundle_id=bdb7d2ab-0581-598f-afa8-bd9acf238138`、`evidence_links=18`。
  - `POST /v1/entity-aliases/runtime/confirm` 字段校验：通过，使用 brand `bebbedda-4479-5fb6-8d0b-bd3b0499ca0a` 写入 `ExampleBrand Australia`，返回 `alias_id=afa132c7-38c9-5d4e-9d07-c97ce4fad723`。
  - `GET /v1/entity-aliases/runtime?entity_kind=brand&limit=5` 字段校验：通过，读回 `alias=ExampleBrand Australia`、`entity_kind=brand`、`audit_event=entity_alias_confirmed`、`after_hash=1ef6cac82b4eba4ee73375f8e072319a5713eeb0da08d97eb5902a288067dcd8`。
  - Playwright 桌面 `1440x1600`：通过，断言 `Entity Alias` 表单、`Confirm alias` 按钮可见，提交 `Playwright ExampleBrand Alias` 后页面可见该 alias 与 `entity_alias_confirmed`；截图保存为 `/tmp/geno-entity-alias-console.png`。
  - Playwright 移动 `390x1600`：通过，断言 `Entity Alias`、`Confirm alias`、`entity_alias_confirmed` 可见；截图保存为 `/tmp/geno-entity-alias-console-mobile.png`。
- 当前边界：本切片完成单条 entity alias 的确认、审计和控制台操作；还不是批量实体审核队列、自动候选生成、parser 对 alias 的实际消歧使用、权限隔离或 traceability bundle 内的独立 alias 节点深链路。
- 相关计划：`PROJECT-PLAN.md` 的 M2a Audit/Provenance、M3 实体/别名表与同名消歧、全局可追溯 DoD 与审计链风险项。

## 2026-06-10 · Alias-aware parser runtime analysis

- 目标：把上一切片确认后的 `entity_aliases` 真正接入 M3 解析链路，让后续 `--persist-analysis` 重跑时，品牌/竞品 alias、域名、产品名或母公司名能参与品牌提及、推荐、排名和竞品识别。
- 范围：rule parser alias matching；analysis pipeline alias 参数；PostgreSQL confirmed alias map 查询；worker `--persist-analysis` alias 注入；answer analysis upsert；core tests；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/parser.py`：`RuleBasedAnswerParser` 升级为 `rule_based_v2_aliases`，支持 canonical name + confirmed aliases 匹配；输出仍保持 canonical brand/competitor，`uncertainty_flags` 标记 `brand_alias_matched` 和 `competitor_alias_matched:*`。
  - `packages/geno_core/geno_core/analysis_pipeline.py`：`analyze_and_score_records()` 新增 `entity_aliases` 参数，并传给 parser。
  - `packages/geno_core/geno_core/repository.py`：新增 `get_confirmed_entity_alias_terms(project_id)`，按项目读取确认后的 alias map；`save_answer_analyses()` 改为 `ON CONFLICT (id) DO UPDATE`，确保确认 alias 后重跑同一 answer run 会更新 parser 版本、payload 和 confidence。
  - `workers/collector_worker/run_collection_slice.py`：`--persist-analysis` 阶段读取 confirmed aliases 并注入分析流程，输出 `entity_alias_entity_count` 和 `entity_alias_term_count` 作为运行摘要。
  - `tests/test_core_contracts.py`：覆盖 alias-only brand/competitor 命中、alias-only mention score、confirmed alias map 读取，以及 answer analysis upsert 行为。
  - `README.md`、`PROJECT-PLAN.md`：同步 confirmed aliases 已进入 `rule_based_v2_aliases` parser，批量审核队列与自动候选推荐仍待接。
- 验证项：
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_core_contracts.CoreContractsTest.test_m3_rule_parser_extracts_brand_competitors_and_citations tests.test_core_contracts.CoreContractsTest.test_m3_rule_parser_uses_confirmed_entity_aliases tests.test_core_contracts.CoreContractsTest.test_m3_analysis_pipeline_scores_alias_only_mentions tests.test_core_contracts.CoreContractsTest.test_postgres_repository_reads_confirmed_entity_alias_terms tests.test_core_contracts.CoreContractsTest.test_postgres_repository_maps_fixture_chain_to_runtime_tables`：通过，5 个目标测试 OK。
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_api_contracts tests.test_core_contracts tests.test_worker_cli`：通过，92 个 Python contract/API/core/worker tests 全部 OK。
  - `npm --prefix apps/web run build`：通过，Next.js 15.5.19 production build 成功。
  - `make test`：通过，92 个 Python contract/API/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API + worker E2E：通过。因本机已有其他服务占用默认端口，本次使用 compose `!override` 将 API 暂映射到 `18000:8000`，Postgres/MinIO/Web 不暴露宿主端口；`docker compose -p geno-alias-parser-test ... up --build -d api` 后，先调用 `POST /v1/projects/runtime/au/dtc-ecommerce` 创建 AU/DTC 项目，再用 brand `bebbedda-4479-5fb6-8d0b-bd3b0499ca0a` 调用 `POST /v1/entity-aliases/runtime/confirm` 写入 `K-Brand AU`，返回 `alias_id=4b867468-631b-5983-b271-1ca767afa2fa` 和 `entity_alias_confirmed`。
  - 随后运行 worker profile：通过，输出 `raw_evidence_records=4`、`analysis_count=4`、`entity_alias_entity_count=1`、`entity_alias_term_count=1`、`score_contributions=8`、`report_export_id=335c876f-9fef-56fc-a449-a3fd4e134506`、`traceability_bundle_id=c983d582-cfd7-54be-9871-a284047fb3f7`、`evidence_links=18`。
  - `GET /v1/visibility-scores/runtime?limit=1` 字段校验：通过，读回 `total_count=1`、`score_snapshot_id=aae868a1-f4b8-468c-9a03-e30b1b90bfbd`、4 条 answer analysis 的 `parser_engine_id=rule_based_v2_aliases`、`analysis_version=rule_based_v2_aliases`。
- 当前边界：本切片让已确认 aliases 参与规则解析和重跑分析；还不是自动 alias 候选生成、批量审核队列、LLM-as-judge A/B、人工复核工作台或 alias 节点在 traceability bundle 中的独立深链路。
- 相关计划：`PROJECT-PLAN.md` 的 M3 Answer Parser + AUVisibilityScore、实体/别名表、全局可追溯 DoD 与审计链风险项。

## 2026-06-10 · Runtime Entity Alias candidates

- 目标：补齐实体别名“候选推荐”的最小路径，让控制台不再只能手填 alias，而是能从现有实体字段生成待确认候选，并继续复用 `entity_alias_confirmed` 审计链。
- 范围：计算型 alias candidate read model；FastAPI candidate endpoint；Runtime Console 候选列表与一键确认；core/API tests；README/PLAN/审计日志同步。
- 代码落盘：
  - `packages/geno_core/geno_core/models.py`：新增 `RuntimeEntityAliasCandidate` 与 `RuntimeEntityAliasCandidatePage`。
  - `packages/geno_core/geno_core/repository.py`：新增 `list_runtime_entity_alias_candidates()`，从 canonical name + Australia、official domain host、product line、parent company 生成候选，排除 canonical name 与已确认 alias；候选是计算型 read model，不新增表。
  - `apps/api/geno_api/main.py`：新增 `GET /v1/entity-aliases/runtime/candidates?project_id=...`，并在 `/v1/contracts` 暴露候选模型和 endpoint。
  - `apps/web/app/page.tsx`、`apps/web/app/globals.css`：Project Bootstrap 面板新增候选数量和前 4 条候选列表，每条候选可通过 `Confirm candidate` 按钮复用现有确认 action，写入 `entity_aliases` 并生成 `entity_alias_confirmed`。
  - `tests/test_core_contracts.py`、`tests/test_api_contracts.py`：覆盖候选生成/去重/排除已确认 alias、endpoint 缺 persistence 503 与 contracts 暴露。
  - `README.md`、`PROJECT-PLAN.md`：同步 alias candidates 最小路径已落，批量审核队列和证据驱动自动候选仍待接。
- 验证项：
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_core_contracts.CoreContractsTest.test_postgres_repository_lists_runtime_entity_alias_candidates tests.test_api_contracts.ApiContractsTest.test_runtime_entity_alias_candidates_endpoint_requires_persistence_config tests.test_api_contracts.ApiContractsTest.test_contracts_include_m7_content_integrations`：通过，3 个目标测试 OK。
  - `PYTHONPATH=packages/geno_core:apps/api python3 -m unittest tests.test_api_contracts tests.test_core_contracts tests.test_worker_cli`：通过，94 个 Python contract/API/core/worker tests 全部 OK。
  - `make test`：通过，94 个 Python contract/API/worker tests 全部 OK。
  - `npm --prefix apps/web run build`：通过，Next.js 15.5.19 production build 成功。
  - `make docker-config`：通过，Compose 配置可解析。
  - Docker API/Web E2E：通过。因本机已有其他服务占用默认端口，本次使用 compose `!override` 将 API/Web 暂映射到 `18000:8000`、`13000:3000`，Postgres/MinIO 不暴露宿主端口；`POST /v1/projects/runtime/au/dtc-ecommerce` 后，`GET /v1/entity-aliases/runtime/candidates?project_id=6624961f-36ae-539b-9d48-51619b42e37e&entity_kind=brand&limit=10` 返回 `candidate_count=1`，首条候选为 `alias=ExampleBrand Australia`、`alias_type=alias`、`source=canonical_name_market`、`confidence=0.72`。
  - Playwright 桌面 `1440x1600`：通过，断言 `Entity Alias`、`Candidate · alias · brand`、`ExampleBrand Australia` 可见；点击 `Confirm candidate` 后页面可见 `entity_alias_confirmed`；截图保存为 `/tmp/geno-alias-candidates-console.png`。
  - Playwright 移动 `390x1600`：通过，断言 `Entity Alias` 与 `entity_alias_confirmed` 可见；截图保存为 `/tmp/geno-alias-candidates-console-mobile.png`。
  - `GET /v1/entity-aliases/runtime?project_id=...&entity_kind=brand&limit=10` 与 candidates 二次校验：通过，确认后 `alias_count=1`、`candidate_count_after_confirm=0`、`alias=ExampleBrand Australia`、`audit_event=entity_alias_confirmed`、`after_hash=626cfa50e6df9f39f356c0bfb9868199f9637aea15685e3f6bbf7627018fbfdd`。
- 当前边界：本切片完成基于实体字段的计算型候选和一键确认；还不是从真实 answers/citations 中挖掘 alias、批量审核队列、权限流转或 alias candidate 的持久化状态机。
- 相关计划：`PROJECT-PLAN.md` 的 M3 实体/别名表与同名消歧、M4/M5 审计链风险项。

## 2026-06-10 · Runtime Report History panel

- 目标：把已持久化的 `ReportExport` 历史接入 Runtime Console，让报告不只展示最新快照，还能按当前 AU runtime project 复盘最近导出版本、冻结 artifact URL、即时下载路径和报告审计摘要。
- 范围：Runtime Console report 查询与历史面板；README/PLAN/审计日志同步；Next.js build、Python tests、Docker/Playwright E2E 验证。
- 代码落盘：
  - `apps/web/app/page.tsx`：runtime report 查询从 `limit=1` 调整为项目内 `project_id + limit=5`；Runtime Filters 区展示实际 `Report query`；新增 `Report History` 宽面板，逐条展示 `report_version`、`exported_at`、`report_type`、sample size、linked answer runs、score snapshot 数、audit event 数、final score、methodology hash、冻结 Markdown/PDF/CSV URL、继承当前筛选/排序的 Markdown/CSV/PDF artifact 下载入口和首个 report audit event。
  - `apps/web/app/globals.css`：新增 `reportHistory`、`reportHistoryItem`、`reportHistoryDownloads` 响应式样式，确保长 URL 和 hash 在桌面/移动视口都可换行。
  - `README.md`：补充 Report History 使用说明、`project_id` report 查询示例，以及历史读取不改写冻结 `ReportExport` 的审计口径。
  - `PROJECT-PLAN.md`：同步 M4/M5/P1 状态，标明单项目导出历史只读面板已落，多客户/多项目切换、白标模板、导出历史管理与权限流转仍待接。
- 验证项：
  - `npm --prefix apps/web run build`：通过，Next.js 15.5.19 production build 成功。
  - `make test`：通过，94 个 Python contract/API/core/worker tests 全部 OK。
  - `make docker-config`：通过，Compose 配置可解析。
  - `git diff --check`：通过，无 whitespace error。
  - Docker API/Web + worker E2E：通过。使用临时 compose override 将 API/Web 映射到 `18000:8000`、`13000:3000`，Postgres/MinIO 不暴露宿主端口；`docker compose -p geno-report-history-test ... up --build -d api web` 后 API health ready，worker profile 成功写库，输出 `raw_evidence_records=4`、`analysis_count=4`、`score_contributions=8`、`report_export_id=335c876f-9fef-56fc-a449-a3fd4e134506`、`report_artifacts.enabled=true`、`traceability_bundle_id=501e9ce7-5379-5d05-bf40-6d14232be695`。
  - `GET /v1/reports/runtime?project_id=6624961f-36ae-539b-9d48-51619b42e37e&limit=5` 字段校验：通过，返回 `report_total_count=1`、`report_version=worker-runtime-v1`、`audit_event=report_export_created`。
  - `GET /v1/reports/runtime/335c876f-9fef-56fc-a449-a3fd4e134506/artifact?type=csv&platform=perplexity&sort=cost_desc` header 校验：通过，返回 `X-GENO-Report-Artifact-Hash=06c34c07f5d6ecf1babad8d745fa9bdc40a85dfae874e8dc9b5a20ac8d62100d`、`X-GENO-Report-Artifact-Filter-Hash=1b9741654d06b6aa2cc52272f1243d0811b7b0562f35752f89dcc6668762e558`、`X-GENO-Report-Artifact-Sort=cost_desc`、`X-GENO-Report-Artifact-Row-Count=2`。
  - Playwright 桌面 `1440x1700`：通过，断言 `Report History`、`worker-runtime-v1`、`Frozen artifact URLs`、`Artifact filter path`、`report_export_created`、`Markdown/CSV/PDF` 链接和 `Report query` 可见；CSV href 含 `type=csv&platform=perplexity&sort=cost_desc`；截图保存为 `/tmp/geno-report-history-console.png`。
  - Playwright 移动 `390x1800`：通过，断言 `Report History`、`worker-runtime-v1`、`Artifact filter path`、`report_export_created` 可见；截图保存为 `/tmp/geno-report-history-console-mobile.png`。
  - 验证后已执行 `docker compose -p geno-report-history-test ... down -v` 清理测试容器与卷。
- 当前边界：本切片只新增已有 `ReportExport` 的历史读取与 UI 展示，不改变 report id/version 生成规则、不新增白标 PDF renderer、不实现多客户/多项目切换、导出历史管理后台或权限隔离；artifact 下载仍是即时渲染的证据附录筛选/排序，不改写冻结报告快照。
- 相关计划：`PROJECT-PLAN.md` 的 M4 看板筛选与导出、M5 Evidence Report Export、M5/P1 代理商工作流与审计链风险项。
