# Pack 08 consolidated external-needs register

Status date: 2026-07-28. This register contains the inputs that an autonomous coding agent cannot
truthfully manufacture. They are non-blocking for locally controllable implementation and must be
provided or approved together before the corresponding roadmap IDs can move from
`BLOCKED_EXTERNAL`/`READY_FOR_REVIEW` to `ACCEPTED`.

Never paste a credential into an issue, chat, manifest, command history or repository file. Supply a
project-scoped Secret Reference, a protected `0600` secret file on the target host, or access through
the approved deployment secret mechanism.

Local closure is complete: the exact stable-ID register has zero `LOCAL_GAP`, and no additional
locally actionable implementation input is being requested. The table below is the single deferred
request for people, credentials, lawful source material, production-equivalent infrastructure and
independent decisions; Board B inputs remain separately excluded.

## Required for non-B acceptance

| Need | Minimum supplied evidence | Unblocks | Status |
|---|---|---|---|
| Named accountable people | Product, engineering, Migration, API/OpenAPI, QA, Security, DevOps and release owners; independent verifier(s); substitutes/on-call; capacity allocation | M0 governance, DoR/DoD, evidence N/A decisions and final signatures | `BLOCKED_EXTERNAL` |
| Approved budget and quotas | Per-provider/model, proxy-free non-B testing, storage and staging budget; quota/rate limits; cost-alert owner and stop rule | Provider/model live calls, nine-channel evaluation and full performance run | `BLOCKED_EXTERNAL` |
| Model and Provider access | The project-scoped DeepSeek Secret Reference has passed all ten Dify technical canaries; the first eight flows also have their existing business-path evidence. Supply the remaining OpenAI, Gemini, Perplexity, Microsoft Foundry/Bing Grounding and Kimi Secret References plus approved exact models/agents, retention/display policy and ten-repeat canary Suites | `REAL-MODEL-AC-01` and five Provider release manifests; Style Profile/Recommendation are blocked by business inputs below, not by DeepSeek access | `PARTIAL / BLOCKED_EXTERNAL` |
| Dify deployment rights | Written confirmation that the selected Dify edition/license permits the intended production user, workspace and commercial deployment model; identify any plugin/model-provider restrictions. The current single operations workspace remains usable while this is pending | multi-user or multi-workspace production rollout and final legal/operational sign-off | `BLOCKED_EXTERNAL` |
| Nine-channel lawful inputs | Approved access or compliant manual-import material for owned site, ProductReview, YouTube, Reddit, Amazon, OzBargain, TikTok, Instagram and Quora; any login account has an approved authorization record | real Style Collection/Profile evidence | `BLOCKED_EXTERNAL` |
| Human style/release review | At least 200 deduplicated, anonymized, Australian-English and human-approved samples per channel; independent review of 40 fixed Cases per channel and 360 total | per-channel `passed >= 95%`, zero subject mix/anti-copy, style `>=4.2/5` and `LAB-*` | `BLOCKED_EXTERNAL` |
| Real Recommendation evidence | One approved non-B selection containing real Observation, Statistic, Fact and Rule lineage plus the responsible business reviewer; do not substitute fixtures or B Attribution | one real `recommendation.generate` Job, Dify run, evidence graph and saved Recommendation/result acceptance | `BLOCKED_EXTERNAL` |
| Governed consumer-surface manual exports | Lawfully supplied, redacted real manual transcript artifacts for AIO, AI Mode and Copilot plus independent maker/checker, when real non-live parser validation is desired | real manual-artifact validation only; never Australian/automated capture claims | `BLOCKED_EXTERNAL` |
| Production-equivalent staging | Candidate image digests; isolated Project/Campaign; OIDC identities; SMTP and signed internal Webhook endpoints; production-equivalent PostgreSQL/MinIO/Valkey/network/resource topology | live staging, notification delivery, production-network and Customer boundary verification | `BLOCKED_EXTERNAL` |
| Key custody and disaster recovery | Separate custodians/escrow for backup plus Secret/Provider/Synthetic/Recommendation/Workflow C keyrings; current encrypted backup; ephemeral restore authority | production empty-environment restore, representative decrypt/connection tests and independent receipt | `BLOCKED_EXTERNAL` |
| Real legacy migration rehearsal | Sanitized or controlled copy of the actual Prompt/Protocol/Observation/Metric dataset and complete old/new writer inventory | real initial/final watermark, two zero-difference rounds, lag zero and rollback-window evidence | `BLOCKED_EXTERNAL` |
| Full frozen performance run | Approval to run the immutable 30-minute `performance-profile-v1-non-b` workload on the production-equivalent topology | `PERF-AC-02/03`, M6 performance Gate | `BLOCKED_EXTERNAL` |
| Independent operational drill | A verifier who did not author the implementation executes deployment/rollback, alert, secret/provider rotation, retention/hold and backup/restore runbooks | `M6-OPS-01`, `PLAN-FINAL-17/20` | `BLOCKED_EXTERNAL` |

## Explicitly excluded Board B inputs

The following are tracked in
`GEO-external-data-cross-engine-sampling-implementation-plan-2026-07-22.md` and do not block the
current non-B coding scope:

- Connector Core and PyAirbyte execution, GSC/GA4 accounts and Google/Bing official-report imports.
- LokiProxy provider-managed sticky residential/mobile pool, Browser Capture Connector,
  consumer-surface automated authorization and per-Attempt pre/target/post egress proof.
- First-party event intake, Session/Touch/Lead/Conversion/Deal/Revenue ledger and one consented real
  attribution journey.

These inputs must not be substituted with another Provider API response, manual UI artifacts, fixtures,
GA4 aggregate reports, synthetic results or a generic Australian IP. A LokiProxy gateway must be a
real provider-managed residential/mobile sticky pool with a per-Attempt session lease; a bare IP is
not an acceptable substitute. Their related IDs remain
`EXCLUDED_B_FOR_CURRENT_ITERATION` or blocked in the independent external plan.

## Unblock protocol

1. Record only the Secret Reference, redacted account/environment identity, owner and approved scope.
2. Re-run the exact canary/live/performance/restore command against the frozen release and manifest.
3. Store the resulting artifact in the controlled evidence bucket with SHA-256 and `0600` local receipt.
4. Have an independent verifier recompute hashes, validate Project/Campaign/environment scope and sign.
5. Update the original roadmap ID; do not mark a parent Gate accepted before every referenced child is
   accepted.
