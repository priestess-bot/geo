# 架构图 · 出版级图注与设计规范（Figure Specifications）

本文件是 [../ARCHITECTURE.md](../ARCHITECTURE.md) 中三张架构图的**正式图注（caption）与设计规范（design spec）**，按顶刊制版要求编写，可直接据此用 TikZ / SVG / Graphviz 产出符合论文要求的矢量图。

- 渲染版（Mermaid，快速预览）：[../ARCHITECTURE.md](../ARCHITECTURE.md)
- 架构来源（规格）：[GENO-SaaS-AU-首发技术落地路径.md](GENO-SaaS-AU-首发技术落地路径.md) 第 3 章
- 落地节奏：[../PROJECT-PLAN.md](../PROJECT-PLAN.md)

> 顶刊图最看重的是**跨图一致性**与**在黑白/色盲条件下仍可无歧义解码**。故第 0 节先定义贯穿三图的统一视觉语言，每个语义都用"形状 + 边框 + 颜色"冗余编码，不单靠颜色。

---

## 0. 统一视觉语言（三图通用）

### 0.1 画布与导出（按期刊制版要求）

- **矢量优先**：PDF 或 EPS（嵌字体）；栅格兜底 ≥ 600 dpi。
- **栏宽**：单栏 **88 mm**、双栏 **180 mm**。图一用单栏纵向（88 × 120 mm），图二用单栏（88 × 100 mm），图三用双栏横向（180 × 70 mm）。
- **基准栅格**：8 pt 对齐网格，所有节点与连线锚点吸附栅格。
- **留白**：图框内边距 ≥ 6 pt；节点间距 ≥ 12 pt，禁止重叠与贴边。
- **色彩**：屏显 sRGB、印刷转 CMYK 仍可辨；**必须通过灰度打印测试**。

### 0.2 形状语法（shape grammar）

| 形状 | 语义 | 用于 |
| --- | --- | --- |
| 直角矩形 | 软件能力模块（无独立运行时） | Collector、Parser… |
| 圆角矩形（r=4pt） | 进程/编排/运行器（有运行时行为） | 编排器、Worker、Answer Runner |
| 虚线描边 + «» 折角标签 | **接口契约**（抽象，非实现） | `«CollectorBackend»` 等 |
| 圆柱 | 数据存储 | PostgreSQL、ClickHouse、MinIO、Evidence Store |
| 折角文档形 | 数据制品（流水线中流动的产物） | AnswerRun、RawAnswer、Citation |
| 虚线矩形 + 右上"⤴外部"角标 | 第三方/可替换实现 | Qdrant、Neo4j、供应商 API |

### 0.3 连线语法（line grammar）

| 线型 | 箭头 | 语义 |
| --- | --- | --- |
| 实线 1.5 pt | 实心三角 | **调用/依赖**（图一）或**主数据流**（图三） |
| 实线 1.2 pt | **空心**三角 | **实现关系**（图二："实现 → 指向 → 接口"，UML realize 反向） |
| 虚线 1.2 pt（dash 4/2） | 开放 V 形 | **反馈/回采**（图三）或可选路径 |
| 细线 0.6 pt 灰 | 无 | 分层泳道边界、图例引导线 |

> 箭尾 = 主动方，箭头 = 被动方。**禁止**双向实线（含义会变模糊）；需要双向时用两条单向线，或在图二用专门的"⇄ 可切换"图元。

### 0.4 配色（Okabe–Ito 色盲安全调色板）

| 语义 | 描边 | 填充 |
| --- | --- | --- |
| L1 控制台层 | 天蓝 `#56B4E9` | `#E8F4FC` |
| L2 应用服务层 | 蓝绿 `#009E73` | `#E4F3EE` |
| L3 能力模块层（核心） | 中性灰 `#6E6E6E` | `#F4F4F4` |
| L4 基础设施层 | 暖沙灰 `#9C8B73` | `#EDE7DD` |
| **接口/实现缝隙（保留色）** | 朱红 `#D55E00` | 无填充 |

要点：**朱红仅保留给"接口契约"与"实现关系"**，让"可替换缝隙"在三张图里是同一个视觉信号；能力模块层刻意去饱和为中性灰，强调它是"被接口包裹的抽象核心"。

### 0.5 字体与标注

- 正文无衬线：拉丁 **Inter / Helvetica Neue**，中文 **思源黑体**；标识符（`au_visibility_v1`、表名、接口名）用**等宽** `JetBrains Mono`。
- 字号：节点标题 **10–11 pt 半粗**、副标/字段 **8 pt**、标注 **7.5 pt 斜体**、图内最小字号 **≥ 7 pt**。
- 对比度 ≥ 4.5:1。
- **标注（callout）**：7.5 pt 斜体 + 细引导线，置于留白区，不压线、不压节点。
- **图例**：每图右下角嵌入式 legend，列出该图用到的形状/线型/颜色子集。

---

## 1. 图一 · 四层引用架构（Layered Reference Architecture）

**正式题注**

> **图 1. GENO 澳大利亚首发系统的四层引用架构。** 系统自上而下分为控制台、应用服务、能力模块与基础设施四个泳道（填充由冷至暖区分层级）。依赖严格单向向下（实心箭头，箭尾为依赖方）。能力模块层的八个模块经接口契约（朱红 «» 标签，虚线描边）与可替换后端解耦，构成贯穿系统的"可替换缝隙"；带 «» 的模块即图 2 枚举的八个可插拔点。采集经隔离的 Worker 执行（圆角矩形），证据写入三类存储（圆柱）。**本图仅表达调用与持久化关系；模块间的数据依赖见图 3。**

**设计意图（视觉论点）**：让读者一眼读出两件事——(1) **严格单向分层**；(2) **接口缝隙的位置**（朱红标签在哪，可替换性就在哪）。

**版式与流向**：纵向，四条等高泳道自上而下堆叠；主依赖流向 **↓**。左缘画一条贯穿四层的细灰竖轴，顶端标注"**依赖方向（单向）↓**"。

**逐层坐标**

- **L1（顶带）**：`Next.js 控制台` 居中。
- **L2**：`编排器`（圆角矩形）居中偏左；`采集·分析 Worker`（圆角矩形）置于其右，**故意右移**以预示采集旁路；Worker 右上角标注"**进程隔离**"。
- **L3（最高带）**：八个能力模块按 **2 行 × 4 列**网格——
  - 上行：`Collector` · `EvidenceStore` · `Parser` · `ScoringEngine`
  - 下行：`CitationGraph` · `CompetitorBenchmark` · `ActionPlanner` · `ReportExporter`
  - 右侧竖排两个接口标签 `«LLMGateway»`、`«GeoProvider»`（朱红）。
  - 带 «» 的模块（Collector / Parser / ScoringEngine / CitationGraph）其接口名以**朱红折角小标**贴在该模块右上角。
- **L4（底带）**：六个存储/平台横向一行——`PostgreSQL+pgvector` · `ClickHouse` · `MinIO` · `PG邻接表/Neo4j` · `Temporal` · `Langfuse/promptfoo`（前四为圆柱，后两为圆角矩形）。

**边与标注**（实心箭头）

- 纵向主链 `控制台 → 编排器 → {七模块}` 走直线、等距分叉，**禁止交叉**。
- `Collector → Worker → «GeoProvider»` 单独走右侧通道，与主链分离，强化"采集旁路"。
- `Collector / Parser / ActionPlanner → «LLMGateway» → Langfuse` 用同色（中性灰）成束。
- L3 → L4 持久化边一律**垂直下落**，落到对应圆柱正上方。
- **关键标注**：在 L3 与右侧 «» 标签之间画一道朱红方括号，标"**可替换缝隙（swap seam）**"；并注"`CompetitorBenchmark` 的数据依赖见图 3"以消除与图三的歧义。

**图例（右下角）**：直角矩形 = 模块、圆角 = 进程、圆柱 = 存储、«» 虚线 = 接口；实心箭头 = 依赖 ↓；冷 → 暖 = 层级。

---

## 2. 图二 · 可插拔点（Pluggable Seams / Realization Map）

**正式题注**

> **图 2. 八个可插拔接口及其可替换实现。** 左列为先定义的接口契约（朱红 «»），右列为满足该契约的实现；空心三角箭头表示"实现 → 契约"的实现关系。每行内的默认实现以实心底色标出，备选实现以虚线外框标出，二者间的 ⇄ 表示可在不改业务代码的前提下整体替换或并存。八行相互独立，不存在跨行连接。**右侧纵贯括号标明：替换仅需新增适配器。**

**设计意图**：把"开源优先·可插拔"做成**可数的、对齐的八条独立缝隙**——读者应能立刻数出"有 8 个点可换"，并看出替换边界统一落在接口处。

**版式**：横向，左右两列 + 居中竖虚线分隔，顶端骑缝标注"**接口 ┃ 实现**"。八行等高、严格基线对齐（顶刊图最忌行高参差）。

**逐行内容**（左 = 接口；右 = 默认实现 ｜ 备选实现）

1. `«CollectorBackend»` ｜ **Perplexity API**、**ChatGPT API** ｜ Google AIO(浏览器/第三方)、SearXNG、人工补录
2. `«VectorStore»` ｜ **pgvector** ｜ Qdrant、Milvus
3. `«GraphStore»` ｜ **PG 邻接表** ｜ Neo4j、Apache Jena
4. `«LLMGateway»` ｜ **LiteLLM** ｜ 任意供应商、vLLM 自托管
5. `«ParserEngine»` ｜ **规则解析** ⇄ **LLM-as-judge**（并列双默认，可 A/B）
6. `«ScoringFormula»` ｜ **au_visibility_v1** ｜ v2…（版本化，旧分可重算）
7. `«GeoProvider»` ｜ **uule 参数** ｜ 代理池、第三方供应商
8. `«ReportExporter»` ｜ **模板引擎** ｜ Metabase（PDF·CSV）

**编码细化**

- 实现关系箭头：**空心三角**由每个实现指向其接口；同一接口的多实现共用一个汇入点，呈"扇入接口"。
- 默认实现：填充 `#F4F4F4`、实线边；备选实现：白底、虚线边 + 右上"⤴外部"角标。
- ⇄ 用朱红双箭头图元，仅出现在"可热切换"处（第 5 行 A/B、第 6 行版本升级）。
- **关键标注**：右缘一道纵贯八行的方括号，标"**替换 = 仅新增适配器，业务代码不变**"，并引到图一的"可替换缝隙"形成跨图呼应。

**图例**：朱红 «» = 接口；空心三角 = 实现关系；实底 = 默认、虚框 = 备选；⇄ = 可切换。

---

## 3. 图三 · 证据优先数据流水线（Evidence-First Pipeline）

**正式题注**

> **图 3. 证据优先的数据流水线与复测闭环。** 数据自左向右流动（实心箭头为主数据流）：市场/行业画像 → 问题集 → 采集运行器 → **原始证据库** → 解析与信源图谱两路并行 → 评分、竞品对标 → 行动计划 → 证据报告 → 复测，复测以虚线"同口径回采"反馈回运行器形成闭环。**原始证据库（朱红描边圆柱，居中加重）是全流程唯一事实源**，所有分数与结论均可回溯至其中的 AnswerRun / RawAnswer / Citation。证据库向解析与信源图谱**双路分叉**；竞品对标由评分与信源图谱**双路汇入**；证据报告由评分、信源图谱、竞品对标、行动计划**四路汇入**。

**设计意图**：让读者一眼读出两点——(1) **证据库是单一事实源**（视觉上最重、居中、朱红）；(2) **全链闭环可复测**（末端虚线回采）。

**版式与流向**：双栏横向，主轴为一条水平"脊"。左 → 右分布处理阶段；证据库**抬升为脊柱节点**（比其他节点大 1.3×、朱红 1.8 pt 描边），其内部叠放三枚折角文档形 `AnswerRun` / `RawAnswer` / `Citation`。

**节点序与分叉/汇聚（精确拓扑）**

- 主干：`MarketProfile+IndustryProfile → Prompt Pack(100×k=3) → AI Answer Runner → 原始证据库`。
- **双路分叉**：证据库 → `Answer Parser`（脊上方）与 `Citation Graph`（脊下方）。
- 上路：`Answer Parser → AUVisibilityScore`。
- **双路汇聚**：`AUVisibilityScore` 与 `Citation Graph` → `Competitor Benchmark → Action Plan`。
- **四路汇聚**：`AUVisibilityScore`、`Citation Graph`、`Competitor Benchmark`、`Action Plan` → `Evidence Report(PDF/CSV)`。
- **闭环**：`Evidence Report → 复测(T+7/14/30)`，复测以**虚线开放箭头**沿底部回弧指向 `AI Answer Runner`，线上标"**同口径回采（prompt_version, k=3）**"。

**编码与标注**

- 证据库标注"**单一证据源 · 所有分数可回溯**"。
- 报告汇入处标"**四源汇聚**"。
- 采集运行器下挂一枚朱红 `«CollectorBackend»` 小标，呼应图二。
- **底部脚注框**（7.5 pt 斜体）："**本图箭头是数据流向；采集构建序（Perplexity 先、Google AIO 后）与平台评分权重（45/30/25）均与箭头无关。**"——固化关键去歧义。

**图例**：圆角 = 处理阶段；圆柱 = 数据存储；折角文档 = 数据制品；实心箭头 = 主数据流；虚线 = 反馈回采。

---

## 4. 三图协同（系统级设计一致性）

- **朱红 = 可替换缝隙** 在三图中是同一个信号：图一标位置、图二展开实现、图三在采集处复现。读者跨图即可建立"接口在哪 → 能换成什么 → 运行时怎么流"的连续认知。
- **互补而不重复**：图一 = 静态依赖、图二 = 可变性、图三 = 运行时数据流；三者对同一系统的三个正交切面，**无一条边在两图中重复语义**。

---

## 5. 如何据本规范出图

无论哪种产线，**第 0 节是唯一样式真源**：改样式只改第 0 节，三图随之统一。
