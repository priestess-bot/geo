[project]
name = "geo-connector-worker"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "airbyte==0.53.2",
  "cryptography==45.0.5",
  "dramatiq[redis]==1.18.0",
  "geo-core",
  "psycopg[binary]==3.3.4",
]

[tool.uv]
package = false

[tool.uv.sources]
geo-core = { path = "../../packages/geo_core", editable = true }
