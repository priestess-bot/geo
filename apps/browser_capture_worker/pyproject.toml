[project]
name = "geo-browser-capture-worker"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "cryptography==45.0.5",
  "dramatiq[redis]==1.18.0",
  "geo-core",
  "playwright==1.60.0",
  "psycopg[binary]==3.3.4",
]

[tool.uv]
package = false

[tool.uv.sources]
geo-core = { path = "../../packages/geo_core", editable = true }
