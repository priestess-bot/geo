# 智推时代 GEO / GENO 调研与 SaaS 落地文档库

本仓库是围绕 **智推时代（GenOptima）** 及其 **GEO（Generative Engine Optimization，生成式引擎优化）** 业务的一次完整调研与产品落地规划。内容包括：公司与行业可审计调研复盘、GENO 方法论与技术栈拆解、竞品格局、合作案例核查，以及面向**澳大利亚首发**的 GENO SaaS MVP 技术设计与需求拆解。

本库已从文档与规划进入工程实现：除调研文档外，当前已包含 FastAPI API、Next.js Runtime Console、Python 核心契约、P0a runtime-checkable plugin contracts 与 `NotConfigured*` stubs、AU 项目启动包、DTC 电商行业模板、100 条 Prompt Pack、M2a evidence chain、M2a Collection Run Summary、M2b Google spike gate fixture、真实 `google-spike` worker 模式、provider-neutral `ThirdPartySerpCollector`、独立 `google-serp-fixture` / `google-serp-spike` 120-run 第三方 SERP 对照入口、payload verifier 与 status report/hash verifier、P0b Google spike runbook/dry-run/env readiness/smoke/status/evidence package/hash verifier、M3 comparative parser（rule primary + judge fixture）+ AUVisibilityScore、ScoringFormula registry（active `au_visibility_v1` + candidate `au_visibility_v1_1_local_boost`）、项目级 Score Weight Config、Human Review Records、M4 Citation Graph + Competitor Benchmark、M5 Markdown/CSV/PDF Evidence Report Export 与 MinIO/S3-compatible artifact 归档、API-vs-browser Fidelity Check、M6 Action Plan + Retest comparison、AU retest scheduler plan（T0/T+7/T+14/T+30、每窗口 2400 planned runs、hash verifier、runtime endpoint）、M7 Knowledge Facts + Content Draft + Integrations fixture、KnowledgeFactEmbedding pgvector runtime search、AU broader platform registry（Gemini/Copilot/Claude/YouTube/Reddit/ProductReview 候选平台默认 disabled 且 weight=0）、Traceability Bundle、PostgreSQL repository 映射、`DATABASE_URL` runtime connection、runtime API PostgreSQL connection pool、runtime readiness/diagnostics 与 Prometheus 文本格式 `/metrics`、AU 项目启动包 runtime 创建/读取 API、AU 启动包/prompt 元数据持久化、worker `--persist` / `--persist-analysis` / `--score-formula-version` 写库开关、runtime project / project brand kit / project brand logo upload / score weight config / score formula catalog / human review / prompt / prompt CSV/file import / prompt import history / evidence / collection run summary / fidelity check / manual backfill / entity alias confirmation / entity alias candidates / evidence CSV export / saved views / score / citation graph / report / report artifact / report artifact signed URL / action plan / content engine / knowledge fact search / traceability 查询 API、Runtime Console Project Bootstrap / Brand Kit / Logo Upload / Theme Editor / Score Weights / Score Formula selector / Human Review Trail / Entity Alias Candidates / Runtime Filters / Evidence Sort / Saved Views / Prompt Pack / Prompt CSV Import / Prompt File Import / Prompt Import History / Question Detail / Manual Backfill / Latest Evidence / Collection Run Quality / Evidence Runs / Score Contributions / Citation Graph & Competitors / Citation Graph Map / Report Snapshot / Report History / Report Method & Evidence Appendix / API-browser Fidelity / Action Plan & Retest Detail / AU Retest Scheduler Plan / Content Engine Detail / pgvector Knowledge Search / Broader Platform Registry / Traceability Detail、Traceability Map、节点级 details 钻取与锚点深链路面板、SQL 迁移、PostgreSQL RLS/GUC 项目隔离、Docker Compose、CI、ADR 与工程实施审计日志。真实 provider preflight 已支持 `preflight_summary`、`preflight_audit_checklist`、`preflight_payload_hash`、`make verify-api-preflight` 离线复核、`make preflight-manifest` 证据索引和 `make au-p0a-runbook` 真实批次命令计划；Google P0b 已支持默认 browser + manual 240-run 核心矩阵，`PlaywrightGoogleAIOCollector` / `PlaywrightAIModeCollector` 已具备 selector-driven Playwright health gate、HTML/screenshot hash 和 fake-browser 合同测试，`ManualBackfillCollector` 已支持 `MANUAL_BACKFILL_PATH` JSONL 文件、health gate、同 prompt/city 的 k 样本顺序消费、120-row template generator 和 strict coverage verifier，`ThirdPartySerpCollector` 可通过 `make au-p0b-google-serp-fixture` / `make verify-au-p0b-google-serp-fixture` / `make au-p0b-google-serp-fixture-manifest` / `make au-p0b-google-serp-health` / `make verify-au-p0b-google-serp-health` / `make au-p0b-google-serp-health-manifest` / `make au-p0b-google-serp-status` / `make verify-au-p0b-google-serp-status` 作为独立第三方 SERP 对照后端运行、复核和汇总，以及 `make au-p0b-google-runbook`、`make verify-au-p0b-google-runbook`、`make au-p0b-google-runbook-dry-run`、`make verify-au-p0b-google-runbook-execution`、`make au-p0b-google-playwright-env`、`make verify-au-p0b-google-playwright-env`、`make au-p0b-google-playwright-smoke`、`make verify-au-p0b-google-playwright-smoke`、`make au-p0b-google-spike-health`、`make au-p0b-google-spike-health-manifest`、`make au-p0b-google-spike`、`make au-p0b-google-spike-manifest`、`make au-p0b-google-status`、`make verify-au-p0b-google-status`、`make au-p0b-google-package`、`make verify-au-p0b-google-package` 固定真实 spike 执行路径、停止条件与单文件证据包。报告 artifact 下载已可继承当前 Runtime Filters/Sort，只过滤导出的证据附录，不改写冻结 `ReportExport`、`report_exports.method_disclosure` 与评分快照；`report_exports.method_disclosure.score_rate_denominators` 会冻结 Trigger/Mention/Recommendation 三类 rate 的 numerator、denominator、formula 和本报告证据行分母计数；PDF artifact 支持 `template=white_label` 白标模板，优先从项目级 Brand Kit 读取客户名、服务商名、Logo URL、主题色和页脚，URL query 可显式覆盖客户名/服务商名，响应头会返回 template 与 template hash；`GET /v1/reports/runtime/{report_export_id}/artifact/signed-url` 可用 `GENO_REPORT_ARTIFACT_SIGNING_SECRET` 生成带 `expires_at/signature` 的 HMAC signed download URL，并对 type/template/filter/sort/actor 参数做完整性保护；Logo Upload 会把图片 raw body 归档到 MinIO/S3-compatible `brand-assets/<project_id>/...`，再把 `s3://...` URI 写回 Brand Kit 并生成 `project_brand_logo_uploaded` 审计事件；Theme Editor 使用同一组 Brand Kit 字段提供白标报告预览、主题色选择和最近 Brand Kit 审计摘要；Report History 会按当前 AU runtime project 读取最近 5 个冻结 `ReportExport`，展示版本、导出时间、方法 hash、Method Disclosure、对象存储 URL、即时 artifact 下载入口、白标 PDF 下载入口、signed URL 生成入口和报告审计摘要，并可追加 `report_export_management_recorded` 管理事件标记 `internal_review/client_ready/archived`，不改写冻结报告。核心原则仍是**可审计**：每一个调研结论尽量回指原始来源（PDF、网页快照、行业报告），每一个工程输出逐步建立 `AuditEvent / EvidenceLink / ScoreContribution / CollectionRunSummary / ReportExport / ApiBrowserFidelityCheck / ActionRecommendation / RetestComparison / ContentDraft / KnowledgeFactEmbedding / HumanReviewRecord / TraceabilityBundle` 溯源链。

补充：Prompt Pack 已从 Console 列表读回推进为 hash-only CSV 复盘附件。`GET /v1/prompts/runtime/export.csv?project_id=...&market_code=AU&intent_type=...` 复用 runtime prompt read model，按 prompt 行导出 prompt id、project、market/industry、prompt text hash、intent/city/language、target brand hash、competitor count/name hashes、priority、intent weight、prompt version 和 status，并通过 `X-GENO-Prompt-Export-Hash`、row count、total count 固定内容。CSV 不导出 raw prompt text、raw target brand 或 raw competitor name；Runtime Console 的 Prompt Pack 面板已展示 `Prompt CSV` 路径和 `Download prompt CSV` 入口，便于 AU handoff 时离线证明 100 条问题集的版本、覆盖面和采集输入口径。

补充：Collection Run Summary 已从 Console 批次质量读回推进为 hash-only CSV 复盘附件。`GET /v1/collection-runs/runtime/export.csv?project_id=...&run_type=...` 复用 runtime collection run read model，按 collection run 行导出 planned/attempted/success/failure、success/trigger/answer rate、cost/duration、collector backend ids、platform/city/access method distribution keys/counts、failure summary key count/hash、answer run ids 和最近 `collection_run_summarized` audit 摘要，并通过 `X-GENO-Collection-Run-Export-Hash`、row count、total count 固定内容。CSV 不导出 raw failure message、raw provider error、raw prompt text 或 raw answer text；Runtime Console 的 Collection Run Quality 面板已展示 `Collection CSV` 路径和 `Download collection CSV` 入口，便于 AU handoff 时离线证明批次质量、成本、耗时和失败口径。

补充：API-vs-browser Fidelity Check 已从 Method Disclosure 面板推进为 hash-only CSV 复盘附件。`GET /v1/fidelity-checks/runtime/export.csv?project_id=...&report_export_id=...&status=...` 复用 runtime fidelity check read model，按 check 行导出官方 API/browser 记录数、可比 prompt-city 对、mismatch count、difference rate、payload hash、payload keys、answer run ids、checked_by hash 和最近 `api_browser_fidelity_checked` audit 摘要，并通过 `X-GENO-Fidelity-Check-Export-Hash`、row count、total count 固定内容。CSV 不导出 raw fidelity payload、raw prompt text、raw answer text、raw mismatch detail 或 raw checked_by；Runtime Console 的 Report Method & Evidence Appendix 面板已展示 `Fidelity CSV` 路径和 `Download fidelity CSV` 入口，便于 AU handoff 时离线复盘 API 结果和消费者界面抽检差异。

补充：Visibility Score / ScoreContribution 解释包已从 Console 读回推进为 hash-only CSV 复盘附件。`GET /v1/visibility-scores/runtime/export.csv?project_id=...&scope_type=...` 复用 runtime score read model，按 contribution 行导出 score snapshot id、公式版本、分数、rate、离散度、平台/组件权重 key、answer run 计数、prompt/analysis payload hash、component score/weight/denominator、evidence answer run ids 和最近 score audit 摘要，并通过 `X-GENO-Score-Snapshot-Export-Hash`、row count、total count 固定内容。CSV 不导出 raw prompt text、raw positive/negative evidence summary 或 raw confidence note；Runtime Console 的 Score Contributions 面板已展示 `Score CSV` 路径和 `Download score CSV` 入口，便于 AU handoff 时离线复算评分解释链。

补充：Content Engine / Knowledge Facts 证据包已从 Console 读回推进为 hash-only CSV 复盘附件。`GET /v1/content-engines/runtime/export.csv?project_id=...&review_status=...` 复用 runtime content engine read model，按 content draft 行导出知识事实数量/id/type/content hash、connector/provider/status、草稿 id/type/template/source gap/review status、草稿标题/正文 hash、目标 prompt hash、证据 answer run ids、source action 文本 hash、manual distribution URL/notes hash 和最近 content draft / content engine audit 摘要，并通过 `X-GENO-Content-Engine-Export-Hash`、row count、total count 固定内容。CSV 不导出 raw draft markdown、raw prompt text、raw target URL、raw notes 或 raw action text；Runtime Console 的 Content Engine Detail 面板已展示 `Content CSV` 路径和 `Download content CSV` 入口，便于 AU handoff 时离线复盘内容草稿、知识事实、证据绑定和人工分发链路。

补充：Citation Graph / Competitor Benchmark 已从 Console 明细推进为 hash-only CSV 复盘附件。`GET /v1/citation-graphs/runtime/export.csv?project_id=...` 复用 runtime citation graph read model，按 source node 行导出 source graph id、source domain/type/topic、source URL hash、answer run ids、prompt hash、graph evidence link ids/relation/citation ids、source gap id/type/expected weight/recommendation hash、competitor benchmark id/name/metric scope/payload keys/payload hash/answer run ids，并通过 `X-GENO-Citation-Graph-Export-Hash`、row count、total count 固定内容。CSV 不导出 raw source URL、raw prompt text、raw source gap recommendation 或完整 competitor payload；Runtime Console 的 Citation Graph & Competitors 面板已展示 `Graph CSV` 路径和 `Download graph CSV` 入口，便于 AU handoff 时离线复盘信源图谱、source gap 和竞品对标链路。

补充：Traceability Bundle 已从交互图谱推进为 hash-only CSV 复盘附件。`GET /v1/traceability/runtime/export.csv?project_id=...&report_export_id=...` 复用 runtime traceability read model，按 evidence link 行导出 bundle id、subject、report/score/contribution/answer/raw/citation/asset/source/action/content/audit id 集、source domain/gap、raw payload hash、prompt/action/content/explanation hash、最近 audit 摘要和 evidence link source/target/relation/answer run ids，并通过 `X-GENO-Traceability-Export-Hash`、row count、total count 固定内容。CSV 不导出 raw prompt text、raw answer text、raw action title/description、raw draft title/markdown、raw citation URL 或 explanation summary；Runtime Console 的 Traceability Detail 面板已展示 `Traceability CSV` 路径和 `Download traceability CSV` 入口，便于 AU handoff 时离线证明报告到评分、证据、图谱、行动、内容和审计事件的溯源链。

补充：P0a provider credential 更新现在有独立 receipt artifact。`make au-p0a-credential-update-receipt` 会读取 credential request、redacted env report、credential fulfillment 和 credential clearance，生成 `docs/runtime_preflight/au-p0a-credential-update-receipt-latest.json`，逐项记录 `DATABASE_URL`、`OPENAI_API_KEY`、`PERPLEXITY_API_KEY` 的 present/fulfilled、source、value length、sha256 prefix、blocking reasons、post-update checks、env-file hygiene、source artifact hash 和 `credential_update_contract` 摘要；receipt 禁止写入 raw secret value，`make verify-au-p0a-credential-update-receipt` 默认证明结构/hash/脱敏/来源自洽，`scripts/verify_au_p0a_credential_update_receipt.py --require-complete` 才作为真实凭证更新完成硬门禁。Runtime API `GET /v1/p0a-credential-update-receipt/au` 与 Runtime Console `P0a credential update receipt` 卡片读取同一口径。当前本地 artifact 为 `p0a_credential_update_receipt_hash=c97b885aab52c1a68c7350762541fd4bdc7b1fc90c078f6b0ff4fe2b9e1d8e31`、`credential_update_receipt_ready=true`、`credential_update_receipt_complete=false`、`credentials_fulfilled=false`、`credential_clearance_ready=false`、`missing_required_count=2`、`next_command=make au-p0a-env`；这证明真实 key 注入后的审计回执路径已经具备，不代表 `OPENAI_API_KEY` / `PERPLEXITY_API_KEY` 已经填充。

补充：P0a provider credentials 的外部依赖交接和 next work item 已把 credential clearance 与 update receipt 纳入同一条强制执行链。当前 `p0a_provider_credentials` dependency group 有 19 条 verification commands、9 个 evidence outputs，除 credential request/fulfillment 外还包含 `make au-p0a-credential-clearance`、`make verify-au-p0a-credential-clearance`、`verify_au_p0a_credential_clearance.py --require-cleared`、`make au-p0a-credential-update-receipt`、`make verify-au-p0a-credential-update-receipt` 和 `verify_au_p0a_credential_update_receipt.py --require-complete`；`make au-next-work-item` 当前生成 26 步 recommended sequence，并在 hard gates 中同步这些命令。当前本地 `external_dependency_handoff_hash=200fc741b8f3a6991693ac87c91e2560b0addddd669cf1fe10e0d41c8581ccd6`、`next_work_item_packet_hash=e02e4e015d4da6dc63b7bffab6bab1a3559e9ea740843f65e1b0375ccf248a15`、`recommended_sequence_count=26`；这防止执行者只完成 request/fulfillment 就误认为第一 blocker 已清零。

补充：AU 交付进度已从口头百分比拆成机器可复算 artifact。`make au-delivery-progress` 会读取 launch status、handoff dossier、customer handoff readiness、next work item、external dependency handoff、clearance dry-run、P0a credential clearance、P0a real batch clearance、P0b Google environment clearance、P0b Google manual backfill clearance 和 P0b Google phase execution clearance，生成 `docs/runtime_preflight/au-delivery-progress-latest.json`，且 Makefile 显式传入 `--p0a-credential-clearance-path ${GENO_AU_P0A_CREDENTIAL_CLEARANCE_OUTPUT_PATH:-docs/runtime_preflight/au-p0a-credential-clearance-latest.json}`、`--p0a-real-batch-clearance-path ${GENO_AU_P0A_REAL_BATCH_CLEARANCE_OUTPUT_PATH:-docs/runtime_preflight/au-p0a-real-batch-clearance-latest.json}`、`--p0b-google-environment-clearance-path`、`--p0b-google-manual-backfill-clearance-path` 与 `--p0b-google-phase-execution-clearance-path`，避免命令入口与脚本默认值脱节；`make verify-au-delivery-progress` 复算 `delivery_progress_hash`、13 个 progress gate、上游 source artifact hash、runtime endpoint、P0a credential/real batch clearance 与三类 P0b clearance summary 和 hard gate，并重新读取每个 `source_artifacts.*.path` 的当前 JSON hash 与 `evidence_sources.file_sha256`，防止源 artifact 已刷新而 delivery progress 仍引用旧 hash。Runtime API `GET /v1/delivery-progress/au` 与 Runtime Console `AU delivery progress` 卡片读取同一口径，并直接展示 P0a credential、P0a real batch、P0b environment、P0b manual backfill 与 P0b phase execution clearance 的 hash、fulfilled/ready 状态、缺失数量、real batch 下一阶段和 P0b 下一阶段。当前本地 artifact 为 `delivery_progress_hash=53a368f34fc13dd893835776e7c5461c4cc9c6280cb69ac69847f12701d4fbfe`、`engineering_progress_percent=46.2`、`customer_report_handoff_readiness_percent=10.0`、`structural_auditability_percent=100.0`、`next_work_item_id=p0a_environment`、`next_command=make au-p0a-env`、`next_work_item_packet_hash=e02e4e015d4da6dc63b7bffab6bab1a3559e9ea740843f65e1b0375ccf248a15`、`external_dependency_handoff_hash=200fc741b8f3a6991693ac87c91e2560b0addddd669cf1fe10e0d41c8581ccd6`、`clearance_execution_hash=1024037598d5e82569a3c075c12394d22514da0e34b3988af5df013156e89506`、`p0a_credential_clearance_hash=7fe7c907c0276adcbf071690964a57187aef29dfe7d498f4c4ec53d1956f8bfd`、`p0a_credential_missing_required_count=2`、`p0a_real_batch_clearance_hash=d4f77a3ff9f122a8a20ba8dac4874e70e437444ccc3411a2ad21b1f4cb9d7081`、`p0a_real_batch_missing_required_count=3`、`p0a_real_batch_next_phase=preflight`、`p0b_google_environment_clearance_hash=bd600344cef51c7489b33da02074b6a6e2b705c46159c98092cfb4d68fda9d63`、`p0b_google_environment_missing_required_count=6`、`p0b_google_manual_backfill_clearance_hash=b75ab86c3e3b1559979d69c84df48e4026f0f49641872820a197e81c8f123708`、`p0b_google_manual_backfill_missing_required_count=4`、`p0b_google_phase_execution_clearance_hash=f07de71d64ddd8dee7ec68d28b9316d44d2ea8f5d1d8fe535d88458c1173e8e0`、`p0b_google_phase_execution_missing_required_count=6`、`p0b_google_phase_execution_next_phase=environment`；这只代表工程交付链路的结构化进度，不代表客户报告已经 ready。

补充：最终客户报告交付 gate 已拆成独立 customer handoff clearance artifact。`make au-customer-handoff-clearance` 会把 handoff dossier、customer handoff readiness、delivery progress、external dependency handoff、clearance dry-run、P0a credential clearance、P0a real batch clearance、P0b Google environment clearance、P0b Google manual backfill clearance 和 P0b Google phase execution clearance 合成 `docs/runtime_preflight/au-customer-handoff-clearance-latest.json`，且 Makefile 显式传入同一 `--p0a-credential-clearance-path`、`--p0a-real-batch-clearance-path` 与三类 P0b clearance path；`make verify-au-customer-handoff-clearance` 复算 `customer_handoff_clearance_hash`、10 个客户 gate、source artifact hash、前置 step、operator steps、post-update validation sequence、runtime endpoint、redaction policy、P0a credential/real batch clearance summary、三类 P0b clearance summary 和 `--require-cleared` hard gate，并重新读取每个 `source_artifacts.*.path` 的当前 JSON hash 与 `evidence_sources.file_sha256`，防止最终交接包继续引用过期 delivery progress 或上游清障包。Runtime API `GET /v1/customer-handoff-clearance/au` 与 Runtime Console `Customer handoff clearance` 卡片读取同一口径，并直接展示 P0a credential、P0a real batch、P0b environment、P0b manual backfill 与 P0b phase execution clearance 的 hash、fulfilled/ready 状态和缺失数量。当前本地 artifact 为 `customer_handoff_clearance_hash=24669abfb2653d6c061b3b51f5b583c70c0353316f570f41528d414b644db55a`、`customer_handoff_clearance_packet_ready=true`、`customer_handoff_ready=false`、`customer_handoff_clearance_ready=false`、`ready_for_report_export_handoff=false`、`blocked_by_prerequisite_step=true`、`missing_required_count=9`、`next_command=make au-p0a-env`、`p0a_credential_clearance_hash=7fe7c907c0276adcbf071690964a57187aef29dfe7d498f4c4ec53d1956f8bfd`、`p0a_credential_missing_required_count=2`、`p0a_real_batch_clearance_hash=d4f77a3ff9f122a8a20ba8dac4874e70e437444ccc3411a2ad21b1f4cb9d7081`、`p0a_real_batch_missing_required_count=3`、`p0b_google_environment_clearance_hash=bd600344cef51c7489b33da02074b6a6e2b705c46159c98092cfb4d68fda9d63`、`p0b_google_environment_missing_required_count=6`、`p0b_google_manual_backfill_clearance_hash=b75ab86c3e3b1559979d69c84df48e4026f0f49641872820a197e81c8f123708`、`p0b_google_manual_backfill_missing_required_count=4`、`p0b_google_phase_execution_clearance_hash=f07de71d64ddd8dee7ec68d28b9316d44d2ea8f5d1d8fe535d88458c1173e8e0`、`p0b_google_phase_execution_missing_required_count=6`；它只证明最终客户交付放行路径可审计，不代表真实客户报告已经 ready。

补充：P0a credential update receipt 现在也是 delivery progress 与 customer handoff clearance 的一手 source artifact，而不只是独立 endpoint 或外部依赖执行链命令。`make au-delivery-progress` 与 `make au-customer-handoff-clearance` 都显式接收 `--p0a-credential-update-receipt-path ${GENO_AU_P0A_CREDENTIAL_UPDATE_RECEIPT_OUTPUT_PATH:-docs/runtime_preflight/au-p0a-credential-update-receipt-latest.json}`，payload 内同步记录 `source_artifacts.p0a_credential_update_receipt`、`verifiers.p0a_credential_update_receipt`、`runtime_endpoints.p0a_credential_update_receipt` 和 `evidence_sources` 文件 sha256；两个 verifier 会校验 receipt hash、当前源文件 hash、`credential_update_receipt_complete`、missing required count、env-file hygiene 和 `--require-complete` hard gate。最新刷新后 `delivery_progress_hash=801817cb420753184ada2723c2e481392ea1332518ae008204f7dd9d150fb7ea`、`customer_handoff_clearance_hash=abebb2daa9ca6f2df91e056f40635aa2203af6db5caaff726486760bc180d62b`、`p0a_credential_update_receipt_hash=753da0cc006a6f5ba9a7d3bb36af5e3951352f3e5e83057a64c5f904db5be99c`、`credential_update_receipt_ready=true`、`credential_update_receipt_complete=false`、`p0a_credential_update_receipt_missing_required_count=2`、`p0a_credential_update_env_file_hygiene_ready=true`、`engineering_progress_percent=46.2`、`customer_report_handoff_readiness_percent=10.0`；这只增强进度和最终放行包的可审计性，不代表 `OPENAI_API_KEY` / `PERPLEXITY_API_KEY` 已填充。

补充：客户交付索引包已独立落盘为 hash-only manifest。`make au-customer-handoff-package` 会索引 handoff dossier JSON/Markdown、customer handoff readiness、delivery progress、customer handoff clearance、external dependency handoff/clearance、P0a credential/real batch clearance、P0a evidence package、P0b Google 三类 clearance、P0b Google evidence package 和 P0c report package，生成 `docs/runtime_preflight/au-customer-handoff-package-latest.json`；`make verify-au-customer-handoff-package` 会复算 `customer_handoff_package_hash`、15 个 source artifact 的 payload hash/file sha256、handoff index、runtime endpoint、hard gate 和 redaction policy，并重读当前源文件防漂移。Runtime API `GET /v1/customer-handoff-package/au` 与 Runtime Console `Customer handoff package` 卡片读取同一口径。当前本地 artifact 为 `customer_handoff_package_hash=38160a6132be194e18243d74cc5fc74ff94ec02655c630743c5bf5b8f6a3afdd`、`customer_handoff_package_manifest_ready=true`、`customer_handoff_package_ready=false`、`ready_source_artifact_count=15/15`、`blocked_source_artifact_count=0`、`engineering_progress_percent=46.2`、`customer_report_handoff_readiness_percent=10.0`、`structural_auditability_percent=100.0`、`p0c_report_contract_ready=true`、`ready_for_report_export_handoff=false`、`next_command=make au-p0a-env`。该包只证明“当前可交付证据索引完整、可审计、可溯源”，不替代 `scripts/verify_au_customer_handoff_package.py --require-ready` 的真实客户交付硬门禁。

补充：AU next work item、delivery progress 与 customer handoff clearance 的 verifier 现在区分落盘 artifact 和 runtime 内存态 payload。CLI/Make 或函数传入 `path` 时会自动启用当前源文件重读校验，逐项比对 linked/source artifact 当前 JSON hash 与 `file_sha256`，用于交付前防止落盘包引用过期上游文件；直接校验 FastAPI 同请求内合成的 in-memory payload 时默认只验证包内自洽，避免被磁盘上另一份旧默认文件误判。需要在内存态也强制重读磁盘时，可显式传 `verify_current_files=True`。

补充：P0a credential fulfillment/clearance/update receipt、P0a real batch clearance 与 P0b Google environment/manual backfill/phase execution 三类 clearance 都已加入当前源文件防漂移校验。落盘 verifier 会重读各自 source artifact 的当前 JSON hash 与 `file_sha256`，提前发现 request、fulfillment、checklist、manual verification 或 external dependency clearance 已刷新但清障包/回执包未刷新的问题；FastAPI 同请求内生成的 in-memory payload 默认仍只做包内自洽校验。`make au-delivery-evidence-refresh` 现在按顺序执行 P0a env、P0a credential fulfillment/clearance/update receipt、external dependency handoff/clearance、P0a real batch fulfillment/clearance、P0b environment/manual/phase fulfillment 与 clearance、next work item、delivery progress、customer handoff clearance 和 customer handoff package 的 build/verify，作为汇报进度或交付前的默认刷新入口；该目标仍不绕过真实 `OPENAI_API_KEY` / `PERPLEXITY_API_KEY`、真实批次、Google manual/browser 阶段或客户交付 ready strict gate。

补充：API 合同测试已改为测试专用 `RuntimeApiTestClient`，通过 `httpx.ASGITransport` 直接驱动同一个 FastAPI ASGI app，继续覆盖路由、中间件、响应头、body 和 runtime endpoint 合同；测试层会把同步 endpoint 的 `run_in_threadpool` 跳转内联执行，绕开当前 Python/anyio/Starlette `TestClient` portal 在线程池路径上的卡住问题。该改动只影响 `tests/test_api_contracts.py`，不改变生产 FastAPI 运行方式；当前完整 `tests.test_api_contracts` 为 301 个测试通过。

补充：Runtime Console 之外已新增独立 `/traceability` 详情页。该页面复用 `GET /v1/projects/runtime`、`GET /v1/traceability/runtime` 与 `GET /v1/citation-graphs/runtime`，支持 `project_id` 查询参数，展示 report -> score -> evidence -> source -> action/content draft 的独立 Traceability Map、节点深链、证据运行、source graph、action/content draft、audit events 和 evidence links；首页 Traceability Detail 面板提供 `Open traceability detail` 入口。该页现在还支持 URL 可分享的复盘筛选工作台：可用 `q`、`node_type`、`platform`、`source_type`、`gap_type` 查询参数过滤 evidence/source/action/draft/audit/evidence link 节点，并显示筛选后节点数和首个深链入口。独立详情页已新增 client-side SVG 交互图谱，支持 pan/zoom、fit/reset、deterministic force-directed layout、节点拖拽编辑、节点聚焦和回跳锚点；仍未覆盖跨页面状态机或真实 design partner 浏览器验收。

补充：实体消歧已从单条候选确认推进到最小批量审核队列，并新增证据驱动候选挖掘、候选审核决策持久化、批量候选审核动作、审核历史恢复入口、轻量审核分配 metadata、assignment queue 统计、最小 reviewer workbench、claim/release、批量 claim/release、overdue 通知入队、最小 SLA 升级和筛选式批量重分配。`GET /v1/entity-aliases/runtime/candidates` 仍以计算型 read model 为主；除 canonical name、官方域名、产品线和母公司字段外，现在会读取同项目已入库 `raw_answers.answer_text` 与 `answer_citations.domain/url`，生成 `evidence_answer_text` / `evidence_citation_domain` 候选，并在候选 payload 中带回 `evidence_count`、`evidence_answer_run_ids`、`evidence_urls` 与 `supporting_sources`。`POST /v1/entity-aliases/runtime/confirm-batch` 会接收最多 25 条 alias 候选，默认先预校验实体存在与项目访问权限，避免失败批次半写入；成功后逐条复用 `entity_alias_confirmed` 审计事件，并在响应中返回 `entity_alias_confirm_batch_v1`、成功/失败计数和 `entity_alias_batch_confirmed` 摘要。`POST /v1/entity-aliases/runtime/candidates/review` 会把 `needs_review/rejected/approved` 决策 upsert 到 `entity_alias_candidate_reviews`，写入 `entity_alias_candidate_review_recorded` 审计事件；`POST /v1/entity-aliases/runtime/candidates/review-batch` 会对最多 25 条候选做同项目批量审核，默认先预校验、失败不半写入，逐条写 `entity_alias_candidate_review_recorded`，并返回 `entity_alias_candidate_review_batch_v1` 与 `entity_alias_candidate_batch_reviewed` 摘要。后续候选列表会带回 `latest_review`，并默认隐藏已 `rejected` 的候选；`GET /v1/entity-aliases/runtime/candidates/reviews` 可按项目、decision、entity_kind、assigned_to、assignment_status、priority 和 due_before 分页读取审核历史、分配队列及最近审计事件，用于复盘被隐藏候选或查看 reviewer 待办。`POST /v1/entity-aliases/runtime/candidates/assign` 会在既有审核记录上写入 `assigned_to/assigned_by/assignment_status/priority/due_at/assignment_note`，并追加 `entity_alias_candidate_assigned` 审计事件；`POST /v1/entity-aliases/runtime/candidates/assignment-action` 支持 reviewer 自助 `claim/release`，claim 只允许领取未分配或本人已领取的 review，release 只允许本人释放，均写入 `entity_alias_candidate_assignment_actioned` 审计；`POST /v1/entity-aliases/runtime/candidates/assignment-actions` 可对最多 50 个 candidate ids 批量 claim/release，逐条复用同一 owner/完成态规则并返回 success/error 明细，同时追加 `entity_alias_candidate_assignment_batch_actioned` 聚合审计；`GET /v1/entity-aliases/runtime/candidates/assignment-stats` 会按项目聚合 `total/active/unassigned/overdue/due_soon`、`status_counts`、`priority_counts`、`oldest_due_at` 和 `next_due_at`，用于 SLA 可见性和运营复盘；`POST /v1/entity-aliases/runtime/candidates/assignment-notifications` 会把 overdue active assignment 入队为 `entity_alias_assignment_overdue` runtime notification；`POST /v1/entity-aliases/runtime/candidates/assignment-escalations` 会把 overdue active review 标为 `escalated` 并追加 `entity_alias_candidate_assignment_escalated` 审计事件；`GET /v1/entity-aliases/runtime/candidates/assignment-workbench` 会按 project 和可选 reviewer_id 读取 active reviewer 待办，返回 `entity_alias_assignment_workbench_v1`、active/overdue/due soon/escalated/blocked 计数、status/priority 分布、next due、records 及最近 assignment/review audit events；`GET /v1/entity-aliases/runtime/candidates/assignment-workload` 会按 reviewer/unassigned 聚合 active assignment workload，返回 `entity_alias_assignment_workload_v1`、total active、reviewer_count、unassigned/overdue/due soon/escalated/blocked 计数、每个 reviewer 的 status/priority 分布、urgent/high 负载和 next due，用于可解释地支撑后续自动调度/值班分配；`GET /v1/entity-aliases/runtime/candidates/assignment-dispatch-plan` 会基于 workload、eligible reviewer、`include_statuses` 和 `max_per_reviewer` 生成 `entity_alias_assignment_dispatch_plan_v1` dry-run 分配建议，返回 reviewer_loads、proposed_assignments、skipped_candidates 和 source_summary，但不写 assignment 状态；`POST /v1/entity-aliases/runtime/candidates/assignment-reassignments` 会要求至少一个筛选条件，将匹配 review 批量改派给目标 reviewer，并逐条追加 `entity_alias_candidate_assignment_reassigned` 审计事件。Runtime Console 的 Entity Alias 面板新增 `Bulk Alias Review Queue`、`Alias Reviewer Workbench`、`Alias Candidate Assignment Queue` 与 `Alias Candidate Review History`，可批量确认、批量标记 needs review、批量 reject 当前可见候选，也可逐条标记 needs review / reject，并能从审核历史把 rejected 候选恢复为 needs review、改判 approved、分配 reviewer / priority / due date、领取/释放 review、批量 claim/release 当前 workbench records，或把已分配项推进为 in_progress / blocked / completed / escalated，同时展示 evidence rows、answer run 引用、review/assignment audit、assignment stats 与 reviewer workbench 摘要，并可触发 overdue 通知入队、逾期升级和 escalated 队列改派。该能力仍不等同于多级审批流、完整候选状态机、值班排班、自动调度、复杂 reviewer inbox 或黑箱 NLP 自动消歧。

补充：实体别名 assignment queue 已新增自助领取/释放、overdue 通知入队、最小 SLA 升级和筛选式批量重分配。`POST /v1/entity-aliases/runtime/candidates/assignment-action` 会对单条 review 执行 `claim/release`，用 `FOR UPDATE` 读取当前 owner，避免普通 reviewer 抢占别人已领取的 review，也避免释放他人任务；成功后写 `entity_alias_candidate_assignment_actioned`，不改写候选审核事实。`POST /v1/entity-aliases/runtime/candidates/assignment-notifications` 会扫描同项目 `assigned/in_progress/blocked` 且 `due_at` 已过期的审核分配项，写入 `runtime_notifications.notification_type=entity_alias_assignment_overdue`，以 review id 作为 target，按 priority 映射 warning/critical，并复用 `runtime_notification_subscriptions` 与 `runtime_notification_deliveries` 生成 webhook、Slack Incoming Webhook 或 SMTP email 投递任务。`POST /v1/entity-aliases/runtime/candidates/assignment-escalations` 会扫描同一类 overdue active review，写回 `assignment_status=escalated`，并追加 `entity_alias_candidate_assignment_escalated`，不改写候选 decision、confirmed alias 主事实或证据链。`GET /v1/entity-aliases/runtime/candidates/assignment-workbench` 会读取 active reviewer 待办并返回 workbench 摘要、有限 records 与最近 audit events；`GET /v1/entity-aliases/runtime/candidates/assignment-workload` 会读取同项目 active review 并按 reviewer/unassigned 聚合 workload 摘要，输出 reviewer_loads 排序、SLA 压力和 priority/status 分布；`GET /v1/entity-aliases/runtime/candidates/assignment-dispatch-plan` 会读取同源 workload 和待分配/升级候选，输出 dry-run dispatch plan，说明哪些候选建议派给哪个 reviewer、哪些因 capacity 或缺 reviewer 被跳过；`POST /v1/entity-aliases/runtime/candidates/assignment-actions` 会批量 claim/release workbench records，并写入逐条 action 审计和 `entity_alias_candidate_assignment_batch_actioned` 摘要；`POST /v1/entity-aliases/runtime/candidates/assignment-reassignments` 会按 `from_assigned_to/from_assignment_status/from_priority/due_before` 至少一个过滤条件锁定匹配 review，批量改派给目标 reviewer，并写入 `entity_alias_candidate_assignment_reassigned`；Runtime Console 默认只对 `escalated` 队列提供 `Reassign escalated reviews`。通知订阅默认 event types 包含 `report_export_job,runtime_alert,entity_alias_assignment_overdue`；`workers/notification_worker/run_entity_alias_assignment_notifications.py`、`workers/notification_worker/run_entity_alias_assignment_escalations.py`、`workers/notification_worker/run_entity_alias_assignment_reassignments.py`、`make entity-alias-assignment-notification-worker`、`make entity-alias-assignment-escalation-worker`、`make entity-alias-assignment-reassignment-worker` 与 Compose `scheduler` profile 可按 AU market 或显式 project id 做单次扫描。该能力补的是可审计 owner 领取/释放、overdue 入队、投递复用、最小升级状态、筛选式改派、只读 reviewer workbench、reviewer workload 决策依据、dispatch dry-run 计划和批量领取/释放入口，不是完整值班排班、自动调度 apply 或复杂 reviewer inbox。

补充：实体别名 dispatch 已从只读 dry-run 推进到显式 apply。`POST /v1/entity-aliases/runtime/candidates/assignment-dispatch-apply` 会按当前 `reviewer_ids/include_statuses/max_per_reviewer/limit` 重新生成 `entity_alias_assignment_dispatch_plan_v1`，再对仍符合资格的 proposed assignments 逐条 `FOR UPDATE` 写入 `assigned_to/assigned_by/assignment_status/priority/due_at/assignment_note`，单条写 `entity_alias_candidate_assignment_dispatch_applied`，整体写 `entity_alias_assignment_dispatch_plan_applied` 摘要，并返回 success/error 明细与原始 dispatch plan。Runtime Console 的 `Alias Assignment Dispatch Plan` 面板新增 `Apply dispatch plan` 显式按钮；该入口仍不是无人值守自动调度、值班排班或复杂 reviewer inbox，计划变成写入动作前会按当前数据库状态重算并拒绝 completed 或已脱离 include_statuses 的 review。

补充：实体别名 dispatch apply 现在也有 cron/K8s CronJob 友好的单次 worker。`make entity-alias-assignment-dispatch-apply-worker` 会运行 `workers/notification_worker/run_entity_alias_assignment_dispatch_apply.py`，默认按 `GENO_ENTITY_ALIAS_ASSIGNMENT_MARKET_CODE=AU` 发现项目，使用 `GENO_ENTITY_ALIAS_ASSIGNMENT_DISPATCH_REVIEWERS`、`GENO_ENTITY_ALIAS_ASSIGNMENT_DISPATCH_MAX_PER_REVIEWER` 和 `GENO_ENTITY_ALIAS_ASSIGNMENT_DISPATCH_LIMIT` 重算并应用 dispatch plan；Compose `scheduler` profile 也新增 `entity-alias-assignment-dispatch-apply-worker` 服务。该 worker 仍复用同一个显式 apply repository 方法和同一组审计事件，输出 processed/status/project/requested/applied/failed JSON 摘要；它是可调度入口，不是复杂值班排班、跨团队审批或长驻调度系统。

补充：人工补录已从单条表单推进到 Runtime CSV 批量导入。`POST /v1/evidence-runs/runtime/manual-backfill/import.csv` 接收项目级 CSV 文本，必填 `prompt_question_id,answer_text`，可选 platform/surface/citation_urls/screenshot_url/html_snapshot_url/sample/device/notes 等字段；后端会先整批反查 prompt 存在且属于同一 `project_id`，通过后再一次性写入标准 `AnswerRun/RawAnswer/AnswerCitation/EvidenceAsset/CollectionCost/CollectorLog`，逐条生成 `manual_backfill_recorded`，并追加 `manual_backfill_batch_imported` 批次审计摘要。Runtime Console 的 Prompt Pack 区域新增 `Manual Backfill CSV` 表单。该能力解决 P0b Google manual path 的运营录入效率和可复盘导入摘要，但不替代 `MANUAL_BACKFILL_PATH` JSONL strict verifier，也不代表真实 120-row manual 文件或 240-run Google spike 已完成。

补充：报告审计摘要已进入冻结方法披露。`report_exports.method_disclosure.audit_summary` 会记录上游 audit event 数量、事件类型/target/method/actor 分布、input/output ref keys、事件时间窗和代表性 event ids；Markdown、PDF、白标 PDF 与 runtime artifact 统一渲染该冻结摘要，旧报告若缺少该字段，runtime artifact 会基于已绑定的 `AuditEvent` 兼容补算展示但不改写历史 `ReportExport`。

补充：Google P0b browser path 已新增脱敏环境 readiness 报告、env-file hygiene gate 和单样本 smoke runner/verifier。`make verify-au-p0b-google-env-template` 会在复制真实 `.env.au-p0b-google` 前校验已提交模板：Google Playwright 必须默认关闭，selector/storage state/manual path/database/SERP key/object store 必须为空，browser/SERP 地区参数和 runtime 输出路径必须是安全默认值；该 gate 只证明模板安全完整，不证明真实 selector、账号/session、manual 文件、数据库或 Google readiness。`make au-p0b-google-playwright-env` 会在 smoke 前读取 P0b runbook、`.env.au-p0b-google` 与当前进程环境，只保存变量存在状态、来源、长度和 sha256 前缀，输出 `ready_for_playwright_smoke`、selector/storage/Playwright 包缺口、`env_file.hygiene` 和 `next_action`；当本地 env-file 存在且含条目时，必须 gitignored、not tracked 且权限为 `0600`，否则 smoke readiness 失败；完全用进程环境注入敏感值时缺失本地 env 文件不阻塞。`make verify-au-p0b-google-playwright-env` 默认证明报告 hash、env-file hygiene 与状态推导可复算，`--require-ready-smoke` 才作为 smoke 前置硬门禁。`make au-p0b-google-playwright-smoke` 会在真实 240-run 前采集一条 Sydney / desktop Google Playwright 样本并写入 hash payload；`make verify-au-p0b-google-playwright-smoke` 默认证明 payload 可审计，`scripts/verify_au_p0b_google_playwright_smoke.py --require-success` 才作为 browser path 晋级门禁，要求真实 browser capture 成功、HTML/screenshot hash 和 `answer_run_collected` 审计事件齐备。`make au-p0b-google-status` 已把该 smoke artifact 纳入总状态报告，strict smoke 未成功时会停在 `next_action=run_google_playwright_smoke`，不继续放行 health/spike。

补充：白标资产库已从审计型 Logo 版本库推进到最小素材库闭环。`GET /v1/project-brand-kits/runtime/assets` 仍会从 `project_brand_logo_uploaded` 和 `project_brand_logo_version_activated` 审计事件读回项目 Logo 版本、来源文件、content hash、上传人和 active 状态；`POST /v1/project-brand-kits/runtime/assets/activate` 可把历史 Logo URI 重新激活为 Brand Kit 默认值，并追加 `project_brand_logo_version_activated` 审计事件。新增 `project_brand_assets` 专表、`GET/POST /v1/project-brand-assets/runtime` 和 Runtime Console 的 Asset Register / Asset Library 面板，可按项目登记 `asset_type/category/asset_url/preview_url/source filename/content type/content hash/storage_version/status/metadata`，并写入 `project_brand_asset_registered` 审计事件；`POST /v1/project-brand-assets/runtime/{asset_id}/scan-status` 可把素材扫描状态更新为 `pending/passed/failed/skipped`，记录 `scan_checked_at/scan_method_version/scan_notes` 并写入 `project_brand_asset_scan_recorded` 审计事件；Logo Upload 归档对象存储后也会同步写入该资产库。该能力已覆盖 MVP 交付复盘、素材分类元数据、预览 URL、手工/外部扫描状态门禁和可审计资产登记，但图片裁剪、真实病毒扫描引擎、CDN、公网对象存储 presigned URL 和对象存储原生版本控制仍是后续增强。

补充：报告导出任务队列已落最小执行闭环，并补了项目内通知 inbox 与 webhook/Slack/SMTP email 投递队列。`report_export_jobs` 记录项目、目标报告、artifact type、template、筛选条件、排序、请求人、状态、artifact URL、错误信息、attempt count、max attempts、lease expiry 和 next attempt；`GET/POST /v1/report-export-jobs/runtime` 可查询或排队导出任务，`GET /v1/report-export-jobs/runtime/stats` 可读回 queued/running/dead_letter、可重试、过期 running、达到最大尝试次数和最早排队时间，`POST /v1/report-export-jobs/runtime/{job_id}/status` 可把任务推进为 `queued/running/succeeded/failed/dead_letter/cancelled`，并写入 `report_export_job_queued` / `report_export_job_status_updated` 审计事件。任务进入 `succeeded/failed/dead_letter/cancelled` 终态时会同步写入 `runtime_notifications`，生成 `runtime_notification_created` 审计事件；`GET /v1/runtime-notifications` 可读取项目内 inbox 与 unread count，`POST /v1/runtime-notifications/{notification_id}/status` 可标记 read/unread 并写 `runtime_notification_status_updated`。`runtime_notification_subscriptions` 可按项目保存 active/paused/disabled webhook、Slack Incoming Webhook 或 SMTP email 订阅，`GET/POST /v1/runtime-notification-subscriptions` 暴露查询与保存接口并写 `runtime_notification_subscription_saved`；`runtime_notification_deliveries` 会为匹配 event type 和 severity threshold 的订阅生成 queued delivery，写入 `runtime_notification_delivery_queued`，`GET /v1/runtime-notification-deliveries` 可复盘投递状态。`POST /v1/runtime-alerts/notifications` 可把当前未 resolved/snoozed 的派生 Runtime Alert 显式入队为 `notification_type=runtime_alert`，并复用同一套项目内 inbox、订阅和 delivery 队列；`make runtime-alert-notification-worker` 提供 cron/K8s CronJob 友好的单次扫描入口，可按 `GENO_RUNTIME_ALERT_MARKET_CODE` 或显式 `--project-id` 扫描项目并调用同一入队逻辑；`make runtime-alert-escalation-worker` 则按默认 `critical=4h/high=24h` SLA 阈值扫描 overdue alerts，把未 acknowledged/resolved/snoozed/escalated 的风险追加为 `runtime_alert_events.status=escalated`，并写入 `runtime_alert_event_recorded`。Runtime Console 已新增 `Report Export Queue`、`Runtime Notifications` 和 Runtime Alerts 通知入队入口，支持按当前项目和筛选条件排队导出、查看最近任务、attempt/lease/next attempt、队列指标、终态通知、alert 通知、read/unread、webhook/Slack/email 订阅和最近投递状态；`make report-export-worker` 会运行 `workers/report_export_worker/run_report_export_jobs.py`，用 `FOR UPDATE SKIP LOCKED` 领取最早可执行 queued job 或 lease 过期的 running job，渲染对应 runtime report artifact，配置对象存储时归档到 `s3://.../report-artifacts/...`，未配置时写入 `runtime-report-artifact://...` fallback URL，失败时按 `--retry-backoff-seconds` 回到 queued，达到 `--max-attempts` 后进入 `dead_letter`；`make notification-delivery-worker` 会运行 `workers/notification_worker/run_notification_deliveries.py`，领取 queued/lease 过期 delivery，按 channel 发送 canonical webhook JSON、Slack payload 或 SMTP `EmailMessage`，记录 response status/body hash/error，失败按 backoff 回到 queued，达到 max attempts 后进入 `dead_letter`。Compose `scheduler` profile 现在也包含 `report-export-worker` 和 `notification-delivery-worker` 两个单次服务：前者默认接 MinIO 并按 `GENO_REPORT_EXPORT_WORKER_MAX_JOBS/MAX_ATTEMPTS/RETRY_BACKOFF_SECONDS/LEASE_SECONDS` 控制批量与重试，后者默认只依赖 Postgres，并按 `GENO_NOTIFICATION_DELIVERY_MAX_DELIVERIES/MAX_ATTEMPTS/RETRY_BACKOFF_SECONDS/LEASE_SECONDS/TIMEOUT_SECONDS` 处理投递，同时通过空默认值暴露 webhook signing secret 与 SMTP 配置占位。该能力仍不等同于 Temporal、长驻 worker、并发池、完整用户偏好中心、托管邮件服务商 API、Slack App/OAuth/交互式消息、复杂 SLA 策略/值班升级或公网 CDN 交付；runtime notification email 已具备单收件人 token 化退订最小闭环。

补充：Report Export Queue 已新增 hash-only CSV 复盘附件。`GET /v1/report-export-jobs/runtime/export.csv` 会复用 job read model，按 project/status/report_export_id 导出 job id、report id、状态、artifact type、template、filter key 集、sort、requested/started/completed 时间、attempt/lease/next attempt、artifact URL hash、error hash 和最近 audit 摘要，并返回 `X-GENO-Report-Export-Job-Export-Hash`、project/status/report id、row count 和 total count 响应头；CSV 不导出 raw artifact URL、raw error message 或完整 filter value。Runtime Console 的 Report Export Queue 面板已展示 `Report Jobs CSV` 路径和 `Download report jobs CSV` 链接，用于客户交付或内部排障时复盘导出任务是否 queued/running/succeeded/dead_letter。

补充：Report History 管理事件也已新增 hash-only CSV 复盘附件。`GET /v1/reports/runtime/management-events/export.csv` 会按 project/status/report_type 导出 report id、report version/type、管理状态、management event id、actor hash、note hash、method version、after hash、sample size、公式版本、methodology hash 和 artifact URL hash，并返回 `X-GENO-Report-Management-Export-Hash`、project/status/report type、row count 和 total count 响应头；CSV 不导出 raw reviewer/actor、raw note 或 raw artifact URL。Runtime Console 的 Report History 面板已展示 `Report management CSV` 和 `Download report management CSV`，用于客户交付时复盘哪版报告被标记为 `client_ready`。

补充：Action Plan 与复测链路已新增 hash-only CSV 复盘附件。`GET /v1/action-plans/runtime/export.csv` 会复用 action plan read model，按 project/status 导出 action id、project/schedule id、prompt version、priority/status、owner/title/description hash、source gap/source type、evidence 和 schedule answer run 计数、sample size、offsets、next check、最新 retest comparison 分数/趋势、answer run count 和最近 audit 摘要，并返回 `X-GENO-Action-Plan-Export-Hash`、project/status、row count 和 total count 响应头；CSV 不导出 raw title、raw description 或 raw owner id。Runtime Console 的 Action Plan & Retest Detail 面板已展示 `Action Plan CSV` 和 `Download action plan CSV`，用于客户交付或内部复盘时证明缺口如何转成任务、复测结果如何回指、最近审计链是什么。

补充：Runtime Alerts 已新增 hash-only CSV 复盘附件。`GET /v1/runtime-alerts/export.csv` 会复用派生 alert read model，按 project/alert_type/severity 导出 alert id、severity、metric/threshold、rule/source、title/summary hash、证据 refs、关联 action id 与 action title hash、最新管理事件状态/actor hash/note hash/metadata key 集和最近评分审计摘要，并返回 `X-GENO-Runtime-Alert-Export-Hash`、project/alert type/severity、row count 和 total count 响应头；CSV 不导出 raw alert title、raw summary、raw action title、raw updated_by 或 raw note。Runtime Console 的 Runtime Alerts 面板已展示 `Alert CSV` 和 `Download alert CSV`，用于客户交付或内部排障时复盘风险如何从评分/信源/竞品/action 派生、如何被处理、最近审计链是什么。

补充：Human Review Trail 已新增 hash-only CSV 复盘附件。`GET /v1/human-reviews/runtime/export.csv` 会复用人工复核 read model，按 project/target_type/review_status 导出 review id、target type/id、review status、decision/reviewer/notes hash、payload key 集和最近 audit 摘要，并返回 `X-GENO-Human-Review-Export-Hash`、project/target type/status、row count 和 total count 响应头；CSV 不导出 raw decision、raw reviewer id、raw notes 或完整 payload value。Runtime Console 的 Human Review Trail 面板已展示 `Review CSV` 和 `Download review CSV`，用于客户交付或内部复盘时证明人工复核对象、结论状态、草稿状态投影和最近审计链是什么。

补充：Runtime Notification inbox 已新增 hash-only CSV 复盘附件。`GET /v1/runtime-notifications/export.csv` 会复用 notification read model，按 project/status/notification type 导出 notification id、类型、severity/status、target、title/message hash、payload key 集、payload status/artifact/template、created/read/updated 字段和最近 audit 摘要，并返回 `X-GENO-Notification-Export-Hash`、project/status/type、row count 和 total count 响应头；CSV 不导出 raw title、raw message 或完整 payload value。Runtime Console 的 Runtime Notifications 面板已展示 `Notification CSV` 路径和 `Download notification CSV` 链接，用于客户交付或内部排障时复盘 inbox 主事实、read/unread 状态和对应审计事件。

补充：Runtime Notification delivery 队列已新增 hash-only CSV 复盘附件。`GET /v1/runtime-notification-deliveries/export.csv` 会复用 delivery read model，按 project/notification/subscription/status 导出 delivery id、状态、attempt/lease/next attempt、response status/body hash、endpoint URL hash、error hash、notification/subscription 摘要、payload key 集和最近 audit 摘要，并返回 `X-GENO-Notification-Delivery-Export-Hash`、project/status、row count 和 total count 响应头；CSV 不导出 raw endpoint URL、raw error message 或完整 payload/metadata value。Runtime Console 的 Runtime Notifications 面板已展示 `Delivery CSV` 路径和 `Download delivery CSV` 链接，用于客户交付或内部排障时复盘投递是否 queued/delivered/retry/dead_letter。

补充：Runtime Notification subscription 配置也已新增 hash-only CSV 复盘附件。`GET /v1/runtime-notification-subscriptions/export.csv` 会按 project/status 导出订阅 id、channel、endpoint URL hash、event types、severity/status、metadata keys、email control URL hash、suppression hash count、webhook signing env 是否配置、key id、Slack channel 是否配置和最近 audit 摘要，并返回 `X-GENO-Notification-Subscription-Export-Hash`、project/status、row count 和 total count 响应头；CSV 不导出 raw webhook/Slack/mailto endpoint、raw control URL、raw recipient、raw secret env value 或 metadata value。Runtime Console 已展示 `Subscription CSV` 和 `Download subscription CSV`，可与 delivery CSV、suppression CSV 一起复盘通知交付链路。

补充：`make report-export-worker` 与 `make notification-delivery-worker` 的本地入口已和 Compose `scheduler` profile 对齐，显式传入同一组 max/attempt/backoff/lease/timeout 环境变量默认值。这样本地手工执行、cron 和 Compose/K8s CronJob 使用同一参数口径，减少排查队列重试行为时的环境漂移。

补充：runtime alert 与 entity-alias assignment 系列本地 worker 入口也已和 Compose `scheduler` profile 对齐。`make runtime-alert-notification-worker` 会显式传入 `GENO_RUNTIME_ALERT_MAX_PROJECTS`，`make runtime-alert-escalation-worker` 会同时传入 `GENO_RUNTIME_ALERT_MAX_PROJECTS` 和 `GENO_RUNTIME_ALERT_ESCALATION_THRESHOLDS`；`make entity-alias-assignment-notification-worker`、`make entity-alias-assignment-escalation-worker`、`make entity-alias-assignment-reassignment-worker`、`make entity-alias-assignment-dispatch-apply-worker` 均显式传入 `GENO_ENTITY_ALIAS_ASSIGNMENT_MAX_PROJECTS`，其中 reassignment 本地入口固定默认筛选 `--from-assignment-status escalated`，dispatch apply 本地入口使用同一 reviewer/capacity/limit/max-projects 参数口径。该补充只消除本地执行、cron 与 Compose/K8s CronJob 的参数漂移，不新增审计事件或后台状态机。

补充：webhook delivery worker 已支持出站 HMAC 签名和最小双 secret 灰度窗口。订阅 metadata 只保存 `signing_secret_env` / `webhook_signing_secret_env`、`signing_secret_key_id`、`previous_signing_secret_env` 和 `previous_signing_secret_key_id` 这类环境变量名或 key id，不把 raw secret 写入数据库；worker 默认可从 `GENO_NOTIFICATION_WEBHOOK_SIGNING_SECRET` 读取当前 secret，也可用 CLI `--default-signing-secret-env` 覆盖，并通过 `--secondary-signing-secret-env` 在输出 JSON 中披露 previous secret env 名供接收端轮换复盘。签名覆盖 `timestamp.delivery_id.notification_id.payload_sha256`，出站请求会携带 `x-geno-signature`、`x-geno-signature-timestamp`、`x-geno-signature-version`、`x-geno-signature-input` 和可选 `x-geno-signature-key-id`；订阅指定的 secret env 缺失时，delivery 会按现有 retry/dead-letter 机制回到 queued 或进入 dead_letter，并把错误写入投递状态审计。`geno_core.webhook_signing` 已提供同口径的 `sign_runtime_notification_webhook()`、`verify_runtime_notification_webhook_signature()` 和 payload hash helper，接收方可校验必需 header、HMAC、payload hash、timestamp tolerance，并可用 `additional_secrets` 在当前/上一版 secret 之间做双 secret 验证，返回 `matched_secret_id` 与 `checked_secret_count`。该能力解决外发 webhook 的最小完整性证明、接收端复算 helper 和双 secret 灰度验证窗口，但仍不等同于托管验证服务、自动 secret rotation 编排、订阅鉴权回调、完整用户偏好中心、托管邮件服务商 API、Slack App/OAuth/交互式消息或完整告警平台；runtime notification email 已具备单收件人 token 化退订最小闭环。

补充：Runtime Notification 外部投递已新增最小 Slack Incoming Webhook 与 SMTP email 通道。`runtime_notification_subscriptions.channel` 现在支持 `webhook`、`slack` 与 `email`；`channel=slack` 仍复用 `runtime_notification_deliveries` 的 queued/sending/delivered/retry/dead_letter 状态机和审计事件，但入队 payload 会额外冻结 `slack.text/blocks/metadata`，`make notification-delivery-worker` 投递时只把 Slack payload POST 到 Incoming Webhook URL，不发送 `x-geno-*` HMAC header；`channel=email` 要求 `mailto:` endpoint，入队时冻结 `email.to/subject/text/headers/metadata`，worker 通过 `GENO_NOTIFICATION_SMTP_HOST/PORT/TLS/USERNAME/PASSWORD/FROM/TIMEOUT_SECONDS` 发送 SMTP 邮件。Runtime Console 的 Runtime Notifications 表单已可选择 channel，填写 webhook、Slack Incoming Webhook URL 或 `mailto:ops@example.com`，并保存可选 `slack_channel` / `email_reply_to` metadata。这里完成的是 Incoming Webhook 和 SMTP 级 payload 适配，不是 Slack App 安装、OAuth、交互式按钮、频道发现、用户映射、完整用户偏好中心、托管邮件服务商 API 或值班平台；runtime notification email tokenized unsubscribe 已作为单收件人最小闭环落地。

补充：Runtime Notification SMTP email 已升级到 `runtime_notification_email_template_v2`，继续冻结 subject/text、template version/hash、subject hash 和 body hash，并可从 email 订阅 metadata 读取 `email_reply_to`、`email_unsubscribe_url`、`email_unsubscribe_mailto`、`email_preferences_url`、`email_suppressed_recipients` 与 `email_suppressed_recipient_hashes`。入队 payload 会生成 `Reply-To`、`List-Unsubscribe`、`List-Unsubscribe-Post` 与 `X-GENO-Notification-Preferences-Url` 头，正文追加 Notification controls 控制入口；若配置了 suppression，入队前会从 `mailto:` 收件人中移除匹配地址或 hash，partial suppression 仍创建 delivery 但冻结 suppressed recipient hash，全部收件人被过滤时不创建空 delivery，而是写 `runtime_notification_delivery_suppressed` 审计事件。配置 `GENO_NOTIFICATION_EMAIL_PREFERENCE_BASE_URL`、`GENO_NOTIFICATION_EMAIL_PREFERENCE_TOKEN_SECRET` 和可选 `GENO_NOTIFICATION_EMAIL_PREFERENCE_TOKEN_TTL_SECONDS` 后，单一过滤后收件人的 email delivery 会生成 HMAC tokenized unsubscribe URL，覆盖正文 Unsubscribe 与 `List-Unsubscribe`，并只在 delivery metadata/audit output refs 中记录 token hash、tokenized URL hash、template hash、body hash 和 recipient hash；`POST /v1/runtime-notification-email-preferences/unsubscribe` 支持 JSON token 和邮件头 one-click query token，验签后把 `recipient_hash` 追加到关联 email subscription 的 `email_suppressed_recipient_hashes`，把 token hash 追加到 `email_unsubscribe_token_hashes`，并写 `runtime_notification_email_preference_unsubscribed` 审计事件。`runtime_notification_delivery_queued` 和 `runtime_notification_subscription_saved` 只在 output refs 中记录 reply-to/control/suppression/preference token hash，便于复算而不把 raw control URL、suppressed address 或 token secret 放进 refs。worker 投递时透传 `X-GENO-Email-Template-Version` 和上述邮件头，但 email 通道不使用 webhook HMAC header。该补充是最小 SMTP 控制入口、token 化单收件人退订、metadata 驱动收件人过滤与可复算审计；完整用户偏好中心、托管邮件服务商 API、富模板编辑器、本地化模板和具体 ESP 原生 payload adapter 仍待后续接入。

补充：Runtime Notification email 偏好中心后端已从单向退订推进到最小可解释读写闭环。email delivery 在单一过滤后收件人且配置 `email_preferences_url` 时会生成 `manage` HMAC tokenized preferences URL，正文与 `X-GENO-Notification-Preferences-Url` 使用该 URL，payload/audit 只记录 `email_preference_manage_token_hash` 和 `email_tokenized_preferences_url_hash`；`GET /v1/runtime-notification-email-preferences/status` 用 manage token 读回 hash-only 偏好状态、delivery/notification/subscription 脱敏摘要和最近 subscription audit events；`POST /v1/runtime-notification-email-preferences/resubscribe` 用同一 manage token 从 `email_suppressed_recipient_hashes` 移除当前 `recipient_hash`，追加 `email_resubscribe_token_hashes`，并写 `runtime_notification_email_preference_resubscribed`。Runtime Console 已暴露 status/resubscribe/unsubscribe 三个偏好 API 路径。项目级 hash-only suppression list 已落；该能力仍不是完整偏好中心 UI、跨项目用户目录、跨项目 suppression 或 ESP 原生 suppression API；审计链只保存 recipient/token/url hash，不保存 raw token、raw recipient 或 token secret。

补充：Runtime Notification email 已新增最小自助偏好页 `/notifications/preferences?token=...`。该页面用 manage token 读取 `status` endpoint，展示 subscribed/unsubscribed、delivery/notification/subscription 脱敏摘要、recipient hash 摘要和最近 audit events，并通过 server action 调用 `resubscribe` 或 `unsubscribe` endpoint；`unsubscribe` endpoint 现在同时接受一键邮件头的 `unsubscribe` token 和偏好页的 `manage` token，仍写同一个 `runtime_notification_email_preference_unsubscribed` 审计事件。页面不持久化 token，不展示 raw recipient；完整账户级偏好中心、登录态用户目录、批量 topic 订阅和 ESP suppression API 仍待后续增强。

补充：Runtime Notification email 反馈事件已新增最小可审计记录、HMAC webhook 摄取、复盘路径和显式 suppression 应用动作。`runtime_notification_email_feedback_events` 以项目级 RLS 表保存 email delivery 的 `bounce/complaint/unsubscribe/suppressed` 反馈事实，`POST /v1/runtime-notification-deliveries/{delivery_id}/email-feedback` 会先反查 delivery 所属项目和 `channel=email`，再写入反馈事件并追加 `runtime_notification_email_feedback_recorded` 审计；`POST /v1/runtime-notification-email-feedback-webhooks/geno` 接受 GENO canonical JSON，用 `GENO_NOTIFICATION_EMAIL_FEEDBACK_WEBHOOK_SECRET` 与 `x-geno-*` HMAC 头校验 payload hash、delivery id、notification id 和 timestamp，再复用同一 feedback 写入链路，metadata 只记录 signature payload hash、matched secret id、checked secret count、header 摘要和 provider metadata，不保存 raw secret；`GET /v1/runtime-notification-email-feedback-events` 可按 project、delivery、notification、subscription、feedback type 和 provider 查询最近反馈，并带回 delivery/notification/subscription/audit 上下文；`POST /v1/runtime-notification-email-feedback-events/{feedback_event_id}/suppress-recipient` 会在管理权限下把已有反馈事件的 `recipient_hash` 显式追加到关联 email subscription 的 `email_suppressed_recipient_hashes`，并记录 `runtime_notification_email_feedback_suppression_applied`；`POST /v1/runtime-notification-email-feedback-events/{feedback_event_id}/project-suppression` 会把同一 feedback event 显式 upsert 到项目级 `runtime_notification_email_suppressions`，source 固定为 `feedback`、source_ref 绑定 feedback event id，并记录 `runtime_notification_email_feedback_project_suppression_applied`。Runtime Console 的 email delivery 项可手工记录反馈，feedback 列表可对有 recipient hash 的事件分别执行 `Apply suppression` 和 `Apply project suppression`。反馈链只保存 `recipient_hash`、`provider_event_id_hash`、provider、metadata、delivery/notification/subscription id 和 audit refs，不保存 raw recipient 或 provider event id；当同一 delivery、feedback type 与 `provider_event_id_hash` 已存在时，写入链路不会新增第二条反馈主事实，而是追加 `runtime_notification_email_feedback_duplicate_ignored` 审计事件并返回已有 feedback event 上下文；feedback 写入本身不会自动改写订阅 metadata、项目级 suppression list 或用户偏好；项目级 hash-only suppression 可通过 `/v1/runtime-notification-email-suppressions` 手工保存，也可从既有 feedback event 经 `/project-suppression` 显式应用。SendGrid/Mailgun/Postmark 原生 payload adapter 已通过 `/v1/runtime-notification-email-feedback-webhooks/{provider}` 接入，使用内部 `x-geno-provider-webhook-secret` 保护摄取入口，把 provider 原生事件归一化为 `bounce/complaint/unsubscribe/suppressed`，metadata 只保存 provider payload hash、event hash、adapter version、delivery id 来源、recipient hash 和 provider event id hash，不落 raw recipient、raw provider event id 或 raw provider payload；provider 原生签名验签已支持可选强制校验：SendGrid 使用 `GENO_NOTIFICATION_EMAIL_SENDGRID_PUBLIC_KEY` 校验 ECDSA 头，Mailgun 使用 `GENO_NOTIFICATION_EMAIL_MAILGUN_SIGNING_KEY` 校验 timestamp+token HMAC，Postmark 使用 `GENO_NOTIFICATION_EMAIL_POSTMARK_BASIC_USERNAME/PASSWORD` 校验 Basic Auth；验签 metadata 只记录 status/method/reason/payload hash/checked count/age，不保存 raw key、token、密码或签名值。全自动后台 suppression mutation、ESP 原生 suppression API、完整偏好中心和跨项目用户目录仍待后续增强；自研 HMAC token 退订最小链路已落。

补充：Runtime Notification email 已新增项目级 hash-only suppression list。`runtime_notification_email_suppressions` 作为项目级 RLS 表保存 `recipient_hash/status/source/source_ref/metadata`，`GET/POST /v1/runtime-notification-email-suppressions` 可按项目读取和 upsert active/inactive suppression，`GET /v1/runtime-notification-email-suppressions/export.csv` 可导出项目级 suppression CSV 并返回 export hash/row count 响应头，写入动作记录 `runtime_notification_email_suppression_saved` 审计事件；API、数据库和 audit refs 只接收/保存 SHA-256 recipient hash，不保存 raw recipient。email delivery 入队会合并订阅 metadata suppression 与项目级 active suppression，过滤命中收件人，并在 payload/audit output refs 中冻结 `email_project_suppression_hashes`、`email_project_suppression_hash_count` 和 filtered recipient count。Runtime Console 已提供 Project suppressions 列表、Save project suppression 表单、Download suppression CSV 链接，并可从 email feedback event 显式执行 Apply project suppression。该补充解决 AU MVP 的项目级退信/投诉复盘和后续投递过滤；账户级偏好中心、跨项目 suppression、托管 ESP 原生 suppression API、全自动后台 provider feedback mutation 仍待后续增强。

当前代理商工作流已推进到项目级 read path、客户项目创建、项目级白标配置、项目成员管理和最小运行时项目访问控制：Runtime Console 可通过 URL/下拉选择 `project_id`，并让 brand kit、prompt、evidence、alias、saved view、score、graph、report、action、content、traceability 查询收敛到同一个 runtime project；Project Bootstrap 面板可提交 tenant/project/brand/category/brand domains/product lines/3-5 competitors，生成新的 AU/DTC 客户项目、100 条 prompt 和启动审计事件；Project Metadata 表单可通过 `PATCH /v1/projects/runtime` 编辑项目名、目标品牌、品类和 `configured/active/paused/archived` 状态，并写入 `project_updated` 审计事件；Project Members 面板可读取 `project_members`，通过 `POST /v1/project-members/runtime` upsert owner/admin/analyst/viewer 成员并写入 `project_member_saved`，也可通过 `DELETE /v1/project-members/runtime` 删除成员并写入 `project_member_deleted`，repository 会阻止删除或降级最后一个 owner；同一面板还可通过 `POST /v1/project-member-invitations/runtime` 创建 pending 成员邀请，写入 `project_member_invitation_created`，通过 `POST /v1/project-member-invitations/runtime/action` 将 pending 邀请撤销或标记过期，分别写入 `project_member_invitation_revoked` / `project_member_invitation_expired`，并可在一次性 token 仍处于创建响应内存态时调用 `POST /v1/project-member-invitations/runtime/email` 通过 `GENO_NOTIFICATION_SMTP_*` 发送邀请邮件、写入 `project_member_invitation_email_sent`；收件人可打开 `/invite/accept?invitation_id=...&invite_token=...` 独立页面，或直接调用 `POST /v1/project-member-invitations/runtime/accept`，以一次性 token 兑换成员身份，写入 `project_member_saved` 与 `project_member_invitation_accepted`，邀请 token 只在创建响应、邮件正文、浏览器 URL/表单和兑换请求中短暂出现，数据库和审计链只保存 `invite_token_hash`、accept URL hash 和发送结果摘要；Brand Kit 表单可保存 `client_name/prepared_by/logo_url/primary_color/secondary_color/footer_text`，生成 `project_brand_kit_saved` 审计事件，并成为白标 PDF 的项目默认值；Logo Upload 表单可上传 PNG/JPEG/WebP/SVG/GIF 到对象存储，写回 Brand Kit 的 `logo_url`，并生成 `project_brand_logo_uploaded` 审计事件；Theme Editor 面板复用 Brand Kit 默认值，提供颜色输入、客户/服务商/页脚预览、white-label artifact path 和最近 Brand Kit 审计事件摘要；Report Snapshot 与 Report History 可生成 Markdown/CSV/PDF/White-label PDF 的 signed URL，签名由 `GENO_REPORT_ARTIFACT_SIGNING_SECRET` 驱动，默认 TTL 为 900 秒，可用 `GENO_REPORT_ARTIFACT_SIGNED_URL_TTL_SECONDS` 调整；Report History 支持对冻结 `ReportExport` 追加 `internal_review/client_ready/archived` 管理事件，通过 `POST /v1/reports/runtime/{report_export_id}/management-events` 写入 `report_export_management_recorded` 审计，不改写报告版本、对象存储 URL、方法披露和评分快照。API 可通过 `GENO_RUNTIME_PROJECT_ACCESS_CONTROL=1` 开启项目成员门禁和最小角色矩阵：runtime project 列表按 `project_members` 过滤，项目创建 owner 绑定当前 actor，项目级读接口要求成员关系；默认 demo 模式仍从 `X-GENO-Actor-Id` 取 actor，设置 `GENO_RUNTIME_AUTH_MODE=jwt` 后会要求 `Authorization: Bearer <JWT>` 并用 `GENO_RUNTIME_JWT_SECRET` 校验 HS256 签名；设置 `GENO_RUNTIME_AUTH_MODE=jwks` 后会优先用 `GENO_RUNTIME_JWKS_JSON` 中的 inline JWKS 校验 RS256 签名，也可用 `GENO_RUNTIME_JWKS_URL` 拉取远端 JWKS 并按 `GENO_RUNTIME_JWKS_CACHE_TTL_SECONDS` 缓存；若未配置显式 JWKS URL，可通过 `GENO_RUNTIME_OIDC_DISCOVERY_URL` 或 URL-form `GENO_RUNTIME_JWT_ISSUER` 拉取 OIDC discovery document 并发现 `jwks_uri`，discovery document 按 `GENO_RUNTIME_OIDC_DISCOVERY_CACHE_TTL_SECONDS` 缓存；生产兜底默认关闭，只有显式设置 `GENO_RUNTIME_JWKS_STALE_IF_ERROR_SECONDS` 或 `GENO_RUNTIME_OIDC_DISCOVERY_STALE_IF_ERROR_SECONDS` 为正数时，远端 refresh 失败才会在对应 stale 窗口内继续使用同 URL 的过期缓存；JWT/JWKS 模式都会从 `sub` 或 `GENO_RUNTIME_JWT_ACTOR_CLAIM` 解析 actor，并支持 issuer/audience/clock skew 校验；Project Metadata、成员维护/邀请、prompt 导入、saved view 保存、Brand Kit/Logo、Score Weights、Report History 管理事件等管理写操作要求 owner/admin；entity alias confirm、manual backfill、human review、fidelity check 等分析复核写操作允许 owner/admin/analyst；viewer 只读。报告 artifact、artifact signed URL、traceability、report management event 等对象级入口会先反查所属项目再校验。数据库层已通过 `0010_runtime_project_rls` 增加 `geno_runtime_app` 非 bypass 运行时角色、PostgreSQL RLS policy 和 `geno.runtime_*` session GUC 上下文，Compose runtime 服务默认使用 app role，并由 runtime E2E 验证 actor 只能看见自己项目；API 常驻服务默认启用进程内 PostgreSQL connection pool（`GENO_RUNTIME_DB_POOL_ENABLED=1`、`GENO_RUNTIME_DB_POOL_MAX_SIZE=10`、`GENO_RUNTIME_DB_POOL_TIMEOUT_SECONDS=5`），每次请求归还连接时会 rollback 业务事务、清空 RLS session GUC 并提交清理，避免 actor/project 上下文跨请求泄漏。该能力用于可审计复盘、多项目操作演示和最小跨客户隔离，但仍不等同于完整 Keycloak/OIDC login/session 与前端 SSO 登录流、完整用户偏好中心、托管邮件服务商 API、真实用户目录、账单隔离、白标资产库版本管理或完整多租户授权后台。

Runtime Console 的 Brand Assets 面板已接入上述审计型版本库，可展示 Logo 历史版本、active 标记、来源文件/content hash 和激活操作；Asset Register / Asset Library 面板已接入 `project_brand_assets` 专表，可登记和读取项目级素材分类、预览 URL、状态、hash、storage version、扫描状态与最近审计事件。这里的“白标资产库增强待接”后续特指图片裁剪、真实病毒扫描引擎、对象存储原生版本、CDN 签名、公网交付策略和跨客户素材权限。

人工复核已从单纯留痕推进到最小队列、内容草稿状态投影和 hash-only CSV 复盘附件：`GET /v1/human-reviews/runtime/queue` 会从 `visibility_score_snapshots` 与 `content_drafts` 聚合待审对象，返回 `target_type/target_id/title/queue_status/priority/reason/latest_review/evidence_refs`，并在 Runtime Console 的 Human Review 面板展示 Review Queue。对 `target_type=content_draft` 的 `POST /v1/human-reviews/runtime` 会先追加 `human_review_records`，再把同项目 `content_drafts.review_status` 投影为本次复核状态，并写入 `content_draft_review_status_updated` 审计事件；`GET /v1/human-reviews/runtime/export.csv` 可导出脱敏复盘附件，只保留 review/target id、状态、payload keys、decision/reviewer/notes hash 和最近 audit 摘要；评分、原始草稿正文和证据绑定仍保持不可变。分配、专用审核工作台、复杂审批流、抽样校准和外部通知渠道仍放在后续阶段。

运行时预警已补成“派生 alert + append-only 管理事件 + 显式通知入队 + 计划任务扫描”模型：`GET /v1/runtime-alerts` 会基于最新 `VisibilityScoreSnapshot`、`ScoreContribution`、`AnswerAnalysis`、`SourceGap`、`CompetitorBenchmark` 和 `ActionRecommendation` 派生 `brand_absent`、`low_recommendation_rate`、`negative_sentiment`、`source_gap`、`competitor_pressure` 五类 alert，返回 severity、阈值、指标值、证据 refs、相关 action、评分审计 refs 和最近管理事件。`POST /v1/runtime-alerts/{alert_id}/events` 会把 acknowledged/resolved/snoozed/reopened/escalated 写入 `runtime_alert_events`，并追加 `runtime_alert_event_recorded` 审计事件，不改写派生 alert 或原始证据。`POST /v1/runtime-alerts/notifications` 可把当前未 resolved/snoozed 的派生 alert 入队为 `runtime_notifications.notification_type=runtime_alert`，并复用 `runtime_notification_subscriptions/deliveries` 生成 webhook、Slack Incoming Webhook 或 SMTP email delivery；订阅表单默认支持 `report_export_job,runtime_alert,entity_alias_assignment_overdue`。`workers/notification_worker/run_runtime_alert_notifications.py` 与 `make runtime-alert-notification-worker` 可按项目或 AU market 扫描并批量调用同一入队逻辑；`workers/notification_worker/run_entity_alias_assignment_notifications.py` 与 `make entity-alias-assignment-notification-worker` 可按项目或 AU market 扫描 overdue alias assignment 并调用同一通知入队逻辑；`workers/notification_worker/run_entity_alias_assignment_escalations.py` 与 `make entity-alias-assignment-escalation-worker` 可按项目或 AU market 扫描 overdue alias assignment，并把 `assigned/in_progress/blocked` 的逾期 review 更新为 `assignment_status=escalated`、追加 `entity_alias_candidate_assignment_escalated` 审计事件；`workers/notification_worker/run_runtime_alert_escalations.py` 与 `make runtime-alert-escalation-worker` 可按项目或 AU market 扫描 critical/high overdue alerts，默认 SLA 阈值为 `critical=4h,high=24h`，跳过最新管理状态为 acknowledged/resolved/snoozed/escalated 的 alert，只追加 `escalated` 事件和审计，不更新派生 alert。Compose `scheduler` profile 已提供通知扫描、alias assignment overdue 通知扫描、alias assignment escalation 与 alert 升级扫描服务入口。Runtime Console 的 Runtime Alerts 面板会展示预警、证据链、管理历史、记录表单、手工 Escalate 选项和通知入队按钮；完整用户偏好中心、托管邮件服务商 API、Slack App/OAuth/交互式消息、复杂值班升级、Temporal/长驻队列化调度仍待后续接入。

补充：实体别名 overdue 通知和最小 SLA 升级现在都纳入可审计链路。通知类型固定为 `entity_alias_assignment_overdue`，创建时写 `runtime_notification_created`，匹配订阅时写 `runtime_notification_delivery_queued`，后续仍由 `make notification-delivery-worker` 统一投递和记录 delivered/retry/dead_letter 状态。升级入口 `POST /v1/entity-aliases/runtime/candidates/assignment-escalations` 与 `make entity-alias-assignment-escalation-worker` 会筛出 overdue 且仍处于 `assigned/in_progress/blocked` 的 review，写回 `assignment_status=escalated` 并追加 `entity_alias_candidate_assignment_escalated`，不会改写 `decision`、confirmed alias 主事实或证据链。

补充：项目生命周期已采用证据保留型 soft archive。`GET /v1/projects/runtime` 默认排除 `status=archived` 的项目，可通过 `status=archived` 或 `include_archived=true` 显式读回；`POST /v1/projects/runtime/action` 支持 owner/admin 执行 `archive` / `restore`，分别写入 `project_archived` / `project_restored` 审计事件。归档审计会记录 `status_before/status_after`，恢复时优先读取最近一次 `project_archived` 的 `status_before`，把项目恢复到 `configured/active/paused` 中的原状态；旧数据缺少归档审计时才 fallback 到 `active`。普通 `PATCH /v1/projects/runtime` 仍用于 Project Metadata，状态只允许 `configured/active/paused`，避免绕过专用归档审计。Runtime Console 的 Project Actions 面板已提供 Archive project / Restore project，直接打开归档项目 URL 时会用 `include_archived=true` 读回以便恢复。该能力保留 prompts、evidence、score、report、members 和 audit history，不做级联删除；项目物理删除、客户级数据保留策略、项目转移审批和账单隔离仍待后续设计。

补充：项目生命周期历史已从 `AuditEvent` 投影为只读 read model。`GET /v1/projects/runtime/lifecycle-events?project_id=...` 会返回 `project_bootstrap_created/project_updated/project_archived/project_restored` 事件、actor、reason、method version、before/after hash、changed fields 和 status before/after；`GET /v1/projects/runtime/lifecycle-events/export.csv?project_id=...` 会导出同一历史的 CSV，并在响应头返回 `X-GENO-Project-Lifecycle-Export-Hash`、row count、total count、project id 和 method version，便于交付包留档后离线复算。开启项目访问控制时，任意项目成员可读，非成员不可读。Runtime Console 的 Project Lifecycle 面板展示同一历史并提供 `Download lifecycle CSV` 入口，不新增可变状态表，也不改写审计事件本身。

补充：项目级 Runtime Audit Trail 已从零散对象审计摘要补成统一只读入口。`GET /v1/audit-events/runtime?project_id=...` 可按 project、event_type、target_type、actor_id 分页读取 append-only `AuditEvent`；`GET /v1/audit-events/runtime/export.csv?project_id=...` 可导出同一结果，并返回 `X-GENO-Audit-Export-Hash`、method version、row count、total count 和 project id。Runtime Console 的 Project Audit Trail 面板展示最近项目级审计事件并提供 `Download audit CSV`。该入口用于项目交付复盘和内部审计检索，不新增可变审计状态，不替代签章、对象存储归档或全租户审计仓库。

> 🛠 **开发与管理入口**：[PROJECT-PLAN.md](PROJECT-PLAN.md) —— 把澳大利亚首发规格拆成 8 个里程碑、任务清单与验收标准（DoD），是从 `docs/` 规格走向工程交付的待办层。
>
> 🗺 **架构图**：[ARCHITECTURE.md](ARCHITECTURE.md) —— GENO SaaS 澳洲首发系统的分层结构、可插拔点、证据优先数据流水线，以及 `AuditEvent / EvidenceLink / ScoreContribution / ReportExport` 审计、溯源、解释链（Mermaid）；出版级图注与设计规范见 [docs/figure-specs.md](docs/figure-specs.md)。

## 目录结构

```
.
├── README.md                           # 本文件：项目导航
├── PROJECT-PLAN.md                     # 开发与管理：里程碑 / 任务 / 验收(DoD)
├── ARCHITECTURE.md                     # 系统架构图（分层 / 可插拔 / 数据流，Mermaid）
├── apps/
│   ├── api/                            # FastAPI API 壳
│   └── web/                            # Next.js Runtime Console
├── packages/
│   └── geno_core/                      # 核心契约、AU 启动包、Prompt Pack、审计与评分模型
├── infra/
│   ├── docker-compose.yml              # PostgreSQL+pgvector、MinIO、API、Web
│   └── db/migrations/                  # up/ 初始化迁移，down/ 回滚脚本
├── workers/                            # 采集 worker 入口预留
├── tests/                              # 核心契约测试
├── decisions/                          # ADR 架构决策记录
├── .github/workflows/                  # CI
└── docs/                               # 规格源、调研报告、审计日志
    ├── 智推时代-全球GEO业务介绍.pdf        # 智推时代官方商业介绍（公司自述原始材料）
    ├── 智推时代GENO项目完整调研报告.md       # ★ 总报告：整合下列全部分项调研
    ├── *-可审计调研复盘.md                  # 7 份分项调研复盘
    ├── GENO-SaaS-*.md                       # 3 份 SaaS 产品落地文档
    ├── AU-P0a-真实批次运行手册.md             # P0a Perplexity/OpenAI 真实批次执行路径
    ├── AU-P0b-Google-Spike-真实运行手册.md    # P0b Google AIO / AI Mode spike 执行路径
    ├── figure-specs.md                      # 架构图出版级图注与设计规范
    └── research_sources/                    # 逐来源证据（摘要 + 原始网页快照 + 报告 PDF）
```

## 本地验证

推荐先跑与 GitHub Actions 对齐的聚合门禁：

```bash
make ci-local
```

该目标会依次执行静态质量门禁、Python 合约/运行时测试、Runtime Console production build、默认/LLM/scheduler/observability/db-smoke 五组 Compose config 静态校验、临时 PostgreSQL schema smoke，以及 Postgres+MinIO runtime E2E。需要拆开排障时可单独执行：

```bash
make quality
make test
make web-build
make docker-config
make docker-config-llm
make docker-config-scheduler
make docker-config-observability
make docker-config-db-smoke
make db-smoke
make runtime-e2e
```

`make db-smoke` 会用独立 Compose project 启动临时 PostgreSQL+pgvector 新卷，让 `/docker-entrypoint-initdb.d` 执行全部 `infra/db/migrations/up/`，随后运行 `scripts/verify_db_smoke.py` 检查 `uuid-ossp`/`vector` 扩展、核心表和关键列、`geno_runtime_app` 非 bypass runtime role、RLS helper functions/policies，并用 admin/runtime 两个连接证明开启 `geno.runtime_*` GUC 后 actor 只能读取授权项目；结束时自动 `down -v` 清理容器和卷。

`make runtime-e2e` 会用独立 Compose project 启动临时 PostgreSQL+pgvector 与 MinIO，构建 `runtime-e2e` 容器，跑 fixture worker `--persist --persist-analysis`，验证 Postgres 中的 answer/score/report/traceability 行、MinIO 中的 Markdown/PDF/CSV report artifact，并用 fake official API response 验证 `geno-api-snapshot://...` 会归档为 `s3://...` EvidenceAsset 和 `api_snapshot_assets_archived` 审计事件；结束时自动 `down -v` 清理容器和卷。

真实 Perplexity/OpenAI key 到位后，用 `make api-preflight` 跑 AU P0a 最小官方 API smoke（1 个 prompt × Sydney × k=3 × 2 平台）。该目标会先检查 collector health，再要求 `P0ACollectionReadinessGate` 通过；缺 key 时底层 worker 以退出码 `3` 在采集前失败，避免把配置缺失误判为真实采集结论。

预检 JSON 会默认写入 `docs/runtime_preflight/api-preflight-latest.json`（可用 `GENO_API_PREFLIGHT_OUTPUT_PATH` 覆盖），内容与 stdout 一致，包含 `collector_health`、`collector_health_gate`、可选 `p0a_readiness_gate`、`preflight_summary`、`preflight_audit_checklist`、`preflight_payload_hash` 和 `preflight_output_path`。`preflight_summary` 会给出本次预检 phase、exit code、是否可进入 design partner、失败原因摘要和建议下一步；`preflight_audit_checklist` 会把 collector health、P0a readiness、collection failures、output path 和 replay args 折叠成可审计 checklist，便于复盘是否可扩大到 design partner；`preflight_payload_hash` 是剔除自身字段后的 canonical JSON sha256，可用于复算 stdout/落盘文件是否一致。

`make verify-api-preflight` 会离线复算该文件的 hash、summary/checklist 版本和必备字段，缺 key 失败预检只要结构完整也会通过审计校验；`make preflight-manifest` 会生成 `docs/runtime_preflight/api-preflight-manifest-latest.json`，记录 preflight 文件 sha256、verifier 结果、run summary、blocking reasons、worker args 和 manifest 自身 hash，方便后续交付包索引。`make au-p0a-runbook` 会生成从 preflight 到 5 prompt 小批次再到 100 prompt × 4 geo 完整批次的机器可读执行计划，`make verify-au-p0a-runbook` 会复算 runbook hash、步骤顺序、planned runs 和 gate 命令；`make verify-au-p0a-env-template` 会先校验已提交的 `.env.au-p0a.example` 是否包含必需键、provider key 是否为空、数据库/对象存储是否为本地占位值、runtime 输出路径是否位于 gitignored 的 `docs/runtime_preflight/*.json`，并拒绝 `sk-`、`pplx-` 等疑似真实 secret 标记；该 gate 只证明模板安全完整，不证明本地 `.env.au-p0a` 或真实 provider/database ready。`make au-p0a-env-bootstrap` 会在本地 `.env.au-p0a` 缺失时从模板创建文件、设置 `0600`、校验 gitignored/not tracked，并把 template/env-file metadata 与 `env_file_bootstrap_hash` 写入 `docs/runtime_preflight/au-p0a-env-bootstrap-latest.json`；`make verify-au-p0a-env-bootstrap` 会离线复算 hash、权限、git hygiene 和脱敏约束。`make au-p0a-env` 会读取 `.env.au-p0a` 或当前 shell 环境，生成脱敏的 `docs/runtime_preflight/au-p0a-env-latest.json`，记录必需/推荐变量是否存在、来源、长度和 sha256 前缀但不输出原始 secret；如果本地 env 文件存在且含条目，还会记录 `env_file.hygiene`，要求文件为 gitignored/not tracked 且权限为 `0600`，否则 `ready_for_real_batch=false` 并返回 `env_file_permissions_not_0600`、`env_file_tracked_by_git` 或 `env_file_not_gitignored` 等错误；完全用进程环境注入 secret 时，`.env.au-p0a` 缺失不会触发该 hygiene hard error。`make verify-au-p0a-env` 会离线复算 environment report hash 并拒绝任何 raw secret 字段；`make au-p0a-environment-checklist` 会把 runbook/env report/status 摘要合成 `docs/runtime_preflight/au-p0a-environment-checklist-latest.json`，列出必填变量、推荐对象存储变量、env-file hygiene、setup commands、hard gate、DB readiness 和后续证据路径，`make verify-au-p0a-environment-checklist` 会复算 checklist hash、summary 计数、脱敏约束、env-file hygiene 摘要和命令完整性，且要求 setup commands 中包含 env template gate；`make au-p0a-runbook-dry-run` 会读取同一份 `GENO_AU_P0A_ENV_FILE` / `.env.au-p0a`，并把 env-file 值按“进程环境优先、文件只补缺”的规则注入执行环境；输出仍只记录来源、长度、sha256 前缀、env-file hygiene 和 `secrets_redacted=true`，不落原始 secret。该命令会把 9 个步骤、预计产物、外部 API 调用风险、必需环境缺口和 `execution_payload_hash` 写入 `docs/runtime_preflight/au-p0a-runbook-execution-latest.json`，默认不执行任何命令；`make verify-au-p0a-runbook-execution` 会离线复算 execution hash、步骤计数、dry-run 不执行命令和 forbidden secret field 等 invariant；`make au-p0a-readiness` 会读取同一份 `GENO_AU_P0A_ENV_FILE` / `.env.au-p0a`，按同样的环境优先级输出脱敏 env-file metadata/hygiene、runbook 校验、必需环境变量、preflight/small batch JSON 与 manifest 的 design-partner gate 状态，写入 `docs/runtime_preflight/au-p0a-readiness-latest.json`，并可用 `GENO_AU_P0A_READINESS_PHASE=preflight|small_batch|full_batch` 切换阶段。readiness 默认只确认 `DATABASE_URL` 是否存在，不主动连接数据库；真实执行前可设置 `GENO_AU_P0A_REQUIRE_DB_CHECK=1` 或直接运行 `python3 scripts/verify_au_p0a_readiness.py --env-file .env.au-p0a --require-db-check`，用只读 `SELECT 1` 验证 PostgreSQL 可连。`make au-p0a-package` 会生成 `docs/runtime_preflight/au-p0a-evidence-package-latest.json`，汇总 runbook、environment report、runbook execution dry-run、readiness、preflight/small/full JSON 和 manifest 的存在状态、file sha256、payload hash、ready 状态和缺口；`make verify-au-p0a-package` 会复算 package hash、校验 summary 与 artifacts 是否一致，并递归拒绝 `value` / `raw_value` 原始字段。`make au-p0a-status` 会用同一份 env-file 重新计算三阶段 readiness，并生成 `docs/runtime_preflight/au-p0a-status-latest.json`，把 runbook、environment report、runbook execution dry-run、三阶段 readiness、证据包 verifier、completion percent、design-ready percent、remaining blockers 和 next action 合成一份总控进度报告；`make verify-au-p0a-status` 会离线复算 status report hash、completion、remaining blockers 与 next action，并递归拒绝 `value` / `raw_value` 原始字段。`make au-p0c-report-package` 会用 fixture 证据链生成 `docs/runtime_preflight/au-p0c-report-package-latest.json`，记录 Markdown/CSV/PDF/白标 PDF 的 size/hash、Method Disclosure、API-vs-browser fidelity、审计摘要和 traceability 合同检查；`make verify-au-p0c-report-package` 会复算 package hash 与 artifact summary。`make au-launch-status` 会把 P0a design partner 状态、P0b Google status/package 和 P0c report package 合并为 `docs/runtime_preflight/au-launch-status-latest.json`，输出 `ready_for_customer_report_handoff`、统一 `next_action`、剩余 blocker 和 `launch_status_hash`；`make verify-au-launch-status` 默认只验证结构/hash/推导自洽，`scripts/verify_au_launch_status.py --require-ready` 才作为真实客户报告交付硬门禁。`make au-launch-remediation-plan` 会把 AU launch status 的 `remaining_blockers` 去重成 `docs/runtime_preflight/au-launch-remediation-plan-latest.json`，为每个 blocker 固定 work item、命令、验证命令、证据产物和外部依赖类型；`make verify-au-launch-remediation-plan` 会复算 plan hash，并要求所有 blocker 都被映射；当前 blocker 会收敛为 8 个 work item，下一步为 `p0a_environment`。提交版说明见 [AU-P0a-真实批次运行手册.md](/home/ymm/ym/gz/20260608-geo/docs/AU-P0a-真实批次运行手册.md)。需要试点准入时可直接运行 `python3 scripts/verify_preflight_payload.py --require-design-partner-ready`；该目录下的运行时 JSON/JSONL/Markdown 已被 gitignore，适合保留本地审计证据但不提交凭证相关上下文。

`make au-handoff-dossier` 会把 AU launch status、launch remediation plan、P0a environment checklist 摘要、P0a execution checklist credential handoff 与 real batch phase handoff 摘要、P0b Google execution checklist 摘要、P0a/P0b/P0c 证据摘要和下一 work item 合并成 `docs/runtime_preflight/au-handoff-dossier-latest.json` 与同名 Markdown，总包记录 source file sha256、stage gate、blocker 分布、P0a 缺失环境、P0a credential handoff ready/missing/redacted、P0a real batch 下一阶段/阻塞阶段数、P0a/P0b env-file hygiene ready/error/warning count、P0b Google 缺失 env/selector/remaining blockers、P0b manual backfill handoff rows/prompt-city coverage/missing/redacted、P0b Google phase handoff next/blocked/full spike runs、work item 命令、runtime endpoint 和 Markdown content hash；同时新增 `customer_handoff_readiness_audit`，用 10 个客户交付 gate 给出 `customer_report_handoff_readiness_percent`，并用 10 个结构化审计 gate 给出 `structural_auditability_percent`，明确区分“真实客户报告交付 readiness”和“当前总包可审计度”。`runtime_endpoints` 必须同时包含 `GET /v1/external-dependency-handoff/au` 与 `GET /v1/external-dependency-clearance/au`，Markdown “Runtime 复盘入口”和 Console Handoff dossier 卡片也展示这两个外部依赖复盘入口。`make verify-au-handoff-dossier` 会复算 dossier hash、Markdown hash、launch/remediation verifier、P0a 环境 summary、P0a credential handoff summary、P0a real batch phase handoff summary、P0a/P0b env-file hygiene summary、P0b Google environment/manual/phase handoff summary、P0b Google execution checklist summary、customer handoff readiness audit gate 计数/百分比/blocked ids 和 summary 计数。默认 verifier 证明“当前交付状态可审计、可交接”，不代表客户报告已经 ready；需要硬门禁时运行 `scripts/verify_au_handoff_dossier.py --require-customer-ready`。Runtime API 同步暴露 `GET /v1/p0a-environment-checklist/au`、`GET /v1/p0a-execution-checklist/au`、`GET /v1/p0b-google-execution-checklist/au` 与 `GET /v1/handoff-dossier/au`，在不写运行期文件的情况下生成当前 P0a 环境清单、P0a 执行清单、P0b Google 执行清单和交付总包；Runtime Console 的 AU Launch Gate 也会展示 customer readiness、auditability、blocked gate ids、missing required/recommended env、P0a credential handoff、P0a real batch phase handoff、P0a/P0b env-file hygiene、Google selector 缺口、P0b manual backfill handoff、P0b Google phase handoff、hard gate、checklist hash 和 handoff 状态，便于自动化与人工交接检查读取。`make au-p0a-credential-request` 会把 P0a execution checklist 内的 `credential_handoff` 拆成独立 `docs/runtime_preflight/au-p0a-credential-request-latest.json`，固定缺失必填项、owner hint、目标 env file、允许注入方式、setup/post-update verification commands、证据输出、source checklist hash 和脱敏策略；`make verify-au-p0a-credential-request` 会复算 packet hash、缺失项/owner 分布/命令计数/endpoint 和 raw secret 禁止字段。Runtime API 同步暴露 `GET /v1/p0a-credential-request/au`，Runtime Console 新增 P0a credential request packet 卡片，展示缺失 provider key、owner 分布、下一命令和 `--require-ready-environment` 硬门禁；默认 verifier 只证明请求包可交接，不代表真实 key 已填充。`make au-p0a-credential-fulfillment` 会进一步把 credential request 与 `au-p0a-env-latest.json` 对齐，生成 `docs/runtime_preflight/au-p0a-credential-fulfillment-latest.json`，逐项记录 `requested_present`、`environment_present`、source、长度/hash 前缀、owner、blocking reasons 和 `credentials_fulfilled`，并通过 `make verify-au-p0a-credential-fulfillment` 复算 hash、request/env 上游 verifier、缺失项、presence mismatch 和 raw secret 禁止字段；`GET /v1/p0a-credential-fulfillment/au` 与 Runtime Console `P0a credential fulfillment` 卡片读取同一状态包。当前本地状态下该包能证明 `DATABASE_URL` 已以脱敏方式被 env report 检测到，但 `OPENAI_API_KEY` 与 `PERPLEXITY_API_KEY` 仍缺失，`--require-fulfilled` strict gate 必须失败。

补充：P0a provider credentials 现在有独立 clearance packet。`make au-p0a-credential-clearance` 会把 credential request、redacted env report、credential fulfillment 和 external dependency clearance dry-run 合成 `docs/runtime_preflight/au-p0a-credential-clearance-latest.json`，固定当前清障 step、缺失 key、owner hint、目标 env file、operator steps、post-update validation sequence、source artifact hash、runtime endpoint、redaction policy 和 `--require-cleared` hard gate；同时写入 `credential_update_contract`，用机器可校验字段固定允许的更新面、禁止落盘的 raw secret 字段、pre/post update 命令、strict gates、完成条件和当前 ready-to-update 状态。`make verify-au-p0a-credential-clearance` 会复算 `p0a_credential_clearance_hash`，并校验 request/fulfillment hash、missing count、update contract、strict gate 和 raw secret 禁止字段。`GET /v1/p0a-credential-clearance/au` 与 Runtime Console `P0a credential clearance` 卡片读取同一状态包；Console 现在直接展示 update contract version、ready-to-update、target env file、required missing keys、allowed update surfaces、forbidden artifact fields、raw-values policy 和 strict gates，避免真实 key 注入契约只停留在底层 JSON。当前本地 artifact 为 `status=pass`、`credential_clearance_packet_ready=true`、`credentials_fulfilled=false`、`credential_clearance_ready=false`、`missing_required=[OPENAI_API_KEY, PERPLEXITY_API_KEY]`、`credential_update_contract.version=au_p0a_credential_update_contract_v1`、`credential_update_contract.required_missing_keys=[OPENAI_API_KEY, PERPLEXITY_API_KEY]`、`credential_update_contract.raw_values_allowed_in_artifacts=false`、`p0a_credential_clearance_hash=7fe7c907c0276adcbf071690964a57187aef29dfe7d498f4c4ec53d1956f8bfd`；该 packet 只证明第一 blocker 的清障路径和真实 key 注入契约可审计，不代表 provider key 已填充。

`make au-p0a-real-batch-request` 会把 P0a execution checklist 内的 `real_batch_phase_handoff` 拆成独立 `docs/runtime_preflight/au-p0a-real-batch-request-latest.json`，固定 `preflight -> small_batch -> full_batch` 三阶段的 command ids、artifact keys、prerequisite gates、planned runs、ready/can-start、blocking reasons、next phase、phase commands、verification commands、evidence outputs、source checklist hash 和 raw secret 禁止字段；`make verify-au-p0a-real-batch-request` 会复算 packet hash、phase 顺序、2436 planned runs、命令/证据计数、endpoint 和脱敏边界。Runtime API 同步暴露 `GET /v1/p0a-real-batch-request/au`，Runtime Console 新增 P0a real batch request packet 卡片，展示下一阶段、阻塞原因、三阶段计划量、证据产物、source checklist hash 和 `--require-real-batches-ready` 硬门禁；默认 verifier 只证明真实批次请求包可交接，不代表 P0a provider preflight、30-run small batch、2400-run full batch 或 design partner gate 已 ready。

补充：P0a real batch request 现在也必须配套 fulfillment artifact 对账。`make au-p0a-real-batch-fulfillment` / `make verify-au-p0a-real-batch-fulfillment` 会把 real batch request 与当前 P0a execution checklist 对齐成 `docs/runtime_preflight/au-p0a-real-batch-fulfillment-latest.json`，逐阶段固定 request/checklist ready、can-start、planned runs、command ids、artifact keys、evidence outputs、owner hint、blocking reasons，并校验 request 引用的 checklist hash 与当前 checklist hash 对齐；`GET /v1/p0a-real-batch-fulfillment/au` 与 Runtime Console `P0a real batch fulfillment` 卡片读取同一状态包。当前本地 artifact 为 `status=pass`、`real_batch_fulfillment_ready=true`、`real_batches_fulfilled=false`、`ready_for_design_partner=false`、`next_phase=preflight`、`missing_required_count=3`；strict `--require-fulfilled` 才代表 provider preflight、30-run small batch、2400-run full batch 和 design partner gate 全部满足。

补充：P0a real batches 现在也有独立 clearance packet。`make au-p0a-real-batch-clearance` 会把 real batch request、P0a execution checklist、real batch fulfillment 与 external dependency clearance dry-run 合成 `docs/runtime_preflight/au-p0a-real-batch-clearance-latest.json`，固定第二 blocker `p0a_real_batches`、前置 `p0a_provider_credentials` 是否阻塞、三阶段缺口、planned runs、operator steps、post-update validation sequence、source artifact hash、runtime endpoint、redaction policy 和 `--require-cleared` hard gate；`make verify-au-p0a-real-batch-clearance` 会复算 `p0a_real_batch_clearance_hash`，并校验 request/checklist/fulfillment hash、phase count、missing phase、strict gate 和 raw provider response 禁止字段。`GET /v1/p0a-real-batch-clearance/au` 与 Runtime Console `P0a real batch clearance` 卡片读取同一状态包。当前本地 artifact 为 `status=pass`、`real_batch_clearance_packet_ready=true`、`real_batches_fulfilled=false`、`real_batch_clearance_ready=false`、`blocked_by_prerequisite_step=true`、`missing_required=[phase:full_batch, phase:preflight, phase:small_batch]`、`p0a_real_batch_clearance_hash=e5671f97c0adef2f8b548d615762acce54c87d7129ec076dd27efe2ee362c940`；该 packet 只证明第二 blocker 的清障路径可审计，不代表真实 provider preflight/small/full batch 已执行。

`make au-p0b-google-environment-request` 会把 P0b Google execution checklist 内的 `environment_handoff` 拆成独立 `docs/runtime_preflight/au-p0b-google-environment-request-latest.json`，固定 `GOOGLE_PLAYWRIGHT_ENABLED`、`DATABASE_URL`、`MANUAL_BACKFILL_PATH`、Google AIO prompt/answer selector、storage state、artifact dir 和 Playwright dependency 的 present/source/truthy/hash 摘要、owner hint、目标 env file、允许注入方式、post-update checks、source checklist hash 和 raw secret 禁止字段；`make verify-au-p0b-google-environment-request` 会复算 packet hash、缺失项/owner 分布/命令计数/endpoint 和 `value/raw_value` 禁止字段。Runtime API 同步暴露 `GET /v1/p0b-google-environment-request/au`，Runtime Console 新增 P0b Google environment request packet 卡片，展示 selector/manual/DB 缺口、owner 分布、下一命令和 `--require-ready-smoke` 硬门禁；默认 verifier 只证明请求包可交接，不代表 Google selector/session/manual JSONL 或 browser smoke 已 ready。

该 P0b Google environment request 还会读取 P0a redacted env report 作为跨阶段复用线索：当 P0b 缺 `full_run_env:DATABASE_URL`，且 P0a env report/verifier/hash 均有效、`DATABASE_URL` 仅以 `value_length` 与 `sha256_prefix` 脱敏出现时，packet 会写入 `cross_stage_reuse_hints`，提示执行者可把同一 DB 配置复制或引用到 `.env.au-p0b-google`。该 hint 只保存 P0a artifact path/hash、verifier status、source/target key、目标 env file、missing id、post-update checks 与 `copy_raw_value_required=false`，不会落盘数据库 URL 原文，也不会把 P0b environment、browser smoke 或 Google 主评分状态自动改成 ready。

`make au-p0b-google-environment-fulfillment` 会进一步把 P0b Google environment request 与 `au-p0b-google-playwright-env-latest.json` 对齐，生成 `docs/runtime_preflight/au-p0b-google-environment-fulfillment-latest.json`，逐项记录 Google Playwright 开关、DB、manual path、selector group、manual file gate 和 Playwright dependency 的 `requested_present`、`environment_present`、source、长度/hash 前缀、owner、blocking reasons、presence mismatch 与 `environment_fulfilled`；`make verify-au-p0b-google-environment-fulfillment` 会复算 hash、request/env 上游 verifier、缺失项、presence mismatch、raw secret/selector/DB URL 禁止字段和 `--require-fulfilled` strict gate。Runtime API 同步暴露 `GET /v1/p0b-google-environment-fulfillment/au`，Runtime Console 新增 P0b Google environment fulfillment 卡片；external dependency handoff/clearance 的 P0b Google environment 步骤也会把 fulfillment build/verify/strict gate 和 artifact 纳入推荐序列。当前本地状态下该包只能证明 request 与 env report 可对账，仍会因 Google 开关、DB、manual path、selector 与 manual file gate 未满足而保持 `environment_fulfilled=false`。

补充：P0b Google environment 现在也有独立 clearance packet。`make au-p0b-google-environment-clearance` 会把 environment request、Playwright env report、environment fulfillment 与 external dependency clearance dry-run 合成 `docs/runtime_preflight/au-p0b-google-environment-clearance-latest.json`，固定第三 blocker `p0b_google_environment`、前置 `p0a_real_batches` 是否阻塞、Google env/selector/file/dependency 缺口、operator steps、post-update validation sequence、source artifact hash、runtime endpoint、redaction policy 和 `--require-cleared` hard gate；`make verify-au-p0b-google-environment-clearance` 会复算 `p0b_google_environment_clearance_hash`，并校验 request/env/fulfillment hash、missing/mismatch、strict gate 和 raw secret/selector/DB URL 禁止字段。`GET /v1/p0b-google-environment-clearance/au` 与 Runtime Console `P0b Google environment clearance` 卡片读取同一状态包。当前本地 artifact 为 `status=pass`、`environment_clearance_packet_ready=true`、`environment_fulfilled=false`、`environment_clearance_ready=false`、`blocked_by_prerequisite_step=true`、`missing_required=[environment:DATABASE_URL, environment:GOOGLE_PLAYWRIGHT_ENABLED, environment:MANUAL_BACKFILL_PATH, file:MANUAL_BACKFILL_PATH, selector:google_aio_answer_selector, selector:google_aio_prompt_selector]`、`p0b_google_environment_clearance_hash=9aebce89f95dd2aa5477ac4af5bbbce67da375ca9100be18a1f1dd2318db917e`；该 packet 只证明第三 blocker 的清障路径可审计，不代表 Google selector/session/manual/DB 环境已满足。

`make au-p0b-google-manual-backfill-request` 会把 P0b Google execution checklist 内的 `manual_backfill_handoff` 拆成独立 `docs/runtime_preflight/au-p0b-google-manual-backfill-request-latest.json`，固定 120-row JSONL 模板、template manifest、verification artifact、30 prompts × Australia/Sydney × k=2 的记录/覆盖口径、必填字段、operator requirements、setup/verification commands、证据输出、source checklist hash 和 raw answer/citation/asset URL 禁止落盘策略；`make verify-au-p0b-google-manual-backfill-request` 会复算 packet hash、120-row 计数、60 个 prompt-city 覆盖、命令/endpoint 和脱敏边界。Runtime API 同步暴露 `GET /v1/p0b-google-manual-backfill-request/au`，Runtime Console 新增 P0b Google manual backfill request packet 卡片，展示记录数、覆盖数、缺失原因、模板/verification 路径、required fields、operator requirements 和 `--require-manual-backfill-ready` 硬门禁；默认 verifier 只证明请求包可交接，不代表真实 120-row manual JSONL 已填充或 Google 可以进入主评分分母。

`make au-p0b-google-manual-backfill-fulfillment` 会进一步把 P0b Google manual backfill request 与 `au-p0b-google-manual-backfill-verification-latest.json` 对齐，生成 `docs/runtime_preflight/au-p0b-google-manual-backfill-fulfillment-latest.json`，逐项固定 verification status/hash、manual JSONL file sha256 存在性、120/120 record count、60/60 prompt-city 覆盖、sample size、answer/citation/asset 缺失和 prompt-city sample 结构；`make verify-au-p0b-google-manual-backfill-fulfillment` 会复算 hash、request/verification 上游 verifier、缺失项、runtime endpoint、raw answer/citation/asset URL 禁止字段和 `--require-fulfilled` strict gate。Runtime API 同步暴露 `GET /v1/p0b-google-manual-backfill-fulfillment/au`，Runtime Console 新增 P0b Google manual backfill fulfillment 卡片；external dependency handoff/clearance 的 P0b manual backfill 步骤也会把 fulfillment build/verify/strict gate 和 artifact 纳入推荐序列。当前本地状态下该包只能证明 request 与当前 verification 可对账，仍会因真实 manual verification 缺失或 strict 失败而保持 `manual_backfill_fulfilled=false`。

`make au-p0b-google-phase-execution-request` 会把 P0b Google execution checklist 内的 `google_spike_phase_handoff` 拆成独立 `docs/runtime_preflight/au-p0b-google-phase-execution-request-latest.json`，固定 `environment -> browser_smoke -> manual_backfill -> health_check -> full_spike -> main_scoring` 六阶段的 planned runs、command ids、commands、artifact keys、evidence outputs、prerequisite phase、blocking reasons、next phase、source checklist hash 和脱敏策略；`make verify-au-p0b-google-phase-execution-request` 会复算 packet hash、阶段顺序、240-run full spike、120-row manual 口径、endpoint、hard gate 和 forbidden raw payload field。Runtime API 同步暴露 `GET /v1/p0b-google-phase-execution-request/au`，Runtime Console 新增 P0b Google phase execution request packet 卡片，展示六阶段 ready/can-start、阻塞原因、下一命令、source checklist hash、证据输出和 `--require-google-phases-ready` / `--require-google-main-scoring-ready` 硬门禁；默认 verifier 只证明请求包可交接，不代表真实 env、browser smoke、manual 120-row、health/full spike 或 Google 主评分已经 ready。

补充：P0b Google phase execution 已从 request-only 升级为 fulfillment-aware。`make au-p0b-google-phase-execution-fulfillment` / `make verify-au-p0b-google-phase-execution-fulfillment` 会把 phase execution request 与 P0b Google execution checklist 对账成 `docs/runtime_preflight/au-p0b-google-phase-execution-fulfillment-latest.json`，逐阶段固定 request/checklist ready、can-start、planned runs、command ids、artifact keys、evidence outputs、owner hint、blocking reasons，并校验 request 引用的 checklist hash 与当前 checklist hash 对齐；`GET /v1/p0b-google-phase-execution-fulfillment/au` 与 Runtime Console `P0b Google phase execution fulfillment` 卡片展示同一状态包。当前本地 artifact 为 `status=pass`、`phase_execution_fulfillment_ready=true`、`phase_execution_fulfilled=false`、`next_phase=environment`、`missing_required_count=6`、`p0b_google_phase_execution_fulfillment_hash=bdfc8ee82e592fba7c9801ab845ace98070382f026ef1fb9765872b8bae9e640`；strict `--require-fulfilled` 才代表真实六阶段和 Google 主评分准入都满足。

补充：P0b Google phase execution 现在也有独立 clearance packet。`make au-p0b-google-phase-execution-clearance` 会把 phase execution request、P0b Google execution checklist、phase execution fulfillment 与 external dependency clearance dry-run 合成 `docs/runtime_preflight/au-p0b-google-phase-execution-clearance-latest.json`，固定第五 blocker `p0b_google_phase_execution`、前置 `p0b_google_manual_backfill` 是否阻塞、六阶段缺口、operator steps、post-update validation sequence、source artifact hash、runtime endpoint、redaction policy 和 `--require-cleared` hard gate；`make verify-au-p0b-google-phase-execution-clearance` 会复算 `p0b_google_phase_execution_clearance_hash`，并校验 request/checklist/fulfillment hash、phase order、missing phase、strict gate 和 raw provider response/answer/citation/asset 禁止字段。`GET /v1/p0b-google-phase-execution-clearance/au` 与 Runtime Console `P0b Google phase execution clearance` 卡片读取同一状态包。当前本地 artifact 为 `status=pass`、`phase_execution_clearance_packet_ready=true`、`phase_execution_fulfilled=false`、`phase_execution_clearance_ready=false`、`blocked_by_prerequisite_step=true`、`ready_phase_count=0`、`blocked_phase_count=6`、`missing_required=[phase:browser_smoke, phase:environment, phase:full_spike, phase:health_check, phase:main_scoring, phase:manual_backfill]`、`next_action=clear_p0b_google_manual_backfill_first`、`p0b_google_phase_execution_clearance_hash=0a97294947af13c593a6429e4b34c7c39aff305acde84f236db9454d7ec68373`；该 packet 只证明第五 blocker 的清障路径可审计，不代表 env/smoke/manual/health/full spike/main scoring 已完成。

`make au-customer-handoff-readiness` 会从同一份 `docs/runtime_preflight/au-handoff-dossier-latest.json` 生成独立 `docs/runtime_preflight/au-customer-handoff-readiness-latest.json`，固定 `customer_handoff_readiness_hash`、上游 handoff dossier hash、`customer_report_handoff_readiness_percent`、`structural_auditability_percent`、blocked customer gate ids、下一 work item 和 hard gate commands。`make verify-au-customer-handoff-readiness` 会复算 readiness hash、上游 handoff verifier、gate 计数/百分比/blocked ids 和 runtime endpoint；默认只证明 readiness artifact 自洽，真实客户报告交付前还必须运行 `scripts/verify_au_customer_handoff_readiness.py --require-customer-ready`。Runtime API 同步暴露 `GET /v1/customer-handoff-readiness/au`，Runtime Console 首页新增 Customer handoff readiness 卡片，展示独立 readiness hash、客户交付 readiness、结构化可审计度、blocked gates、source dossier hash 和硬门禁命令。

`make au-next-work-item` 会把 handoff dossier 中当前 `next_work_item` 拆成独立 `docs/runtime_preflight/au-next-work-item-latest.json`，固定 `next_work_item_packet_hash`、source handoff dossier hash、下一 work item id/title/stage/dependency class、命令、验证命令、证据输出、客户交付 readiness 百分比、blocked gate ids 和 hard gate commands；同时新增 `execution_context`，把当前 work item 绑定到对应 external dependency group、linked request/fulfillment artifact、`artifact_type`、artifact hash、recommended sequence 和 artifact-level strict gate。字段名 `linked_request_packet` 保留兼容，但必须用 `artifact_type=request_packet|fulfillment_artifact` 区分“请求包已生成”和“履约证据已对账”。当前 `p0a_environment` 仍绑定 `p0a_provider_credentials` 与 `p0a_credential_request`，并从 `docs/runtime_preflight/au-external-dependency-handoff-latest.json` 带入 group status、next command、blocking reasons、source hash、group commands、group verification commands 和 group evidence outputs；后续 `p0a_small_batch`/`p0a_full_batch` 会绑定 `p0a_real_batch_fulfillment`，P0b Google environment/manual/phase work items 分别绑定 `p0b_google_environment_fulfillment`、`p0b_google_manual_backfill_fulfillment` 和 `p0b_google_phase_execution_fulfillment`。next-work-item 会把 work item 本身命令与 linked dependency group 的验证/证据链去重合并，当前 P0a recommended sequence 为 20 步，包含 `make au-p0a-credential-fulfillment`、`make verify-au-p0a-credential-fulfillment` 和 `verify_au_p0a_credential_fulfillment.py --require-fulfilled`，避免 next-work-item 仍停留在旧的 15 步 request-only 口径。`make verify-au-next-work-item` 会复算 packet hash、上游 handoff verifier、summary 计数、work-item/group/combined execution context、external dependency handoff source、runtime endpoint、recommended sequence 和 hard gate；默认只证明“下一工作项交接包可执行且可审计”，不代表外部依赖已经清零或客户报告 ready。Runtime API 同步暴露 `GET /v1/next-work-item/au`，Runtime Console 首页新增 Next work item packet 卡片，直接展示 linked group/artifact、artifact type/hash、work item counts、dependency group counts、fulfillment gate、recommended sequence、下一命令、下一验证命令、首个证据输出、source dossier hash、external dependency handoff hash、artifact strict gate 与 `--require-customer-ready` 硬门禁。

`make au-external-dependency-handoff` 会把 launch remediation plan、P0a environment checklist、P0a execution checklist 和 P0b Google execution checklist 合并成 `docs/runtime_preflight/au-external-dependency-handoff-latest.json`，按 `p0a_provider_credentials`、`p0a_real_batches`、`p0b_google_environment`、`p0b_google_manual_backfill`、`p0b_google_phase_execution` 五组固定外部依赖边界、下一步顺序、work item、source verifier 和文件 sha256；每个 dependency group 现在也直接携带 `commands`、`next_command` 和 `blocking_reasons`，例如当前本地 `.env.au-p0a` hygiene 已存在且 ready 时，`p0a_provider_credentials.next_command=make au-p0a-env`，无需再跨 `clearance_sequence` 拼接；模板校验和 bootstrap 仍留在 commands/recommended sequence 中用于复跑或新环境恢复。同一包内的 `clearance_sequence` 会进一步固定 `p0a_provider_credentials -> p0a_real_batches -> p0b_google_environment -> p0b_google_manual_backfill -> p0b_google_phase_execution -> customer_report_handoff_gate` 六步清零顺序、当前可启动 step、下一条命令、每步阻塞原因、验证命令和证据输出。`make verify-au-external-dependency-handoff` 会复算交接包 hash、source verifier、当前 blocker 映射、5 个 dependency group、group-level execution fields、6 个 clearance step、脱敏策略和 summary 计数。`make au-external-dependency-clearance` 会读取同一 handoff，生成 `docs/runtime_preflight/au-external-dependency-clearance-latest.json` dry-run 执行计划，只记录当前会执行的 step、不会执行真实 provider/DB/Google/manual 命令；`make verify-au-external-dependency-clearance` 会复算 dry-run hash、handoff verifier、step 前缀、`would_execute` 唯一步骤、hard gate 和 forbidden raw field。默认 verifier 只证明“外部依赖交接包/清零执行计划结构可审计”，当前 `external_dependency_handoff_ready=false` 是预期状态，dry-run 当前 `current_step_id=p0a_provider_credentials`、`next_command=make au-p0a-env`、`would_execute_step_count=1`；真实凭证、数据库、Google selector、manual 120-row 和真实批次完成后，才运行 `scripts/verify_au_external_dependency_handoff.py --require-ready` 与 `scripts/verify_au_external_dependency_clearance.py --require-handoff-ready` 作为外部依赖清零硬门禁。Runtime API 同步暴露 `GET /v1/external-dependency-handoff/au` 与 `GET /v1/external-dependency-clearance/au`，其中 clearance endpoint 会在内存中构建同源 handoff 后生成 dry-run，不依赖预先落盘文件；handoff dossier 的 `runtime_endpoints` 和 Runtime Console AU Launch Gate 会展示外部依赖 replay 入口、clearance 当前步骤和 dry-run 当前 would-execute 步骤；External dependency handoff 卡片还会逐个 dependency group 展示 Next 命令、commands 数量和前几条 blocking reasons，避免只看控制台时仍需人工跨 JSON 拼接。

补充：external dependency handoff 现在还会显式读取 `p0b_google_manual_backfill_fulfillment` 作为 P0b manual 的结构化进度源，Makefile 通过 `--p0b-google-manual-backfill-fulfillment-path` 固定路径，API 端点则复用同请求内构建的 fulfillment payload。当前 handoff summary 已与 fulfillment 对齐为 `p0b_google_manual_backfill_record_count=120/120`、`p0b_google_manual_backfill_covered_prompt_city_count=60/60`、`p0b_google_manual_backfill_fulfillment_verified=true`、`p0b_google_manual_backfill_fulfillment_missing_required_count=4`；这只说明模板/文件层面的 120 行和 60 个 prompt-city 结构已可审计，不代表 answer_text、citation_urls、evidence_asset 和 verification status 已满足，`--require-fulfilled` 与 `--require-ready` 仍会保持阻塞。

补充：external dependency handoff/clearance 的 `p0a_real_batches` 与 `p0b_google_phase_execution` 步骤现在都绑定 fulfillment artifact，而不是只绑定 request packet。P0a real batches 绑定 `p0a_real_batch_fulfillment`、`make au-p0a-real-batch-fulfillment`、`make verify-au-p0a-real-batch-fulfillment`、`GET /v1/p0a-real-batch-fulfillment/au` 和 `verify_au_p0a_real_batch_fulfillment.py --require-fulfilled`；P0b phase execution 绑定 `p0b_google_phase_execution_fulfillment`。当前 `au-external-dependency-handoff-latest.json` 为 `status=pass`、`external_dependency_handoff_ready=false`；`au-external-dependency-clearance-latest.json` 为 `status=pass`、`ready_to_execute=true`、`current_step_id=p0a_provider_credentials`。这些仍只证明可审计清障路径，不证明外部依赖已清零。

clearance dry-run 现在还会为每个清零步骤绑定 `linked_request_context`：P0a provider credentials 绑定 `p0a_credential_request`，P0a real batch 绑定 `p0a_real_batch_fulfillment`，P0b Google environment/manual/phase 分别绑定对应 fulfillment artifact，最终客户交付 gate 绑定 `customer_handoff_readiness`。每个 context 都记录 artifact id/title、输出路径、hash 字段、当前 artifact hash、文件 sha256、build/verify 命令、strict gate 和 runtime endpoint；顶层同步暴露当前 step 的 `current_step_request_context`、`current_recommended_sequence`、`current_recommended_sequence_count` 和 `current_strict_gate_command`。这让执行者可以从 `GET /v1/external-dependency-clearance/au` 直接看到“先生成或对账哪个 artifact、如何验证、随后跑哪些环境/批次命令、最终用哪个 strict gate 判断完成”，而不用在多个 JSON 之间手工拼接。

补充：P0b Google environment 的 clearance context 已从 request-only 升级为 fulfillment artifact，绑定 `p0b_google_environment_fulfillment`、`make au-p0b-google-environment-fulfillment`、`make verify-au-p0b-google-environment-fulfillment`、`GET /v1/p0b-google-environment-fulfillment/au` 和 `verify_au_p0b_google_environment_fulfillment.py --require-fulfilled`。这保证执行者在完成 DB/selector/manual/session 配置后，用当前 Playwright env report 与 request packet 对账，而不是只验证请求包结构。

handoff dossier 的 `runtime_endpoints` 现在也固定项目复盘入口：`GET /v1/projects/runtime/lifecycle-events?project_id={project_id}`、`GET /v1/projects/runtime/lifecycle-events/export.csv?project_id={project_id}`、`GET /v1/audit-events/runtime?project_id={project_id}` 和 `GET /v1/audit-events/runtime/export.csv?project_id={project_id}`。Markdown 交接稿会生成“Runtime 复盘入口”小节，Runtime Console 的 Handoff dossier 卡片也展示 Lifecycle replay/CSV 与 Audit replay/CSV，确保客户交接后能按同一项目 id 回看生命周期和审计轨道，并下载可 hash 校验的 CSV。

Project access 复盘入口也已补齐为可下载附件：`GET /v1/project-members/runtime/export.csv?project_id={project_id}` 导出成员 id、角色、创建时间、user_id hash 和最近 audit 摘要，响应头返回 `X-GENO-Project-Member-Export-Hash`、project id、row count 与 total count；`GET /v1/project-member-invitations/runtime/export.csv?project_id={project_id}&status=pending` 导出邀请 id、role/status、email hash、invited_by hash、token hash presence、过期/接受/撤销时间和最近 audit 摘要，响应头返回 `X-GENO-Project-Member-Invitation-Export-Hash`、project id、status、row count 与 total count。Runtime Console 的 Project Members 面板展示 `Members CSV` / `Invitations CSV` 路径和下载链接；CSV 不导出 raw user_id、raw email、raw invite token 或 stored token hash。

Runtime API 也暴露 `GET /v1/launch-status/au` 和 `GET /v1/launch-remediation-plan/au`，按同一套环境变量路径覆盖规则读取或内存生成 AU launch status 与清障计划，不依赖数据库连接即可返回当前 `ready_for_customer_report_handoff`、`remaining_blockers`、`next_action`、`launch_status_hash`、`next_work_item_id`、work items 和 remediation plan hash。Runtime Console 首页 AU Launch Gate 面板直接展示 P0a/P0b/P0c 摘要、剩余 blocker、remediation plan 摘要、P0a environment checklist、P0b Google execution checklist、handoff dossier readiness、客户交付硬门禁状态、Markdown hash、前几个 work item、下一条命令、验证命令和查询路径，便于演示或交付前复核。

P0a/P0b 清障计划现在也锁定 env-file hygiene 修复命令：`p0a_environment` 必须先运行 `make verify-au-p0a-env-template`，再用 `make au-p0a-env-bootstrap` / `make verify-au-p0a-env-bootstrap` 创建或校验本地 `.env.au-p0a`、设置 `0600`、证明 gitignored/not tracked 并落盘 `docs/runtime_preflight/au-p0a-env-bootstrap-latest.json`；`p0b_google_playwright_env` 必须先运行 `make verify-au-p0b-google-env-template`，再用 `make au-p0b-google-env-bootstrap` / `make verify-au-p0b-google-env-bootstrap` 创建或校验本地 `.env.au-p0b-google`、设置 `0600`、证明 gitignored/not tracked 并落盘 `docs/runtime_preflight/au-p0b-google-env-bootstrap-latest.json`，与 environment checklist、execution checklist 和 handoff dossier 的 env-file hygiene gate 保持同一口径。

P0a 真实批次现在还补了一层总控执行清单：`make au-p0a-execution-checklist` / `make verify-au-p0a-execution-checklist` 会把 env template gate、environment checklist、runbook dry-run、preflight、small batch、full batch、package/status hard gates 和证据输出路径合成 `docs/runtime_preflight/au-p0a-execution-checklist-latest.json`，冻结 `p0a_execution_checklist_hash`、missing/failed artifacts、remaining blockers、completion/design-ready 百分比、setup/execution/verification commands。该清单内的 `credential_handoff` 会把 `PERPLEXITY_API_KEY`、`OPENAI_API_KEY`、`DATABASE_URL` 拆成逐项交接表，标出 owner hint、允许的注入方式（process env、`GENO_AU_P0A_ENV_FILE`、`.env.au-p0a`）、env-file key、填充后的验证命令和脱敏策略；`real_batch_phase_handoff` 会把 `preflight -> small_batch -> full_batch` 三阶段的 command ids、artifact keys、prerequisite gates、planned runs、ready/can-start、blocking reasons 和 next phase 固定到同一 JSON，当前本地应为 `ready=false`、`next_phase=preflight`、`blocked_phase_count=3`、`total_planned_runs=2436`。原始 secret 仍禁止进入 JSON，只保留 present/source/value_length/sha256_prefix/secret_redacted。当前本地该清单预期为 `status=fail`、`ready_for_design_partner=false`，说明真实 P0a 还缺 provider env 与 small/full batch 证据，不代表真实 2400-run 已完成。Runtime API 同步暴露 `GET /v1/p0a-execution-checklist/au`，handoff dossier 和 Runtime Console AU Launch Gate 会展示同一份 P0a execution checklist 摘要、hash、阻塞项、real batch next phase 和查询路径。

核心服务一键启动入口：

```bash
docker compose -f infra/docker-compose.yml up --build
```

LiteLLM proxy 作为可选 profile 接入，默认不会随核心服务启动。需要用真实 LLM judge 时，先提供供应商 key 和 master key，再启动 profile；`collector-worker-litellm` 会把 `--judge-gateway litellm --judge-model geno-gpt-4.1-mini` 走到同一个 `LiteLLMGateway` adapter：

```bash
OPENAI_API_KEY=... LITELLM_MASTER_KEY=... \
docker compose -f infra/docker-compose.yml --profile llm-gateway up --build litellm

OPENAI_API_KEY=... LITELLM_MASTER_KEY=... \
docker compose -f infra/docker-compose.yml --profile llm-gateway run --rm collector-worker-litellm
```

默认地址：API `http://localhost:8000/health`，控制台 `http://localhost:3000`。控制台会读取 runtime project / project brand kit / project brand logo upload / score weight config / score formula catalog / human review / prompt / prompt CSV/file import / prompt import history / evidence / collection run summary / fidelity check/trend / entity alias / entity alias candidates / saved views / score / graph / report / report artifact / report artifact signed URL / report management event / action / content / knowledge fact search / traceability API；Runtime Filters 支持通过 URL 查询参数按 `project_id`、`platform`、evidence `city`、`intent_type` 筛选 evidence，并用 `intent_type` 同步筛选 prompt 列表；Evidence Sort 支持 `collected_at_desc`、`collected_at_asc`、`cost_desc`、`cost_asc`、`citation_count_desc`、`audit_count_desc` 受控排序；Saved Views 可把当前筛选、排序、evidence query path 与 export path 保存到 PostgreSQL，并写入 `runtime_saved_view_saved` 审计事件；控制台同时展示实际 API 查询路径和筛选后 Evidence CSV 下载入口，CSV 响应含 `X-GENO-Evidence-Export-Hash` 与 `X-GENO-Evidence-Export-Sort`；Project Bootstrap 面板可创建并读回可配置 AU/DTC 客户启动包，输入 tenant、project、brand、category、brand domains、product lines 和 3-5 competitors 后生成 100 条 prompt、品牌/竞品实体和 `project_bootstrap_created` 审计事件，并提供 Brand Kit 表单保存项目级白标默认值、Logo Upload 表单归档品牌图片、Score Weights 表单读取 `/v1/score-formulas/runtime`、选择评分公式版本并保存项目级 AUVisibilityScore 组件权重、Entity Alias 表单和候选列表；Logo Upload 通过 `POST /v1/project-brand-kits/runtime/logo` 发送图片 raw body，先校验项目存在，再归档到对象存储，最后把 `s3://...` 写入 `project_brand_kits.logo_url` 并生成 `project_brand_logo_uploaded` 审计事件，审计 `input_refs/output_refs` 包含 filename、content type、content hash 和 logo URI；Score Weight Config 会写入 `score_weight_configs` 并生成 `score_weight_config_saved` 审计事件，后续 worker `--persist-analysis --score-formula-version ...` 会读取该配置并把本次使用的 `formula_version` 与 `component_weights_snapshot` 冻结到 `visibility_score_snapshots`；Human Review Trail 可对 score snapshot、content draft、answer analysis/run、score weight config 或 project 记录复核状态、decision、reviewer、notes 和 payload，写入 `human_review_records` 并生成 `human_review_recorded` 审计事件。Prompt Pack 面板展示 100 条 AU prompt 的总数、intent/city 覆盖和样本文本，提供 Prompt CSV Import 表单把 `text,intent_type,city,priority,intent_weight` 等字段导入为项目级 `PromptQuestion`，也提供 Prompt File Import 表单把 `.csv/.txt` UTF-8 文件或 `.xlsx` 第一工作表通过 raw-body endpoint 导入为同一套 `PromptQuestion` 和 `runtime_prompts_imported` 审计链路，并展示 Prompt Import History，按项目读回每次导入的 source format、filename、content type、hash、prompt count、method version 和审计事件，同时提供 Manual Backfill 表单，把人工答案、citation、截图/HTML URL 写成标准 `RawEvidenceRecord` 与 `manual_backfill_recorded` 审计事件；Collection Run Quality 面板展示最近批次的 planned/attempted/success/failure、success rate、trigger rate、answer rate、总成本、单次平均成本、总耗时、单次平均耗时、平台/access method 分布、失败摘要和 `collection_run_summarized` 审计事件数；Evidence Runs 面板展示每条 answer run 的 prompt、平台、城市、access method、触发状态、采样、collector、cost、duration、raw hash、citation、asset 和 audit；Score Contributions 面板展示 8 个评分组件的原始分、权重、加权贡献、分母、正负证据、confidence note、parser A/B agreement、冻结权重快照和关联 answer run，并能从组件跳到相关 answer run；Citation Graph & Competitors 面板展示 Citation Graph Map、source nodes、source gaps、graph evidence links 和竞品 benchmark，并能从 source node / graph link 跳到 answer run；Report Snapshot 面板提供 Markdown/CSV/PDF/White-label PDF 下载入口，并把当前 `platform/city/intent_type/sort` 传给 artifact API，白标 PDF 会优先使用当前项目 Brand Kit，响应头返回 `X-GENO-Report-Artifact-Hash`、`X-GENO-Report-Artifact-Filter-Hash`、`X-GENO-Report-Artifact-Template`、`X-GENO-Report-Artifact-Template-Hash`、`X-GENO-Report-Artifact-Sort`、row count 与 total count；Signed PDF URL 和 Signed white-label URL 会调用 `/v1/reports/runtime/{report_export_id}/artifact/signed-url`，返回 `download_url/expires_at/signed_url_hash/signature_payload_hash`，下载时校验 HMAC 与过期时间；Report History 面板按当前 runtime project 查询最近 5 个 `ReportExport`，逐条展示版本、导出时间、样本量、score snapshot 数、审计事件数、methodology hash、冻结对象存储 URL、继承当前筛选/排序的 Markdown/CSV/PDF/White-label PDF 下载入口，并可把某个历史报告标记为 `internal_review`、`client_ready` 或 `archived`；该操作只追加 `report_export_management_recorded` 审计事件和 note/actor/status，不更新 `report_exports` 冻结快照；Report Method & Evidence Appendix 面板展示冻结 methodology hash、采样窗口、平台/access method/city 覆盖、双分母评分、平台权重、Method Disclosure（Google coverage/gate、limited coverage、API/browser fidelity、access/platform distribution）、API-vs-browser fidelity check 的 status、mismatch count、difference rate、payload hash、查询路径和 `api_browser_fidelity_checked` 审计事件，并展示最近已落库 check 的趋势方向、sampled/total、平均/峰值差异率、趋势窗口和趋势查询路径；Action Plan & Retest Detail 面板展示任务 owner/status/next check/source gap/evidence runs、T+0/7/14/30 复测计划、前后分数对比和 action/retest audit trail；Content Engine Detail 面板展示本地 facts、pgvector Knowledge Search、证据绑定草稿、target questions/evidence runs、source action、connector 计划、manual distribution records 与 content/embedding audit trail，知识检索使用 `/v1/knowledge-facts/runtime/search`，按 `project_id/query/market_code/city` 查询 `knowledge_fact_embeddings`，并展示 `fixture-knowledge-embedding-v1` 与 `knowledge_fact_embeddings_indexed`；Traceability Detail 面板展示 Traceability Map、报告到评分、证据、图谱、行动、内容、审计事件和 evidence links 的聚合链路；节点级 details 区可展开查看 score components、answer evidence、citation/asset nodes、actions/content drafts 和 audit event nodes，页面内锚点深链路会高亮被跳转节点。如果还没有数据，先运行 worker profile 写入一批 fixture runtime 数据。

运行时健康检查分两层：`/health` 只证明 API 进程可响应；`/ready` 会执行 PostgreSQL `SELECT 1`，DB 不可用时返回 503；`/v1/runtime-diagnostics` 返回 database/object_store/runtime_auth 三类诊断，包含连接池配置快照、对象存储配置完整性和 runtime auth/JWT/JWKS/OIDC discovery 配置状态。对象存储诊断默认只做配置检查，不主动访问 MinIO/S3，避免健康检查产生副作用：

```bash
curl http://localhost:8000/ready
curl http://localhost:8000/v1/runtime-diagnostics
```

运行时指标导出使用 Prometheus 文本格式 `/metrics`，当前覆盖 API 请求总量、按 route template 聚合的状态码、请求延迟 histogram，以及 runtime PostgreSQL connection pool 的 enabled/max size/timeout/created/available 快照；`/metrics` 自身不计入请求指标，避免抓取放大时序。API 还会为每个非 `/metrics` 请求回传 `X-GENO-Request-Id`，接受调用方传入的安全 request id，或自动生成 32 位 hex id，并向 `geno_api.access` logger 输出 `runtime_api_request` JSON access log，包含 request id、method、path、route、status、duration_ms 和 client_host；日志不写 query string，避免把筛选参数或潜在敏感上下文放进低层 access log。该端点与 access log 是 MVP 轻量可观测入口，不替代 Prometheus/Grafana/OpenTelemetry、集中式日志采集、慢查询追踪或告警订阅：

```bash
curl http://localhost:8000/metrics
```

可选 `observability` profile 会启动 Prometheus `http://localhost:9090` 和 Grafana `http://localhost:3001`，Prometheus 通过 `infra/prometheus/prometheus.yml` 抓取 `api:8000/metrics`，Grafana 通过 provisioning 自动配置 `GENO Prometheus` datasource。默认账号密码为 `admin/admin`，可用 `GRAFANA_ADMIN_USER` / `GRAFANA_ADMIN_PASSWORD` 覆盖：

```bash
docker compose -f infra/docker-compose.yml --profile observability up --build prometheus grafana
```

采集 worker 默认只输出 JSON；显式启用持久化时会先把 AU `ProjectBootstrap`、品牌/竞品和 100 条 `PromptQuestion` 写入 PostgreSQL，再把成功的 `RawEvidenceRecord`、失败的 `CollectionFailureRecord` 和本次批次级 `CollectionRunSummary` 写入 PostgreSQL。`CollectionCost.duration_ms` 会记录每条采集的 collector wall-clock 耗时；`CollectionRunSummary` 会记录 planned/attempted/success/failure、success rate、trigger rate、answer rate、总成本、单次平均成本、总耗时、单次平均耗时、平台/城市/access method 分布、失败原因分布和关联 `answer_run_ids`，并追加 `collection_run_summarized` 审计事件。`make worker-fixture-persist` 还会启用 `--persist-analysis`，继续写入分析、评分、图谱、报告、action/content/traceability，并在配置 `OBJECT_STORE_ENDPOINT` 时把 Markdown/CSV/PDF 报告 artifact 归档到 MinIO/S3-compatible bucket；若成功记录包含官方 API `geno-api-snapshot://...` HTML snapshot，也会在落库前归档到 `evidence/<project_id>/<answer_run_id>/<asset_id>.html`，用实际 `s3://...` 与 content hash 更新 `EvidenceAsset`，并追加 `api_snapshot_assets_archived` 审计事件：

```bash
DATABASE_URL=postgresql://geno_runtime_app:geno_runtime_app@localhost:5432/geno make worker-fixture-persist
```

worker 输出会包含 `p0a_readiness_gate`。默认 fixture slice 的 `--sample-size 1` 只用于 smoke test，会因 P0a k=3 要求 fail；`--mode fixture --prompt-limit 1 --sample-size 3` 会通过本地 gate。真实 `--mode api` 必须同样通过 required platforms、必备元数据、`answer_present/surface_triggered`、citation、screenshot/HTML 和 k=3 检查后，才可把该批次视为 design partner 试点证据；`--preflight-output-path` 可把同一份 worker JSON 落盘，`make api-preflight` 默认写到 `docs/runtime_preflight/api-preflight-latest.json`，即使 collector health 失败也会先写入失败原因。worker 同时输出 `preflight_summary`，把 `collector_health`、P0a gate、collection failure 和最终 exit code 折叠成稳定摘要，供自动化审计判断 `ready_for_design_partner` 与 `recommended_next_action`；`preflight_audit_checklist` 会进一步列出阻塞原因、证据字段引用、运行总量和可复跑 CLI 参数；`preflight_payload_hash` 会对剔除自身后的 canonical JSON payload 计算 sha256，便于复算 stdout、落盘文件和审计留档是否一致；`scripts/verify_preflight_payload.py` / `make verify-api-preflight` 提供独立复算入口，区分“可审计”和“可进入 design partner”；`scripts/build_preflight_manifest.py` / `make preflight-manifest` 会把 preflight 文件 hash、verifier 输出和关键 gate 摘要固化为可索引 manifest；`scripts/verify_au_p0a_runbook.py` / `make verify-au-p0a-runbook` 会校验真实批次命令计划本身没有漂移。采集执行可通过 `--collection-max-retries`、`--collection-retry-backoff-seconds` 和 `--collection-rate-limit-delay-seconds` 启用 worker 内重试/backoff/节流；默认不重试不等待，启用后每个计划样本仍只产出一个最终成功或失败记录，并在 collector log / audit event 中保留 `attempt_count`、`retry_errors` 和 `collection_execution_policy`。Perplexity/OpenAI 官方 API adapter 会把 API response 冻结成 `geno-api-snapshot://...` HTML snapshot asset，并把 snapshot hash 写入 `EvidenceAsset.content_hash`；`--persist` 且配置 `OBJECT_STORE_ENDPOINT` 时，worker 会在落库前把该 snapshot 归档为 `s3://...` EvidenceAsset 并写 `api_snapshot_assets_archived` 审计事件，未配置对象存储时则保留 `geno-api-snapshot://...` 可复盘引用。这是原始 API 响应证据，不等同于消费者界面截图，后者仍靠 browser fidelity 抽检披露。`--include-browser-fidelity-fixture` 会在 stable fixture 批次里额外加入同 prompt/city 的 `chatgpt_search.browser.fixture` 浏览器抽检样本，用于 `ApiBrowserFidelityCheck` 与 Report Method Disclosure；这些 browser fidelity samples 会保留为原始证据和审计输入，但通过 `score_input_policy.excluded_fidelity_sample_answer_run_ids` 排除出主评分分母。`make browser-fidelity-plan` / `--plan-browser-fidelity-sampling` 会按 run date/cadence/seed 从 100 条 prompt 和 AU 城市中确定性抽样，输出 `BrowserFidelitySamplingPlan`、`browser_fidelity_sampling_planned` 审计事件和可复跑的 `recommended_worker_args`；`scripts/run_browser_fidelity_scheduler.py` 与 Compose `scheduler` profile 会把“生成计划 -> 可选执行推荐 worker 参数”包成 cron/K8s CronJob 友好的 JSON wrapper，本地脚本默认只输出计划，Compose 默认持久化计划审计，只有设置 `GENO_BROWSER_FIDELITY_EXECUTE=1` 或传 `--execute` 才执行真实采集。真实浏览器抽检执行入口为 `--include-browser-fidelity-playwright` / `make api-browser-fidelity-preflight`：它在 `--mode api` 中额外选择 `chatgpt_search.browser.playwright`，要求 `GENO_BROWSER_COLLECTOR_ENABLED=1`、Playwright、prompt/answer selector、可选 storage state 与 artifact dir 就绪；未就绪时 `--require-ready-collectors` 会在采集前 exit 3 并输出 `not_configured`、`selector_missing`、`session_state_missing` 或 `playwright_missing`；health 通过后若浏览器启动、登录态、selector 交互或官方 API 调用失败，`--require-no-collection-failures` 会在输出 JSON 后 exit 5。成功 browser capture 会把 screenshot 与 HTML snapshot hash 写入标准 RawEvidence 链；配置 `GENO_BROWSER_ARTIFACT_DIR` 且 `OBJECT_STORE_ENDPOINT` 可用时，worker 会把本地 `file://` browser HTML/PNG 在落库前归档为 `s3://...` EvidenceAsset，并写 `browser_capture_assets_archived` 审计事件；未落本地文件的 `geno-browser-*://` 引用不会被伪装成 durable object。真实账号/selector 联调、分布式失败重试队列和 Temporal 深度编排仍待接。Google spike worker 会同时输出 `google_spike_gate` 与 `google_spike_readiness_gate`：前者检查 Google AIO 成功率，后者检查 browser / third_party_api / manual 中是否至少有两条采集路径；`--persist-analysis` 还会把 `score_input_policy` 写入评分审计和报告 Method Disclosure，只有两个 gate 同时通过时 Google answer runs 才能进入主评分分母。默认 browser-only fixture 会通过成功率 gate 但失败两路径 readiness gate，因此只进入 limited coverage 证据附录。

缺少 `DATABASE_URL` 时，`--persist` 会直接失败并提示配置缺失，避免误以为证据已经落库。若设置了 `OBJECT_STORE_ENDPOINT`，但 `OBJECT_STORE_ACCESS_KEY` / `OBJECT_STORE_SECRET_KEY` 或 bucket 配置错误，API snapshot 或报告 artifact 归档会失败并让 worker 退出，避免 `s3://...` URL 与真实对象不一致。

如需把同一批成功采集记录继续解析、评分，并保存 `AnswerAnalysis`、`VisibilityScoreSnapshot`、`ScoreContribution`、Citation Graph、ReportExport、ApiBrowserFidelityCheck、ActionRecommendation、RetestSchedule、RetestComparison、Knowledge Facts、KnowledgeFactEmbedding、Content Drafts、Integration Connectors、Manual Distribution Records 与 Traceability Bundle：`--persist-analysis` 会读取当前项目已确认的 `entity_aliases`、项目级 `score_weight_configs` 和 `--score-formula-version` 指定的 `SCORE_FORMULA_REGISTRY` 公式，让 `ComparativeAnswerParser` 以 `rule_based_v2_aliases` 作为主解析器、`llm_judge_fixture_v1` 作为本地 judge 对照，用品牌/竞品 alias、域名、产品名或母公司名参与品牌提及、推荐、排名和竞品识别，并通过 `FixtureLLMGateway` 生成可审计的 judge 调用日志；也可用 `--judge-gateway litellm --judge-model <model>` 切到 `LiteLLMGateway`，通过 `LITELLM_BASE_URL` / `LITELLM_API_KEY` 调用 OpenAI-compatible LiteLLM `/chat/completions`。`LiteLLMGateway` 的 chat 路径具备 bounded exponential backoff、重试次数/错误留痕和上游响应 cost 优先读取；失败调用同样保留 request/response hash、latency、attempts、retry errors 和 error message，并以 `llm_gateway_failed` uncertainty flag 降级继续解析。`parser_ab_compare_v1` 的 agreement、mismatch fields、judge result 与 `llm_call_log` 会写入 `answer_analyses.payload.parser_comparison`，同一调用日志同步 upsert 到 `llm_call_logs`，记录 purpose、provider、model、prompt_version、request/response hash、tokens、estimated cost、latency 和 status；评分会把本次使用的 `formula_version`、组件权重和 `score_input_policy` 写入评分审计与报告方法说明，`VisibilityScoreSnapshot.answer_run_ids` 只包含主评分允许的 answer runs，同一 `answer_run_id` 的分析重跑会更新 `answer_analyses` payload 与 parser 版本。核心包提供 `rescore_snapshot_with_formula()`，可用历史 `AnswerAnalysis` 和指定公式版本重放旧口径或候选口径，生成 `visibility_score_snapshot_rescored` 审计事件；批量重算 UI 和审批流仍待后续产品化。内容引擎保存时会把 `LocalizedKnowledgeFact` 的规范文本写入 8 维 deterministic fixture embedding，upsert 到 `knowledge_fact_embeddings`，并追加 `knowledge_fact_embeddings_indexed` 审计事件；运行时可通过 `/v1/knowledge-facts/runtime/search` 用 pgvector `<=>` 检索 AU facts，并在 AU facts 不足时保留 global fallback 标记。生成报告时会把 Google spike gate、limited coverage、`score_input_policy`、API-vs-browser fidelity、score rate denominators、access/platform distribution 和截图/HTML 覆盖率写入 `report_exports.method_disclosure` 冻结快照，同时把同批 `AnswerRun` 生成独立的 `api_browser_fidelity_checks` 运行时对象，冻结 status、official_api/browser 记录数、可比较 prompt-city 对、mismatch count、difference rate、payload hash 和 `api_browser_fidelity_checked` 审计事件；若启用 `--include-browser-fidelity-fixture`，报告分母和证据附录仍只使用 `score_input_records`，但 Method Disclosure 的 fidelity payload 使用全量 official_api + browser 抽检样本，因此可得到 `sampled` 且不会污染评分分母；未跑真实浏览器后端时该对象会如实显示 `not_run`。runtime Markdown/PDF artifact 与 Runtime Console 复用这些快照；未跑真实 Google 双 gate 时，Google 明确标注为 limited coverage，不进入主评分分母；Trigger Rate 使用本报告窗口全部 attempted evidence records 作分母，Mention/Recommendation Rate 使用 surface_triggered 子集作分母。

```bash
DATABASE_URL=postgresql://geno_runtime_app:geno_runtime_app@localhost:5432/geno \
PYTHONPATH=packages/geno_core:apps/api \
python3 workers/collector_worker/run_collection_slice.py --mode fixture --prompt-limit 1 --persist --persist-analysis
```

Docker worker profile：

```bash
docker compose -f infra/docker-compose.yml --profile worker run --rm collector-worker
docker compose -f infra/docker-compose.yml --profile e2e run --rm runtime-e2e
docker compose -f infra/docker-compose.yml --profile scheduler run --rm browser-fidelity-scheduler
OPENAI_API_KEY=... LITELLM_MASTER_KEY=... docker compose -f infra/docker-compose.yml --profile llm-gateway run --rm collector-worker-litellm
```

Runtime Console 默认读取最近 20 个 AU runtime project 作为项目下拉选项；如果 URL 带 `?project_id=...` 且该项目不在第一页，控制台会额外按 `project_id + market_code=AU` 读取一次选中项目，再用同一个 `project_id` 构造 prompts/evidence/export/alias/saved views/scores/graphs/reports/actions/content/traceability 查询路径。Saved Views 链接也会保留 `project_id`，避免跨客户或跨项目复盘时误读最新项目。

运行时证据查询 API：

```bash
curl -X POST "http://localhost:8000/v1/projects/runtime/au/dtc-ecommerce"
curl -X POST "http://localhost:8000/v1/projects/runtime/au/dtc-ecommerce" -H "content-type: application/json" -d '{"tenant_name":"Agency Client AU","project_name":"Koala Mattress GEO Pilot","target_brand":"Koala","category":"mattresses","competitors":["Emma Sleep","Sleeping Duck","Ecosa"],"brand_official_domains":["koala.com"],"brand_product_lines":["Mattress","Sofa Bed"],"owner_user_id":"agency-owner"}'
curl "http://localhost:8000/v1/projects/runtime?market_code=AU&limit=20"
curl "http://localhost:8000/v1/projects/runtime?project_id={project_id}&market_code=AU&limit=1"
curl "http://localhost:8000/v1/entity-aliases/runtime?project_id={project_id}&entity_kind=brand&limit=20"
curl "http://localhost:8000/v1/entity-aliases/runtime/candidates?project_id={project_id}&entity_kind=brand&limit=20"
curl -X POST "http://localhost:8000/v1/entity-aliases/runtime/confirm" -H "content-type: application/json" -d '{"entity_id":"{brand_or_competitor_entity_id}","entity_kind":"brand","alias":"ExampleBrand Australia","alias_type":"alias","confidence":1.0,"confirmed_by":"runtime-console","notes":"Runtime entity alias confirmation"}'
curl -X POST "http://localhost:8000/v1/entity-aliases/runtime/confirm-batch" -H "content-type: application/json" -d '{"confirmed_by":"runtime-console","notes":"Batch entity alias confirmation for parser disambiguation review queue","aliases":[{"entity_id":"{brand_or_competitor_entity_id}","entity_kind":"brand","alias":"ExampleBrand Australia","alias_type":"alias","confidence":0.72},{"entity_id":"{brand_or_competitor_entity_id}","entity_kind":"brand","alias":"examplebrand.com.au","alias_type":"domain","confidence":0.9}]}'
curl "http://localhost:8000/v1/prompts/runtime?project_id={project_id}&market_code=AU&intent_type=brand_awareness&limit=20"
curl -X POST "http://localhost:8000/v1/prompts/runtime/import.csv" -H "content-type: application/json" -d '{"project_id":"{project_id}","csv_content":"text,intent_type,city,priority,intent_weight\nIs ExampleBrand visible in Sydney AI recommendations?,brand_awareness,Sydney,1,0.9\nBest DTC ecommerce products for Melbourne shoppers,category_recommendation,Melbourne,2,1.0\n","imported_by":"runtime-console","max_rows":100}'
curl -X POST "http://localhost:8000/v1/prompts/runtime/import.file?project_id={project_id}&filename=prompts.xlsx&imported_by=runtime-console&max_rows=100" -H "content-type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" --data-binary @prompts.xlsx
curl "http://localhost:8000/v1/evidence-runs/runtime?project_id={project_id}&platform=perplexity&city=Sydney&intent_type=brand_awareness&sort=cost_desc&limit=20"
curl "http://localhost:8000/v1/collection-runs/runtime?project_id={project_id}&run_type=p0a_slice&limit=20"
curl "http://localhost:8000/v1/fidelity-checks/runtime?project_id={project_id}&status=not_run&limit=20"
curl "http://localhost:8000/v1/fidelity-checks/runtime/trend?project_id={project_id}&limit=20"
curl -X POST "http://localhost:8000/v1/fidelity-checks/runtime" -H "content-type: application/json" -d '{"project_id":"{project_id}","report_export_id":"{report_export_id}","checked_by":"runtime-console"}'
curl -X POST "http://localhost:8000/v1/evidence-runs/runtime/manual-backfill" -H "content-type: application/json" -d '{"prompt_question_id":"{prompt_question_id}","platform":"google","surface":"google_ai_mode","answer_text":"Manual Google AI Mode answer for audit backfill.","citation_urls":["https://examplebrand.example/au/manual-backfill"],"screenshot_url":"s3://manual-backfill/examplebrand-google-ai-mode.png","html_snapshot_url":"s3://manual-backfill/examplebrand-google-ai-mode.html","submitted_by":"runtime-console","notes":"Google spike manual backfill"}'
curl -X POST "http://localhost:8000/v1/evidence-runs/runtime/manual-backfill/import.csv" -H "content-type: application/json" -d '{"project_id":"{project_id}","csv_content":"prompt_question_id,platform,surface,answer_text,citation_urls,screenshot_url,html_snapshot_url,sample_index,sample_size,device,notes\n{prompt_question_id},google,google_ai_mode,Manual Google AI Mode sample,https://examplebrand.example/au/manual|https://reviews.example/manual,s3://manual-backfill/google-ai-mode-1.png,s3://manual-backfill/google-ai-mode-1.html,1,2,desktop,Google spike manual batch\n","submitted_by":"runtime-console","max_rows":120,"notes":"Google spike manual CSV import"}'
curl -D - "http://localhost:8000/v1/evidence-runs/runtime/export.csv?project_id={project_id}&platform=perplexity&city=Sydney&intent_type=brand_awareness&sort=cost_desc&limit=200"
curl "http://localhost:8000/v1/runtime-saved-views?project_id={project_id}&view_type=runtime_evidence&limit=20"
curl -X POST "http://localhost:8000/v1/project-brand-kits/runtime" -H "content-type: application/json" -d '{"project_id":"{project_id}","client_name":"Koala AU","prepared_by":"Partner Agency","logo_url":"https://koala.example/logo.png","primary_color":"#0f766e","secondary_color":"#111827","footer_text":"Prepared for Koala AU board review","updated_by":"runtime-console"}'
curl -X POST "http://localhost:8000/v1/project-brand-kits/runtime/logo?project_id={project_id}&filename=logo.png&uploaded_by=runtime-console" -H "content-type: image/png" --data-binary @logo.png
curl "http://localhost:8000/v1/project-brand-kits/runtime?project_id={project_id}"
curl "http://localhost:8000/v1/human-reviews/runtime?project_id={project_id}&target_type=content_draft&limit=20"
curl "http://localhost:8000/v1/human-reviews/runtime/queue?project_id={project_id}&queue_status=pending_review&limit=20"
curl -X POST "http://localhost:8000/v1/human-reviews/runtime" -H "content-type: application/json" -d '{"project_id":"{project_id}","target_type":"visibility_score_snapshot","target_id":"{score_snapshot_id}","review_status":"approved","decision":"approved_for_report","reviewer_id":"runtime-console","notes":"Reviewed score evidence and traceability bundle","payload":{"source":"runtime-console"}}'
curl "http://localhost:8000/v1/visibility-scores/runtime?project_id={project_id}&limit=20"
curl "http://localhost:8000/v1/citation-graphs/runtime?project_id={project_id}&limit=20"
curl "http://localhost:8000/v1/reports/runtime?project_id={project_id}&limit=20"
curl -D - "http://localhost:8000/v1/reports/runtime/{report_export_id}/artifact?type=markdown&platform=perplexity&city=Sydney&intent_type=brand_awareness&sort=cost_desc"
curl -D - "http://localhost:8000/v1/reports/runtime/{report_export_id}/artifact?type=csv&platform=perplexity&city=Sydney&intent_type=brand_awareness&sort=cost_desc"
curl -D - "http://localhost:8000/v1/reports/runtime/{report_export_id}/artifact?type=pdf&platform=perplexity&city=Sydney&intent_type=brand_awareness&sort=cost_desc"
curl -D - "http://localhost:8000/v1/reports/runtime/{report_export_id}/artifact?type=pdf&template=white_label&client_name=ExampleBrand%20AU&prepared_by=Partner%20Agency&platform=perplexity&sort=cost_desc"
curl -D - "http://localhost:8000/v1/reports/runtime/{report_export_id}/artifact?type=pdf&template=white_label&platform=perplexity&sort=cost_desc"
curl "http://localhost:8000/v1/score-formulas/runtime"
curl "http://localhost:8000/v1/action-plans/runtime?project_id={project_id}&limit=20"
curl "http://localhost:8000/v1/runtime-alerts?project_id={project_id}&limit=10"
curl "http://localhost:8000/v1/content-engines/runtime?project_id={project_id}&limit=20"
curl "http://localhost:8000/v1/knowledge-facts/runtime/search?project_id={project_id}&query=Australia%20shipping%20returns&market_code=AU&city=Sydney&limit=5"
curl "http://localhost:8000/v1/traceability/runtime?project_id={project_id}"
```

这些接口从 PostgreSQL 读取 `Project -> Tenant/Brand/Competitor/PromptQuestion/AuditEvent` 项目启动页、`ProjectBrandKit -> AuditEvent` 白标配置页、`HumanReviewRecord -> AuditEvent` 人工复核留痕页、`EntityAlias -> BrandEntity/CompetitorEntity -> AuditEvent` 别名确认页、`PromptQuestion` 分页、`AnswerRun -> PromptQuestion -> RawAnswer -> Citation/Asset/Log/Cost/Audit` 聚合页、`CollectionRunSummary -> AuditEvent` 批次级采集质量/成本/耗时摘要页、`ApiBrowserFidelityCheck -> AuditEvent` 保真度抽检页、`ApiBrowserFidelityCheck -> RuntimeFidelityTrend` 趋势读模型、`VisibilityScoreSnapshot -> ScoreContribution -> ScoreSnapshotRun -> AnswerRun/PromptQuestion -> AnswerAnalysis/AuditEvent` 评分解释页、`SourceGraph -> SourceGraphEvidence -> SourceGap -> CompetitorBenchmark` 图谱/竞品页、`ReportExport -> ReportEvidence -> ScoreSnapshot -> CitationGraph` 报告快照与历史列表、`RetestSchedule -> ActionRecommendation -> RetestComparison -> AnswerRun/PromptQuestion -> AuditEvent` 行动与复测页、`ContentDraft -> LocalizedKnowledgeFact -> ActionRecommendation -> AnswerRun/PromptQuestion -> ManualDistributionRecord/IntegrationConnector/AuditEvent` 内容引擎页、`LocalizedKnowledgeFact -> KnowledgeFactEmbedding -> AuditEvent` pgvector 知识检索页，以及 `TraceabilityBundle -> ReportExport -> VisibilityScoreSnapshot -> RuntimeEvidenceRun -> CitationGraph -> ActionRecommendation -> ContentDraft -> AuditEvent/EvidenceLink` 溯源详情页；未配置 `DATABASE_URL` 时返回 503。Citation Graph 的 `GraphStore` 可插拔验证已覆盖本地 PG adjacency projection 与 Neo4j node/relation projection，二者写入同一 `CitationGraphResult` 后通过统一 summary 查询得到一致 source nodes/evidence links/source gaps/competitor benchmark 口径；真实 Neo4j 服务联调仍待后续。知识检索的 `VectorStore` 可插拔验证已覆盖本地 pgvector projection 与 Qdrant projection，二者写入同一批 deterministic knowledge fact embeddings 后通过统一 search summary 得到一致 top-k id/score 排序；真实 Qdrant/Milvus 服务联调仍待后续。保真度抽检 API 可按 `project_id/report_export_id/status` 查询已冻结的 check，也可用 POST 对某个 report 或项目最新 report 重新生成 check；`GET /v1/fidelity-checks/runtime/trend` 会基于最近已落库 checks 返回 sampled/total、latest/earliest/average/max difference rate、趋势窗口和 `improving/worsening/flat/insufficient_sampled_data/no_data` 方向；当前差异率来自同批 prompt-city 的 `official_api` 与 `browser` answer run 对比，真实浏览器 collector 待接时会保持 `not_run` 或 `no_overlap`，不伪造 sampled。知识检索 API 会校验 `project_id/query`，用 `fixture-knowledge-embedding-v1` 生成查询向量，通过 `knowledge_fact_embeddings.embedding <=> query_vector` 排序，优先返回 `market_code=AU` 且匹配城市或全局城市的 active facts，再返回 global fallback，并读回 `knowledge_fact_embeddings_indexed` 审计事件；真实 embedding provider、Qdrant/Milvus 切换和内容生成时的在线 RAG 策略仍待后续产品化。实体别名候选是计算型 read model，不新增候选表，会从 canonical name、官方域名、产品线和母公司字段生成待确认项并排除已确认 alias；同时会读取同项目最近已入库的 `raw_answers.answer_text` 与 `answer_citations.domain/url`，用高置信规则生成 `evidence_answer_text` / `evidence_citation_domain` 候选，并把 `evidence_count`、`evidence_answer_run_ids`、`evidence_urls` 和 `supporting_sources` 带回候选 payload；若证据命中的是已有官方域名候选，会合并为 supporting evidence，而不是生成重复项。实体别名确认只接收已有 `BrandEntity` 或 `CompetitorEntity`，后端会使用稳定 alias id 幂等写入 `entity_aliases`，追加 `entity_alias_confirmed` 审计事件，并在后续 `--persist-analysis` 中参与 alias-aware parser 识别；批量确认 endpoint 默认先预校验实体和项目访问权限，响应返回 `entity_alias_confirm_batch_v1` 与批量摘要，但主审计事实仍是逐条 `entity_alias_confirmed`。Prompt CSV Import 和 Prompt File Import 会以稳定 prompt id upsert 到 `prompt_questions`，不删除已有 prompt，保存时追加 `runtime_prompts_imported` 审计事件；JSON CSV endpoint 接收 CSV 文本，raw-body 文件 endpoint 支持 `.csv/.txt` UTF-8 和 `.xlsx` 第一工作表，并在审计 `input_refs` 记录 `csv_sha256/source_format/source_filename/source_content_type`；`GET /v1/prompts/runtime/imports` 会从同一批 `AuditEvent` 读回项目级导入历史，不新增表，返回 source、hash、prompt count、method version 和审计行。项目级 Brand Kit 会写入 `project_brand_kits`，保存时追加 `project_brand_kit_saved` 审计事件；Logo Upload 会把图片上传到配置的 MinIO/S3-compatible bucket 后更新 `project_brand_kits.logo_url` 并追加 `project_brand_logo_uploaded` 审计事件，未配置对象存储或上传失败时返回 503 且不写假的 `s3://` 引用；白标 PDF 若未传 `client_name/prepared_by`，会读取当前 report 所属项目的 Brand Kit 作为默认客户名、服务商名、Logo URL、主题色和页脚。人工复核会追加写入 `human_review_records`，不覆盖原始 score/content/analysis 对象，审计事件为 `human_review_recorded`，用于记录 reviewer、review status、decision、notes 和被复核对象引用；复核队列会从 `visibility_score_snapshots` 与 `content_drafts` 聚合待审对象，返回 priority、reason、latest review 和 evidence refs；content draft 复核会把同项目草稿 `review_status` 投影为本次复核状态，并写入 `content_draft_review_status_updated`，但不改写评分、草稿正文或证据绑定；分配、通知、权限、复杂审批流和抽样校准仍待后续产品化。人工补录只接收已有 `PromptQuestion` 的答案证据，后端会用 prompt 元数据生成 `AnswerRun.access_method=manual`、`RawAnswer.raw_payload_hash`、citation/asset/cost/log 和 `manual_backfill_recorded` 审计事件。报告历史读取使用 `project_id` 约束当前项目；报告 artifact 的筛选下载只作用于即时渲染的 Markdown/CSV/PDF 证据附录，白标 PDF 使用 `template=white_label` 生成客户版标题页/执行摘要/页脚说明，并返回独立 `template_hash`；冻结 `ReportExport.answer_run_ids`、`score_snapshot_ids`、methodology hash、fidelity check payload hash 与对象存储 URL 不会被改写。

实体别名候选审核状态已新增最小持久化层：`entity_alias_candidate_reviews` 以 `project_id + candidate_id` 幂等保存 `needs_review/rejected/approved`、reviewer、notes、证据 answer run 和 URL 引用；`POST /v1/entity-aliases/runtime/candidates/review` 写入该状态并生成 `entity_alias_candidate_review_recorded` 审计事件，`POST /v1/entity-aliases/runtime/candidates/review-batch` 支持批量 needs review / reject / approved，并生成 `entity_alias_candidate_batch_reviewed` 聚合审计摘要；候选列表会读取 `latest_review` 并默认过滤 rejected 候选。`GET /v1/entity-aliases/runtime/candidates/reviews` 读取审核历史、decision/entity_kind 过滤结果、assigned_to/assignment_status/priority/due_before 分配队列过滤结果和最近审计事件，Runtime Console 可用该历史把 rejected 候选恢复为 needs review 或改判 approved，也可用 `Alias Candidate Assignment Queue` 查看已分配审核待办。`POST /v1/entity-aliases/runtime/candidates/assign` 在既有审核记录上保存 `assigned_to/assigned_by/assignment_status/priority/due_at/assignment_note`，生成 `entity_alias_candidate_assigned` 审计事件，Runtime Console 可从审核历史直接分配 reviewer、优先级和截止时间，并可从 assignment queue 把状态推进为 `in_progress/blocked/completed`；`POST /v1/entity-aliases/runtime/candidates/assignment-escalations` 会把 overdue active review 升级为 `escalated` 并生成 `entity_alias_candidate_assignment_escalated` 审计事件；`GET /v1/entity-aliases/runtime/candidates/assignment-workbench` 以只读 read model 返回 reviewer 待办摘要、有限 records 和最近审计事件；`GET /v1/entity-aliases/runtime/candidates/assignment-workload` 以只读 read model 返回跨 reviewer workload 摘要，便于复盘当前负载和后续调度依据。该表只保存人工审核决策、轻量分配 metadata 和最小 SLA 升级状态，不替代 confirmed alias 主事实，也不提供多级审批流、值班排班或复杂 reviewer inbox。

实体别名候选审核状态已新增最小 overdue 通知入口：`POST /v1/entity-aliases/runtime/candidates/assignment-notifications` 会筛出 overdue 且仍 active 的 assignment review，按稳定 notification id 写入 `runtime_notifications`，并复用现有 subscription/delivery 队列和审计事件。计划任务入口 `make entity-alias-assignment-notification-worker` 只负责扫描项目和调用同一入队逻辑；它不修改 `entity_alias_candidate_reviews` 状态，也不替代人工推进 `in_progress/blocked/completed` 的 `entity_alias_candidate_assigned` 审计。最小 SLA 升级入口 `make entity-alias-assignment-escalation-worker` 与 Console `Escalate overdue assignments` 会单独把 overdue active review 标成 `escalated`，让通知和升级可以分开审计。

Runtime Alerts 的风险判断仍是只读派生层：后端用最新评分快照和贡献项判断品牌缺失、推荐率过低，用 answer analysis 的 `sentiment_score` 判断负面情绪风险，用 source gaps 输出信源缺口风险，用 competitor benchmarks 对比竞品提及率并识别竞品压制；每条 alert 都返回 `rule_version=runtime_alerts_v1`、`evidence_refs`、`related_actions` 和评分审计事件，便于从预警一路回查到分数、answer analysis、信源缺口、竞品 benchmark、answer run 和 action/retest 链路。告警处理状态不覆盖这些派生结论，而是通过 `runtime_alert_events` 追加 acknowledged/resolved/snoozed/reopened/escalated 管理事件，并用 `runtime_alert_event_recorded` 保存 actor、note、metadata、before/after hash 和输出事件 id。通知也不改写 alert；`POST /v1/runtime-alerts/notifications` 基于当前 read model 显式生成 `runtime_alert` notification，并按 active webhook subscription 的 event types 与 severity threshold 入队 delivery，默认跳过最近管理状态为 resolved/snoozed 的 alert，除非请求传入 `include_resolved=true`。计划任务入口 `make runtime-alert-notification-worker` 只负责扫描项目和调用同一显式入队逻辑；`make runtime-alert-escalation-worker` 负责按 `critical=4h/high=24h` 默认 SLA 阈值追加 `escalated` 管理事件，同样不新增可变 alert 状态表，也不改写派生 alert。

运行时项目访问控制默认关闭，便于本地 fixture 和 demo 无鉴权运行；设置 `GENO_RUNTIME_PROJECT_ACCESS_CONTROL=1` 后，主要 runtime API 需要可信 actor。默认 `GENO_RUNTIME_AUTH_MODE=header` 时仍读取请求头 `X-GENO-Actor-Id`；设置 `GENO_RUNTIME_AUTH_MODE=jwt` 后会要求 `Authorization: Bearer <JWT>`，用 `GENO_RUNTIME_JWT_SECRET` 校验 HS256；设置 `GENO_RUNTIME_AUTH_MODE=jwks` 后会要求 Bearer JWT，并优先用 `GENO_RUNTIME_JWKS_JSON` inline JWKS 校验 RS256，也可用 `GENO_RUNTIME_JWKS_URL` 拉取远端 JWKS，按 `GENO_RUNTIME_JWKS_CACHE_TTL_SECONDS` 缓存，拉取超时由 `GENO_RUNTIME_JWKS_FETCH_TIMEOUT_SECONDS` 控制；若未配置显式 JWKS URL，可通过 `GENO_RUNTIME_OIDC_DISCOVERY_URL` 或 URL-form `GENO_RUNTIME_JWT_ISSUER` 拉取 OIDC discovery document 并发现 `jwks_uri`，discovery document 按 `GENO_RUNTIME_OIDC_DISCOVERY_CACHE_TTL_SECONDS` 缓存。生产兜底默认关闭：只有显式设置 `GENO_RUNTIME_JWKS_STALE_IF_ERROR_SECONDS` 或 `GENO_RUNTIME_OIDC_DISCOVERY_STALE_IF_ERROR_SECONDS` 为正数时，远端 refresh 失败才会在对应 stale 窗口内继续使用同 URL 的过期缓存。JWT/JWKS 模式共同支持 `GENO_RUNTIME_JWT_ACTOR_CLAIM`、`GENO_RUNTIME_JWT_ISSUER`、`GENO_RUNTIME_JWT_AUDIENCE` 和 `GENO_RUNTIME_JWT_CLOCK_SKEW_SECONDS`。`GET /v1/projects/runtime` 会把 actor 作为 `project_members.user_id` 过滤条件；`POST /v1/projects/runtime/au/dtc-ecommerce` 会把当前 actor 写为项目 owner/member；prompt、evidence、collection run、saved view、brand kit、score weight、human review、score、graph、report、fidelity、action、alert、content、knowledge search 和 traceability 等项目级接口会要求 `project_id` 并执行成员校验；`report_export_id` 或 entity id 入口会先反查所属项目后再校验。最小角色矩阵已接入 API 层：任意成员可读；owner/admin 可维护成员、创建成员邀请、导入 prompt、保存 saved view、修改 Brand Kit/Logo 和 Score Weights；owner/admin/analyst 可确认 entity alias、人工补录、记录 human review、创建 fidelity check；viewer 不允许执行写操作。拒绝语义为：缺 actor 或缺 Bearer token 返回 401，缺 JWT secret/JWKS JSON/JWKS URL/OIDC discovery source/不支持的 auth mode 返回 503，远端 JWKS/OIDC discovery 拉取失败或签名 key 配置坏返回 503，签名错误或 issuer/audience/时间窗不合法返回 401，开启访问控制后项目级接口缺 `project_id` 返回 400，actor 不是成员或角色不足返回 403，删除/降级最后一个 owner 返回 400，未配置持久化仍返回 503。`/v1/runtime-diagnostics` 只校验 JWKS JSON/URL/OIDC discovery URL/TTL/stale-if-error/timeout 配置形态，不主动访问远端 JWKS 或 OIDC discovery document，避免 readiness 产生外部网络副作用。数据库级最小隔离已接入 PostgreSQL RLS：迁移会创建 `geno_runtime_app` app role，Compose 的 API/worker/scheduler/e2e runtime 连接默认使用该 role；API 在校验 actor 成员关系后通过 `set_runtime_project_access_context()` 写入 `geno.runtime_project_access_control`、`geno.runtime_actor_id` 和 `geno.runtime_project_id` session GUC，让同一请求内的写后读路径在 commit 后仍受同一 RLS 上下文约束；接受邀请时会先写入 `geno.runtime_invitation_token_hash`，只允许匹配的 pending invitation 被读取和兑换，再把 actor/project GUC 切到邀请邮箱与目标项目后写入成员。API 连接池默认仅在常驻 API 服务启用，请求结束归还连接时会 rollback、清空上述 session GUC 和 invitation token hash GUC 并提交清理；worker、scheduler 和 runtime E2E 默认保持短生命周期直连。Keycloak/OIDC login/session、前端 SSO 登录流、真实用户目录、跨租户管理员授权、完整权限后台和生产压测仍是下一阶段增强。

项目成员管理使用既有 `project_members` 表，不新增用户目录：`GET /v1/project-members/runtime?project_id=...` 读回成员和 `project_member_saved/project_member_deleted` 审计历史，`POST /v1/project-members/runtime` 按 `project_id + user_id` 幂等新增或更新 `owner/admin/analyst/viewer` 角色，`DELETE /v1/project-members/runtime` 按 `project_id + user_id` 删除成员；两类写操作都会记录 before/after hash、actor、reason 和 member id。成员邀请使用 `project_member_invitations` 表：`GET /v1/project-member-invitations/runtime?project_id=...&status=pending` 读取邀请和 `project_member_invitation_created/project_member_invitation_revoked/project_member_invitation_expired` 审计历史，`POST /v1/project-member-invitations/runtime` 创建 pending 邀请，返回一次性 `invite_token`，但数据库和审计只保留 `invite_token_hash`、email、role、expires_at、invited_by、metadata 和 reason；`POST /v1/project-member-invitations/runtime/action` 支持 owner/admin 对 pending 邀请执行 `revoke` 或 `expire`，使用 `FOR UPDATE` 锁定记录，非 pending 状态返回冲突，撤销后再次邀请同一邮箱/角色会生成新的 pending invitation id；`POST /v1/project-member-invitations/runtime/email` 支持 owner/admin 在 raw token 仍可用时发送 SMTP 邀请邮件，邮件正文由 `project_member_invitation_email_template_v1` 渲染，包含一次性 accept 链接，但数据库和审计只保存 token hash、accept URL hash、template version/hash、subject hash、body hash、SMTP host/port/from、response status/body hash 和 `project_member_invitation_email_sent`；`/invite/accept` 页面可从邮件链接读取 `invitation_id/invite_token` 并调用 accept endpoint；`POST /v1/project-member-invitations/runtime/accept` 支持用一次性 token 接受 pending invitation，把邀请邮箱写入 `project_members` 并将 invitation 标记为 accepted，过期、撤销或已接受的邀请返回冲突。开启访问控制时，成员列表允许项目内任意成员读取；成员 upsert/delete 和 invitation read/create/action/email 只允许 owner/admin，accept endpoint 使用 token-hash RLS 上下文而不是既有成员身份。repository 会阻止删除最后一个 owner，也阻止把最后一个 owner 降级成非 owner。真实用户资料、完整 IAM、邀请邮件退订/服务商 API 和 SSO 登录会话仍待后续补齐。

P0b Google spike 的机器可读执行路径已固定：`make au-p0b-google-runbook` / `make verify-au-p0b-google-runbook` 生成并校验 11 步 runbook，顺序为环境准备、Playwright env readiness、env strict verify、Playwright smoke、smoke strict verify、manual backfill strict verify、health-only、manifest、240-run、manifest、结论交接。health/full spike 真实执行已收口为 `make au-p0b-google-spike-health`、`make au-p0b-google-spike-health-manifest`、`make au-p0b-google-spike`、`make au-p0b-google-spike-manifest`，底层仍固定 `google-spike --require-ready-collectors --health-check-only` 与 `google-spike --require-ready-collectors --require-no-collection-failures --require-google-spike-gates --persist`，并支持 `GENO_AU_P0B_GOOGLE_SPIKE_*` 路径覆盖。`.env.au-p0b-google.example` 提供默认关闭的脱敏环境模板，先用 `make verify-au-p0b-google-env-template` 校验模板完整和无 selector/secret 后，再用 `make au-p0b-google-env-bootstrap` / `make verify-au-p0b-google-env-bootstrap` 创建或校验本地 `.env.au-p0b-google`、设置 `0600`、证明 gitignored/not tracked 并落盘 bootstrap hash，然后填真实 selector、storage state、manual path、database 和对象存储。`make au-p0b-google-manual-backfill-request` 会先把 120-row manual backfill 交接要求独立落盘为可复算 request packet，`make au-p0b-google-phase-execution-request` 会再把六阶段执行顺序独立落盘为可复算 request packet，执行者可直接看到当前应停在 environment、哪些阶段可开始、哪些证据缺失以及 full spike planned runs=240；`make verify-au-p0b-google-manual-backfill` 会读取 `MANUAL_BACKFILL_PATH`，strict 校验 120-row Google AI Mode manual JSONL，并写出 `docs/runtime_preflight/au-p0b-google-manual-backfill-verification-latest.json`，其中包含原始文件 `file_sha256` 和 `verification_hash`。`make au-p0b-google-status` 会读取 env、smoke、manual verification、health、spike 和 manifest artifacts；manual verification 缺失或未通过时会先停在 `run_verify_google_manual_backfill` / `prepare_google_manual_backfill_file` / `fix_google_manual_backfill_coverage`，不会放行 health-only 或 240-run。`make au-p0b-google-package` 会在 status 之后生成 `docs/runtime_preflight/au-p0b-google-evidence-package-latest.json`，汇总 status report、runbook、execution、env、smoke、manual、health、spike 与 manifest 的文件 sha256、verifier hash、blockers 和 `google_main_scoring_allowed`；`make verify-au-p0b-google-package` 可离线复算 package hash 与 summary/artifacts 一致性。`make au-p0b-google-execution-checklist` 会进一步合成 `docs/runtime_preflight/au-p0b-google-execution-checklist-latest.json`，把 env template/bootstrap gate、runbook、Playwright env、status、package、selector group、manual path、DB、smoke/health/full spike 命令和 hard gates 汇成一份脱敏执行清单；`make verify-au-p0b-google-execution-checklist` 只证明清单 hash、计数、脱敏和推导自洽，真正允许 Google 进入主评分仍必须通过 status/package 的 `--require-google-main-scoring-allowed` 硬门禁。

补充：P0b Google execution checklist 现在包含 `environment_handoff` 脱敏交接 payload，会把 `GOOGLE_PLAYWRIGHT_ENABLED`、`DATABASE_URL`、`MANUAL_BACKFILL_PATH`、`google_aio_prompt_selector`、`google_aio_answer_selector`、文件 gate、Playwright dependency 和 env-file hygiene 汇总成 ready/missing/redacted 状态；只记录 `present/source/truthy/value_length/sha256_prefix/exists/is_file/is_dir/secret_redacted` 等摘要，不保存 selector、session path、数据库 URL 或 secret 原文。该 handoff 也可通过 `make au-p0b-google-environment-request` / `make verify-au-p0b-google-environment-request` 独立落盘为可复算请求包，并由 `GET /v1/p0b-google-environment-request/au` 与 Runtime Console P0b Google environment request packet 卡片读取。清单还包含 `manual_backfill_handoff`，固定 120-row JSONL 模板、template manifest、verification artifact、预期/实际行数、60 个 prompt-city 覆盖数、缺失原因、file/verification hash 和内容脱敏策略；该 handoff 也可通过 `make au-p0b-google-manual-backfill-request` / `make verify-au-p0b-google-manual-backfill-request` 独立落盘为可复算请求包，并由 `GET /v1/p0b-google-manual-backfill-request/au` 与 Runtime Console P0b Google manual backfill request packet 卡片读取。该 payload 不保存人工答案、citation URL、screenshot URL 或 HTML snapshot URL 原文。新增的 `google_spike_phase_handoff` 会把 `environment -> browser_smoke -> manual_backfill -> health_check -> full_spike -> main_scoring` 六个阶段的 command ids、artifact keys、ready/can-start、blocking reasons、next phase、blocked phase count 和 full spike planned runs 固定进同一 hash，防止在 env/smoke/manual 未 ready 时跳到 health-only、240-run 或主评分；该 handoff 已进一步通过 `make au-p0b-google-phase-execution-request` / `make verify-au-p0b-google-phase-execution-request`、`GET /v1/p0b-google-phase-execution-request/au` 与 Runtime Console P0b Google phase execution request packet 卡片拆成独立执行请求包，并提供 `--require-google-phases-ready` 与 `--require-google-main-scoring-ready` 严格硬门禁。AU handoff dossier、Markdown 和 Runtime Console Handoff/P0b execution 卡片会同步展示 `P0b environment handoff ready/missing/redacted`、`P0b manual backfill handoff rows/missing/redacted` 与 `P0b Google phase handoff next/blocked/full spike runs`，并由 `make verify-au-handoff-dossier` 复算一致性。

补充：P0b Google phase execution request 现在还必须配套 `make au-p0b-google-phase-execution-fulfillment` / `make verify-au-p0b-google-phase-execution-fulfillment`。fulfillment artifact 会把 request packet 与当前 execution checklist 逐阶段对账，固定 request/checklist ready、can-start、planned runs、证据输出、source checklist hash alignment、`phase_execution_fulfilled`、`google_main_scoring_allowed` 和 `--require-fulfilled` hard gate，并通过 `GET /v1/p0b-google-phase-execution-fulfillment/au` 与 Runtime Console fulfillment 卡片展示。当前本地 `phase_execution_fulfilled=false`、`next_phase=environment`，所以 request packet ready 不能被解释为 Google env/smoke/manual/health/full spike/main scoring 已完成。

补充：P0b Google phase execution fulfillment 之后还必须配套 `make au-p0b-google-phase-execution-clearance` / `make verify-au-p0b-google-phase-execution-clearance`。clearance artifact 会把 request、checklist、fulfillment 与 external dependency clearance dry-run 对齐，固定 `p0b_google_phase_execution` 目标 step、`p0b_google_manual_backfill` 前置阻塞、六阶段 missing、operator steps、source hash、`--require-google-phases-ready`、`--require-google-main-scoring-ready`、`--require-fulfilled` 和 `--require-cleared` hard gates，并通过 `GET /v1/p0b-google-phase-execution-clearance/au` 与 Runtime Console clearance 卡片展示。当前本地 `phase_execution_clearance_ready=false`、`blocked_by_prerequisite_step=true`、`next_command=make au-p0b-google-manual-backfill-clearance`，所以 fulfillment 自洽不能被解释为第五 blocker 已清零。

补充：P0b Google manual backfill 现在也有独立清障包：`make au-p0b-google-manual-backfill-clearance` / `make verify-au-p0b-google-manual-backfill-clearance` 会把 manual request、strict verification、manual fulfillment 与 external dependency clearance dry-run 合成 `docs/runtime_preflight/au-p0b-google-manual-backfill-clearance-latest.json`，并通过 `GET /v1/p0b-google-manual-backfill-clearance/au` 与 Runtime Console `P0b Google manual backfill clearance` 卡片展示。该 artifact 固定 `p0b_google_manual_backfill` 目标 step、`p0b_google_environment` 前置阻塞、120-row record / 60 prompt-city coverage、answer/citation/evidence asset 缺口、operator steps、post-update validation sequence、source artifact hash、`--require-manual-backfill-ready`、`--require-fulfilled` 和 `--require-cleared` hard gates；当前 hash 为 `4a323f88315e02a08d079d864332f13daeeeff30b9184aaec90d0449c909fdcf`，`manual_backfill_clearance_ready=false`，因为前置 Google environment 未清且模板内容仍缺 answer/citation/evidence asset。

P0b Google environment request 的跨阶段 DB 复用提示也遵循同一脱敏边界：它只在 P0a env report 中已有脱敏 `DATABASE_URL` 证据、且 P0b 自身仍缺 `full_run_env:DATABASE_URL` 时出现，供执行者减少重复配置排查；真实晋级仍必须重新生成并验证 P0b Google Playwright env report、execution checklist、status/package 与对应 strict gates。

AU 更广平台扩展已先落成显式注册表，而不是直接扩大采集分母：`build_au_market_profile()` 已登记 Gemini、Bing Copilot、Claude、YouTube、Reddit、ProductReview 六个候选 surface，但全部 `enabled=false` 且 `weight=0.0`，所以 P0a 仍只计划 ChatGPT Search + Perplexity Sonar，P0b 仍只隔离 Google。`make au-broader-platform-registry` 会生成 `docs/runtime_preflight/au-broader-platform-registry-latest.json`，`make verify-au-broader-platform-registry` 会复算 hash 并校验候选平台顺序、stage、disabled/zero-weight policy 和 P0a/P0b 范围不漂移；Runtime API `GET /v1/au-broader-platform-registry` 与 Runtime Console 的 Broader Platform Registry 面板可读取同一清单。该清单只证明 P1/P2 平台扩展顺序和证据要求可审计，不代表 Gemini/Copilot/Claude/YouTube/Reddit/ProductReview 采集器已经实现。

AU 复测调度也已先落成可审计执行契约：`make au-retest-scheduler-plan` 会生成 `docs/runtime_preflight/au-retest-scheduler-plan-latest.json`，固定 T0/T+7/T+14/T+30 四个窗口，每个窗口沿用 `au_dtc_ecommerce_v1`、100 prompts、Australia/Sydney/Melbourne/Brisbane、ChatGPT Search + Perplexity Sonar、k=3，即每窗口 2400 planned runs、总计 9600 planned runs；`make verify-au-retest-scheduler-plan` 会复算 `retest_scheduler_plan_hash` 并校验窗口、命令、证据输出、design-partner gate 和边界。`make au-retest-execution-status` 会读取同一 plan，并逐个检查 `docs/runtime_preflight/au-retest-*.json` 与 `docs/runtime_preflight/au-retest-*-manifest.json` 是否存在、hash 是否有效、payload/manifest 是否都通过 design-partner gate，生成 `docs/runtime_preflight/au-retest-execution-status-latest.json`；`make verify-au-retest-execution-status` 复算 `retest_execution_status_hash` 和窗口推导。Runtime API `GET /v1/au-retest-scheduler-plan`、`GET /v1/au-retest-execution-status` 与 Runtime Console 的 AU Retest Scheduler Plan 区块读取同一清单和执行状态。当前本地无真实 T 窗口产物时 execution status 会是 `status=fail`、`missing_artifact_count=8`、`next_action=run_retest_window:baseline`，这是正确阻塞状态，不代表 Temporal 已接入，也不代表真实 T 窗口外部采集已经完成。

## 核心文档

| 文档 | 内容 |
| --- | --- |
| [完整调研报告](docs/智推时代GENO项目完整调研报告.md) | 总报告，整合公司、行业、GENO 方法论、技术栈、案例、竞品、澳洲首发差异与落地路径 |

### 可审计调研复盘（7 份）

| 主题 | 文档 |
| --- | --- |
| 公司 | [智推时代公司](docs/智推时代公司-可审计调研复盘.md) |
| 用户搜索习惯变迁 | [全球互联网用户搜索习惯变迁](docs/全球互联网用户搜索习惯变迁-可审计调研复盘.md) |
| SEO → GEO 转变 | [SEO到GEO时代转变](docs/SEO到GEO时代转变-可审计调研复盘.md) |
| 搜索/AI 占比、内容与信源偏好 | [搜索引擎AI搜索占比内容偏好信源偏好](docs/搜索引擎AI搜索占比内容偏好信源偏好-可审计调研复盘.md) |
| GENO 四阶闭环技术栈与开源替代 | [智推时代GENO四阶闭环技术栈与开源替代](docs/智推时代GENO四阶闭环技术栈与开源替代-可审计调研复盘.md) |
| 合作案例核查 | [智推时代合作案例](docs/智推时代合作案例-可审计调研复盘.md) |
| 竞品格局 | [智推时代相似服务与竞争企业](docs/智推时代相似服务与竞争企业-可审计调研复盘.md) |

### GENO SaaS 产品落地（3 份）

| 文档 | 内容 |
| --- | --- |
| [MVP 技术设计文档](docs/GENO-SaaS-MVP-技术设计文档.md) | 通用 GENO SaaS 技术参考；已标注 AU 首发覆盖规则 |
| [MVP 一期需求拆解表](docs/GENO-SaaS-MVP-一期需求拆解表.md) | 通用 Epic/需求参考；AU 首发优先级以 P0a/P0b/P0c 覆盖 |
| [澳大利亚首发技术落地路径](docs/GENO-SaaS-AU-首发技术落地路径.md) | AU 首发规格真源：Evidence-first MVP、15 步实施方案与数据模型增量 |

## 证据目录 `research_sources/`

每个主题一个子目录，内含：编号化的逐来源摘要（`*.md`）、原始网页快照（`raw_pages/*.html`）、本地抽取（`local_extracts/`）、来源索引（`README.md`）。

| 子目录 | 编号 | 内容 |
| --- | --- | --- |
| `智推时代公司调研/` | C | 官网、融资聚合页、艾瑞 GEO 行业报告、媒体转载 |
| `全球互联网用户搜索习惯变迁/` | S | Gartner、World Bank、DataReportal、Pew、Google、OpenAI、StatCounter、SparkToro |
| `SEO到GEO时代转变/` | G | Google SEO/AI 官方文档、GEO 学术论文、零点击研究 |
| `搜索引擎AI搜索占比内容偏好信源偏好/` | H | 全球/中国/澳洲搜索与 AI 数据源 |
| `GENO四阶闭环技术栈开源替代/` | T | GENO 摘录 + 69 个开源技术资料 + 网页快照 |
| `智推时代合作案例调研/` | K | 合作案例 PDF 摘录、客户候选与媒体快照 |
| `智推时代相似服务竞品调研/` | V | 海外/国内竞品官网与文档快照、逐来源摘要 |

## 证据等级口径

| 等级 | 含义 |
| --- | --- |
| A | 客户/官方/权威原始资料，可直接核验 |
| B | 主流媒体、行业报告、工商/融资聚合页、第三方资料 |
| C | 公司官网、公司 PDF、企业访谈、营销稿（公司自述） |
| D | 无法访问、二级转述、身份不明或缺少原文（待补证据） |

## 核心判断（摘自总报告）

- **赛道逻辑成立**：搜索行为从"找链接"走向"看摘要、问 AI、追问验证来源"；GEO 是 SEO 在生成式答案环境中的上层扩展，而非替代。
- **智推时代产品叙事完整、公开技术证据不足**：能看到方法论、案例叙事与行业背书，未见公开 API、试用入口、评分公式、采样方法或第三方技术评测。
- **澳洲首发应做"证据型平台"**：先把 AU Market Profile → Prompt Pack → AI Answer Runner → Raw Evidence Store → Answer Parser → Citation Graph → Competitor Benchmark → Evidence Report 做扎实，再扩展内容生成、分发与集成。

> 产品第一原则：不要把 GEO 做成"黑箱投喂大模型"的服务，而要做成"可复盘的 AI 答案证据平台"——每个分数、建议和优化效果都能点回原始 prompt、平台、地区、时间、答案、引用 URL 和截图/快照。

---

生成日期：2026-06-08 起持续更新。本库不替代法律、财务或合同尽调，重点聚焦技术与产品可行性。
