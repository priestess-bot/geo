# GENO SaaS 澳大利亚首发 · 项目管理计划（PROJECT-PLAN）

本文件是**待办层**：把 `docs/` 里的规格拆成可追踪、可增量交付、可验收的里程碑与任务。规格变动时，改代码 + 改 `docs/` + 回写 `decisions/` ADR 在同一个 PR 内完成。

- 规格源（不动）：[docs/GENO-SaaS-AU-首发技术落地路径.md](docs/GENO-SaaS-AU-首发技术落地路径.md)、[docs/GENO-SaaS-MVP-一期需求拆解表.md](docs/GENO-SaaS-MVP-一期需求拆解表.md)、[docs/GENO-SaaS-MVP-技术设计文档.md](docs/GENO-SaaS-MVP-技术设计文档.md)
- 三层对应：`docs/`（规格）→ 本文件（待办）→ 代码（分模块）→ 分支/PR/验收（节奏）

## 0. 使用约定

**状态标记**：`[ ]` 待办 · `[~]` 进行中 · `[x]` 完成（用 issue 时以 issue 状态为准，本文件做总览）

**优先级**（沿用 AU 路径 §4 分层，覆盖需求表中偏国内口径）：

| 级别 | 含义 |
| --- | --- |
| P0a | 稳定证据链 MVP，缺则不能进入 design partner 试点 |
| P0b | Google AIO / AI Mode 高风险 spike，必须限时出结论但不阻塞 P0a |
| P0c | 客户可交付证据报告，组合 P0a 数据与 P0b 结论形成可售 MVP |
| P1 | 第二阶段，影响交付效率与客户体验 |
| P2 | 验证市场后再做（内容生成、集成、更广平台） |

**完成定义（DoD）**：每条验收勾选框来自 AU 路径 §9 与需求表各 Epic 的「验收标准」列。任务勾完 + 该里程碑 DoD 勾完 = 里程碑达成。

**任务来源标注**：`Step N` = AU 路径实施步骤；`§8.x` = AU 路径数据模型；`EN-xx` = MVP 需求表 Epic 条目。

### 0.1 范围对齐说明（重要，防止把旧流程带回来）

本计划以 **AU 首发技术路径**为准，对通用《MVP 一期需求拆解表》做三处覆盖：

1. **流程证据优先**：主线是「证据 → 信源图谱 → 竞品差距 → 行动 → 报告 → 复测 → 内容」，不是「配置 → 采集 → 评分 → 知识库 → 内容生成 → 分发」。
2. **平台换成澳洲**：P0a 稳定链路先做 `chatgpt`、`perplexity`；P0b 单独验证 `google_aio`、`google_ai_mode`；需求表中 DeepSeek/豆包/Kimi/元宝/文小言等国内平台**不在 AU 首发范围**。
3. **内容/知识库/分发降级**：需求表的 E6 知识库、E7 内容工作台、E8 分发在 AU 首发里整体降到 **P2（M7）**；P0a/P0b/P0c/P1 只做证据、评分、信源、竞品、报告、行动、复测。

确定性口径（与规格一致，不再浮动）：**1 个行业**首发、**100 条** prompt（上限 200）、P0a 稳定平台每条重复采样 **k=3**、Google spike **30 条 prompt / Australia + Sydney / k=2**、**3–5 个**竞品、默认评分公式 `au_visibility_v1`（8 项权重和 1.00，候选公式 `au_visibility_v1_1_local_boost` 通过 registry 管理）、平台权重 Google 45 / ChatGPT 30 / Perplexity 25。Google 权重最高，但进入全量评分必须先通过 P0b 健康闸门。

## 1. 里程碑总览

里程碑 = AU 路径 §7 的 8 个阶段。**平台评分权重与采集构建顺序是两回事**：权重 Google 最高，但构建先做 Perplexity + OpenAI 稳定证据链，Google AIO / AI Mode 当 P0b 独立 spike。

| 里程碑 | 阶段 | 覆盖 Epic | 出口标准（一句话） | P 级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| **M0** | 接口契约与轻量开源底座 | E10（部分） | 核心依赖一键起，8 个接口有 stub，P0a/P0b/P0c 表可迁移，CI 绿 | P0a | `[~]` |
| **M1** | AU MarketProfile + 行业模板 + Prompt Pack | E1、E2 | 能建 market=AU 项目，配 1 行业 + 100 prompt + 3–5 竞品 | P0a | `[~]` |
| **M2a** | Stable AI Answer Runner + Raw Evidence Store | E3 | Perplexity + OpenAI 可采，证据全留，含触发状态、k=3、成本 | P0a | `[~]` |
| **M2b** | Google AIO / AI Mode Spike | E3 | Google 自建/第三方/人工路径限时对比，输出 pass/fail gate | P0b | `[~]` |
| **M3** | Answer Parser + AUVisibilityScore | E4 | 自动解析 + 可拆解可版本化评分 + 双分母 | P0a | `[~]` |
| **M4** | Citation Graph + Competitor Benchmark | E5 | 信源图谱 + source gap + 3–5 竞品对标 | P0c | `[~]` |
| **M5** | Evidence Report Export | E9（部分） | 导出含方法说明、Google spike 结论与证据附录的 PDF/CSV | P0c | `[~]` |
| **M6** | Action Plan + 复测 | E9（部分） | 缺口转任务 + T+7/14/30 复测前后对比 | P1 | `[~]` |
| **M7** | Knowledge Facts + Content Engine + Integrations | E6、E7、E8 | 本地事实库 + 证据驱动内容 + 集成 | P2 | `[~]` |

**P0a design partner 试点 = M0 + M1 + M2a + M3**。

**P0 可售 MVP = M0 + M1 + M2a + M2b + M3 + M4 + M5**，其中 M2b 若未过健康闸门，Google 数据只能进入 limited coverage 附录，不进入主评分分母。

## 2. 全局完成定义（通用 DoD）

每个任务合并前都要满足：

- `[~]` 代码有单测；关键路径有集成测试（fixture/API/core 与本地 Postgres+MinIO runtime E2E 已覆盖；真实外部采集 E2E 待接）
- `[~]` 通过 CI（lint + 测试 + 迁移可起）（GitHub Actions `contracts-and-runtime` 与本地 `make ci-local` 已覆盖 `make quality`、`make test`、`make web-build`、默认/LLM/scheduler/observability/db-smoke Compose config、`make db-smoke` 临时 PostgreSQL schema/RLS smoke 和 `make runtime-e2e`；真实外部 provider E2E 与生产发布迁移策略待接）
- `[x]` 若改了行为/口径，同 PR 更新对应 `docs/`，必要时加 `decisions/` ADR
- `[~]` P0a/P0b/P0c 数据写入可追溯：能点回 `PromptQuestion -> AnswerRun` / `answer_run_ids`（runtime project create/read API、runtime project `project_id` 过滤、可配置 AU/DTC 客户项目创建、runtime prompt API、runtime prompt CSV/file import API、runtime prompt import history API、fixture TraceabilityBundle、worker `--persist` 写 AU 启动包/prompt 元数据、evidence 与 `CollectionRunSummary`，runtime project/prompt/evidence/collection run/manual backfill/entity alias API 已读回项目、竞品、prompt 计数、prompt 文本、prompt metadata、采集批次成功率/触发率、回答率、成本/耗时/失败摘要与别名确认审计；worker `--persist-analysis` 已读取确认后的 `entity_aliases`、项目级 `score_weight_configs` 和 `--score-formula-version`，并保存 alias-aware `AnswerAnalysis`、parser A/B comparison、公式版本化 `VisibilityScoreSnapshot` 与 TraceabilityBundle，runtime traceability API 可按 `project_id` 读回报告/评分/证据/图谱/action/content/audit/evidence link 聚合详情，`GET /v1/launch-status/au` 可读回 P0a/P0b/P0c 交付准入总控状态，`GET /v1/launch-remediation-plan/au` 可读回 blocker 到 work item/命令/验证/证据产物的清障计划，`GET /v1/p0a-environment-checklist/au` 可读回当前 P0a 必填/推荐环境变量缺项、hard gate、verification commands 和 checklist hash，`GET /v1/p0b-google-execution-checklist/au` 可读回当前 P0b Google env/selector/manual path/DB/smoke/health/full spike 缺口、hard gates、verification commands 和 checklist hash，`GET /v1/handoff-dossier/au` 可读回同一 launch/remediation/P0a/P0b checklist payload 合成的交付状态总包，并在 `summary` 中固定 P0a/P0b env-file hygiene ready/error/warning count，在 `runtime_endpoints` 中固定 project lifecycle/audit 的 read/export 复盘入口；`make au-handoff-dossier` / `make verify-au-handoff-dossier` 已把 launch status、remediation plan、P0a environment checklist、P0b Google execution checklist、P0a/P0b env-file hygiene、P0a/P0b/P0c 证据摘要、source file sha256、Markdown hash、下一 work item、项目生命周期读回/CSV 和项目审计轨道读回/CSV 合成可复算交付状态总包；Runtime Console 已展示 Project Bootstrap 客户配置表单、项目下拉、Entity Alias、Alias Assignment Queue Stats、Alias Assignment Claim/Release、Alias Assignment Overdue Notifications、Runtime Filters、AU Launch Gate、Remediation plan、P0a environment checklist、P0b Google execution checklist、Handoff dossier、Evidence Sort、Saved Views、筛选后 Evidence CSV 导出、筛选/排序后的报告 artifact 下载、Report History、Prompt Pack、Prompt CSV Import、Prompt File Import、Prompt Import History、Manual Backfill、Latest Evidence、Collection Run Quality、Evidence Runs 明细、Score Weights 公式目录/版本选择、Score Contributions 完整解释包、parser A/B agreement、Citation Graph & Competitors 明细、Citation Graph Map、Report Method & Evidence Appendix、Action Plan & Retest Detail、Runtime Alerts、Content Engine Detail、Traceability Detail、Traceability Map、节点级 details 钻取和页面内锚点深链路；独立 `/traceability` 详情页已复用 runtime traceability/graph API 提供项目级可分享详情入口，并支持 `q/node_type/platform/source_type/gap_type` URL 筛选 evidence/source/action/draft/audit/evidence link 节点、筛选后节点计数和首个节点深链入口；client-side SVG 交互图谱已支持 pan/zoom、fit/reset、节点聚焦和回跳锚点；力导向自动布局、节点拖拽编辑和真实 design partner 浏览器验收仍待增强）
- `[~]` AU 外部依赖交接包与清零 dry-run 可审计：`make au-external-dependency-handoff` / `make verify-au-external-dependency-handoff` 已把 launch remediation、P0a environment checklist、P0a execution checklist、P0b Google execution checklist 合成 `docs/runtime_preflight/au-external-dependency-handoff-latest.json`，按 `p0a_provider_credentials`、`p0a_real_batches`、`p0b_google_environment`、`p0b_google_manual_backfill`、`p0b_google_phase_execution` 五组固定 29 个当前外部依赖 blocker、8 个 work item、下一项 `p0a_environment`、source verifier、文件 sha256 和脱敏策略；`clearance_sequence` 进一步固定六步清零顺序、当前 step `p0a_provider_credentials`、下一条命令 `make verify-au-p0a-env-template`、每步阻塞原因、验证命令和证据输出；`make au-external-dependency-clearance` / `make verify-au-external-dependency-clearance` 已把该序列转成不执行真实外部调用的 dry-run 执行计划，固定 `ready_to_execute`、`external_dependency_handoff_ready`、`would_execute_step_count`、step 前缀、hard gate 和脱敏字段校验。`GET /v1/external-dependency-handoff/au`、`GET /v1/external-dependency-clearance/au`、handoff dossier runtime endpoint 和 Runtime Console 已读取同一交接/执行计划；clearance runtime endpoint 会在内存中构建同源 handoff 后生成 dry-run，不依赖默认 JSON 已存在。当前 `external_dependency_handoff_ready=false` 是预期阻塞态，真实凭证、DB、selector、manual 120-row 和批次证据到位后才可用 `scripts/verify_au_external_dependency_handoff.py --require-ready` 与 `scripts/verify_au_external_dependency_clearance.py --require-handoff-ready` 作为清零硬门禁。
- `[~]` 关键动作写入 `AuditEvent`；关键输出能生成 provenance 链路和解释包（采集/采集批次摘要/评分/报告/action/content fixture、人工补录最小路径、实体别名确认最小路径、计算型 alias candidates、基于 `raw_answers` / `answer_citations` 的证据驱动候选推荐、批量实体消歧确认动作、候选 `needs_review/rejected/approved` 审核决策持久化、批量候选 needs-review/reject 动作、候选审核历史查询与 rejected 恢复入口、轻量候选审核 reviewer/priority/due date 分配 metadata、assignment queue read filters、assignment queue stats/overdue/due soon read model、claim/release、overdue notification enqueue 和 in_progress/blocked/completed 状态推进动作、`entity_alias_candidate_review_recorded`、`entity_alias_candidate_batch_reviewed`、`entity_alias_candidate_assigned`、`entity_alias_candidate_assignment_actioned`、`runtime_notification_created` 与 `runtime_notification_delivery_queued` 审计已落；完整审批流、完整 SLA 自动升级和值班升级仍待接）
- `[~]` 有一次可演示（API fixture endpoints 与 Runtime Console MVP 已可演示；真实 design partner 数据演示待接）

**P0a 稳定链路验收门槛**（M0 + M1 + M2a + M3 全绿才算可进入 design partner 试点）：

- `[~]` 可创建 `market=AU` 项目，配 1 行业模板 + 100 条澳洲问题集 + 3–5 竞品（可配置客户项目 API/控制台与项目资料编辑已落；`GENO_RUNTIME_PROJECT_ACCESS_CONTROL=1` 时已按 actor + `project_members` 做 API 级项目成员门禁，并接入 owner/admin/analyst/viewer 最小角色矩阵、成员删除审计、最后 owner 保护和 `project_member_invitations` 最小邀请创建/撤销/过期/SMTP 邮件发送/接受记录与审计；默认 header demo 模式读取 `X-GENO-Actor-Id`，`GENO_RUNTIME_AUTH_MODE=jwt` 时已支持 HS256 Bearer JWT actor 解析，`GENO_RUNTIME_AUTH_MODE=jwks` 时已支持 inline/remote/OIDC-discovered JWKS RS256 Bearer JWT actor 解析，远端 JWKS 与 OIDC discovery document 均有 TTL 缓存，并可显式开启 stale-if-error 兜底；PostgreSQL RLS/GUC 最小项目隔离与 `geno_runtime_app` runtime role 已落并进入 runtime E2E；Keycloak/OIDC login/session、前端 SSO 登录流、邮件模板/退订/服务商 API、真实用户目录和真实客户数据验收待接）
- `[~]` 完成 Perplexity Sonar + OpenAI web search 两个平台采集，每条有 answer + citation + 截图/HTML（fixture k=3 可通过 `P0ACollectionReadinessGate`；真实 API adapter 已为官方 API 响应生成 `geno-api-snapshot://...` HTML snapshot 证据，并可在配置对象存储时归档为 `s3://...` EvidenceAsset 与 `api_snapshot_assets_archived` 审计事件；`make api-preflight` 已提供真实 key 到位后的 1 prompt × Sydney × k=3 × 2 平台预检，并默认把成功/失败 JSON 写入 `docs/runtime_preflight/api-preflight-latest.json`，可用 `GENO_API_PREFLIGHT_OUTPUT_PATH` 覆盖；预检 payload 已包含 `preflight_summary`、`preflight_audit_checklist` 与 `preflight_payload_hash`，`make verify-api-preflight` 可离线复算 hash 与结构，`make preflight-manifest` 可生成文件 sha256、verifier 结果、阻塞原因、复跑参数和 manifest 自身 hash 的审计索引，`make au-p0a-runbook` 可生成 preflight -> small batch -> full batch 的机器可读执行计划，`make verify-au-p0a-runbook` 可校验 runbook hash、步骤顺序、planned runs 和 gate 参数，`make verify-au-p0a-env-template` 可先校验已提交 `.env.au-p0a.example` 的必需键、空 provider key、本地占位 DATABASE_URL/对象存储、runtime 输出路径和疑似 secret 标记，`make au-p0a-env` 可生成不泄露 secret 的脱敏环境报告并由 `make verify-au-p0a-env` 复算 hash；当本地 env-file 存在且含条目时，报告会追加 `env_file.hygiene`，要求文件 gitignored、not tracked 且权限为 `0600`，否则 `ready_for_real_batch=false`，完全用进程环境注入 secret 时缺失 `.env.au-p0a` 不触发该 hard error；`make au-p0a-environment-checklist` / `make verify-au-p0a-environment-checklist` 可把 runbook、environment report 和 status 摘要合成脱敏配置清单，固定必填/推荐变量、env-file hygiene、setup/verification commands、evidence outputs 与 `environment_checklist_hash`，并要求 setup commands 从 env template gate 开始，`make au-p0a-runbook-dry-run` 可读取同一份 `GENO_AU_P0A_ENV_FILE` / `.env.au-p0a` 并在不执行外部 provider 调用的前提下输出步骤、产物、external_call_risk、环境缺口、ready_to_execute、env-file 元数据/hygiene 和 execution_payload_hash，`make verify-au-p0a-runbook-execution` 可离线复算 execution hash、步骤计数、dry-run invariant 和 forbidden secret field，`make au-p0a-readiness` 可读取同源 env-file，并按 preflight/small_batch/full_batch 阶段合并必需环境、env-file hygiene、runbook、上游 payload 与 manifest gate 状态，也可用 `GENO_AU_P0A_REQUIRE_DB_CHECK=1` 对 env-file 或进程环境里的 `DATABASE_URL` 强制只读 PostgreSQL 连通性检查，`make au-p0a-package` 可汇总 runbook/environment report/runbook execution/readiness/preflight/small/full 产物 hash、ready 状态和缺失项，`make verify-au-p0a-package` 可离线复算 package hash 与 summary/artifacts 一致性，`make au-p0a-status` 可用同源 env-file 复算三阶段 readiness，并汇总 runbook、environment report、runbook execution、package verifier、completion/design-ready 百分比、remaining blockers 和 next action，`make verify-au-p0a-status` 可离线复算 status report hash、completion、remaining blockers 与 next action，用于机器可读判断 phase、exit code、`ready_for_design_partner`、下一步建议、证据字段引用和 stdout/落盘 JSON 完整性；真实 Perplexity/OpenAI API 凭证批次待跑）
- `[x]` P0a 交付证据脱敏硬门禁：environment report、environment checklist、runbook execution、readiness、evidence package 和 status report verifier 均递归拒绝 `value` / `raw_value` 原始字段；env-file、process env 和数据库连接串只能以来源、长度、sha256 前缀和 `secret_redacted=true` 形式进入可审计产物；含真实条目的 env-file 还必须进入 hygiene gate，证明 gitignored/not tracked 且权限为 `0600`。
- `[~]` P0a execution checklist 总控清单：`make au-p0a-execution-checklist` / `make verify-au-p0a-execution-checklist` 已把 env template gate、environment checklist、runbook dry-run、preflight、small batch、full batch、package/status hard gates、证据输出路径、setup/execution/verification commands、completion/design-ready 百分比和 remaining blockers 合成 `docs/runtime_preflight/au-p0a-execution-checklist-latest.json`；清单内 `credential_handoff` 会逐项固定 `PERPLEXITY_API_KEY`、`OPENAI_API_KEY`、`DATABASE_URL` 的 owner hint、允许注入方式、env-file key、`chmod 600 .env.au-p0a` setup command、填充后的验证命令和 raw secret 禁止字段；`real_batch_phase_handoff` 会固定 `preflight -> small_batch -> full_batch` 三阶段的 command ids、artifact keys、prerequisite gates、planned runs、ready/can-start、blocking reasons 和 next phase；`GET /v1/p0a-execution-checklist/au`、handoff dossier 和 Runtime Console AU Launch Gate 已读取同一清单，且 handoff dossier 顶层 summary、Markdown 和 Console Handoff dossier 卡片会展示 P0a credential handoff ready/missing/redacted 与 real batch phase handoff next phase/blocked phase count。当前本地为缺真实 provider env 与 small/full batch 证据的预期阻塞态，`real_batch_phase_handoff.next_phase=preflight`、`blocked_phase_count=3`、`total_planned_runs=2436`，真实 design partner 仍需外部凭证和批次产物。
- `[~]` 每条采集记录 platform/surface/access_method/city/language/device/collected_at/collector_version/collector_backend_id（`P0ACollectionReadinessGate` 已自动检查必备元数据；真实外部批次待跑）
- `[~]` 每条采集记录 `answer_present`/`surface_triggered`；P0a 每 prompt 重复采样 k=3（worker 输出 readiness gate；默认 sample-size=1 会 fail，`--sample-size 3` fixture 会 pass；真实外部批次待跑）
- `[~]` 采集、采集批次摘要、解析、评分、人工补录、实体确认、报告导出均写入 `AuditEvent`（runtime `collection_run_summarized`、`manual_backfill_recorded` 与 `entity_alias_confirmed` 已落；真实外部解析/采集链路仍需凭证联调）
- `[~]` 每个 collector_backend 写入 CollectionCost；可估算 planned_runs、成功率、触发率、回答率、失败摘要、单位成本和平均耗时（真实外部采集凭证联调待接）
- `[~]` 自动解析品牌提及/推荐/排名/竞品/引用/本地相关性（rule parser、confirmed alias-aware parser、本地 judge fixture A/B、`LiteLLMGateway` adapter、`llm_call_logs` 调用日志、`human_review_records` 复核留痕和只读 Review Queue 已落；真实 LiteLLM 服务联调、抽样复核校准流程待接）
- `[x]` 生成可拆解、公式版本化的 `AUVisibilityScore`，能点回原始 answer run
- `[x]` 生成 `ScoreContribution` 分数解释包，展示子指标贡献、权重、分母、正负证据和局限
- `[x]` 报告区分 Trigger Rate 与 Mention/Recommendation Rate（`report_exports.method_disclosure.score_rate_denominators` 已冻结三类 rate 的 numerator/denominator/formula；Markdown/PDF/白标 artifact 与 Runtime Console 均展示 Trigger 使用 all attempted evidence records，Mention/Recommendation 使用 surface_triggered 子集）

**P0b Google spike 验收门槛**：

- `[~]` 对 30 条高意图 prompt 跑 Google AIO / AI Mode，地理范围 Australia + Sydney，k=2（`google-spike` 真实 worker 模式、默认关闭的 `.env.au-p0b-google.example` 脱敏模板、`make verify-au-p0b-google-env-template`、`make au-p0b-google-playwright-env`、`make verify-au-p0b-google-playwright-env`、`make au-p0b-google-playwright-smoke`、`make verify-au-p0b-google-playwright-smoke`、`make verify-au-p0b-google-manual-backfill`、`make au-p0b-google-spike-health`、`make au-p0b-google-spike-health-manifest`、`make au-p0b-google-spike`、`make au-p0b-google-spike-manifest`、`make au-p0b-google-runbook`、`make verify-au-p0b-google-runbook`、`make au-p0b-google-runbook-dry-run`、`make verify-au-p0b-google-runbook-execution`、`make au-p0b-google-status`、`make verify-au-p0b-google-status`、`make au-p0b-google-package`、`make verify-au-p0b-google-package`、`make au-p0b-google-execution-checklist`、`make verify-au-p0b-google-execution-checklist` 已落；runbook/status/remediation plan 已把 env template gate、manual backfill strict verification artifact 放在 smoke 之前/之后的正确位置，并把 health/full spike 原始命令收口为 Make 入口；P0b Playwright env report 现在追加 `env_file.hygiene`，当 `.env.au-p0b-google` / `GENO_AU_P0B_GOOGLE_ENV_FILE` 存在且含条目时要求 gitignored、not tracked 且权限为 `0600`，否则 smoke readiness 失败；P0b package 会汇总 status report、runbook、execution、env、smoke、manual、health、spike 与 manifest 的文件 sha256、verifier hash、blockers 和 `google_main_scoring_allowed`；P0b execution checklist 会把 env template gate、env-file hygiene、selector group、manual path、DB、smoke、health、full spike、status/package hard gates 合成脱敏执行清单并复算 hash，且已通过 `GET /v1/p0b-google-execution-checklist/au`、handoff dossier 与 Runtime Console 暴露给交接检查；handoff dossier 顶层 summary、Markdown 和 Runtime Console Handoff dossier 卡片现在也展示 P0b env-file hygiene ready/error/warning count，便于客户交付前复核 Google secret 文件 hygiene；`google_spike_phase_handoff` 已把 `environment -> browser_smoke -> manual_backfill -> health_check -> full_spike -> main_scoring` 六阶段、命令、证据、next phase、blocked phase count 和 240 planned runs 固定为可复算交接状态，防止 env/smoke/manual 未 ready 时跳到 health/full spike 或主评分；真实 `.env.au-p0b-google` 填充、Google Playwright env ready、smoke success、真实 manual 120-row 填充与真实 240-run 待跑）
- `[~]` 至少对比自建浏览器、第三方 SERP API、人工补录中的两条路径（`GoogleSpikeReadinessGate` 已自动检查 access method 路径数；browser-only fixture 会 fail，browser+third_party fixture 可 pass；真实 `google-spike` 默认核心矩阵为 browser + manual 以保持 240 planned runs；`ThirdPartySerpCollector` 已有 provider-neutral JSON adapter，并已新增独立 `google-serp-fixture` / `google-serp-spike` 120-run 对照入口、payload verifier 与 status/hash verifier，真实供应商凭证/endpoint 联调后再纳入 P0b 对照复盘）
- `[~]` 输出 pass/fail gate：成功率、触发率、失败原因、截图/HTML 证据、成本/耗时估算（`GoogleSpikeGateResult` + `GoogleSpikeReadinessGate`、P0b runbook/dry-run/env readiness/smoke/status report/hash verifier 已落；真实 spike JSON 与 manifest 待生成）
- `[~]` 未通过健康闸门时，Google 只进入 limited coverage 附录，不进入主评分分母（`score_input_policy` 已在分析/评分层硬性排除未同时通过 `GoogleSpikeGateResult` 与 `GoogleSpikeReadinessGate` 的 Google answer runs，并写入评分审计与报告 Method Disclosure；真实 spike 待跑）

**P0c 可售报告验收门槛**：

- `[~]` 生成 Citation Graph，识别 source gap，输出 3–5 竞品 Benchmark（fixture/runtime graph API、Runtime Console graph 明细与图谱 map、独立 `/traceability` source graph 详情与基础交互图谱已落；真实客户数据、力导向自动布局和节点拖拽编辑待接）
- `[~]` 导出含方法说明（含 API/消费者界面差异抽检结论、Google spike 结论、平台覆盖/降级口径）、审计摘要、分数解释包与原始证据附录的 PDF/CSV（Markdown/CSV/PDF/白标 PDF、Method Disclosure、ScoreContribution、Evidence Appendix、`method_disclosure.audit_summary` 冻结与 runtime artifact 渲染已落；`make au-p0c-report-package` / `make verify-au-p0c-report-package` 已生成并复算 P0c 报告交付包，覆盖 Markdown/CSV/PDF/白标 PDF hash、API-vs-browser fixture sampled、审计摘要和 traceability 合同；`make au-launch-status` 已把 P0a/P0b/P0c 交付准入合并为可复算总控状态，`make au-launch-remediation-plan` / `make verify-au-launch-remediation-plan` 已把当前 29 个 blocker 映射为 8 个可执行 work item，下一步为 `p0a_environment`，且 P0a/P0b env work item 均在复制真实 env-file 后固定执行 `chmod 600` 以满足 hygiene gate；`make au-handoff-dossier` / `make verify-au-handoff-dossier` 已把 launch gate、清障计划、P0a environment checklist 摘要、source file hash、stage summary、blocker 分布、项目 lifecycle/audit 读回与 CSV 复盘入口和 Markdown 人工交接稿合成可审计总包；`GET /v1/launch-status/au`、`GET /v1/launch-remediation-plan/au`、`GET /v1/p0a-environment-checklist/au`、`GET /v1/handoff-dossier/au` 与 Runtime Console AU Launch Gate 已可直接展示 gate、清障计划、P0a 环境缺项、交付总包状态和 lifecycle/audit replay endpoint；真实 P0a/P0b 数据报告待跑）
- `[~]` 任意报告数值可沿 `ReportExport -> VisibilityScoreSnapshot -> ScoreContribution -> AnswerAnalysis -> PromptQuestion -> AnswerRun -> RawAnswer/AnswerCitation/EvidenceAsset -> SourceGraph/SourceGap/CompetitorBenchmark` 追溯（fixture TraceabilityBundle、prompt-linked runtime evidence API、runtime score API、runtime citation graph API、runtime report API、runtime report artifact API、runtime traceability detail API 与 Runtime Console Evidence Runs、Score Contributions、Citation Graph & Competitors、Citation Graph Map、Report History、Traceability Detail / Traceability Map / node drilldown / anchor deep links 已落；独立 `/traceability` 页面已支持 `project_id`、Traceability Map、节点深链、证据/source/action/content/audit/evidence link 详情和 client-side pan/zoom/节点聚焦图谱；基础 PDF artifact、MinIO/S3-compatible artifact 归档、附录级筛选/排序 artifact 下载与最小白标 PDF 模板已落，力导向自动布局和节点拖拽编辑待接）

**架构验收门槛**（开源·可插拔，搬自 AU 路径 §9）：

- `[x]` P0a 完成接口级可插拔：CollectorBackend、ParserEngine、ScoringFormula、ReportExporter 均有 stub 与至少一个工作实现（`NotConfigured*` stubs、fixture/规则/registry/Markdown-PDF-CSV 工作实现和 runtime-checkable Protocol 合约测试已落）
- `[~]` 向量库 pgvector ↔ Qdrant 切换后业务不变（pgvector runtime knowledge search 已落；本地 `VectorStore` projection contract 已落：`InMemoryPgVectorStore` 与 `InMemoryQdrantVectorStore` 对同一批 knowledge fact embeddings 返回一致 search summary；真实 Qdrant/Milvus service/driver 联调待接，P0c/P1 前完成）
- `[~]` 图库 PG 邻接表 ↔ Neo4j 切换后 citation graph 查询不变（本地 `GraphStore` projection contract 已落：`InMemoryPostgresAdjacencyGraphStore` 与 `InMemoryNeo4jCitationGraphStore` 可写入同一 `CitationGraphResult`，并通过统一 query summary 证明 source nodes/evidence links/source gaps/competitor benchmark 口径一致；真实 Neo4j container/driver 联调待接，不阻塞 P0a）
- `[~]` LLM 供应商经 LiteLLM 切换，解析与生成不改（adapter、parser 注入、失败降级、retry/backoff 与响应 cost 读取已可测；真实 LiteLLM 服务编排和供应商联调仍待接）
- `[~]` 解析器规则实现与 LLM-as-judge 实现可对同一答案并行对比并保留版本（本地 judge fixture 已通过 `LLMGateway` 写入 `parser_comparison.llm_call_log` 与 `llm_call_logs`；LiteLLM adapter 已可注入同一路径；真实 LiteLLM 服务联调待接）
- `[x]` 评分公式可升级到新版本，历史分数仍可按旧版本重算（`SCORE_FORMULA_REGISTRY` 已管理 active/candidate 公式，`rescore_snapshot_with_formula()` 可用指定历史 `AnswerAnalysis` 重放，runtime API 暴露公式目录，worker 支持 `--score-formula-version`；批量重算 UI/审批流待接）

## 3. 里程碑与任务拆解

### M0 · Phase 0：接口契约与轻量开源底座（P0a）

> 出口标准：`docker-compose up` 起核心依赖；8 个接口有 stub + 类型；P0a/P0b/P0c 相关表可迁移可回滚；CI 绿。ClickHouse/Temporal/Langfuse/promptfoo/SearXNG 保留接口和接入点，不作为 P0a 阻塞项。

任务：

- `[x]` (P0a) 仓库骨架：`apps/`、`packages/`、`workers/`、`infra/`、`tests/`、`decisions/`
- `[x]` (P0a/P0b/P0c) 数据契约：优先实现 MarketProfile、IndustryProfile、PromptQuestion、GeoSample、AnswerRun、RawAnswer、AnswerCitation、AnswerAnalysis、SourceGraph、CompetitorBenchmark、VisibilityScoreSnapshot、BrandEntity/CompetitorEntity/EntityAlias、CollectionCost、AuditEvent、ReportExport、ScoreContribution、EvidenceLink、TraceabilityBundle 关联表；P1/P2 表可延后 — `§8`
- `[x]` (P0a) 接口契约 stub（先定义不实现）：CollectorBackend、LLMGateway、ParserEngine、VectorStore、GraphStore、GeoProvider、ScoringFormula、ReportExporter — `Step3.2`（P0a 四个关键工作流接口已用 runtime-checkable Protocol 对齐真实实现，并有 `NotConfigured*` stub + 至少一个工作实现的合约测试）
- `[~]` (P0a) `infra/docker-compose.yml` 核心底座：PostgreSQL+pgvector、MinIO、FastAPI、Next.js、LiteLLM、simple worker/cron — `§6`（已落 PostgreSQL+pgvector、MinIO、API、Web、repository 映射、`DATABASE_URL` connection factory、runtime API connection pool、runtime readiness/diagnostics API、AU launch status API、Prometheus 文本格式 `/metrics` 请求/延迟/pool 指标、`X-GENO-Request-Id` 与结构化 runtime access log、可选 `observability` Compose profile、Prometheus scrape config 和 Grafana datasource provisioning、S3-compatible object store client、AU 启动包/prompt 元数据持久化、worker `--persist` / `--persist-analysis`、官方 API snapshot 对象归档、`db-smoke` 临时 PostgreSQL schema/RLS smoke profile、`runtime-e2e` Postgres+MinIO 验证 profile、runtime project/prompt/evidence/collection run/evidence CSV export/saved views/score API、runtime citation graph API、runtime report API、runtime action plan API、runtime alerts API、runtime content engine API、runtime knowledge fact search API、runtime traceability API 与 Runtime Console Project Bootstrap/Runtime Filters/AU Launch Gate/Prompt Pack/Collection Run Quality/Evidence Runs/Score Contributions/Citation Graph & Competitors/Citation Graph Map/Report Method & Evidence Appendix/Action Plan & Retest Detail/Runtime Alerts/Content Engine Detail/pgvector Knowledge Search/Traceability Map/MVP；LiteLLM adapter、retry/backoff、响应 cost 读取、可选 `llm-gateway` Compose profile、`litellm` proxy config 和 `collector-worker-litellm` 已可测；runtime 服务已切到 `geno_runtime_app` 非 bypass role，并接入基于 `geno.runtime_*` session GUC 的 PostgreSQL RLS 最小项目隔离；API Compose 默认启用 `GENO_RUNTIME_DB_POOL_ENABLED=1` 连接池，归还连接时清空 RLS session GUC；生产压测、OTel/集中式日志、生产 dashboard/SLO/告警、独立详情页、client-side pan/zoom 基础交互图谱已落；力导向自动布局、节点拖拽编辑、Keycloak/OIDC login/session、前端 SSO 登录流、细粒度角色和复杂审批流待接）
- `[x]` (P0c/P1) 重组件接入点：ClickHouse、Temporal、Langfuse、promptfoo、SearXNG、Metabase 写 ADR 和接口适配计划，但不阻塞 P0a — `§6`
- `[~]` (P0a) 空 CI：lint + 测试 + 迁移起服（GitHub Actions `contracts-and-runtime` 与本地 `make ci-local` 已覆盖 Ruff Python lint、Python compileall、Runtime Console TypeScript typecheck、contract/runtime tests、Runtime Console build、默认/LLM/scheduler/observability/db-smoke Compose config、临时 PostgreSQL schema/RLS smoke 和 Postgres+MinIO runtime E2E；真实外部 provider E2E 与生产发布迁移策略待补）
- `[~]` (P0a) LLM 网关配置 + 调用日志 + 对象存储配置 — `E10-01 / E10-03 / E10-05`（已落 LLMGateway 接口、`FixtureLLMGateway`、`LiteLLMGateway` HTTP adapter、`llm_call_logs` 运行时调用日志与对象存储配置；worker `--judge-gateway litellm` 可把 parser judge 切到 LiteLLM adapter；retry/backoff、失败调用审计、上游响应 cost 读取、LiteLLM Compose profile 和 env-secret config 已落；真实 provider key 联调、供应商账单 reconciliation 待接）

DoD：

- `[~]` 一键起核心依赖；P0a/P0b/P0c 相关表可建可回滚；8 接口 stub + CI 绿（本地配置、repository runtime mapping、AU 启动包持久化、prompt-linked runtime evidence、runtime collection run summary、runtime score/runtime citation graph/runtime report/runtime traceability read model、Docker 写库验证、GitHub Actions quality/db-smoke/runtime gate、`make db-smoke` schema/RLS 验证和 `make runtime-e2e` Postgres+MinIO artifact/snapshot 归档验证已完成；真实外部 provider E2E 与生产发布迁移策略待补）
- `[x]` 三个可插拔点（向量库/图库/LLM）已留好接口，替换演示排入 P0c/P1
- `[x]` AuditEvent / ReportExport / ScoreContribution / EvidenceLink / TraceabilityBundle 相关表可建可回滚

### M1 · Phase 1：AU MarketProfile + 行业模板 + Prompt Pack（P0a）

> 出口标准：能创建 market=AU 项目，配 1 行业模板、100 条 AU prompt、3–5 竞品，且平台/城市/语言/货币/权重全部从 MarketProfile 读取。

任务：

- `[~]` (P0a) 租户/项目/品牌/竞品/角色权限 — `E1-01..05`（已落契约、启动包、PostgreSQL 幂等持久化、runtime AU/DTC 项目创建 API 与项目聚合读取 API；API/控制台已支持 tenant/project/brand/category/brand domains/product lines/3-5 competitors 客户配置创建，并已补 `PATCH /v1/projects/runtime` 项目资料编辑与 `project_updated` 审计；已补 `POST /v1/projects/runtime/action` soft archive/restore，默认项目列表隐藏 archived，显式 `status=archived` 或 `include_archived=true` 可读回，动作分别写 `project_archived/project_restored` 并保留证据链；已补 `GET/POST/DELETE /v1/project-members/runtime` 成员读取/upsert/delete、`project_member_saved/project_member_deleted` 审计和 Runtime Console Project Members 面板；已补 `project_member_invitations`、`GET/POST /v1/project-member-invitations/runtime`、一次性 `invite_token` 创建响应、hash-only 持久化、`project_member_invitation_created/project_member_invitation_revoked/project_member_invitation_expired/project_member_invitation_email_sent/project_member_invitation_accepted` 审计、pending 邀请 `revoke/expire/email` 状态动作、`/invite/accept` 独立接受邀请页和 token-hash 接受邀请最小兑换流；SMTP 邀请邮件只在发送时短暂使用 raw token，数据库/审计只保存 token hash、accept URL hash 和发送摘要；已补默认关闭的 API 级运行时项目访问控制，开启 `GENO_RUNTIME_PROJECT_ACCESS_CONTROL=1` 后默认要求 `X-GENO-Actor-Id`，也可通过 `GENO_RUNTIME_AUTH_MODE=jwt + GENO_RUNTIME_JWT_SECRET` 切到 HS256 Bearer JWT actor 解析，或通过 `GENO_RUNTIME_AUTH_MODE=jwks + GENO_RUNTIME_JWKS_JSON/GENO_RUNTIME_JWKS_URL/GENO_RUNTIME_OIDC_DISCOVERY_URL` 切到 inline/remote/OIDC-discovered JWKS RS256 Bearer JWT actor 解析，远端 JWKS 与 OIDC discovery document 会按 TTL 缓存，并可显式开启 stale-if-error 兜底，项目列表按 `project_members` 过滤，项目级读接口校验 actor 成员关系，项目创建 owner 绑定当前 actor；已补 API 层最小角色矩阵：owner/admin 可做项目资料/归档恢复、成员/邀请、prompt 导入、Brand Kit/Logo、Score Weights 等管理写操作，owner/admin/analyst 可做 alias confirm、manual backfill、human review、fidelity check 等分析复核写操作，viewer 只读；repository 已阻止删除或降级最后一个 owner；PostgreSQL RLS/GUC 最小项目隔离、runtime app role、连接池归还清理和 E2E 隔离断言已落；项目物理删除/客户级数据保留策略、邮件模板/退订/服务商 API、真实用户目录、Keycloak/OIDC login/session、前端 SSO 登录流、完整权限后台和生产压测待接）
- `[x]` (P0a) AU MarketProfile 固定值（locale/timezone/currency/cities/平台权重/信源分类）— `Step1`
- `[x]` (P0a) 1 个 IndustryProfile 行业模板 — `Step2`
- `[x]` (P0a) Prompt Pack：100 条 AU 英文问题集（上限 200），每条绑 intent_type/city/prompt_version，并可通过 runtime prompt API 分页/过滤读回 — `Step3 / E2-03..05`
- `[x]` (P0a/P0b) 平台字典（P0a：chatgpt/perplexity；P0b：google_aio/google_ai_mode）+ 平台权重 + build_stage — `E2-01..02`
- `[x]` (P1) 批量导入 prompt（CSV/XLSX）— `E2-06`（CSV 文本导入 API、raw-body 文件导入 API、`.csv/.txt` UTF-8 与 `.xlsx` 第一工作表解析、Runtime Console 文本/文件表单和 Prompt Import History 已落，写入并读回 `runtime_prompts_imported`；复杂多工作表、公式求值、导入前 diff 预览和错误行下载待接）
- `[~]` (P1) 操作审计日志 — `E1-06`（启动包创建已生成 `project_bootstrap_created`；Project Lifecycle History 已可从 `AuditEvent` 投影 `project_bootstrap_created/project_updated/project_archived/project_restored`，通过 `GET /v1/projects/runtime/lifecycle-events`、`GET /v1/projects/runtime/lifecycle-events/export.csv` 和 Runtime Console 读回/导出 actor、reason、method、before/after hash、changed fields 与 status before/after；CSV 导出响应头会返回 lifecycle export hash、row count、total count、project id 和 method version，用于交付后离线复算；通用项目级 Audit Trail 已可通过 `GET /v1/audit-events/runtime`、`GET /v1/audit-events/runtime/export.csv` 和 Runtime Console 按 project/event_type/target_type/actor_id 查询与导出 append-only `AuditEvent`，响应头返回 audit export hash、method version、row count 和 total count；prompt CSV/file 导入已生成并可通过 import history 读回 `runtime_prompts_imported`，记录 `source_format/source_filename/source_content_type/hash/prompt_count/method_version`；完整用户目录级 CRUD 审计、登录会话审计、全租户审计仓库和生产 IAM 审计待接）

DoD：

- `[~]` 可创建 market=AU 项目，配 3–5 竞品（启动包/API 已可生成，worker `--persist` 已可写入 tenant/project/brand/competitor/prompt；控制台客户配置创建流、项目资料编辑、soft archive/restore 和 prompt CSV/file 导入已接入，项目物理删除/客户级数据保留策略、复杂 XLSX 多工作表/公式处理待接）
- `[x]` 可选 1 行业模板并生成 100 条澳洲问题集
- `[x]` 平台、城市、语言、货币、权重无写死，全部来自 MarketProfile

### M2a · Phase 2a：Stable AI Answer Runner + Raw Evidence Store（P0a，第一条垂直切片）

> 出口标准：Perplexity Sonar + OpenAI web search 均可采到 answer+citation+证据；每条记录触发状态、采集元数据、成本与 k=3；后续新增 Google 后端不改业务代码。

任务（构建顺序：稳定 API → 证据 → 成本 → 地理）：

- `[~]` (P0a) CollectorBackend 接口落地 + **Perplexity Sonar 后端**（最易采，先打通全链路）— `Step4`（fixture + 真实 API adapter + API HTML snapshot 证据已落；配置对象存储时可落为 `s3://...` EvidenceAsset；`collector_health`、`--require-ready-collectors`、`--preflight-output-path`、`preflight_summary`、`preflight_audit_checklist`、`preflight_payload_hash`、`make api-preflight`、`make verify-api-preflight`、`make preflight-manifest`、`make au-p0a-runbook`、`make verify-au-p0a-runbook`、`make au-p0a-env`、`make verify-au-p0a-env`、`make au-p0a-environment-checklist`、`make verify-au-p0a-environment-checklist`、`make au-p0a-readiness`、`make au-p0a-runbook-dry-run`、`make verify-au-p0a-runbook-execution`、`make au-p0a-package`、`make verify-au-p0a-package`、`make au-p0a-status` 和 `make verify-au-p0a-status` 已落，缺 key 时可在采集前失败并落盘 `not_configured` 证据、下一步建议、可复算 payload hash、manifest 索引、脱敏环境报告、环境清单、同源 env-file readiness、批次命令计划、证据包缺口与总控进度报告；显式开启时可在采集前做只读 DB 连通性 gate；真实凭证联调待验证）
- `[~]` (P0a) **OpenAI web search / ChatGPT Search** 官方 API 后端 — `Step4`（fixture + 真实 API adapter + API HTML snapshot 证据已落；配置对象存储时可落为 `s3://...` EvidenceAsset；`collector_health`、`--require-ready-collectors`、`--preflight-output-path`、`preflight_summary`、`preflight_audit_checklist`、`preflight_payload_hash`、`make api-preflight`、`make verify-api-preflight`、`make preflight-manifest`、`make au-p0a-runbook`、`make verify-au-p0a-runbook`、`make au-p0a-env`、`make verify-au-p0a-env`、`make au-p0a-environment-checklist`、`make verify-au-p0a-environment-checklist`、`make au-p0a-readiness`、`make au-p0a-runbook-dry-run`、`make verify-au-p0a-runbook-execution`、`make au-p0a-package`、`make verify-au-p0a-package`、`make au-p0a-status` 和 `make verify-au-p0a-status` 已落，缺 key 时可在采集前失败并落盘 `not_configured` 证据、下一步建议、可复算 payload hash、manifest 索引、脱敏环境报告、环境清单、同源 env-file readiness、批次命令计划、证据包缺口与总控进度报告；显式开启时可在采集前做只读 DB 连通性 gate；真实凭证联调待验证）
- `[x]` (P0a) Raw Evidence Store：AnswerRun/RawAnswer/AnswerCitation/EvidenceAsset/CollectorLog，含 `answer_present`/`surface_triggered`/`sample_index`/`sample_size`/`access_method`，并有 PostgreSQL repository 写入映射、JSONB/UUID runtime adapter、worker `--persist` 写库开关、AU 启动包/prompt 元数据先写入和 prompt-linked runtime evidence 查询 API — `Step5 / §8.5..8.7`
- `[~]` (P0a) Audit / Provenance 基础：采集开始/完成/失败、采集批次摘要、原始证据入库、人工补录和实体别名确认写 `AuditEvent`；`ReportEvidence` / `ScoreSnapshotRun` / `SourceGraphEvidence` 关联表先建表 — `Step5.1 / §8.16..8.19`（采集完成/失败审计、批次级 `collection_run_summarized`、人工补录最小路径 `manual_backfill_recorded`、单条实体别名确认 `entity_alias_confirmed`、证据驱动 alias candidate metadata、批量候选确认 endpoint/Console 队列、候选审核决策表、候选审核历史查询、rejected 恢复/改判入口、批量候选 review/reject endpoint/Console 动作、轻量候选审核分配 metadata、assignment queue read filters、stats/overdue 可见性、claim/release、overdue notification enqueue 和状态推进动作、`entity_alias_candidate_review_recorded`、`entity_alias_candidate_batch_reviewed`、`entity_alias_candidate_assigned`、`entity_alias_candidate_assignment_actioned`、`runtime_notification_created` 与 `runtime_notification_delivery_queued` 已落；完整审批流、完整 SLA 自动升级和值班升级待接）
- `[x]` (P0a) P0a 采样量闸门：100 prompts × 2 platforms × 4 geo × k=3 = 2400 planned_runs，可配置降级 prompt/geo 但不降级证据字段 — `Step9.3`
- `[x]` (P0a) GeoProvider 抽象 + 城市采样（Australia/Sydney/Melbourne/Brisbane）— `Step6 / §8.4`
- `[x]` (P0a) 截图/HTML 快照 + `raw_payload_hash` — `E3-05`
- `[~]` (P0a) CollectionCost、CollectionRunSummary 与 P0ACollectionReadinessGate 记录（从首个采集器起），输出 collector health、planned/attempted/success/failure、成功率、触发率、回答率、失败摘要、单位成本、平均耗时和 P0a 采集门禁 pass/fail/reasons；`--require-p0a-readiness` 可让真实 smoke 在 gate fail 时非零退出；`preflight_summary` 已把 collector health、P0a gate、collection failure、exit code 和建议下一步折叠成稳定审计摘要，`preflight_audit_checklist` 已把阻塞原因、证据字段引用、运行总量、output path 与 replay args 固化为 checklist，`preflight_payload_hash` 与 `scripts/verify_preflight_payload.py` 可复算 stdout/落盘 JSON 完整性并区分审计通过与 design-partner ready，`scripts/build_preflight_manifest.py` 可生成 preflight evidence package manifest，`scripts/build_au_p0a_runbook.py` / `scripts/verify_au_p0a_runbook.py` / `scripts/run_au_p0a_runbook.py` / `scripts/verify_au_p0a_runbook_execution.py` 可生成、校验、dry-run 并校验执行预演真实 AU P0a 小批次/全量批次执行计划，`scripts/verify_au_p0a_readiness.py` 可在每个阶段开始前落盘环境/runbook/上游证据 readiness，并可显式强制只读 DB 连通性检查；`scripts/build_au_p0a_evidence_package.py` / `scripts/verify_au_p0a_evidence_package.py` 可生成并校验 AU P0a 交付证据包清单，`scripts/build_au_p0a_status_report.py` / `scripts/verify_au_p0a_status_report.py` 可生成并校验当前还差多少、卡在哪一步、下一步动作的总控状态报告 — `§8.15`
- `[~]` (P0c/P1) 保真度抽检：同批 prompt 跑 官方 API vs 浏览器 两后端，量化差异率并入报告 — `Step4`（`api_browser_fidelity_checks` 表、runtime GET/POST API、趋势读模型 `GET /v1/fidelity-checks/runtime/trend`、worker `--persist-analysis` 自动生成、报告 Method Disclosure 复用、Runtime Console 展示 status/mismatch/difference/payload hash、趋势方向、sampled/total、平均/峰值差异率、趋势窗口、查询路径和 `api_browser_fidelity_checked` 审计事件已落；`--include-browser-fidelity-fixture` 已可生成同 prompt/city 的 official_api + browser fixture 配对样本并得到 `sampled`，browser fidelity samples 通过 `score_input_policy` 排除出主评分分母；`PlaywrightChatGPTSearchCollector`、`--include-browser-fidelity-playwright` 和 `make api-browser-fidelity-preflight` 已接入真实浏览器抽检入口，可在缺启用开关/selector/session/Playwright 时采集前 fail 并输出 health reason；配置 `GENO_BROWSER_ARTIFACT_DIR` 和对象存储时，本地 `file://` browser HTML/PNG 会归档为 `s3://...` EvidenceAsset 并写 `browser_capture_assets_archived` 审计事件；`BrowserFidelitySamplingPlan`、`--plan-browser-fidelity-sampling` 和 `make browser-fidelity-plan` 已提供确定性抽样计划与 `browser_fidelity_sampling_planned` 审计事件；`scripts/run_browser_fidelity_scheduler.py`、`make browser-fidelity-scheduler-plan/run` 和 Compose `scheduler` profile 已提供 cron/K8s CronJob 友好的轻量调度入口；真实 ChatGPT 账号/selector 联调、真实周期样本数据、复杂失败重试队列和 Temporal 深度编排待接）
- `[~]` (P1) 定时采集（Temporal）/复杂失败重试/限流/人工补录工作台 — `E3-03/06/07/08`（worker CLI 与 `--persist` 已落；worker 内 retry/backoff/rate-limit 策略已可配置并写入审计；Runtime Console 已有单条人工补录表单和项目级 Manual Backfill CSV 批量导入；分布式重试队列、复杂调度、全局限流、正式审核工作台和真实 120-row/240-run 补录执行待接）

DoD：

- `[~]` Perplexity + OpenAI 两个平台均能采到 answer+citation+截图/HTML（fixture k=3 可由 `P0ACollectionReadinessGate` 验证通过；真实 API adapter 已生成官方 API response HTML snapshot 资产，且 `--persist` 配对象存储时会归档为可追溯 `s3://...` EvidenceAsset；`make api-preflight` 已可在 key 到位后验证最小真实 API k=3 gate，且默认把预检结果写入 gitignored 的 `docs/runtime_preflight/api-preflight-latest.json` 供本地审计复盘，`make verify-api-preflight` 会复算 payload 完整性，`make preflight-manifest` 会生成 evidence package manifest，`make au-p0a-runbook` 会生成 30-run small batch 和 2400-run full batch 的命令计划，`make verify-au-p0a-runbook` 会校验命令计划完整性，`make au-p0a-env` / `make verify-au-p0a-env` 会生成并校验脱敏环境报告，`make au-p0a-environment-checklist` / `make verify-au-p0a-environment-checklist` 会把必需环境、推荐对象存储环境、setup/verification commands 和证据路径固定为可复算清单，`make au-p0a-runbook-dry-run` 会读取同源 env-file 并在不执行 provider 调用的前提下输出脱敏执行预演，`make verify-au-p0a-runbook-execution` 会复算执行预演完整性并拒绝原始 secret 字段，`make au-p0a-readiness` 会读取同源 env-file 并输出当前阶段是否可开始运行，且可显式强制只读 DB 连通性检查；`make au-p0a-package` 会汇总 environment report 和 runbook execution 在内的当前证据包缺口和 ready 状态，`make verify-au-p0a-package` 会复算证据包完整性，`make au-p0a-status` 会用同源 env-file 复算 readiness 并生成 completion/design-ready 百分比、remaining blockers 和 next action，`make verify-au-p0a-status` 会复算总状态报告完整性；`preflight_summary.ready_for_design_partner=true`、`preflight_audit_checklist.overall_status=pass` 且 `python3 scripts/verify_preflight_payload.py --require-design-partner-ready` 通过才可作为扩大真实批次的前置证据；真实外部 API 凭证批次待跑；消费者界面截图仍通过浏览器抽检补足）
- `[~]` P0a 真实批次总控执行清单已落：`make au-p0a-execution-checklist` / `make verify-au-p0a-execution-checklist` 会把 setup、execution、verification、package/status hard gates、credential handoff、real batch phase handoff 和 12 个证据输出路径合成可复算 JSON，并通过 `GET /v1/p0a-execution-checklist/au`、handoff dossier 与 Runtime Console 展示；handoff dossier 与 Console 已上提 credential handoff ready/missing required/redaction，以及 real batch phase handoff ready/next phase/blocked phase count。当前 `next_phase=preflight`，仍需真实 `.env.au-p0a`、provider preflight、30-run small batch 和 2400-run full batch 证据才能把 `ready_for_design_partner` 推到 true。
- `[x]` 每条记录平台/surface/access_method/city/language/device/时间/collector_version/collector_backend_id
- `[x]` 记录 answer_present/surface_triggered；P0a 每 prompt k=3
- `[~]` 采集事件、采集批次摘要、人工补录事件和实体别名确认事件写 AuditEvent；原始证据能通过 EvidenceLink 关联到后续报告和评分（采集完成/失败审计、`CollectionRunSummary` 与 `collection_run_summarized` 审计、人工补录写入 `AnswerRun/RawAnswer/AnswerCitation/EvidenceAsset/CollectorLog/CollectionCost/AuditEvent` 已落；实体别名确认写入 `EntityAlias/AuditEvent` 已落；confirmed alias 已进入 `rule_based_v2_aliases` parser 和 `AnswerAnalysis`；alias candidates 已能带回 answer/citation 证据引用；批量消歧队列已支持确认当前可见候选并逐条写 `entity_alias_confirmed`；候选 `needs_review/rejected/approved` 决策会写入 `entity_alias_candidate_reviews` 和 `entity_alias_candidate_review_recorded`，批量 review/reject 会额外写 `entity_alias_candidate_batch_reviewed`，rejected 候选默认从候选列表隐藏，但可通过审核历史 API/Console 恢复为 needs review 或改判 approved；候选审核历史可保存 assigned_to/priority/due_at、按 assigned_to/assignment_status/priority/due_before 查询待办、通过 `GET /v1/entity-aliases/runtime/candidates/assignment-stats` 聚合 total/active/unassigned/overdue/due soon/status/priority 分布，并把分配项推进为 in_progress/blocked/completed 且写 `entity_alias_candidate_assigned`；`POST /v1/entity-aliases/runtime/candidates/assignment-notifications` 可把 overdue active assignment 入队为 `entity_alias_assignment_overdue` notification，并写 `runtime_notification_created` / `runtime_notification_delivery_queued`）
- `[~]` 每个采集器写 CollectionCost，并有 CollectionRunSummary 估算单项目 2400 planned_runs 的成功率、触发率、回答率、失败摘要、总成本、单位成本、总耗时和平均耗时（真实外部采集凭证联调待接）
- `[x]` 采集后端可插拔：新增后端只实现 CollectorBackend，不改业务代码

### M2b · Phase 2b：Google AIO / AI Mode Spike（P0b，高风险限时）

> 出口标准：对 Google AIO / AI Mode 输出明确 pass/fail gate。通过后进入 P0c 主评分；未通过时只进入 limited coverage 附录，不阻塞 P0a 和 P0c 报告链。

任务：

- `[~]` (P0b·spike) `PlaywrightGoogleAIOCollector`：SERP 内嵌 AIO 采集，记录触发状态、截图/HTML、失败原因 — `Step4`（selector-driven Playwright adapter、health gate、HTML/screenshot hash、fake browser 合同测试、默认关闭的 `.env.au-p0b-google.example`、`make verify-au-p0b-google-env-template` 模板 gate、脱敏 env readiness report、单样本 smoke runner/verifier 已落；澳洲真实 Google selector、账号/session、env ready、smoke success 和 240-run 待跑）
- `[~]` (P0b·spike) `PlaywrightAIModeCollector`：AI Mode 独立界面采集，记录账号状态、地理、设备、失败原因 — `Step4`（selector-driven Playwright adapter、health gate、HTML/screenshot hash、fake browser 合同测试、脱敏 env readiness report、单样本 smoke runner/verifier 已落；真实 AI Mode 入口、selector、账号/session、env ready、smoke success 和 240-run 待跑）
- `[~]` (P0b·spike) `ThirdPartySerpCollector`：至少接入一个第三方 SERP/AI-answer 供应商做对照 — `Step4`（provider-neutral JSON adapter、health gate、snapshot hash、响应解析测试、独立 `google-serp-fixture` / `google-serp-spike` 120-run comparison entry、Make 入口、payload verifier、manifest 与 status/hash verifier 已落；真实供应商凭证/endpoint 联调、真实 comparison 120-run 与默认 240-run 待跑）
- `[~]` (P0b·spike) `ManualBackfillCollector`：人工补录最小路径，保证样本可审计 — `Step4`（JSONL file-backed collector、health gate、顺序消费 k 样本、120-row template generator、strict coverage verifier、verification hash artifact、runbook/status manual gate、runtime manual backfill 单条 API、`POST /v1/evidence-runs/runtime/manual-backfill/import.csv` 项目级 CSV 批量导入和 Runtime Console Manual Backfill CSV 表单已落；P0b execution checklist 已新增 `manual_backfill_handoff`，固定模板/manifest/verification artifact、120-row 预期、60 个 prompt-city 覆盖、缺失原因、file/verification hash、验证命令和 raw answer/citation/asset URL 禁止落入交接 JSON 的脱敏边界，并上提到 handoff dossier、Markdown 与 Runtime Console；CSV 导入会先整批校验 prompt 存在和项目归属，通过后逐条写 `manual_backfill_recorded` 并追加 `manual_backfill_batch_imported` 批次审计；真实 120-row/240-run 人工补录文件和 Google spike 运行仍待准备）
- `[x]` (P0b·spike) Google spike 采样：30 prompts × 2 surfaces × 2 geo（Australia + Sydney）× k=2 = 240 planned_runs — `Step4 / Step9.3`
- `[x]` (P0b·spike) 失败分类：not_triggered / layout_changed / blocked / timeout / geo_mismatch / account_state — `Step4`
- `[~]` (P0b·spike) pass/fail gate 报告：成功率、触发率、截图/HTML 样本、成本/耗时、推荐路径 — `Step4 / Step13`（Google spike 结论由 `GoogleSpikeGateResult` 与 `GoogleSpikeReadinessGate` 承载；browser-only fixture 会通过 AIO 成功率 gate 但 fail 两路径 readiness gate；真实 `google-spike` worker 模式、Playwright env readiness、smoke gate、manual backfill strict verification gate、health-only/full spike Make 入口、runbook、dry-run、status report、evidence package、execution checklist、P0b environment handoff ready/missing/redacted 摘要、P0b manual backfill handoff rows/prompt-city/missing/redacted 摘要、P0b Google phase handoff next/blocked/full spike planned runs、handoff dossier/Console 上提与 hash verifier 已落，真实 spike 报告待跑）

DoD：

- `[~]` 至少两条 Google 采集路径完成对照（自建浏览器、第三方 API、人工补录三选二）（readiness gate 已可执行；browser+third_party fixture 可 pass；真实 `google-spike` 默认验证 browser + manual 两路径；manual 120-row strict verification artifact 已接入 runbook/status；第三方 API adapter 与独立 120-run 对照切片已实现，真实 supplier health/collection 待跑）
- `[x]` 每条结果可靠记录 surface_triggered / answer_present

补充状态：人工补录运营路径已新增 Runtime CSV 批量导入，但它是把已整理好的人工答案写入标准 RawEvidence/AuditEvent 链路，不替代 P0b 的 `MANUAL_BACKFILL_PATH` JSONL strict verifier。P0b execution checklist 现在还会把 Google smoke/full-run 所需的开关、DB、manual path、selector groups、文件 gate、Playwright dependency 和 env-file hygiene 合成为脱敏 `environment_handoff`，并由 AU handoff dossier 与 Runtime Console 展示 `ready/missing/redacted`；同时新增 `manual_backfill_handoff`，把 120-row 模板、manifest、verification artifact、预期/实际记录数、prompt-city 覆盖、缺失原因和脱敏策略做成可复算交接摘要，不保存人工答案、citation URL 或资产 URL 原文；`google_spike_phase_handoff` 则按 `environment -> browser_smoke -> manual_backfill -> health_check -> full_spike -> main_scoring` 固定阶段顺序、命令、证据、ready/can-start、blocking reasons 和 next phase。真实 Google spike 仍要求 30 prompts × Australia/Sydney × k=2 的 manual 样本填充、strict verification artifact、browser/manual 两路径 readiness gate、health/full spike manifest 和最终 status/package hard gate。

补充状态：AU external dependency handoff 已把 P0a provider credentials、P0a real batches、P0b Google environment、P0b manual backfill 和 P0b Google phase execution 汇总为独立可复算交接包，并新增 dry-run clearance execution。该 dry-run 只读取 handoff 与 clearance sequence，输出当前会执行的一步和后续 blocked 步骤，不运行 provider、数据库、Google browser/manual 或客户交付命令；verifier 会复算执行 hash、handoff verifier、step order、`would_execute` 唯一步骤、hard gate 与 forbidden raw field。`GET /v1/external-dependency-clearance/au` 与 Runtime Console 的 Clearance dry-run 卡片会展示同一执行预演，包括 mode/status、ready-to-execute、handoff ready、当前步骤、would-execute 步骤、next command 和 hard gate。当前 29 个 blocker、5 个 dependency group、6 个 clearance step、下一项 `p0a_provider_credentials` 仍保持 blocked，dry-run 当前 `would_execute_step_count=1`、`next_command=make verify-au-p0a-env-template`，避免把“交接清楚/可开始第一步”误判成“外部依赖完成”。
- `[x]` 至少一个 google_aio 后端在同一窗口完成 >= 80% 计划样本，才允许进入主评分
- `[x]` 未达标时，Google 只进 limited coverage 附录，报告明确标注不进入主评分分母（评分层 `score_input_policy` 会把未同时通过成功率 gate 与两路径 readiness gate 的 Google run 排除出 `VisibilityScoreSnapshot.answer_run_ids`）

### M3 · Phase 3：Answer Parser + AUVisibilityScore（P0a）

> 出口标准：自动解析六要素 + 生成可拆解可版本化评分 + 双分母口径 + 能点回 AnswerRun。

任务：

- `[x]` (P0a) ParserEngine 接口 + 规则解析实现（brand/competitor/recommend/rank/sentiment/local_relevance/citations…）— `Step7 / E4-01..06`
- `[~]` (P0a) 实体/别名表 + 同名消歧人工确认 — `§8.14 / Step7`（实体契约、runtime entity alias confirm API、计算型 alias candidate API、基于 `raw_answers.answer_text` 与 `answer_citations.domain/url` 的证据驱动候选推荐、控制台最小确认表单、候选确认按钮、批量候选审核队列、候选审核决策持久化、批量 needs-review/reject、审核历史 API、rejected 候选恢复/改判入口、轻量 reviewer/priority/due date 分配、assignment queue read filters、stats/overdue/due soon 可见性、claim/release、overdue notification enqueue、状态推进动作、`entity_alias_confirmed` / `entity_alias_candidate_review_recorded` / `entity_alias_candidate_batch_reviewed` / `entity_alias_candidate_assigned` / `entity_alias_candidate_assignment_actioned` / `runtime_notification_created` / `runtime_notification_delivery_queued` 审计和 `rule_based_v2_aliases` parser 使用 confirmed aliases 已落；完整审批流、完整 SLA 自动升级、值班升级、专用 reviewer workbench 和黑箱 NLP 自动消歧仍待接）
- `[x]` (P0a) ScoringFormula 接口 + `SCORE_FORMULA_REGISTRY`：active `au_visibility_v1` 与 candidate `au_visibility_v1_1_local_boost`（8 项，权重和 1.00，版本化，可按旧版本重算）— `Step9 / E4-08`
- `[x]` (P0a) 双分母：Trigger Rate vs Mention/Recommendation Rate — `Step9.2`
- `[x]` (P0a) k 次聚合 + 均值/离散度；P0a k=3，Google spike k=2 且单独标注 — `Step9.3`
- `[x]` (P0a) VisibilityScoreSnapshot 聚合表（project/platform/city/intent/prompt），并支持 worker `--persist-analysis` 写库与 runtime score API 查询 — `§8.13`
- `[x]` (P0a) ScoreContribution 分数解释包：子指标贡献、权重、分母、正负证据、局限说明；runtime score API 可读回贡献项、关联 prompt/answer run/analysis/audit，Runtime Console 已展示完整评分解释包、parser A/B agreement 和权重快照 — `Step5.1 / §8.18`
- `[~]` (P1) LLM-as-judge 解析实现（与规则 A/B）— `Step7`（`llm_judge_fixture_v1` 本地 judge、`ComparativeAnswerParser`、`parser_ab_compare_v1` payload、`llm_judge_prompt_v1`、`llm_call_logs` 调用日志和 `LiteLLMGateway` adapter 已落；LiteLLM retry/backoff、响应 cost 读取和 `llm-gateway` Compose profile 已可测；真实 provider key 联调、供应商账单 reconciliation 和人工抽检待接）
- `[~]` (P1) 评分权重可配置 + 审计；人工复核留痕 — `E4-10/11`（项目级 `score_weight_configs`、runtime GET/POST API、`score_weight_config_saved` 审计、`/v1/score-formulas/runtime` 公式目录、Runtime Console 公式版本选择、worker `--persist-analysis --score-formula-version` 读取配置和 `VisibilityScoreSnapshot.component_weights_snapshot` 冻结已落；通用 `human_review_records`、runtime GET/POST API、`human_review_recorded` 审计和 Runtime Console Human Review Trail 已落；`GET /v1/human-reviews/runtime/queue` 已从 score snapshot 与 content draft 聚合待审对象并展示 priority/reason/latest review/evidence refs；content draft 复核会把同项目 `content_drafts.review_status` 投影为本次复核状态并写入 `content_draft_review_status_updated` 审计；报告导出终态已接项目内 `runtime_notifications` inbox 和 read/unread 审计；复杂审批流、分配、专用审核工作台、外部通知渠道、权限后台和抽样校准待接）

DoD：

- `[x]` 自动解析提及/推荐/排名/竞品/引用/本地相关性
- `[x]` 生成可拆解、公式版本化的 AUVisibilityScore，能点回 AnswerRun
- `[x]` 任意总分/平台分/城市分/intent 分都有 ScoreContribution 解释包
- `[x]` 报告能区分 Trigger Rate 与 Mention/Recommendation Rate
- `[x]` 评分公式可升级，历史分数按旧版本可重算
- `[~]` 规则解析与 judge 解析可对同一答案并行对比并保留版本（fixture judge + gateway 调用日志 + LiteLLM adapter + 人工复核记录和只读 Review Queue 已落；真实 LiteLLM 服务联调、抽样复核校准与解析器校准待接）

### M4 · Phase 4：Citation Graph + Competitor Benchmark（P0c）

> 出口标准：生成信源图谱与 source gap，输出 3–5 竞品对标，每个数字可追溯原始回答。

任务：

- `[x]` (P0c) GraphStore 接口 + PG 邻接表实现 + SourceGraph/SourceGap 表，并支持 runtime citation graph API 查询 — `Step8 / §8.9`
- `[x]` (P0c) Citation Graph 输出：常被引/竞品独占/过旧/本地缺失信源；worker `--persist-analysis` 已写入 source graph evidence 与 source gaps — `Step8`
- `[x]` (P0c) CompetitorBenchmark（3–5 竞品：mention/recommend/position/citation overlap/local relevance…），并支持 runtime citation graph API 读回 — `Step10 / §8.10`
- `[~]` (P0c) 监测看板：总览/平台对比/竞品对比/问题明细/风险 — `E5-01..05`（Runtime Console 已展示 Project Bootstrap、Runtime Filters、Prompt Pack、Question Detail 问题明细矩阵、Evidence Runs 明细、Score Contributions 完整解释包、Citation Graph & Competitors 明细、Citation Graph Map、source gaps、actions、Runtime Alerts、Content Engine Detail、Traceability Detail、Traceability Map、节点级 details 钻取和页面内锚点深链路；Question Detail 会按 prompt 聚合当前筛选 evidence window，展示覆盖率、platform gap、trigger/answer/source gap、平台/城市/access method 分布、引用/资产/审计计数和最新运行）
- `[~]` (P1) 看板筛选与导出 — `E5-06`（platform/evidence city/intent_type URL 查询筛选已接入 evidence runtime API 与控制台，prompt runtime API 同步按 intent_type 筛选；evidence 支持 `collected_at/cost/citation/audit` 受控排序；筛选后 evidence CSV 导出已落并带 hash/sort header；Runtime Saved Views 已可保存筛选、排序、query/export path 并写入审计事件；Report Snapshot 与 Report History 的 Markdown/CSV/PDF/White-label PDF artifact 下载已继承当前筛选/排序并返回 filter hash、template hash、sort、row/total count header；report artifact signed URL endpoint 已用 HMAC 保护 type/template/filter/sort/actor 与过期时间；项目级 Brand Kit 白标默认值、Logo 上传与 Theme Editor 预览已接入）
- `[~]` (P1) GraphStore 切 Neo4j 验证可插拔 — `架构验收`（本地 PG 邻接表 projection 与 Neo4j 节点/关系 projection 的 GraphStore contract test 已落；真实 Neo4j driver/container、Cypher query 和迁移回归待接）

DoD：

- `[x]` 生成 Citation Graph，识别 source gap
- `[x]` 输出 3–5 竞品 Benchmark
- `[x]` 每个分数/引用可点回原始回答

### M5 · Phase 5：Evidence Report Export（P0c）

> 出口标准：导出客户可审计 PDF/CSV，含方法说明与原始证据附录，每个数字可追溯。

任务：

- `[~]` (P0c) ReportExporter 接口 + Markdown/CSV/PDF 导出（方法说明 + 证据附录）、runtime report API、Markdown/CSV/PDF artifact 下载、附录级筛选/排序下载、项目级 Brand Kit 默认值、白标 PDF 模板 renderer 与 MinIO/S3-compatible artifact 归档；`make runtime-e2e` 已验证报告 Markdown/PDF/CSV artifact 在真实 MinIO profile 中存在；Brand Kit Logo 上传已归档到 MinIO/S3-compatible bucket 并写 `project_brand_logo_uploaded` 审计事件，Runtime Console Theme Editor 已复用 Brand Kit 字段提供白标预览和最近审计摘要；审计型 Brand Assets 版本列表与历史 Logo 激活已接入 `project_brand_logo_version_activated` — `Step13`
- `[x]` (P0c) ReportExport 快照：冻结 score_snapshot_ids、answer_run_ids、prompt_version、公式版本、平台权重、采样窗口；worker `--persist-analysis` 已写入不可覆盖版本，runtime report API 可读回 — `Step5.1 / §8.17`
- `[~]` (P0c) 报告展示：采集窗口/平台覆盖/access_method/样本量(k)/离散度/双分母/公式版本/API-界面差异抽检结论/Google spike pass/fail/limited coverage/审计摘要/分数解释包/非确定性说明 — `Step13`（Runtime Console 已展示 Report Snapshot、Report History 与 Report Method & Evidence Appendix：冻结 methodology hash、采样窗口、平台/access method/city 覆盖、样本量、双分母评分、离散度、公式版本、平台权重、Method Disclosure、证据附录、citation/audit 摘要、API-vs-browser fidelity 最近检查和趋势摘要、历史 report version/exported_at/object store URL/artifact path、白标 PDF template path 与项目级 Brand Kit 默认值；标准/运行时报告 artifact 已写入 Google gate/limited coverage/API-vs-browser fidelity/access distribution/score rate denominators 方法披露，`report_exports.method_disclosure` 已冻结同一快照供 runtime artifact/console 复用；`make au-p0c-report-package` / `make verify-au-p0c-report-package` 已把 P0c 报告 artifact hash、方法披露、审计摘要、white-label PDF 与 traceability 合同合成可复算交付包，且 AU launch status 已引用该 P0c 包；`make au-launch-remediation-plan` 已把 launch blocker 映射到命令、验证命令、证据产物和外部依赖类型，避免真实联调只看到 fail 而没有清障路径；核心方法/分数/证据/source gap/competitor/report snapshot、基础 PDF artifact 与项目级白标 PDF artifact 已落；真实 Google 运行结论和真实 API-vs-browser 周期抽检数据待接）
- `[~]` (P1) 代理商工作流：多客户/多项目/白标/导出历史 — `Step13`（当前已接入 Runtime Console 项目下拉/URL `project_id` 选择，并把 brand kit/prompt/evidence/export/alias/saved view/score/graph/report/action/content/traceability read path 收敛到选中项目；已接入客户项目创建表单、Project Metadata 项目资料编辑表单、Project Members 成员读取/upsert/delete 表单、成员邀请创建/撤销/过期/发送/接受表单和 `/invite/accept` 独立接受页、项目级 Brand Kit 表单、Logo Upload raw-body 上传、Brand Assets 审计型版本列表、历史 Logo 激活、Theme Editor 白标预览、Asset Register / Asset Library 素材登记和 `project_brand_assets` 专表读取、Report History/导出历史面板和 `template=white_label` PDF 下载，按 `project_id` 读取最近 5 个 `ReportExport` 并展示冻结 URL、artifact 下载、白标 artifact path、signed URL 生成入口和报告审计摘要；Report History 已可对冻结报告追加 `internal_review/client_ready/archived` 管理事件，写入 `report_export_management_recorded` 审计但不更新 `report_exports`；报告导出任务已用 `report_export_jobs` 落最小持久化队列，Runtime Console `Report Export Queue` 可排队 Markdown/CSV/PDF/white-label PDF 导出、查看最近任务、推进 `queued/running/succeeded/failed/dead_letter/cancelled` 状态，并展示 attempt/lease/next attempt、queued/running/dead_letter、可重试、过期 running 和最早排队时间；终态任务会写入 `runtime_notifications`，Runtime Console `Runtime Notifications` 可展示 unread count、severity、target、payload 摘要并支持 read/unread 标记，审计事件为 `runtime_notification_created` / `runtime_notification_status_updated`；Runtime Alert 通知与最小 SLA 升级已落显式入队和计划任务入口：`POST /v1/runtime-alerts/notifications` 可把未 resolved/snoozed 的派生 alert 入队为 `runtime_alert` notification，`make runtime-alert-notification-worker` / Compose `scheduler` profile 可按 AU market 或显式项目扫描并复用同一入队逻辑；`make runtime-alert-escalation-worker` / Compose `scheduler` profile 可按默认 `critical=4h/high=24h` 阈值扫描 overdue critical/high alerts，并追加 `runtime_alert_events.status=escalated`；外部 webhook/Slack Incoming Webhook 已落最小投递链路：`runtime_notification_subscriptions.channel` 支持 `webhook` 与 `slack`，保存项目级 endpoint、event types、severity threshold 和 active/paused/disabled 状态，`runtime_notification_deliveries` 为匹配通知生成 queued delivery 并写 `runtime_notification_delivery_queued`；`channel=webhook` 投递 canonical GENO JSON 并可带 HMAC 签名，`channel=slack` 入队时冻结 `slack.text/blocks/metadata`，worker 只把 Slack payload POST 到 Incoming Webhook URL；`make notification-delivery-worker` 领取 queued/lease 过期 delivery 后记录 response status/body hash/error，失败可按 backoff 重新 queued，达到 max attempts 后进入 `dead_letter`；`make report-export-worker` 已提供单次执行 worker，用 `FOR UPDATE SKIP LOCKED` 领取可执行 queued job 或 lease 过期 running job，渲染 runtime report artifact，配置对象存储时归档到 `s3://.../report-artifacts/...`，未配置对象存储时写 `runtime-report-artifact://...` fallback URL，失败按 retry backoff 重新排队，达到 max attempts 后进入 `dead_letter`；`/artifact/signed-url` 已可在配置 `GENO_REPORT_ARTIFACT_SIGNING_SECRET` 后生成带 `expires_at/signature` 的 HMAC 下载 URL，保护 report/type/template/filter/sort/actor 参数不被篡改；Logo 上传会先写对象存储 `brand-assets/<project_id>/...`，再把 `s3://...` 写回 Brand Kit、同步 upsert `project_brand_assets`（`asset_type=logo/category=brand_logo/status=active/content_hash/storage_version`），并记录 `project_brand_logo_uploaded` 与 `project_brand_asset_registered` 审计；Brand Assets 从 `project_brand_logo_uploaded` / `project_brand_logo_version_activated` 审计事件读回 Logo 版本，激活历史 URI 时只更新 `project_brand_kits.logo_url` 并追加审计；Asset Library 通过 `GET/POST /v1/project-brand-assets/runtime` 登记和读取项目素材，冻结 asset URL、preview URL、分类、状态、content hash、storage version 与 metadata；`POST /v1/project-brand-assets/runtime/{asset_id}/scan-status` 已可记录 `pending/passed/failed/skipped` 扫描状态、checked time、method version 和 notes，并追加 `project_brand_asset_scan_recorded` 审计；Theme Editor 使用 `client_name/prepared_by/logo_url/primary_color/secondary_color/footer_text` 预览报告头、指标条、页脚、white-label artifact path 和最近 `project_brand_kit_saved/project_brand_logo_uploaded` 审计；API 级 project_members 门禁、header/HS256 JWT/inline、remote 或 OIDC-discovered JWKS RS256 actor 解析与显式 stale-if-error 兜底、owner/admin/analyst/viewer 最小角色矩阵、成员删除审计、最后 owner 保护和 project member invitation hash-only 创建/撤销/过期/接受审计已可通过环境变量开启，覆盖项目列表、项目创建、成员管理/邀请、主要项目级读写接口、管理写操作、分析复核写操作、report artifact/report management event/report export job/runtime notification/traceability/alias confirm 对象反查校验；PostgreSQL RLS/GUC 最小项目隔离、`geno_runtime_app` runtime role 和 API connection pool 已落；Temporal/长驻 worker、并发池、邮件模板/退订/服务商 API、Slack App/OAuth/交互式消息、复杂 SLA/值班升级、图片裁剪、真实病毒扫描引擎、对象存储原生版本、CDN 签名、Keycloak/OIDC login/session、前端 SSO 登录流、账单隔离、生产压测与客户级授权流转待接）

DoD：

- Webhook 签名补充：`make notification-delivery-worker` 已支持出站 HMAC 签名。订阅 metadata 只保存 `signing_secret_env` / `webhook_signing_secret_env` 环境变量名，worker 从订阅指定 env 或默认 `GENO_NOTIFICATION_WEBHOOK_SIGNING_SECRET` 读取 secret，不把 raw secret 写入数据库；签名覆盖 `timestamp.delivery_id.notification_id.payload_sha256`，请求携带 `x-geno-signature`、`x-geno-signature-timestamp`、`x-geno-signature-version` 和 `x-geno-signature-input`。`geno_core.webhook_signing` 已提供同口径签名、payload hash 与接收端验证 helper，可校验必需 header、HMAC、payload hash 和 timestamp tolerance。订阅指定 env 缺失时 delivery 会按现有 retry/dead-letter 状态机处理并写入审计；secret rotation、双 secret 轮换窗口、托管验证服务、订阅鉴权回调、邮件模板/退订/服务商 API、Slack App/OAuth/交互式消息仍待接。Slack Incoming Webhook 通道已复用 delivery 队列并发送 Slack payload，SMTP email 通道已复用同一队列并发送冻结 email payload；两者都不使用 GENO HMAC header。
- `[~]` 可导出含方法说明和证据附录的 Markdown/CSV/PDF，并通过 runtime report API 读取冻结快照和项目内报告历史、通过 artifact API 下载 Markdown/CSV/PDF/White-label PDF；artifact API 支持按当前 `project_id/platform/city/intent_type/status/sort` 即时过滤/排序证据附录并返回 hash、template hash 与 row count，不改写冻结报告；白标 PDF 可从项目级 Brand Kit 默认读取客户名、服务商名、Logo URL、主题色和页脚；worker 可把 Markdown/CSV/PDF 归档到 MinIO/S3-compatible bucket，且 `make runtime-e2e` 已验证临时 MinIO 中存在三类 report artifact；Runtime Console 已展示项目级查询路径、Brand Kit 表单、Logo Upload、Brand Assets 审计型版本列表、Asset Register / Asset Library、Theme Editor 白标预览、报告方法说明、证据附录详情、Report History、导出历史管理事件、signed artifact URL、白标 PDF 下载、Report Export Queue 和 Runtime Notifications；Asset Library 已展示 preview URL 与 scan status，并可通过 scan status 表单写入 `project_brand_asset_scan_recorded`；Report History 可追加 `internal_review/client_ready/archived` 审计状态；报告导出任务队列已用 `report_export_jobs` + `report_export_job_queued/status_updated` 最小落地，`make report-export-worker` 可领取 queued/expired running job、渲染 runtime artifact、归档或写 fallback URL、按 backoff 重试并在 max attempts 后进入 dead_letter，队列 stats endpoint 已展示核心积压指标，项目内 `runtime_notifications` 可展示终态通知并标记 read/unread，`runtime_notification_subscriptions/deliveries` 与 `make notification-delivery-worker` 已覆盖终态通知的 webhook/Slack Incoming Webhook/SMTP email 订阅、入队、投递、重试和 dead-letter 审计，`make runtime-alert-notification-worker` 已覆盖 Runtime Alert 通知的计划任务入队入口，`make runtime-alert-escalation-worker` 已覆盖 critical/high overdue alert 的最小 SLA 升级事件入口；Temporal/长驻 worker、并发池、复杂 SLA/值班升级、邮件模板/退订/服务商 API、Slack App/OAuth/交互式消息、图片裁剪、真实病毒扫描引擎、对象存储原生版本/CDN 仍待接
- `[x]` 报告每个数字可追溯 answer_run_ids、prompt metadata、score snapshot 和 citation graph
- `[x]` 报告导出写 AuditEvent；重复导出生成新 ReportExport 版本，不覆盖旧报告

### M6 · Phase 6：Action Plan + 复测（P1）

> 出口标准：缺口转可执行任务；按 T+7/14/30 复测并展示前后变化。

任务：

- `[x]` (P1) ActionRecommendation：缺口转任务，绑 evidence/source_gap/related_runs/owner/next_check — `Step11 / §8.11`
- `[~]` (P1) 复测调度 T0/T+7/T+14/T+30（Temporal 可重放，同 prompt_version、同 k）— `Step14`（同口径 RetestSchedule 已落，worker `--persist-analysis` 可写入并通过 runtime action plan API 读回；`make au-retest-scheduler-plan` / `make verify-au-retest-scheduler-plan` 已生成并复算 AU 复测执行契约，固定 T0/T+7/T+14/T+30、`au_dtc_ecommerce_v1`、100 prompts、4 geo、P0a 两平台、k=3、每窗口 2400 planned runs、总计 9600 planned runs、collection/manifest 命令、证据输出和当前边界；`make au-retest-execution-status` / `make verify-au-retest-execution-status` 已逐窗口检查 payload 与 manifest 是否存在、hash 是否有效、design-partner gate 是否通过，并输出 `retest_execution_ready`、`comparison_allowed`、missing artifacts、next action 和 `retest_execution_status_hash`；当前未跑真实 T 窗口时状态正确停在 `status=fail` / `missing_artifact_count=8` / `run_retest_window:baseline`；`GET /v1/au-retest-scheduler-plan`、`GET /v1/au-retest-execution-status` 与 Runtime Console AU Retest Scheduler Plan 区块已展示 plan hash、执行状态、窗口 readiness、缺失 artifact 和 Temporal/真实运行边界；Temporal 可重放调度和真实 T 窗口外部采集待接）
- `[x]` (P1) 前后窗口对比 + 趋势聚合，保留全部 raw runs；worker `--persist-analysis` 已保存 RetestComparison，runtime action plan API 可读回 comparison、prompt metadata 与 action/retest audit events，Runtime Console 已展示 baseline/retest score、score delta、trend 和 action/retest audit trail — `Step14`
- `[~]` (P1) 预警：负面/品牌缺失/竞品压制 — `E9-04/05/06`（Runtime Alerts 已形成派生 alert + append-only 管理事件 + 显式通知入队 + cron-friendly 扫描入口最小闭环：`GET /v1/runtime-alerts` 从最新 `VisibilityScoreSnapshot`、`ScoreContribution`、`AnswerAnalysis`、`SourceGap`、`CompetitorBenchmark` 和 `ActionRecommendation` 生成 `brand_absent`、`low_recommendation_rate`、`negative_sentiment`、`source_gap`、`competitor_pressure`，返回 severity/threshold/metric/evidence refs/related actions/audit refs/management events；`POST /v1/runtime-alerts/{alert_id}/events` 可追加 acknowledged/resolved/snoozed/reopened/escalated 到 `runtime_alert_events` 并写 `runtime_alert_event_recorded`，不改写派生 alert 或原始证据；`POST /v1/runtime-alerts/notifications` 可把当前未 resolved/snoozed 的 alert 显式入队为 `runtime_alert` notification，并复用 webhook/Slack Incoming Webhook/SMTP email subscription/delivery 队列；`make runtime-alert-notification-worker` / Compose `scheduler` profile 可按 AU market 或显式项目扫描并批量触发同一入队逻辑；`make runtime-alert-escalation-worker` / Compose `scheduler` profile 可按默认 `critical=4h/high=24h` 阈值扫描 overdue critical/high alerts，跳过 acknowledged/resolved/snoozed/escalated，并追加 `escalated` 事件；Runtime Console 已展示 Runtime Alerts、管理历史、事件记录表单、手工 Escalate 选项和通知入队按钮；邮件模板/退订/服务商 API、Slack App/OAuth/交互式消息、复杂值班升级和 Temporal 工作流待接）

DoD：

- `[x]` 基于 source gap 生成 Action Plan
- `[x]` 任务有 owner/status/next_check_date
- `[~]` 按 T+7/14/30 复测，报告展示前后变化（runtime action plan API 已返回 RetestSchedule、RetestComparison、关联 AnswerRun/PromptQuestion 与 audit events；AU retest scheduler plan 已把 T0/T+7/T+14/T+30 的 replay key、collection/manifest 命令和证据输出固定为可复算 JSON；AU retest execution status 已把每个窗口的 payload/manifest 存在性、hash、design-partner gate、missing artifact count、comparison allowed 与 next action 固定为可复算 JSON；Runtime Console 已展示 Action Plan & Retest Detail、AU Retest Scheduler Plan 和 AU Retest Execution Status；Temporal 调度、真实 T 窗口采集和报告模板正式嵌入待接）

### M7 · Phase 7：Knowledge Facts + Content Engine + Integrations（P2）

> 出口标准：本地事实库可检索；内容生成绑证据并过审核；接入主要英语市场工具。

任务：

- `[~]` (P2) LocalizedKnowledgeFact 本地事实库 + VectorStore 检索（AU 优先，回退 global 标记）— `Step12 / §8.12 / E6`（内存检索 fixture 已落；worker `--persist-analysis` 已写入 PostgreSQL，保存内容引擎时同步 upsert `knowledge_fact_embeddings`，使用 `fixture-knowledge-embedding-v1` 写入 pgvector 并生成 `knowledge_fact_embeddings_indexed` 审计事件；runtime content engine API 可读回 facts，`/v1/knowledge-facts/runtime/search` 可按 project/query/market/city 做 pgvector `<=>` 检索并返回 AU/global fallback 标记，Runtime Console Content Engine Detail 已展示 pgvector Knowledge Search；真实 embedding provider、Qdrant/Milvus adapter 和在线内容 RAG 策略待接）
- `[~]` (P2) Content Engine：基于 source/prompt gap 生成 FAQ/comparison/schema/landing outline，绑 evidence 并过人工审核 — `Step15 / E7`（证据绑定草稿已落，worker 可持久化并通过 runtime content engine API 读回 draft -> fact/action/prompt/answer_run/manual distribution，Runtime Console 已展示草稿、target questions、evidence runs、source action、人工分发记录和草稿级 audit events；Human Review Trail 已可对 content draft 记录审核状态、decision 和审计事件，并将 `content_drafts.review_status` 投影为本次复核状态；Review Queue 已把 `pending_human_review` / `needs_changes` content draft 纳入待审列表，已审核草稿会进入 reviewed 队列；LLMGateway 真实生成、专用审核工作台和发布审批流待接）
- `[~]` (P2) Integrations：GSC/GA4/Shopify/WordPress/Webflow/HubSpot/Cloudflare — `Step15`（connector 计划对象已落，worker 可写入并通过 runtime content engine API 读回，Runtime Console 已展示 provider/status/auth/capabilities；真实 OAuth/API 接入待接）
- `[~]` (P2) 更广平台：Gemini/Copilot/Claude/YouTube/Reddit/ProductReview — `§4.3`（AU MarketProfile 已登记 6 个候选 surface：Gemini Search、Bing Copilot Search、Claude Search、YouTube Search、Reddit Search、ProductReview reviews，全部默认 `enabled=false` 且 `weight=0.0`，不影响 P0a planned runs 或主评分分母；`make au-broader-platform-registry` / `make verify-au-broader-platform-registry` 已生成并复算候选平台、stage、role、access methods、required env、evidence requirements、scoring policy、recommended sequence 和 registry hash；`GET /v1/au-broader-platform-registry` 与 Runtime Console Broader Platform Registry 面板已可读取；真实采集器/来源 adapter、evidence package 和准入 gate 待接）
- `[x]` (P2) Manual Distribution Record（仅记录 URL/状态，不自动发布）— `E8`

DoD：

- `[~]` 内容生成绑 evidence/source_gap/knowledge_fact，过人工审核（runtime content engine API 已读回 evidence/fact/action/prompt/manual distribution/audit 关联，Runtime Console 已展示证据绑定细节，草稿默认 `pending_human_review`；Human Review Trail 已可记录 content draft 的人工审核留痕并投影更新草稿 `review_status`，Review Queue 已可聚合待审/已审草稿；专用审核工作台和审批流待接）
- `[~]` 本地事实库可检索、可回退标记（内存检索和 global fallback 标记已落，facts 可持久化读回并在 Runtime Console 展示；pgvector runtime search、`knowledge_fact_embeddings` 和 `knowledge_fact_embeddings_indexed` 审计已落；真实 embedding provider、Qdrant/Milvus 切换和内容生成在线检索待接）

## 4. 风险登记册

源自《首发技术路径》复核，按"何时必须处理"排期，逐条给出口判据。

| 风险 | 何时处理 | 缓解动作 | 出口判据 |
| --- | --- | --- | --- |
| 采集保真度：API ≠ 消费者界面 | M2a 起，M5 披露 | 接口化采集；官方 API 默认交付，浏览器抽检放入 P0c/P1 | `api_browser_fidelity_checks` 已作为独立运行时对象落库，冻结 status、official_api/browser 记录数、comparable pairs、mismatch count、difference rate、payload hash 和 `api_browser_fidelity_checked` 审计事件；`GET /v1/fidelity-checks/runtime/trend` 已基于最近 checks 提供 sampled/total、latest/earliest/average/max difference rate、趋势窗口和 improving/worsening/flat/insufficient_sampled_data/no_data 方向，Runtime Console 已展示趋势摘要；官方 API adapter 已生成 response HTML snapshot 资产，满足原始响应可追溯，但不冒充消费者界面截图；报告 Method Disclosure 与 Runtime Console 已展示该对象；`--include-browser-fidelity-fixture` 已可生成 paired fixture sampled 数据，且 browser fidelity samples 不进入主评分分母；`PlaywrightChatGPTSearchCollector` 已把真实 browser capture 接入 CollectorBackend 和 worker health gate，成功时输出 screenshot/html snapshot hash，未配置时采集前解释失败；本地 browser artifact 已可在对象存储配置下归档为 `s3://...` EvidenceAsset 并写 `browser_capture_assets_archived`；`BrowserFidelitySamplingPlan` 已支持按日期/周期/seed 确定性选择 prompt/city 并输出可复跑 worker args 与审计事件；轻量 scheduler wrapper、Make 目标和 Compose `scheduler` profile 已可用于 cron/K8s CronJob 生成计划并可选执行；真实账号/selector 联调、真实周期样本数据、复杂重试队列和 Temporal 深度编排待接 |
| Google AIO / AI Mode 选择性触发与采集脆弱 | M2b（spike） | 拆 AIO/AI Mode 两后端；建模 answer_present；自建/第三方/人工补录限时对比 | pass/fail gate、两路径 readiness gate、limited coverage 与 `score_input_policy` 已进入评分审计和报告 Method Disclosure；`google-spike` worker 真实模式、provider-neutral `ThirdPartySerpCollector`、独立 `google-serp-fixture` / `google-serp-spike` 120-run 对照入口、SERP comparison payload verifier/status/hash verifier、P0b runbook/dry-run/Google Playwright env readiness/smoke/manual strict verification/status/hash verifier、P0b execution checklist/hash verifier 已落；真实 Google gate 与真实 third-party supplier 对照待跑 |
| AI 非确定性导致评分噪声 | M2a–M3 | P0a k=3；Google spike k=2 单独标注；报告展示离散度；parser A/B agreement 进入评分解释 | 同 prompt 多次采样 + 置信展示 + parser agreement |
| 架构可插拔是否为真 | M0 起持续 | 接口先行；P0a 先完成接口级可插拔，深度切换演示排到 P0c/P1 | P0a Collector/Parser/Scoring/Report 已有 runtime-checkable Protocol、`NotConfigured*` stubs 和工作实现合约测试；parser rule + judge fixture 已可并行；ScoringFormula registry、候选公式、旧版本重算和 worker 公式参数已落；pgvector runtime knowledge search 已落；VectorStore 已有 pgvector 与 Qdrant projection contract test，证明 deterministic embedding search 排序口径一致；GraphStore 已有 PG adjacency 与 Neo4j projection contract test，证明 Citation Graph 关键查询口径一致；LiteLLM adapter、parser 注入、retry/backoff、响应 cost 读取与可选 Compose profile 已可测；AU broader platform registry 已把 Gemini/Copilot/Claude/YouTube/Reddit/ProductReview 作为 disabled/zero-weight 候选 surface 纳入 MarketProfile 与 runtime 可见清单；真实 Qdrant/Milvus service、真实 Neo4j driver/container、真实 LLM provider 和更广平台 adapter 联调待接 |
| 城市级地理定位实现成本 | M2a/M2b | GeoProvider 抽象（uule/代理池/供应商可换）；P0a 四地理样本可降级但保留字段 | 地理样本可区分且成本可控 |
| 单位经济不透明 | M2a 起 | CollectionCost 从首个采集器记录；CollectionRunSummary 汇总批次 planned/attempted/success/failure、触发率、回答率、失败摘要、总成本、单位成本、总耗时和平均耗时；P0a planned_runs 默认 2400，Google spike 默认 240 | 每份采集批次的成本、成功率、触发率、回答率、失败摘要、单位成本和平均耗时可估算；真实外部采集凭证联调待接 |
| 审计链/解释链断裂 | M0 起，M5/M6 验收 | AuditEvent、CollectionRunSummary、ReportExport、ScoreContribution、EvidenceLink 从 P0 建表并写入关键事件 | TraceabilityBundle fixture 已证明报告可追到原始证据；runtime project API 已支持 `project_id` 过滤，并在 `GENO_RUNTIME_PROJECT_ACCESS_CONTROL=1` 时按 actor + `project_members` 做 API 级项目成员门禁，actor 可来自默认 `X-GENO-Actor-Id`、`GENO_RUNTIME_AUTH_MODE=jwt` 下的 HS256 Bearer JWT 或 `GENO_RUNTIME_AUTH_MODE=jwks` 下的 inline/remote/OIDC-discovered JWKS RS256 Bearer JWT；PostgreSQL RLS 已通过 `geno_runtime_app` 非 bypass runtime role 和 `geno.runtime_*` session GUC 对 projects/project_members/证据/报告/评分/图谱/content/audit 等项目级表做最小隔离，runtime E2E 已验证同一 actor 只能看见自己项目；project brand kit API 已保存项目级白标默认值并写入 `project_brand_kit_saved` 审计事件；project brand logo upload 已把图片归档到 MinIO/S3-compatible bucket、更新 `project_brand_kits.logo_url` 并写入 `project_brand_logo_uploaded` 审计事件，审计 refs 含 filename/content type/content hash/logo URI；runtime prompt CSV/file import API 已写入项目级 prompt，支持 `.csv/.txt` 与 `.xlsx` 第一工作表，并生成 `runtime_prompts_imported` 审计事件；runtime prompt import history API 已从同一批 `AuditEvent` 读回 source/hash/prompt count/method version；runtime evidence API 已读回 prompt 文本并支持 `project_id`、platform/evidence city/intent_type 过滤、受控排序和即时 CSV 导出；runtime collection run API 已读回采集批次 planned/attempted/success/failure、成功率、触发率、回答率、失败摘要、总成本、单位成本、总耗时、平均耗时和 `collection_run_summarized` 审计事件；runtime fidelity check API 已读回 `api_browser_fidelity_checks` 并生成 `api_browser_fidelity_checked` 审计事件，runtime fidelity trend API 已基于同表提供可解释趋势摘要；runtime manual backfill API 已把人工答案写入标准 RawEvidence 表并生成 `manual_backfill_recorded`；runtime entity alias API 已把品牌/竞品别名确认写入 `entity_aliases` 并生成 `entity_alias_confirmed`，runtime alias candidate API 已生成可确认候选，confirmed aliases 已进入 `rule_based_v2_aliases` parser 与重跑后的 `AnswerAnalysis`；runtime saved views API 已保存项目级筛选/排序/query/export path 并写入 `runtime_saved_view_saved` 审计事件；runtime score weight config API 已保存项目级评分权重并生成 `score_weight_config_saved`，评分历史快照冻结 `component_weights_snapshot`；runtime human reviews API 已追加 `human_review_records` 并生成 `human_review_recorded`，对 content draft 目标会投影更新 `content_drafts.review_status` 并生成 `content_draft_review_status_updated`；runtime human review queue API 已从 `visibility_score_snapshots` 与 `content_drafts` 聚合待审对象并返回 priority/reason/latest review/evidence refs；runtime score API 已读回评分解释包；runtime graph API 已读回 source gap/竞品对标；runtime report API 已读回报告快照和项目内报告历史，且 `report_exports.method_disclosure.score_rate_denominators` 冻结 Trigger/Mention/Recommendation 三类 rate 的分母口径；runtime report artifact API 已支持附录级筛选/排序与项目级白标 PDF 下载并返回 filter hash、template hash、sort、row/total count；runtime action plan API 已读回 action/retest audit events；runtime alerts API 已从评分、answer analysis、source gap、竞品 benchmark 和 action 生成品牌缺失/低推荐率/负面情绪/source gap/竞品压制预警，并返回 evidence refs、related actions、评分审计 refs 与最近 management events；runtime alert event API 已追加 `runtime_alert_events` 并生成 `runtime_alert_event_recorded`，可复盘 acknowledged/resolved/snoozed/reopened/escalated 的 actor、note、metadata 和 before/after hash；runtime content engine API 已读回 fact/draft/connector/manual distribution/audit；runtime knowledge fact search API 已通过 `knowledge_fact_embeddings` 和 `knowledge_fact_embeddings_indexed` 读回 pgvector 检索结果与索引审计事件；runtime traceability API 已按 `project_id` 聚合报告/评分/证据/图谱/action/content/audit/evidence link；Runtime Console 已展示 Project Bootstrap、项目下拉、Brand Kit、Logo Upload、Score Weights、Human Review Trail 与 Review Queue、Prompt CSV Import、Prompt File Import、Prompt Import History、Entity Alias Candidates、Runtime Filters、Evidence Sort、Saved Views、Manual Backfill、筛选后 Evidence CSV 导出、Collection Run Quality、API-browser Fidelity 与趋势摘要、筛选/排序后报告 artifact 下载、Report History、White-label PDF 下载、Evidence Runs 明细、Score Contributions 完整解释包、Citation Graph & Competitors 明细、Citation Graph Map、Report Method & Evidence Appendix、Action Plan & Retest Detail、Runtime Alerts 管理事件、Content Engine Detail、pgvector Knowledge Search、Traceability Detail、Traceability Map、节点级 details 钻取和页面内锚点深链路；独立 `/traceability` 详情页和 client-side pan/zoom 基础交互图谱已落；Keycloak/OIDC login/session、前端 SSO 登录流、细粒度角色和复杂审批流待接 |
| 打不过 Semrush/Ahrefs 数据规模 | 全程定位 | 押证据链/本地信源/代理商工作流，不拼分数广度 | design partner 认可证据价值 |
| 评分构念效度未验证 | M6 后 | 复测展示变化；拿到客户转化数据再做相关性 | 报告标注 MVP 阶段不声称强因果 |

审计链补充：Report History 管理动作已采用 append-only 口径，对冻结 `ReportExport` 追加 `report_export_management_recorded` 审计事件来表达 `internal_review/client_ready/archived` 状态，不更新报告快照、方法披露、对象存储 URL 或评分快照；Runtime Alerts 管理动作同样采用 append-only 口径，`runtime_alert_events` 记录 `alert_id/alert_type/source/source_id/status/updated_by/note/metadata`，`runtime_alert_event_recorded` 记录 before/after hash 和输出事件 id，不改写派生 alert、评分、answer analysis、source gap 或竞品 benchmark；Runtime Alert 通知入队同样不改写派生 alert，`POST /v1/runtime-alerts/notifications` 会基于当前 read model 生成 `runtime_notifications.notification_type=runtime_alert`，写入 `runtime_notification_created`，并按 active webhook/Slack Incoming Webhook subscription 的 event types 与 severity threshold 追加 `runtime_notification_delivery_queued`，默认跳过 latest management status 为 resolved/snoozed 的 alert；`make runtime-alert-notification-worker` 只是批量调用同一入队逻辑的计划任务入口，输出每项目 notification/delivery/skipped/failed 计数；`make runtime-alert-escalation-worker` 按 SLA 阈值批量追加 `escalated` 管理事件，输出每项目 alert/escalated/skipped/failed 计数；二者都不新增可变 alert 状态表；report artifact signed URL 已采用 HMAC + `expires_at` 口径保护下载参数完整性，但不替代对象存储原生 presigned URL、CDN token 或不可变归档；Brand Assets 最小版采用审计事件投影，Logo 上传写 `project_brand_logo_uploaded`，历史版本激活写 `project_brand_logo_version_activated`，可复盘 active Logo 的来源与切换原因；Asset Library 最小版采用 `project_brand_assets` 专表持久化项目级素材元数据，Logo 上传和手工登记都会追加 `project_brand_asset_registered`，可复盘 asset URL、preview URL、分类、状态、content hash、storage version 和 metadata；素材扫描门禁采用对象级 `project_brand_asset_scan_recorded` 事件，只更新 `scan_status/scan_checked_at/scan_method_version/scan_notes`，不改写素材登记来源；Report Export Queue 最小版采用 `report_export_jobs` 持久化任务状态并追加 `report_export_job_queued` / `report_export_job_status_updated` 审计事件，`make report-export-worker` 可把 queued 或 lease 过期 running 任务推进到 running/succeeded/queued/dead_letter，并把 artifact URL、失败原因、attempt、lease 和 next attempt 写回任务；终态任务会写入 `runtime_notifications` 并追加 `runtime_notification_created`，read/unread 状态变更追加 `runtime_notification_status_updated`；webhook/Slack Incoming Webhook/SMTP email 订阅写入 `runtime_notification_subscription_saved`，通知入队写入 `runtime_notification_delivery_queued`，worker claim/delivered/retry/dead-letter 均通过 `runtime_notification_delivery_status_updated` 记录 response status、response body hash、错误和 next attempt；webhook 出站签名采用 env indirection，不保存 raw secret，签名 header 与 payload hash 一起形成接收端可复算的完整性线索；`geno_core.webhook_signing` 已把发送端签名与接收端验证口径固化为可测试 helper；Temporal/长驻 worker、并发池、secret rotation、托管验证服务、复杂 SLA/值班升级、邮件模板/退订/服务商 API、Slack App/OAuth/交互式消息、图片裁剪、真实病毒扫描引擎、CDN 和对象存储原生版本仍是后续 P1 增强。

## 5. 工作流与约定

### 5.1 分支与提交

- `main` 受保护；每个任务开短命 feature 分支 → PR → 合并（建议 squash）。
- 分支名：`m2a/perplexity-collector`、`m2b/google-aio-spike`、`m3/scoring-v1`。
- 提交/PR 关联 issue 号；PR 模板含「关联任务、DoD 勾选、是否回写 docs/ADR」。

### 5.2 issue 命名与标签体系（建议）

> 本地无远程时，用本文件的勾选框即可；推到 GitHub/GitLab 时按下表建 Milestone + Label。

- **Milestone**：`M0`、`M1`、`M2a`、`M2b`、`M3`…`M7`（对应本文件里程碑）
- **issue 标题**：`[M2a][collector] Perplexity Sonar 采集后端`
- **标签**：

| 维度 | 标签 |
| --- | --- |
| 优先级 | `P0a` `P0b` `P0c` `P1` `P2` |
| 类型 | `type:feature` `type:infra` `type:spike` `type:test` `type:docs` `type:bug` |
| 区域 | `area:collector` `area:evidence` `area:parser` `area:scoring` `area:citation` `area:benchmark` `area:report` `area:console` `area:platform` |
| 状态 | `status:blocked` `status:in-progress`（用看板列时可省） |
| 特殊 | `risk`（对应第 4 节）`good-first-slice`（垂直切片起步任务） |

### 5.3 ADR（架构决策记录）

- 位置：`decisions/NNNN-title.md`，每动一个可插拔点选型记一条（为什么 pgvector 起步、为什么 simple worker/cron 先于 Temporal、为什么 Perplexity 先建、Google spike 是否过闸）。
- 换实现时新开一条 ADR `supersede` 旧条。这是"文档保持活规格"的机制。

### 5.4 节奏

- **一周一个垂直切片 + 一次自演示**；P0a/P0b/P0c 阶段每周推进一个里程碑出口标准。
- **尽快锁定一个澳洲 design partner 品牌**：它的真实 prompt 既是验收场景，也是最好的测试数据。
- 每周回顾：勾选本文件进度，更新里程碑状态列。

## 6. 立即可做的下一步（建 issue 用）

1. `[M0][infra]` 仓库骨架 + 轻量 docker-compose 核心底座 + 空 CI（`risk`、`good-first-slice`）
2. `[M0][platform]` P0a/P0b/P0c 表迁移 + 8 个接口 stub
3. `[M1][platform]` MarketProfile=AU 固定配置 + 1 行业模板
4. `[M1][console]` 项目/品牌/竞品/prompt pack 最小后台
5. `[M2a][collector]` Perplexity Sonar 后端 + Raw Evidence Store（打通第一条垂直切片）
6. `[M2a][collector]` OpenAI web search 后端 + CollectionCost
7. `[M2b][collector]` Google AIO / AI Mode 采集 spike（限时，`risk`、`type:spike`）
8. `[M3][scoring]` ScoreContribution 分数解释包 + 报告追溯链

---

维护：本文件随里程碑推进更新状态列与勾选框；规格变更先改 `docs/` 再回链本文件。
